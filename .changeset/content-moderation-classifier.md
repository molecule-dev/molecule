---
'@molecule/api-content-moderation': minor
---

Add `ContentClassifierProvider` and `setClassifier` / `requireClassifier`: a separate slot for providers that score content without storing reports.
