---
'@molecule/api-ai-translation-deepl': minor
---

Support `protect`, escape `&`/`<`/`>` for XML requests and decode entities in results, retry 5xx responses, and include the HTTP `status` on errors.
