---
'@molecule/app-ide-react': minor
'@molecule/app-locales-ide': minor
---

The `/test` card now names the test on screen while a run is in flight — the runner's own group and title with a live per-file tally — and a running file can be skipped without ending the run; a skipped file reads `Skipped`, never as a pass. The executor's own long-running command gains the same Skip.
