---
'@molecule/api-ai-tools': minor
---

exec_command runs a command that asks for more time than its ceiling, with output held until the end, in the background (returning a task id) instead of refusing it.
