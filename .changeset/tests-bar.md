---
'@molecule/app-ide-react': minor
'@molecule/app-locales-ide': minor
---

Adds the Tests bar: an expandable row above the composer that lists the project's end-to-end specs and unit tests and runs them, one at a time or all together, streaming each result inline (hosts provide `listTests`/`runTests`); the locale bond gains the `ide.tests.*` strings.
