---
'@molecule/app-theme-css-variables': patch
---

`systemDefault` option: with no persisted choice, start on the theme whose mode matches the OS preference; `getServerTheme()` reports `defaultTheme` for server and build-time renders.
