---
'@molecule/app-ide-react': minor
'@molecule/app-locales-ide': minor
---

The `/cost` card now names the allowance window the host reports (`allowanceWindow`), so a monthly allowance reads "this month's" rather than "today's", and reset countdowns beyond a day render as "in about N days" instead of "tomorrow".
