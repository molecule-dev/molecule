---
'@molecule/app-e2e-fixtures-default': patch
---

`test` and `expect` now come from `@molecule/app-e2e`, so the same specs run on real browsers locally and against the live preview inside a molecule sandbox.
