---
'@molecule/app-theme': patch
---

`ThemeProvider.getServerTheme()` (optional): the theme a render without a browser produces, so a hydrating client can render the markup it was sent before switching to the live theme.
