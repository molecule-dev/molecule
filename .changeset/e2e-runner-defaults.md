---
'@molecule/app-e2e': minor
---

Adds `e2eRunnerDefaults()`, the Playwright runner settings (workers, parallelism, retries, failure cap, per-test timeout) for the current environment, to spread into `playwright.config.ts`.
