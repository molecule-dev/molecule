---
'@molecule/app-e2e-fixtures-default': patch
---

Docs: a molecule sandbox runs the specs on its own Chromium through the playwright bond; the preview bond is the opt-in for driving the live IDE tab and fails fast when no tab is attached.
