# @molecule/app-rule-builder-react

Visual condition / rule builder — when X and Y and (Z or W) — used for feature flag rules, automation triggers, and segmentation

## Type
`feature`

## Injection Notes

### Requirements
- None

### Post-Injection Steps
- Run `npm install` to install dependencies
- Run `npm run build` to compile

### Known Limitations
- None yet
