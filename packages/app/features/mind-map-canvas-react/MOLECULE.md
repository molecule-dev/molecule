# @molecule/app-mind-map-canvas-react

React mind-map canvas — tree-structured nodes with parent/child links, automatic radial/horizontal-tree layout, fold/unfold subtrees, inline edit-on-double-click; consumes @molecule/app-feature-canvas-react.

## Type
`feature`

## Injection Notes

### Requirements
- None

### Post-Injection Steps
- Run `npm install` to install dependencies
- Run `npm run build` to compile

### Known Limitations
- None yet
