import type { MedicationRowTranslations } from './types.js'

/** MedicationRow translations for ha. */
export const ha: Partial<MedicationRowTranslations> = {
  'medication.prescribedBy': 'An rubuta ta',
}
