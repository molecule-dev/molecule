import type { MedicationRowTranslations } from './types.js'

/** MedicationRow translations for bs. */
export const bs: Partial<MedicationRowTranslations> = {
  'medication.prescribedBy': 'Propisano od strane',
}
