import type { MedicationRowTranslations } from './types.js'

/** MedicationRow translations for tr. */
export const tr: Partial<MedicationRowTranslations> = {
  'medication.prescribedBy': 'Reçeteyi yazan',
}
