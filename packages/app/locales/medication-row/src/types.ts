/** Translation keys for the medication-row locale package. */
export type MedicationRowTranslationKey = 'medication.prescribedBy'

/** Translation record mapping medication-row-react keys to translated strings. */
export type MedicationRowTranslations = Record<MedicationRowTranslationKey, string>
