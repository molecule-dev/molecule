import type { MedicationRowTranslations } from './types.js'

/** MedicationRow translations for zu. */
export const zu: Partial<MedicationRowTranslations> = {
  'medication.prescribedBy': 'Kunqunywe ngu',
}
