import type { MedicationRowTranslations } from './types.js'

/** MedicationRow translations for si. */
export const si: Partial<MedicationRowTranslations> = {
  'medication.prescribedBy': 'නියම කරන ලද්දේ',
}
