import type { MedicationRowTranslations } from './types.js'

/** MedicationRow translations for mr. */
export const mr: Partial<MedicationRowTranslations> = {
  'medication.prescribedBy': 'द्वारे विहित केलेले',
}
