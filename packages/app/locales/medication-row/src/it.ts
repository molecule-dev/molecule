import type { MedicationRowTranslations } from './types.js'

/** MedicationRow translations for it. */
export const it: Partial<MedicationRowTranslations> = {
  'medication.prescribedBy': 'Prescritto da',
}
