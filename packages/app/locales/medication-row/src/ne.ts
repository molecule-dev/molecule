import type { MedicationRowTranslations } from './types.js'

/** MedicationRow translations for ne. */
export const ne: Partial<MedicationRowTranslations> = {
  'medication.prescribedBy': 'द्वारा निर्धारित',
}
