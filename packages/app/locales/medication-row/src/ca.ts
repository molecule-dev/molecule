import type { MedicationRowTranslations } from './types.js'

/** MedicationRow translations for ca. */
export const ca: Partial<MedicationRowTranslations> = {
  'medication.prescribedBy': 'Prescrit per',
}
