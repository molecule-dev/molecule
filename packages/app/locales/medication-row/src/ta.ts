import type { MedicationRowTranslations } from './types.js'

/** MedicationRow translations for ta. */
export const ta: Partial<MedicationRowTranslations> = {
  'medication.prescribedBy': 'பரிந்துரைக்கப்பட்டது',
}
