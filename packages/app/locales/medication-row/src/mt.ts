import type { MedicationRowTranslations } from './types.js'

/** MedicationRow translations for mt. */
export const mt: Partial<MedicationRowTranslations> = {
  'medication.prescribedBy': 'Preskritt minn',
}
