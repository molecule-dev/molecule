import type { MedicationRowTranslations } from './types.js'

/** MedicationRow translations for ig. */
export const ig: Partial<MedicationRowTranslations> = {
  'medication.prescribedBy': 'Edere ya site na',
}
