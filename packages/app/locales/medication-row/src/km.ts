import type { MedicationRowTranslations } from './types.js'

/** MedicationRow translations for km. */
export const km: Partial<MedicationRowTranslations> = {
  'medication.prescribedBy': 'បានចេញវេជ្ជបញ្ជាដោយ',
}
