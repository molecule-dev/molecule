import type { MedicationRowTranslations } from './types.js'

/** MedicationRow translations for mk. */
export const mk: Partial<MedicationRowTranslations> = {
  'medication.prescribedBy': 'Пропишано од',
}
