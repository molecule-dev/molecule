import type { MedicationRowTranslations } from './types.js'

/** MedicationRow translations for de. */
export const de: Partial<MedicationRowTranslations> = {
  'medication.prescribedBy': 'Verordnet von',
}
