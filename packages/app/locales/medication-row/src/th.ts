import type { MedicationRowTranslations } from './types.js'

/** MedicationRow translations for th. */
export const th: Partial<MedicationRowTranslations> = {
  'medication.prescribedBy': 'สั่งจ่ายโดย',
}
