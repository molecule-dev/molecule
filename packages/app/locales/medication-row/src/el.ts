import type { MedicationRowTranslations } from './types.js'

/** MedicationRow translations for el. */
export const el: Partial<MedicationRowTranslations> = {
  'medication.prescribedBy': 'Συνταγογραφήθηκε από',
}
