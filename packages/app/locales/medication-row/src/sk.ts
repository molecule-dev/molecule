import type { MedicationRowTranslations } from './types.js'

/** MedicationRow translations for sk. */
export const sk: Partial<MedicationRowTranslations> = {
  'medication.prescribedBy': 'Predpísané',
}
