import type { MedicationRowTranslations } from './types.js'

/** MedicationRow translations for eu. */
export const eu: Partial<MedicationRowTranslations> = {
  'medication.prescribedBy': 'Agindua',
}
