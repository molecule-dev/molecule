import type { MedicationRowTranslations } from './types.js'

/** MedicationRow translations for az. */
export const az: Partial<MedicationRowTranslations> = {
  'medication.prescribedBy': 'Təyin edən',
}
