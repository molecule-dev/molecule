import type { MedicationRowTranslations } from './types.js'

/** MedicationRow translations for bn. */
export const bn: Partial<MedicationRowTranslations> = {
  'medication.prescribedBy': 'নির্ধারিত',
}
