import type { MedicationRowTranslations } from './types.js'

/** MedicationRow translations for lo. */
export const lo: Partial<MedicationRowTranslations> = {
  'medication.prescribedBy': 'ກຳນົດໂດຍ',
}
