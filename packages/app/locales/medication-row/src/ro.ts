import type { MedicationRowTranslations } from './types.js'

/** MedicationRow translations for ro. */
export const ro: Partial<MedicationRowTranslations> = {
  'medication.prescribedBy': 'Prescris de',
}
