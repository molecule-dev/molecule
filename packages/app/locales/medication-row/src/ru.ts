import type { MedicationRowTranslations } from './types.js'

/** MedicationRow translations for ru. */
export const ru: Partial<MedicationRowTranslations> = {
  'medication.prescribedBy': 'Назначено',
}
