import type { MedicationRowTranslations } from './types.js'

/** MedicationRow translations for fa. */
export const fa: Partial<MedicationRowTranslations> = {
  'medication.prescribedBy': 'تجویز شده توسط',
}
