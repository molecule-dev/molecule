import type { MedicationRowTranslations } from './types.js'

/** MedicationRow translations for pl. */
export const pl: Partial<MedicationRowTranslations> = {
  'medication.prescribedBy': 'Przepisał(a)',
}
