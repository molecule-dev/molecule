import type { MedicationRowTranslations } from './types.js'

/** MedicationRow translations for lv. */
export const lv: Partial<MedicationRowTranslations> = {
  'medication.prescribedBy': 'Noteicis',
}
