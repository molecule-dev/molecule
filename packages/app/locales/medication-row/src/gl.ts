import type { MedicationRowTranslations } from './types.js'

/** MedicationRow translations for gl. */
export const gl: Partial<MedicationRowTranslations> = {
  'medication.prescribedBy': 'Prescrito por',
}
