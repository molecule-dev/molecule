import type { MedicationRowTranslations } from './types.js'

/** MedicationRow translations for bg. */
export const bg: Partial<MedicationRowTranslations> = {
  'medication.prescribedBy': 'Предписано от',
}
