import type { MedicationRowTranslations } from './types.js'

/** MedicationRow translations for ml. */
export const ml: Partial<MedicationRowTranslations> = {
  'medication.prescribedBy': 'നിർദ്ദേശിച്ചത്',
}
