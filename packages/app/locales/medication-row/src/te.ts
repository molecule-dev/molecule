import type { MedicationRowTranslations } from './types.js'

/** MedicationRow translations for te. */
export const te: Partial<MedicationRowTranslations> = {
  'medication.prescribedBy': 'సూచించినది',
}
