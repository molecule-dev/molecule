import type { CalendarTranslations } from './types.js'

/** Calendar translations for Arabic. */
export const ar: CalendarTranslations = {
  'calendar.error.noProvider':
    '@molecule/app-calendar: لم يتم تعيين مزود. قم باستدعاء setProvider() مع تنفيذ CalendarProvider (مثلاً من @molecule/app-calendar-capacitor).',
}
