import type { CalendarTranslations } from './types.js'

/** Calendar translations for Hausa. */
export const ha: CalendarTranslations = {
  'calendar.error.noProvider':
    '@molecule/app-calendar: Ba a saita mai bayarwa ba. Kira setProvider() tare da aiwatar da CalendarProvider (misali, daga @molecule/app-calendar-capacitor).',
}
