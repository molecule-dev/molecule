import type { CalendarTranslations } from './types.js'

/** Calendar translations for Hebrew. */
export const he: CalendarTranslations = {
  'calendar.error.noProvider':
    '@molecule/app-calendar: לא הוגדר ספק. קראו ל-setProvider() עם מימוש של CalendarProvider (למשל, מ-@molecule/app-calendar-capacitor).',
}
