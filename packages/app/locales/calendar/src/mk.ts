import type { CalendarTranslations } from './types.js'

/** Calendar translations for Macedonian. */
export const mk: CalendarTranslations = {
  'calendar.error.noProvider':
    '@molecule/app-calendar: Не е поставен давател. Повикајте setProvider() со имплементација на CalendarProvider (на пр., од @molecule/app-calendar-capacitor).',
}
