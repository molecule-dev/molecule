import type { CalendarTranslations } from './types.js'

/** Calendar translations for Indonesian. */
export const id: CalendarTranslations = {
  'calendar.error.noProvider':
    '@molecule/app-calendar: Provider belum diatur. Panggil setProvider() dengan implementasi CalendarProvider (mis., dari @molecule/app-calendar-capacitor).',
}
