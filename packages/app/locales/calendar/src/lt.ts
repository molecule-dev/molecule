import type { CalendarTranslations } from './types.js'

/** Calendar translations for Lithuanian. */
export const lt: CalendarTranslations = {
  'calendar.error.noProvider':
    '@molecule/app-calendar: Teikėjas nenustatytas. Iškvieskite setProvider() su CalendarProvider realizacija (pvz., iš @molecule/app-calendar-capacitor).',
}
