import type { CalendarTranslations } from './types.js'

/** Calendar translations for Bengali. */
export const bn: CalendarTranslations = {
  'calendar.error.noProvider':
    '@molecule/app-calendar: কোনো প্রোভাইডার সেট করা হয়নি। একটি CalendarProvider ইমপ্লিমেন্টেশন দিয়ে setProvider() কল করুন (যেমন @molecule/app-calendar-capacitor থেকে)।',
}
