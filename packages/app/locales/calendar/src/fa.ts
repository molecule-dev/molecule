import type { CalendarTranslations } from './types.js'

/** Calendar translations for Persian. */
export const fa: CalendarTranslations = {
  'calendar.error.noProvider':
    '@molecule/app-calendar: هیچ ارائه‌دهنده‌ای تنظیم نشده است. setProvider() را با یک پیاده‌سازی CalendarProvider فراخوانی کنید (مثلاً از @molecule/app-calendar-capacitor).',
}
