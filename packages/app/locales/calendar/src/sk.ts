import type { CalendarTranslations } from './types.js'

/** Calendar translations for Slovak. */
export const sk: CalendarTranslations = {
  'calendar.error.noProvider':
    '@molecule/app-calendar: Poskytovateľ nie je nastavený. Zavolajte setProvider() s implementáciou CalendarProvider (napr. z @molecule/app-calendar-capacitor).',
}
