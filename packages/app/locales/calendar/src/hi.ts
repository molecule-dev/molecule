import type { CalendarTranslations } from './types.js'

/** Calendar translations for Hindi. */
export const hi: CalendarTranslations = {
  'calendar.error.noProvider':
    '@molecule/app-calendar: कोई प्रोवाइडर सेट नहीं है। CalendarProvider कार्यान्वयन के साथ setProvider() को कॉल करें (उदा., @molecule/app-calendar-capacitor से)।',
}
