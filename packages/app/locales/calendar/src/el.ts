import type { CalendarTranslations } from './types.js'

/** Calendar translations for Greek. */
export const el: CalendarTranslations = {
  'calendar.error.noProvider':
    '@molecule/app-calendar: Δεν έχει οριστεί πάροχος. Καλέστε setProvider() με μια υλοποίηση CalendarProvider (π.χ. από @molecule/app-calendar-capacitor).',
}
