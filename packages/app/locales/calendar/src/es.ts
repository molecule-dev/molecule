import type { CalendarTranslations } from './types.js'

/** Calendar translations for Spanish. */
export const es: CalendarTranslations = {
  'calendar.error.noProvider':
    '@molecule/app-calendar: No se ha establecido un proveedor. Llame a setProvider() con una implementación de CalendarProvider (p. ej., de @molecule/app-calendar-capacitor).',
}
