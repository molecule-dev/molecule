import type { CalendarTranslations } from './types.js'

/** Calendar translations for Swahili. */
export const sw: CalendarTranslations = {
  'calendar.error.noProvider':
    '@molecule/app-calendar: Hakuna mtoa huduma aliyewekwa. Piga simu setProvider() na utekelezaji wa CalendarProvider (mf., kutoka @molecule/app-calendar-capacitor).',
}
