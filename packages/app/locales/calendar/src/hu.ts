import type { CalendarTranslations } from './types.js'

/** Calendar translations for Hungarian. */
export const hu: CalendarTranslations = {
  'calendar.error.noProvider':
    '@molecule/app-calendar: Nincs szolgáltató beállítva. Hívja meg a setProvider()-t egy CalendarProvider implementációval (pl. a @molecule/app-calendar-capacitor-ból).',
}
