import type { CalendarTranslations } from './types.js'

/** Calendar translations for Georgian. */
export const ka: CalendarTranslations = {
  'calendar.error.noProvider':
    '@molecule/app-calendar: პროვაიდერი არ არის დაყენებული. გამოიძახეთ setProvider() CalendarProvider იმპლემენტაციით (მაგ., @molecule/app-calendar-capacitor-დან).',
}
