import type { CalendarTranslations } from './types.js'

/** Calendar translations for Romanian. */
export const ro: CalendarTranslations = {
  'calendar.error.noProvider':
    '@molecule/app-calendar: Niciun furnizor setat. Apelați setProvider() cu o implementare CalendarProvider.',
}
