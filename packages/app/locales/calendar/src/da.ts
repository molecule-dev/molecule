import type { CalendarTranslations } from './types.js'

/** Calendar translations for Danish. */
export const da: CalendarTranslations = {
  'calendar.error.noProvider':
    '@molecule/app-calendar: Ingen udbyder er sat. Kald setProvider() med en CalendarProvider-implementering (f.eks. fra @molecule/app-calendar-capacitor).',
}
