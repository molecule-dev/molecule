import type { CalendarTranslations } from './types.js'

/** Calendar translations for German. */
export const de: CalendarTranslations = {
  'calendar.error.noProvider':
    '@molecule/app-calendar: Kein Anbieter gesetzt. Rufen Sie setProvider() mit einer CalendarProvider-Implementierung auf (z. B. von @molecule/app-calendar-capacitor).',
}
