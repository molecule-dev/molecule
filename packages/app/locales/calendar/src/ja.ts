import type { CalendarTranslations } from './types.js'

/** Calendar translations for Japanese. */
export const ja: CalendarTranslations = {
  'calendar.error.noProvider':
    '@molecule/app-calendar: プロバイダーが設定されていません。CalendarProviderの実装でsetProvider()を呼び出してください（例：@molecule/app-calendar-capacitorから）。',
}
