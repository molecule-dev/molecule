import type { CalendarTranslations } from './types.js'

/** Calendar translations for Galician. */
export const gl: CalendarTranslations = {
  'calendar.error.noProvider':
    '@molecule/app-calendar: Non se estableceu ningún provedor. Chame a setProvider() cunha implementación de CalendarProvider (p. ex., de @molecule/app-calendar-capacitor).',
}
