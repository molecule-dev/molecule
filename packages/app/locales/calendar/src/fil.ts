import type { CalendarTranslations } from './types.js'

/** Calendar translations for Filipino. */
export const fil: CalendarTranslations = {
  'calendar.error.noProvider':
    '@molecule/app-calendar: Walang provider na naka-set. Tumawag ng setProvider() na may CalendarProvider na implementasyon (hal., mula sa @molecule/app-calendar-capacitor).',
}
