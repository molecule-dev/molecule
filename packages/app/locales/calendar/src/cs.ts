import type { CalendarTranslations } from './types.js'

/** Calendar translations for Czech. */
export const cs: CalendarTranslations = {
  'calendar.error.noProvider':
    '@molecule/app-calendar: Poskytovatel není nastaven. Zavolejte setProvider() s implementací CalendarProvider (např. z @molecule/app-calendar-capacitor).',
}
