import type { CalendarTranslations } from './types.js'

/** Calendar translations for Croatian. */
export const hr: CalendarTranslations = {
  'calendar.error.noProvider':
    '@molecule/app-calendar: Pružatelj nije postavljen. Pozovite setProvider() s implementacijom CalendarProvider.',
}
