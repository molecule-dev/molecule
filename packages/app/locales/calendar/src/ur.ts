import type { CalendarTranslations } from './types.js'

/** Calendar translations for Urdu. */
export const ur: CalendarTranslations = {
  'calendar.error.noProvider':
    '@molecule/app-calendar: کوئی فراہم کنندہ مقرر نہیں ہے۔ CalendarProvider کے نفاذ کے ساتھ setProvider() کو کال کریں (مثلاً، @molecule/app-calendar-capacitor سے)۔',
}
