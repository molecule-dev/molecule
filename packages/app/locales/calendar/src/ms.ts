import type { CalendarTranslations } from './types.js'

/** Calendar translations for Malay. */
export const ms: CalendarTranslations = {
  'calendar.error.noProvider':
    '@molecule/app-calendar: Tiada pembekal ditetapkan. Panggil setProvider() dengan pelaksanaan CalendarProvider (cth., dari @molecule/app-calendar-capacitor).',
}
