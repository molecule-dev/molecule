import type { CalendarTranslations } from './types.js'

/** Calendar translations for Vietnamese. */
export const vi: CalendarTranslations = {
  'calendar.error.noProvider':
    '@molecule/app-calendar: Chưa thiết lập nhà cung cấp. Gọi setProvider() với một triển khai CalendarProvider.',
}
