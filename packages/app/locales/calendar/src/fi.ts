import type { CalendarTranslations } from './types.js'

/** Calendar translations for Finnish. */
export const fi: CalendarTranslations = {
  'calendar.error.noProvider':
    '@molecule/app-calendar: Palveluntarjoajaa ei ole asetettu. Kutsu setProvider() CalendarProvider-toteutuksella (esim. @molecule/app-calendar-capacitor).',
}
