import type { CalendarTranslations } from './types.js'

/** Calendar translations for Polish. */
export const pl: CalendarTranslations = {
  'calendar.error.noProvider':
    '@molecule/app-calendar: Nie ustawiono dostawcy. Wywołaj setProvider() z implementacją CalendarProvider (np. z @molecule/app-calendar-capacitor).',
}
