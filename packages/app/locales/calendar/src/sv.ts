import type { CalendarTranslations } from './types.js'

/** Calendar translations for Swedish. */
export const sv: CalendarTranslations = {
  'calendar.error.noProvider':
    '@molecule/app-calendar: Ingen leverantör inställd. Anropa setProvider() med en CalendarProvider-implementering (t.ex. från @molecule/app-calendar-capacitor).',
}
