import type { CalendarTranslations } from './types.js'

/** Calendar translations for Yoruba. */
export const yo: CalendarTranslations = {
  'calendar.error.noProvider':
    '@molecule/app-calendar: Ko si olupese ti a ṣeto. Pe setProvider() pẹlu imuse CalendarProvider (fun apẹẹrẹ, lati @molecule/app-calendar-capacitor).',
}
