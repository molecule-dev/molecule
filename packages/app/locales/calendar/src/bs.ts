import type { CalendarTranslations } from './types.js'

/** Calendar translations for Bosnian. */
export const bs: CalendarTranslations = {
  'calendar.error.noProvider':
    '@molecule/app-calendar: Provajder nije postavljen. Pozovite setProvider() sa CalendarProvider implementacijom (npr. iz @molecule/app-calendar-capacitor).',
}
