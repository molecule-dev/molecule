import type { CalendarTranslations } from './types.js'

/** Calendar translations for Bulgarian. */
export const bg: CalendarTranslations = {
  'calendar.error.noProvider':
    '@molecule/app-calendar: Не е зададен доставчик. Извикайте setProvider() с имплементация на CalendarProvider (напр. от @molecule/app-calendar-capacitor).',
}
