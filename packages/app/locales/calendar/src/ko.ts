import type { CalendarTranslations } from './types.js'

/** Calendar translations for Korean. */
export const ko: CalendarTranslations = {
  'calendar.error.noProvider':
    '@molecule/app-calendar: 공급자가 설정되지 않았습니다. CalendarProvider 구현체로 setProvider()를 호출하세요 (예: @molecule/app-calendar-capacitor에서).',
}
