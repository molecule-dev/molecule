import type { CalendarTranslations } from './types.js'

/** Calendar translations for Dutch. */
export const nl: CalendarTranslations = {
  'calendar.error.noProvider':
    '@molecule/app-calendar: Geen provider ingesteld. Roep setProvider() aan met een CalendarProvider-implementatie (bijv. van @molecule/app-calendar-capacitor).',
}
