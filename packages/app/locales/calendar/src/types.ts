/** Translation keys for the calendar locale package. */
export type CalendarTranslationKey = 'calendar.error.noProvider'

/** Translation record mapping calendar keys to translated strings. */
export type CalendarTranslations = Record<CalendarTranslationKey, string>
