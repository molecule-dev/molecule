import type { CalendarTranslations } from './types.js'

/** Calendar translations for Thai. */
export const th: CalendarTranslations = {
  'calendar.error.noProvider':
    '@molecule/app-calendar: ยังไม่ได้ตั้งค่าผู้ให้บริการ เรียก setProvider() พร้อมการใช้งาน CalendarProvider (เช่น จาก @molecule/app-calendar-capacitor)',
}
