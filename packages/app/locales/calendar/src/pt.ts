import type { CalendarTranslations } from './types.js'

/** Calendar translations for Portuguese. */
export const pt: CalendarTranslations = {
  'calendar.error.noProvider':
    '@molecule/app-calendar: Nenhum fornecedor definido. Chame setProvider() com uma implementação de CalendarProvider (ex., de @molecule/app-calendar-capacitor).',
}
