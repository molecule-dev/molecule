import type { CalendarTranslations } from './types.js'

/** Calendar translations for French. */
export const fr: CalendarTranslations = {
  'calendar.error.noProvider':
    '@molecule/app-calendar: Aucun fournisseur défini. Appelez setProvider() avec une implémentation de CalendarProvider (par ex., depuis @molecule/app-calendar-capacitor).',
}
