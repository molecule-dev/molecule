import type { CalendarTranslations } from './types.js'

/** Calendar translations for Russian. */
export const ru: CalendarTranslations = {
  'calendar.error.noProvider':
    '@molecule/app-calendar: Провайдер не установлен. Вызовите setProvider() с реализацией CalendarProvider (например, из @molecule/app-calendar-capacitor).',
}
