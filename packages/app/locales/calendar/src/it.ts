import type { CalendarTranslations } from './types.js'

/** Calendar translations for it. */
export const it: Partial<CalendarTranslations> = {
  'calendar.error.noProvider':
    '@molecule/app-calendar: Nessun provider impostato. Chiama setProvider() con un&#39;implementazione di CalendarProvider (ad esempio, da @molecule/app-calendar-capacitor).',
}
