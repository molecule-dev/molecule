import type { CalendarTranslations } from './types.js'

/** Calendar translations for Italian. */
export const it: CalendarTranslations = {
  'calendar.error.noProvider':
    "@molecule/app-calendar: Nessun provider impostato. Chiamare setProvider() con un'implementazione di CalendarProvider (ad es., da @molecule/app-calendar-capacitor).",
}
