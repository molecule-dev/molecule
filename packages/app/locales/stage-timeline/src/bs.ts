import type { StageTimelineTranslations } from './types.js'

/** StageTimeline translations for bs. */
export const bs: Partial<StageTimelineTranslations> = {
  'stageTimeline.group': 'Vremenska linija faze',
}
