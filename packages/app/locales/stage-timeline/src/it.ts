import type { StageTimelineTranslations } from './types.js'

/** StageTimeline translations for it. */
export const it: Partial<StageTimelineTranslations> = {
  'stageTimeline.group': 'Cronologia delle fasi',
}
