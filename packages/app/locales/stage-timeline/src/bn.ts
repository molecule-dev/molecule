import type { StageTimelineTranslations } from './types.js'

/** StageTimeline translations for bn. */
export const bn: Partial<StageTimelineTranslations> = {
  'stageTimeline.group': 'পর্যায়ের সময়রেখা',
}
