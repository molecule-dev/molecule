import type { StageTimelineTranslations } from './types.js'

/** StageTimeline translations for cs. */
export const cs: Partial<StageTimelineTranslations> = {
  'stageTimeline.group': 'Časová osa fáze',
}
