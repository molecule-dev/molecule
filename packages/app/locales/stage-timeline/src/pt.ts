import type { StageTimelineTranslations } from './types.js'

/** StageTimeline translations for pt. */
export const pt: Partial<StageTimelineTranslations> = {
  'stageTimeline.group': 'Cronograma da etapa',
}
