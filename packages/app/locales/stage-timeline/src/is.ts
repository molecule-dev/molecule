import type { StageTimelineTranslations } from './types.js'

/** StageTimeline translations for is. */
export const is: Partial<StageTimelineTranslations> = {
  'stageTimeline.group': 'Tímalína stigs',
}
