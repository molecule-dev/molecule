import type { StageTimelineTranslations } from './types.js'

/** StageTimeline translations for km. */
export const km: Partial<StageTimelineTranslations> = {
  'stageTimeline.group': 'ពេលវេលាកំណត់សម្រាប់ដំណាក់កាល',
}
