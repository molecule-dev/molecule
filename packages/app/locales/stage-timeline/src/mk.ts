import type { StageTimelineTranslations } from './types.js'

/** StageTimeline translations for mk. */
export const mk: Partial<StageTimelineTranslations> = {
  'stageTimeline.group': 'Временска рамка на етапите',
}
