import type { StageTimelineTranslations } from './types.js'

/** StageTimeline translations for gl. */
export const gl: Partial<StageTimelineTranslations> = {
  'stageTimeline.group': 'Cronoloxía da etapa',
}
