import type { StageTimelineTranslations } from './types.js'

/** StageTimeline translations for si. */
export const si: Partial<StageTimelineTranslations> = {
  'stageTimeline.group': 'වේදිකා කාලරේඛාව',
}
