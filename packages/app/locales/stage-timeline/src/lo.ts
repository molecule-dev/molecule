import type { StageTimelineTranslations } from './types.js'

/** StageTimeline translations for lo. */
export const lo: Partial<StageTimelineTranslations> = {
  'stageTimeline.group': 'ເສັ້ນເວລາຂອງເວທີ',
}
