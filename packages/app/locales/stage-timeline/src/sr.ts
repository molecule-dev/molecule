import type { StageTimelineTranslations } from './types.js'

/** StageTimeline translations for sr. */
export const sr: Partial<StageTimelineTranslations> = {
  'stageTimeline.group': 'Временска линија фазе',
}
