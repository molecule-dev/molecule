import type { StageTimelineTranslations } from './types.js'

/** StageTimeline translations for lv. */
export const lv: Partial<StageTimelineTranslations> = {
  'stageTimeline.group': 'Posma laika grafiks',
}
