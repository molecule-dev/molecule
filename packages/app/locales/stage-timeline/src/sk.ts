import type { StageTimelineTranslations } from './types.js'

/** StageTimeline translations for sk. */
export const sk: Partial<StageTimelineTranslations> = {
  'stageTimeline.group': 'Časová os fázy',
}
