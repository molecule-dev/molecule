import type { StageTimelineTranslations } from './types.js'

/** StageTimeline translations for ms. */
export const ms: Partial<StageTimelineTranslations> = {
  'stageTimeline.group': 'Garis masa pentas',
}
