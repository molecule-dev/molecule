import type { VideoScrubberTranslations } from './types.js'
import { en } from './en.js'

/** Video-scrubber translations for gl — currently mirrors English (translation TBD). */
export const gl: VideoScrubberTranslations = { ...en }
