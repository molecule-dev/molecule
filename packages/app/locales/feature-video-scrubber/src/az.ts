import type { VideoScrubberTranslations } from './types.js'
import { en } from './en.js'

/** Video-scrubber translations for az — currently mirrors English (translation TBD). */
export const az: VideoScrubberTranslations = { ...en }
