import type { VideoScrubberTranslations } from './types.js'
import { en } from './en.js'

/** Video-scrubber translations for is — currently mirrors English (translation TBD). */
export const is: VideoScrubberTranslations = { ...en }
