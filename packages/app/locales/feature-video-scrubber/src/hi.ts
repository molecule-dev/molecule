import type { VideoScrubberTranslations } from './types.js'
import { en } from './en.js'

/** Video-scrubber translations for hi — currently mirrors English (translation TBD). */
export const hi: VideoScrubberTranslations = { ...en }
