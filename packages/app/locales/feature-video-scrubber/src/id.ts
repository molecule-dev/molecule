import type { VideoScrubberTranslations } from './types.js'
import { en } from './en.js'

/** Video-scrubber translations for id — currently mirrors English (translation TBD). */
export const id: VideoScrubberTranslations = { ...en }
