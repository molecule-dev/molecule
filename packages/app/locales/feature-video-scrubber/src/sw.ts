import type { VideoScrubberTranslations } from './types.js'
import { en } from './en.js'

/** Video-scrubber translations for sw — currently mirrors English (translation TBD). */
export const sw: VideoScrubberTranslations = { ...en }
