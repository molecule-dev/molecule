import type { VideoScrubberTranslations } from './types.js'
import { en } from './en.js'

/** Video-scrubber translations for ar — currently mirrors English (translation TBD). */
export const ar: VideoScrubberTranslations = { ...en }
