import type { VideoScrubberTranslations } from './types.js'
import { en } from './en.js'

/** Video-scrubber translations for ms — currently mirrors English (translation TBD). */
export const ms: VideoScrubberTranslations = { ...en }
