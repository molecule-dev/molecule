import type { VideoScrubberTranslations } from './types.js'
import { en } from './en.js'

/** Video-scrubber translations for de — currently mirrors English (translation TBD). */
export const de: VideoScrubberTranslations = { ...en }
