import type { NetworkTranslations } from './types.js'

/** Network translations for pl. */
export const pl: Partial<NetworkTranslations> = {
  'network.none': 'Rozłączono',
  'network.unknown': 'Nieznany',
}
