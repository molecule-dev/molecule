import type { FilterBarTranslations } from './types.js'

/** FilterBar translations for English. */
export const en: FilterBarTranslations = {
  'filterBar.clear': 'Clear filters',
  'filterBar.from': '{{label}} from',
  'filterBar.to': '{{label}} to',
}
