import type { FilterBarTranslations } from './types.js'

/** FilterBar translations for my. */
export const my: Partial<FilterBarTranslations> = {
  'filterBar.clear': 'စစ်ထုတ်မှုများကို ဖယ်ရှားပါ',
}
