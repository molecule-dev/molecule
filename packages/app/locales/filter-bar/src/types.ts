/** Translation keys for the filter-bar locale package. */
export type FilterBarTranslationKey = 'filterBar.clear'

/** Translation record mapping filter-bar-react keys to translated strings. */
export type FilterBarTranslations = Record<FilterBarTranslationKey, string>
