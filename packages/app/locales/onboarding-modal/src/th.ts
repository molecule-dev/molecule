import type { OnboardingModalTranslations } from './types.js'

/** OnboardingModal translations for th. */
export const th: Partial<OnboardingModalTranslations> = {
  'onboarding.done': 'เริ่มต้น',
}
