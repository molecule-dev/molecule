import type { GradebookTranslations } from './types.js'
import { en } from './en.js'

/** Gradebook translations for kn — currently mirrors English (translation TBD). */
export const kn: GradebookTranslations = { ...en }
