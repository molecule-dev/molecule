import type { GradebookTranslations } from './types.js'
import { en } from './en.js'

/** Gradebook translations for mr — currently mirrors English (translation TBD). */
export const mr: GradebookTranslations = { ...en }
