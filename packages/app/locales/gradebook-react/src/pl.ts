import type { GradebookTranslations } from './types.js'
import { en } from './en.js'

/** Gradebook translations for pl — currently mirrors English (translation TBD). */
export const pl: GradebookTranslations = { ...en }
