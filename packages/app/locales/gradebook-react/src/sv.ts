import type { GradebookTranslations } from './types.js'
import { en } from './en.js'

/** Gradebook translations for sv — currently mirrors English (translation TBD). */
export const sv: GradebookTranslations = { ...en }
