import type { GradebookTranslations } from './types.js'
import { en } from './en.js'

/** Gradebook translations for it — currently mirrors English (translation TBD). */
export const it: GradebookTranslations = { ...en }
