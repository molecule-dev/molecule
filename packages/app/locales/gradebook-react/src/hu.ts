import type { GradebookTranslations } from './types.js'
import { en } from './en.js'

/** Gradebook translations for hu — currently mirrors English (translation TBD). */
export const hu: GradebookTranslations = { ...en }
