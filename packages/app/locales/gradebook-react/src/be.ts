import type { GradebookTranslations } from './types.js'
import { en } from './en.js'

/** Gradebook translations for be — currently mirrors English (translation TBD). */
export const be: GradebookTranslations = { ...en }
