import type { GradebookTranslations } from './types.js'
import { en } from './en.js'

/** Gradebook translations for gu — currently mirrors English (translation TBD). */
export const gu: GradebookTranslations = { ...en }
