import type { GradebookTranslations } from './types.js'
import { en } from './en.js'

/** Gradebook translations for hy — currently mirrors English (translation TBD). */
export const hy: GradebookTranslations = { ...en }
