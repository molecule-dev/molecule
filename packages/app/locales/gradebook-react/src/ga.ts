import type { GradebookTranslations } from './types.js'
import { en } from './en.js'

/** Gradebook translations for ga — currently mirrors English (translation TBD). */
export const ga: GradebookTranslations = { ...en }
