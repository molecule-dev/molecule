import type { GradebookTranslations } from './types.js'
import { en } from './en.js'

/** Gradebook translations for is — currently mirrors English (translation TBD). */
export const is: GradebookTranslations = { ...en }
