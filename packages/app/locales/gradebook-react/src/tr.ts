import type { GradebookTranslations } from './types.js'
import { en } from './en.js'

/** Gradebook translations for tr — currently mirrors English (translation TBD). */
export const tr: GradebookTranslations = { ...en }
