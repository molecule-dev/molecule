import type { GradebookTranslations } from './types.js'
import { en } from './en.js'

/** Gradebook translations for cy — currently mirrors English (translation TBD). */
export const cy: GradebookTranslations = { ...en }
