import type { GradebookTranslations } from './types.js'
import { en } from './en.js'

/** Gradebook translations for lv — currently mirrors English (translation TBD). */
export const lv: GradebookTranslations = { ...en }
