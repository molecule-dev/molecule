import type { GradebookTranslations } from './types.js'
import { en } from './en.js'

/** Gradebook translations for ro — currently mirrors English (translation TBD). */
export const ro: GradebookTranslations = { ...en }
