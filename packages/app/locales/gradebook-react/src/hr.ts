import type { GradebookTranslations } from './types.js'
import { en } from './en.js'

/** Gradebook translations for hr — currently mirrors English (translation TBD). */
export const hr: GradebookTranslations = { ...en }
