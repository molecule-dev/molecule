import type { GradebookTranslations } from './types.js'
import { en } from './en.js'

/** Gradebook translations for lt — currently mirrors English (translation TBD). */
export const lt: GradebookTranslations = { ...en }
