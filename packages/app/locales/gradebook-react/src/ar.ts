import type { GradebookTranslations } from './types.js'
import { en } from './en.js'

/** Gradebook translations for ar — currently mirrors English (translation TBD). */
export const ar: GradebookTranslations = { ...en }
