import type { GradebookTranslations } from './types.js'
import { en } from './en.js'

/** Gradebook translations for kk — currently mirrors English (translation TBD). */
export const kk: GradebookTranslations = { ...en }
