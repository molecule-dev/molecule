import type { GradebookTranslations } from './types.js'
import { en } from './en.js'

/** Gradebook translations for mk — currently mirrors English (translation TBD). */
export const mk: GradebookTranslations = { ...en }
