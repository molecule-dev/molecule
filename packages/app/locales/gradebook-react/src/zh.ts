import type { GradebookTranslations } from './types.js'
import { en } from './en.js'

/** Gradebook translations for zh — currently mirrors English (translation TBD). */
export const zh: GradebookTranslations = { ...en }
