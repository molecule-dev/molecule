import type { GradebookTranslations } from './types.js'
import { en } from './en.js'

/** Gradebook translations for ta — currently mirrors English (translation TBD). */
export const ta: GradebookTranslations = { ...en }
