import type { GradebookTranslations } from './types.js'
import { en } from './en.js'

/** Gradebook translations for ja — currently mirrors English (translation TBD). */
export const ja: GradebookTranslations = { ...en }
