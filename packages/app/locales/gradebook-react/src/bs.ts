import type { GradebookTranslations } from './types.js'
import { en } from './en.js'

/** Gradebook translations for bs — currently mirrors English (translation TBD). */
export const bs: GradebookTranslations = { ...en }
