import type { GradebookTranslations } from './types.js'
import { en } from './en.js'

/** Gradebook translations for el — currently mirrors English (translation TBD). */
export const el: GradebookTranslations = { ...en }
