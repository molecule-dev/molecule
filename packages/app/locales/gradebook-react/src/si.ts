import type { GradebookTranslations } from './types.js'
import { en } from './en.js'

/** Gradebook translations for si — currently mirrors English (translation TBD). */
export const si: GradebookTranslations = { ...en }
