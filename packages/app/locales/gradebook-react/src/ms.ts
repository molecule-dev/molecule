import type { GradebookTranslations } from './types.js'

/** Gradebook translations for ms. */
export const ms: Partial<GradebookTranslations> = {
  'gradebook.col.title': 'Kursus',
  'gradebook.col.numeric': 'Skor',
}
