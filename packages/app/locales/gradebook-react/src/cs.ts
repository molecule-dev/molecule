import type { GradebookTranslations } from './types.js'
import { en } from './en.js'

/** Gradebook translations for cs — currently mirrors English (translation TBD). */
export const cs: GradebookTranslations = { ...en }
