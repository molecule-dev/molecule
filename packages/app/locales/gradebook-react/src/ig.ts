import type { GradebookTranslations } from './types.js'
import { en } from './en.js'

/** Gradebook translations for ig — currently mirrors English (translation TBD). */
export const ig: GradebookTranslations = { ...en }
