import type { GradebookTranslations } from './types.js'
import { en } from './en.js'

/** Gradebook translations for es — currently mirrors English (translation TBD). */
export const es: GradebookTranslations = { ...en }
