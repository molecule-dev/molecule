import type { GradebookTranslations } from './types.js'
import { en } from './en.js'

/** Gradebook translations for te — currently mirrors English (translation TBD). */
export const te: GradebookTranslations = { ...en }
