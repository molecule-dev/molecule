import type { GradebookTranslations } from './types.js'
import { en } from './en.js'

/** Gradebook translations for ka — currently mirrors English (translation TBD). */
export const ka: GradebookTranslations = { ...en }
