import type { GradebookTranslations } from './types.js'
import { en } from './en.js'

/** Gradebook translations for sk — currently mirrors English (translation TBD). */
export const sk: GradebookTranslations = { ...en }
