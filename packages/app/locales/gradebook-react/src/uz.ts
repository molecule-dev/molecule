import type { GradebookTranslations } from './types.js'
import { en } from './en.js'

/** Gradebook translations for uz — currently mirrors English (translation TBD). */
export const uz: GradebookTranslations = { ...en }
