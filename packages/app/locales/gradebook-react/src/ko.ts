import type { GradebookTranslations } from './types.js'
import { en } from './en.js'

/** Gradebook translations for ko — currently mirrors English (translation TBD). */
export const ko: GradebookTranslations = { ...en }
