import type { GradebookTranslations } from './types.js'
import { en } from './en.js'

/** Gradebook translations for ne — currently mirrors English (translation TBD). */
export const ne: GradebookTranslations = { ...en }
