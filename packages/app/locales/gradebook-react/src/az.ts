import type { GradebookTranslations } from './types.js'
import { en } from './en.js'

/** Gradebook translations for az — currently mirrors English (translation TBD). */
export const az: GradebookTranslations = { ...en }
