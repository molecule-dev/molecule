import type { GradebookTranslations } from './types.js'
import { en } from './en.js'

/** Gradebook translations for uk — currently mirrors English (translation TBD). */
export const uk: GradebookTranslations = { ...en }
