import type { GradebookTranslations } from './types.js'
import { en } from './en.js'

/** Gradebook translations for fi — currently mirrors English (translation TBD). */
export const fi: GradebookTranslations = { ...en }
