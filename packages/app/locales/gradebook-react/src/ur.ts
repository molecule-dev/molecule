import type { GradebookTranslations } from './types.js'
import { en } from './en.js'

/** Gradebook translations for ur — currently mirrors English (translation TBD). */
export const ur: GradebookTranslations = { ...en }
