import type { GradebookTranslations } from './types.js'
import { en } from './en.js'

/** Gradebook translations for ml — currently mirrors English (translation TBD). */
export const ml: GradebookTranslations = { ...en }
