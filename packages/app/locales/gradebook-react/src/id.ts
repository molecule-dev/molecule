import type { GradebookTranslations } from './types.js'
import { en } from './en.js'

/** Gradebook translations for id — currently mirrors English (translation TBD). */
export const id: GradebookTranslations = { ...en }
