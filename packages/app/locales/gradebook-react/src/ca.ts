import type { GradebookTranslations } from './types.js'
import { en } from './en.js'

/** Gradebook translations for ca — currently mirrors English (translation TBD). */
export const ca: GradebookTranslations = { ...en }
