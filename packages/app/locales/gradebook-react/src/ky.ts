import type { GradebookTranslations } from './types.js'
import { en } from './en.js'

/** Gradebook translations for ky — currently mirrors English (translation TBD). */
export const ky: GradebookTranslations = { ...en }
