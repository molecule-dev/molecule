import type { GradebookTranslations } from './types.js'
import { en } from './en.js'

/** Gradebook translations for nb — currently mirrors English (translation TBD). */
export const nb: GradebookTranslations = { ...en }
