import type { GradebookTranslations } from './types.js'
import { en } from './en.js'

/** Gradebook translations for sr — currently mirrors English (translation TBD). */
export const sr: GradebookTranslations = { ...en }
