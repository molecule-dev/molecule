import type { FilesystemTranslations } from './types.js'

/** Filesystem translations for Maltese. */
export const mt: FilesystemTranslations = {
  'filesystem.error.noProvider':
    "@molecule/app-filesystem: L-ebda fornitur mhux issettjat. Sejjah setProvider() b'implimentazzjoni ta' FilesystemProvider (eż., minn @molecule/app-filesystem-capacitor).",
}
