import type { FilesystemTranslations } from './types.js'

/** Filesystem translations for mt. */
export const mt: Partial<FilesystemTranslations> = {
  'filesystem.error.noProvider':
    '@molecule/app-filesystem: L-ebda fornitur ma ġie stabbilit. Sejjaħ setProvider() b&#39;implimentazzjoni ta&#39; FilesystemProvider (eż., minn @molecule/app-filesystem-capacitor).',
}
