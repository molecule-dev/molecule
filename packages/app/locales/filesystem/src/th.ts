import type { FilesystemTranslations } from './types.js'

/** Filesystem translations for Thai. */
export const th: FilesystemTranslations = {
  'filesystem.error.noProvider':
    '@molecule/app-filesystem: ยังไม่ได้ตั้งค่าผู้ให้บริการ เรียก setProvider() พร้อมการใช้งาน FilesystemProvider (เช่น จาก @molecule/app-filesystem-capacitor)',
}
