import type { FilesystemTranslations } from './types.js'

/** Filesystem translations for Portuguese. */
export const pt: FilesystemTranslations = {
  'filesystem.error.noProvider':
    '@molecule/app-filesystem: Nenhum fornecedor definido. Chame setProvider() com uma implementação de FilesystemProvider (ex., de @molecule/app-filesystem-capacitor).',
}
