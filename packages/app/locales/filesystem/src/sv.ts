import type { FilesystemTranslations } from './types.js'

/** Filesystem translations for Swedish. */
export const sv: FilesystemTranslations = {
  'filesystem.error.noProvider':
    '@molecule/app-filesystem: Ingen leverantör inställd. Anropa setProvider() med en FilesystemProvider-implementering (t.ex. från @molecule/app-filesystem-capacitor).',
}
