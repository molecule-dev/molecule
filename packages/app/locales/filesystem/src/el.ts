import type { FilesystemTranslations } from './types.js'

/** Filesystem translations for Greek. */
export const el: FilesystemTranslations = {
  'filesystem.error.noProvider':
    '@molecule/app-filesystem: Δεν έχει οριστεί πάροχος. Καλέστε setProvider() με μια υλοποίηση FilesystemProvider (π.χ. από @molecule/app-filesystem-capacitor).',
}
