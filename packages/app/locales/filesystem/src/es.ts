import type { FilesystemTranslations } from './types.js'

/** Filesystem translations for Spanish. */
export const es: FilesystemTranslations = {
  'filesystem.error.noProvider':
    '@molecule/app-filesystem: No se ha establecido un proveedor. Llame a setProvider() con una implementación de FilesystemProvider (p. ej., de @molecule/app-filesystem-capacitor).',
}
