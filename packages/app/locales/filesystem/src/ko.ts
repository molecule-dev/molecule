import type { FilesystemTranslations } from './types.js'

/** Filesystem translations for Korean. */
export const ko: FilesystemTranslations = {
  'filesystem.error.noProvider':
    '@molecule/app-filesystem: 공급자가 설정되지 않았습니다. FilesystemProvider 구현체로 setProvider()를 호출하세요 (예: @molecule/app-filesystem-capacitor에서).',
}
