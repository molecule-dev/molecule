import type { FilesystemTranslations } from './types.js'

/** Filesystem translations for Macedonian. */
export const mk: FilesystemTranslations = {
  'filesystem.error.noProvider':
    '@molecule/app-filesystem: Не е поставен давател. Повикајте setProvider() со имплементација на FilesystemProvider (на пр., од @molecule/app-filesystem-capacitor).',
}
