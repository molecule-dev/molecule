import type { FilesystemTranslations } from './types.js'

/** Filesystem translations for Bengali. */
export const bn: FilesystemTranslations = {
  'filesystem.error.noProvider':
    '@molecule/app-filesystem: কোনো প্রোভাইডার সেট করা হয়নি। একটি FilesystemProvider ইমপ্লিমেন্টেশন দিয়ে setProvider() কল করুন (যেমন @molecule/app-filesystem-capacitor থেকে)।',
}
