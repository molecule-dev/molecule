import type { FilesystemTranslations } from './types.js'

/** Filesystem translations for Lithuanian. */
export const lt: FilesystemTranslations = {
  'filesystem.error.noProvider':
    '@molecule/app-filesystem: Teikėjas nenustatytas. Iškvieskite setProvider() su FilesystemProvider realizacija (pvz., iš @molecule/app-filesystem-capacitor).',
}
