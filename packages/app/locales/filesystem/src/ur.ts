import type { FilesystemTranslations } from './types.js'

/** Filesystem translations for Urdu. */
export const ur: FilesystemTranslations = {
  'filesystem.error.noProvider':
    '@molecule/app-filesystem: کوئی فراہم کنندہ مقرر نہیں ہے۔ FilesystemProvider کے نفاذ کے ساتھ setProvider() کو کال کریں (مثلاً، @molecule/app-filesystem-capacitor سے)۔',
}
