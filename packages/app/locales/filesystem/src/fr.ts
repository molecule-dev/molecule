import type { FilesystemTranslations } from './types.js'

/** Filesystem translations for French. */
export const fr: FilesystemTranslations = {
  'filesystem.error.noProvider':
    '@molecule/app-filesystem: Aucun fournisseur défini. Appelez setProvider() avec une implémentation de FilesystemProvider (par ex., depuis @molecule/app-filesystem-capacitor).',
}
