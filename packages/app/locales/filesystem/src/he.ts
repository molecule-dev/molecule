import type { FilesystemTranslations } from './types.js'

/** Filesystem translations for Hebrew. */
export const he: FilesystemTranslations = {
  'filesystem.error.noProvider':
    '@molecule/app-filesystem: לא הוגדר ספק. קראו ל-setProvider() עם מימוש של FilesystemProvider (למשל, מ-@molecule/app-filesystem-capacitor).',
}
