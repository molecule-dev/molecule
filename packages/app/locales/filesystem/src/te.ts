import type { FilesystemTranslations } from './types.js'

/** Filesystem translations for Telugu. */
export const te: FilesystemTranslations = {
  'filesystem.error.noProvider':
    '@molecule/app-filesystem: ప్రొవైడర్ సెట్ చేయబడలేదు. FilesystemProvider అమలుతో setProvider() ను కాల్ చేయండి (ఉదా., @molecule/app-filesystem-capacitor నుండి).',
}
