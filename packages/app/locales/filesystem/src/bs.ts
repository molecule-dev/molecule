import type { FilesystemTranslations } from './types.js'

/** Filesystem translations for Bosnian. */
export const bs: FilesystemTranslations = {
  'filesystem.error.noProvider':
    '@molecule/app-filesystem: Provajder nije postavljen. Pozovite setProvider() sa FilesystemProvider implementacijom (npr. iz @molecule/app-filesystem-capacitor).',
}
