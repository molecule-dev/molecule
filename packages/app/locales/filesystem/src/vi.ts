import type { FilesystemTranslations } from './types.js'

/** Filesystem translations for Vietnamese. */
export const vi: FilesystemTranslations = {
  'filesystem.error.noProvider':
    '@molecule/app-filesystem: Chưa thiết lập nhà cung cấp. Gọi setProvider() với một triển khai FilesystemProvider (ví dụ, từ @molecule/app-filesystem-capacitor).',
}
