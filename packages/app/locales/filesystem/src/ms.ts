import type { FilesystemTranslations } from './types.js'

/** Filesystem translations for Malay. */
export const ms: FilesystemTranslations = {
  'filesystem.error.noProvider':
    '@molecule/app-filesystem: Tiada pembekal ditetapkan. Panggil setProvider() dengan pelaksanaan FilesystemProvider (cth., dari @molecule/app-filesystem-capacitor).',
}
