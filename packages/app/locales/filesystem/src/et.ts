import type { FilesystemTranslations } from './types.js'

/** Filesystem translations for Estonian. */
export const et: FilesystemTranslations = {
  'filesystem.error.noProvider':
    '@molecule/app-filesystem: Teenusepakkuja pole määratud. Kutsuge setProvider() koos FilesystemProvider teostusega (nt @molecule/app-filesystem-capacitor).',
}
