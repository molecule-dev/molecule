import type { FilesystemTranslations } from './types.js'

/** Filesystem translations for Dutch. */
export const nl: FilesystemTranslations = {
  'filesystem.error.noProvider':
    '@molecule/app-filesystem: Geen provider ingesteld. Roep setProvider() aan met een FilesystemProvider-implementatie (bijv. van @molecule/app-filesystem-capacitor).',
}
