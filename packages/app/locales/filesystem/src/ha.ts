import type { FilesystemTranslations } from './types.js'

/** Filesystem translations for Hausa. */
export const ha: FilesystemTranslations = {
  'filesystem.error.noProvider':
    '@molecule/app-filesystem: Ba a saita mai bayarwa ba. Kira setProvider() tare da aiwatar da FilesystemProvider (misali, daga @molecule/app-filesystem-capacitor).',
}
