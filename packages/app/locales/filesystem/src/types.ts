/** Translation keys for the filesystem locale package. */
export type FilesystemTranslationKey = 'filesystem.error.noProvider'

/** Translation record mapping filesystem keys to translated strings. */
export type FilesystemTranslations = Record<FilesystemTranslationKey, string>
