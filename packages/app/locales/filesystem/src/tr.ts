import type { FilesystemTranslations } from './types.js'

/** Filesystem translations for Turkish. */
export const tr: FilesystemTranslations = {
  'filesystem.error.noProvider':
    "@molecule/app-filesystem: Sağlayıcı ayarlanmamış. setProvider() fonksiyonunu bir FilesystemProvider uygulamasıyla çağırın (ör., @molecule/app-filesystem-capacitor'dan).",
}
