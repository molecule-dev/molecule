import type { FilesystemTranslations } from './types.js'

/** Filesystem translations for tr. */
export const tr: Partial<FilesystemTranslations> = {
  'filesystem.error.noProvider':
    '@molecule/app-filesystem: Sağlayıcı ayarlanmadı. Bir FilesystemProvider uygulamasıyla (örneğin, @molecule/app-filesystem-capacitor&#39;dan) setProvider() işlevini çağırın.',
}
