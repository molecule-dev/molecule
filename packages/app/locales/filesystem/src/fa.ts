import type { FilesystemTranslations } from './types.js'

/** Filesystem translations for Persian. */
export const fa: FilesystemTranslations = {
  'filesystem.error.noProvider':
    '@molecule/app-filesystem: هیچ ارائه‌دهنده‌ای تنظیم نشده است. setProvider() را با یک پیاده‌سازی FilesystemProvider فراخوانی کنید (مثلاً از @molecule/app-filesystem-capacitor).',
}
