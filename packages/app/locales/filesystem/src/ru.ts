import type { FilesystemTranslations } from './types.js'

/** Filesystem translations for Russian. */
export const ru: FilesystemTranslations = {
  'filesystem.error.noProvider':
    '@molecule/app-filesystem: Провайдер не установлен. Вызовите setProvider() с реализацией FilesystemProvider (например, из @molecule/app-filesystem-capacitor).',
}
