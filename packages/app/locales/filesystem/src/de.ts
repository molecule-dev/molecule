import type { FilesystemTranslations } from './types.js'

/** Filesystem translations for German. */
export const de: FilesystemTranslations = {
  'filesystem.error.noProvider':
    '@molecule/app-filesystem: Kein Anbieter gesetzt. Rufen Sie setProvider() mit einer FilesystemProvider-Implementierung auf (z. B. von @molecule/app-filesystem-capacitor).',
}
