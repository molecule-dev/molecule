import type { FilesystemTranslations } from './types.js'

/** Filesystem translations for Filipino. */
export const fil: FilesystemTranslations = {
  'filesystem.error.noProvider':
    '@molecule/app-filesystem: Walang provider na naka-set. Tumawag ng setProvider() na may FilesystemProvider na implementasyon (hal., mula sa @molecule/app-filesystem-capacitor).',
}
