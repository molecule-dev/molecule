import type { FilesystemTranslations } from './types.js'

/** Filesystem translations for Galician. */
export const gl: FilesystemTranslations = {
  'filesystem.error.noProvider':
    '@molecule/app-filesystem: Non se estableceu ningún provedor. Chame a setProvider() cunha implementación de FilesystemProvider (p. ex., de @molecule/app-filesystem-capacitor).',
}
