import type { FilesystemTranslations } from './types.js'

/** Filesystem translations for Georgian. */
export const ka: FilesystemTranslations = {
  'filesystem.error.noProvider':
    '@molecule/app-filesystem: პროვაიდერი არ არის დაყენებული. გამოიძახეთ setProvider() FilesystemProvider იმპლემენტაციით (მაგ., @molecule/app-filesystem-capacitor-დან).',
}
