import type { FilesystemTranslations } from './types.js'

/** Filesystem translations for Finnish. */
export const fi: FilesystemTranslations = {
  'filesystem.error.noProvider':
    '@molecule/app-filesystem: Palveluntarjoajaa ei ole asetettu. Kutsu setProvider() FilesystemProvider-toteutuksella (esim. @molecule/app-filesystem-capacitor).',
}
