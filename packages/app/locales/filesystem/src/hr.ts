import type { FilesystemTranslations } from './types.js'

/** Filesystem translations for Croatian. */
export const hr: FilesystemTranslations = {
  'filesystem.error.noProvider':
    '@molecule/app-filesystem: Pružatelj nije postavljen. Pozovite setProvider() s implementacijom FilesystemProvider.',
}
