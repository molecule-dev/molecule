import type { FilesystemTranslations } from './types.js'

/** Filesystem translations for Indonesian. */
export const id: FilesystemTranslations = {
  'filesystem.error.noProvider':
    '@molecule/app-filesystem: Provider belum diatur. Panggil setProvider() dengan implementasi FilesystemProvider (mis., dari @molecule/app-filesystem-capacitor).',
}
