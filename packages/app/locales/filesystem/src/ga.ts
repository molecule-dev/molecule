import type { FilesystemTranslations } from './types.js'

/** Filesystem translations for Irish. */
export const ga: FilesystemTranslations = {
  'filesystem.error.noProvider':
    '@molecule/app-filesystem: Níl aon soláthraí socraithe. Glaoigh ar setProvider() le cur i bhfeidhm FilesystemProvider (m.sh., ó @molecule/app-filesystem-capacitor).',
}
