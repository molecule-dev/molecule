import type { FilesystemTranslations } from './types.js'

/** Filesystem translations for Yoruba. */
export const yo: FilesystemTranslations = {
  'filesystem.error.noProvider':
    '@molecule/app-filesystem: Ko si olupese ti a ṣeto. Pe setProvider() pẹlu imuse FilesystemProvider (fun apẹẹrẹ, lati @molecule/app-filesystem-capacitor).',
}
