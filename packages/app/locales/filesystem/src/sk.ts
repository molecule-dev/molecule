import type { FilesystemTranslations } from './types.js'

/** Filesystem translations for Slovak. */
export const sk: FilesystemTranslations = {
  'filesystem.error.noProvider':
    '@molecule/app-filesystem: Poskytovateľ nie je nastavený. Zavolajte setProvider() s implementáciou FilesystemProvider (napr. z @molecule/app-filesystem-capacitor).',
}
