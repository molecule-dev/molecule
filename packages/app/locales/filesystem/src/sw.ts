import type { FilesystemTranslations } from './types.js'

/** Filesystem translations for Swahili. */
export const sw: FilesystemTranslations = {
  'filesystem.error.noProvider':
    '@molecule/app-filesystem: Hakuna mtoa huduma aliyewekwa. Piga simu setProvider() na utekelezaji wa FilesystemProvider (mf., kutoka @molecule/app-filesystem-capacitor).',
}
