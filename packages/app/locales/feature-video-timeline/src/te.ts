import type { VideoTimelineTranslations } from './types.js'
import { en } from './en.js'

/** Video-timeline translations for te — currently mirrors English (translation TBD). */
export const te: VideoTimelineTranslations = { ...en }
