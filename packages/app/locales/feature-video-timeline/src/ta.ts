import type { VideoTimelineTranslations } from './types.js'
import { en } from './en.js'

/** Video-timeline translations for ta — currently mirrors English (translation TBD). */
export const ta: VideoTimelineTranslations = { ...en }
