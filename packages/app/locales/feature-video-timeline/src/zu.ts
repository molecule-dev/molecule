import type { VideoTimelineTranslations } from './types.js'
import { en } from './en.js'

/** Video-timeline translations for zu — currently mirrors English (translation TBD). */
export const zu: VideoTimelineTranslations = { ...en }
