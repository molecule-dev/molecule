import type { VideoTimelineTranslations } from './types.js'
import { en } from './en.js'

/** Video-timeline translations for ca — currently mirrors English (translation TBD). */
export const ca: VideoTimelineTranslations = { ...en }
