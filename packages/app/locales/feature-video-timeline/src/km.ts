import type { VideoTimelineTranslations } from './types.js'
import { en } from './en.js'

/** Video-timeline translations for km — currently mirrors English (translation TBD). */
export const km: VideoTimelineTranslations = { ...en }
