import type { VideoTimelineTranslations } from './types.js'
import { en } from './en.js'

/** Video-timeline translations for si — currently mirrors English (translation TBD). */
export const si: VideoTimelineTranslations = { ...en }
