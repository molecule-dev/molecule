import type { VideoTimelineTranslations } from './types.js'
import { en } from './en.js'

/** Video-timeline translations for ga — currently mirrors English (translation TBD). */
export const ga: VideoTimelineTranslations = { ...en }
