import type { GraphViewTranslations } from './types.js'

/** GraphView translations for uz. */
export const uz: Partial<GraphViewTranslations> = {
  'graphView.aria.canvas': "Grafik ko'rinishi",
  'graphView.empty': "Ko'rsatish uchun tugunlar yo'q",
}
