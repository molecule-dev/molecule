import type { GraphViewTranslations } from './types.js'

/** GraphView translations for bg. */
export const bg: Partial<GraphViewTranslations> = {
  'graphView.aria.canvas': 'Графичен изглед',
  'graphView.empty': 'Няма възли за показване',
}
