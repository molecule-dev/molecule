import type { GraphViewTranslations } from './types.js'

/** GraphView translations for zh. */
export const zh: Partial<GraphViewTranslations> = {
  'graphView.aria.canvas': '图表视图',
  'graphView.empty': '没有要显示的节点',
}
