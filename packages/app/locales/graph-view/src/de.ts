import type { GraphViewTranslations } from './types.js'

/** GraphView translations for de. */
export const de: Partial<GraphViewTranslations> = {
  'graphView.aria.canvas': 'Grafische Ansicht',
  'graphView.empty': 'Keine Knoten zum Anzeigen',
}
