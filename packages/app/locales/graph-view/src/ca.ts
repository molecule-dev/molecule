import type { GraphViewTranslations } from './types.js'

/** GraphView translations for ca. */
export const ca: Partial<GraphViewTranslations> = {
  'graphView.aria.canvas': 'Vista gràfica',
  'graphView.empty': 'No hi ha nodes per mostrar',
}
