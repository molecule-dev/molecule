import type { GraphViewTranslations } from './types.js'

/** GraphView translations for be. */
export const be: Partial<GraphViewTranslations> = {
  'graphView.aria.canvas': 'Выгляд графіка',
  'graphView.empty': 'Няма вузлоў для адлюстравання',
}
