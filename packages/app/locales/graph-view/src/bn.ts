import type { GraphViewTranslations } from './types.js'

/** GraphView translations for bn. */
export const bn: Partial<GraphViewTranslations> = {
  'graphView.aria.canvas': 'গ্রাফ ভিউ',
  'graphView.empty': 'প্রদর্শনের জন্য কোনো নোড নেই',
}
