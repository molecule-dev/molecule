import type { GraphViewTranslations } from './types.js'

/** GraphView translations for is. */
export const is: Partial<GraphViewTranslations> = {
  'graphView.aria.canvas': 'Grafísk sýn',
  'graphView.empty': 'Engir hnútar til að birta',
}
