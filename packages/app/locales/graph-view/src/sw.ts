import type { GraphViewTranslations } from './types.js'

/** GraphView translations for sw. */
export const sw: Partial<GraphViewTranslations> = {
  'graphView.aria.canvas': 'Mwonekano wa grafu',
  'graphView.empty': 'Hakuna nodi za kuonyesha',
}
