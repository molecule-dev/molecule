import type { GraphViewTranslations } from './types.js'

/** GraphView translations for sv. */
export const sv: Partial<GraphViewTranslations> = {
  'graphView.aria.canvas': 'Grafvy',
  'graphView.empty': 'Inga noder att visa',
}
