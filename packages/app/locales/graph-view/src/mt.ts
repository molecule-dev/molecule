import type { GraphViewTranslations } from './types.js'

/** GraphView translations for mt. */
export const mt: Partial<GraphViewTranslations> = {
  'graphView.aria.canvas': 'Veduta tal-grafika',
  'graphView.empty': 'L-ebda nodi x&#39;jintwerew',
}
