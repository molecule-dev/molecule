import type { GraphViewTranslations } from './types.js'

/** GraphView translations for bs. */
export const bs: Partial<GraphViewTranslations> = {
  'graphView.aria.canvas': 'Grafički prikaz',
  'graphView.empty': 'Nema čvorova za prikaz',
}
