import type { GraphViewTranslations } from './types.js'

/** GraphView translations for id. */
export const id: Partial<GraphViewTranslations> = {
  'graphView.aria.canvas': 'Tampilan grafik',
  'graphView.empty': 'Tidak ada node untuk ditampilkan',
}
