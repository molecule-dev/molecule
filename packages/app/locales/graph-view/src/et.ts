import type { GraphViewTranslations } from './types.js'

/** GraphView translations for et. */
export const et: Partial<GraphViewTranslations> = {
  'graphView.aria.canvas': 'Graafikuvaade',
  'graphView.empty': 'Kuvatavaid sõlmi pole',
}
