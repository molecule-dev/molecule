import type { GraphViewTranslations } from './types.js'

/** GraphView translations for sk. */
export const sk: Partial<GraphViewTranslations> = {
  'graphView.aria.canvas': 'Zobrazenie grafu',
  'graphView.empty': 'Žiadne uzly na zobrazenie',
}
