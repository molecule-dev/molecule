import type { GraphViewTranslations } from './types.js'

/** GraphView translations for pl. */
export const pl: Partial<GraphViewTranslations> = {
  'graphView.aria.canvas': 'Widok wykresu',
  'graphView.empty': 'Brak węzłów do wyświetlenia',
}
