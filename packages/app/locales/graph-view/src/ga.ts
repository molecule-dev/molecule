import type { GraphViewTranslations } from './types.js'

/** GraphView translations for ga. */
export const ga: Partial<GraphViewTranslations> = {
  'graphView.aria.canvas': 'Radharc grafaice',
  'graphView.empty': 'Gan nóid le taispeáint',
}
