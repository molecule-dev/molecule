import type { GraphViewTranslations } from './types.js'

/** GraphView translations for nb. */
export const nb: Partial<GraphViewTranslations> = {
  'graphView.aria.canvas': 'Grafvisning',
  'graphView.empty': 'Ingen noder å vise',
}
