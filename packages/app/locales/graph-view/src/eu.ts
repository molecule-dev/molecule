import type { GraphViewTranslations } from './types.js'

/** GraphView translations for eu. */
export const eu: Partial<GraphViewTranslations> = {
  'graphView.aria.canvas': 'Grafiko ikuspegia',
  'graphView.empty': 'Ez dago bistaratzeko nodorik',
}
