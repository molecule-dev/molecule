import type { GraphViewTranslations } from './types.js'

/** GraphView translations for af. */
export const af: Partial<GraphViewTranslations> = {
  'graphView.aria.canvas': 'Grafiekaansig',
  'graphView.empty': 'Geen nodusse om te vertoon nie',
}
