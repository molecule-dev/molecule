import type { GraphViewTranslations } from './types.js'

/** GraphView translations for gl. */
export const gl: Partial<GraphViewTranslations> = {
  'graphView.aria.canvas': 'Vista gráfica',
  'graphView.empty': 'Non hai nodos para mostrar',
}
