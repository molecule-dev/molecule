import type { GraphViewTranslations } from './types.js'

/** GraphView translations for ro. */
export const ro: Partial<GraphViewTranslations> = {
  'graphView.aria.canvas': 'Vizualizare grafică',
  'graphView.empty': 'Niciun nod de afișat',
}
