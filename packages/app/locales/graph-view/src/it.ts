import type { GraphViewTranslations } from './types.js'

/** GraphView translations for it. */
export const it: Partial<GraphViewTranslations> = {
  'graphView.aria.canvas': 'Visualizzazione del grafico',
  'graphView.empty': 'Nessun nodo da visualizzare',
}
