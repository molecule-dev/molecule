import type { GraphViewTranslations } from './types.js'

/** GraphView translations for pt. */
export const pt: Partial<GraphViewTranslations> = {
  'graphView.aria.canvas': 'Visualização em gráfico',
  'graphView.empty': 'Não há nós para exibir.',
}
