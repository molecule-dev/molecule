import type { GraphViewTranslations } from './types.js'

/** GraphView translations for sl. */
export const sl: Partial<GraphViewTranslations> = {
  'graphView.aria.canvas': 'Pogled grafa',
  'graphView.empty': 'Ni vozlišč za prikaz',
}
