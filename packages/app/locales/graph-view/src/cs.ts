import type { GraphViewTranslations } from './types.js'

/** GraphView translations for cs. */
export const cs: Partial<GraphViewTranslations> = {
  'graphView.aria.canvas': 'Zobrazení grafu',
  'graphView.empty': 'Žádné uzly k zobrazení',
}
