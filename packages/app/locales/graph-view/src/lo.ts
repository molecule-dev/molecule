import type { GraphViewTranslations } from './types.js'

/** GraphView translations for lo. */
export const lo: Partial<GraphViewTranslations> = {
  'graphView.aria.canvas': 'ມຸມມອງກຣາຟ',
  'graphView.empty': 'ບໍ່ມີໂນດທີ່ຈະສະແດງ',
}
