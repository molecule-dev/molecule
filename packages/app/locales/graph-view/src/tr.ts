import type { GraphViewTranslations } from './types.js'

/** GraphView translations for tr. */
export const tr: Partial<GraphViewTranslations> = {
  'graphView.aria.canvas': 'Grafik görünümü',
  'graphView.empty': 'Görüntülenecek düğüm yok',
}
