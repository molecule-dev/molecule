import type { GraphViewTranslations } from './types.js'

/** GraphView translations for ms. */
export const ms: Partial<GraphViewTranslations> = {
  'graphView.aria.canvas': 'Paparan graf',
  'graphView.empty': 'Tiada nod untuk dipaparkan',
}
