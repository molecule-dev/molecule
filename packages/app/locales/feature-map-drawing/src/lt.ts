import type { MapDrawingTranslations } from './types.js'
import { en } from './en.js'

/** Map-drawing translations for lt — currently mirrors English (translation TBD). */
export const lt: MapDrawingTranslations = { ...en }
