import type { MapDrawingTranslations } from './types.js'
import { en } from './en.js'

/** Map-drawing translations for ta — currently mirrors English (translation TBD). */
export const ta: MapDrawingTranslations = { ...en }
