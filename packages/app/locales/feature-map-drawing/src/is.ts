import type { MapDrawingTranslations } from './types.js'
import { en } from './en.js'

/** Map-drawing translations for is — currently mirrors English (translation TBD). */
export const is: MapDrawingTranslations = { ...en }
