import type { MapDrawingTranslations } from './types.js'
import { en } from './en.js'

/** Map-drawing translations for my — currently mirrors English (translation TBD). */
export const my: MapDrawingTranslations = { ...en }
