import type { BrightnessTranslations } from './types.js'

/** Brightness translations for French. */
export const fr: BrightnessTranslations = {
  'brightness.error.noProvider':
    '@molecule/app-brightness: Aucun fournisseur défini. Appelez setProvider() avec une implémentation de BrightnessProvider (par ex., depuis @molecule/app-brightness-capacitor).',
}
