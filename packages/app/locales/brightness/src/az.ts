import type { BrightnessTranslations } from './types.js'

/** Brightness translations for Azerbaijani. */
export const az: BrightnessTranslations = {
  'brightness.error.noProvider':
    '@molecule/app-brightness: Provayder təyin edilməyib. BrightnessProvider tətbiqi ilə setProvider() çağırın (məs., @molecule/app-brightness-capacitor-dan).',
}
