import type { BrightnessTranslations } from './types.js'

/** Brightness translations for Arabic. */
export const ar: BrightnessTranslations = {
  'brightness.error.noProvider':
    '@molecule/app-brightness: لم يتم تعيين مزود. قم باستدعاء setProvider() مع تنفيذ BrightnessProvider (مثلاً من @molecule/app-brightness-capacitor).',
}
