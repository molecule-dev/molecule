import type { BrightnessTranslations } from './types.js'

/** Brightness translations for Bosnian. */
export const bs: BrightnessTranslations = {
  'brightness.error.noProvider':
    '@molecule/app-brightness: Provajder nije postavljen. Pozovite setProvider() sa BrightnessProvider implementacijom (npr. iz @molecule/app-brightness-capacitor).',
}
