import type { BrightnessTranslations } from './types.js'

/** Brightness translations for Portuguese. */
export const pt: BrightnessTranslations = {
  'brightness.error.noProvider':
    '@molecule/app-brightness: Nenhum provedor definido. Chame setProvider() com uma implementação de BrightnessProvider (ex., de @molecule/app-brightness-capacitor).',
}
