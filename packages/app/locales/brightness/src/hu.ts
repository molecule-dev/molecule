import type { BrightnessTranslations } from './types.js'

/** Brightness translations for Hungarian. */
export const hu: BrightnessTranslations = {
  'brightness.error.noProvider':
    '@molecule/app-brightness: Nincs szolgáltató beállítva. Hívja meg a setProvider()-t egy BrightnessProvider implementációval (pl. a @molecule/app-brightness-capacitor-ból).',
}
