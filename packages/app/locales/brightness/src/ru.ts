import type { BrightnessTranslations } from './types.js'

/** Brightness translations for Russian. */
export const ru: BrightnessTranslations = {
  'brightness.error.noProvider':
    '@molecule/app-brightness: Провайдер не установлен. Вызовите setProvider() с реализацией BrightnessProvider (например, из @molecule/app-brightness-capacitor).',
}
