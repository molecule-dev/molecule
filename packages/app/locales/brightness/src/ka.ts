import type { BrightnessTranslations } from './types.js'

/** Brightness translations for Georgian. */
export const ka: BrightnessTranslations = {
  'brightness.error.noProvider':
    '@molecule/app-brightness: პროვაიდერი არ არის დაყენებული. გამოიძახეთ setProvider() BrightnessProvider იმპლემენტაციით (მაგ., @molecule/app-brightness-capacitor-იდან).',
}
