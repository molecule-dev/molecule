import type { BrightnessTranslations } from './types.js'

/** Brightness translations for Thai. */
export const th: BrightnessTranslations = {
  'brightness.error.noProvider':
    '@molecule/app-brightness: ยังไม่ได้ตั้งค่าผู้ให้บริการ เรียก setProvider() พร้อมการใช้งาน BrightnessProvider (เช่น จาก @molecule/app-brightness-capacitor)',
}
