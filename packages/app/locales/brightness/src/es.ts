import type { BrightnessTranslations } from './types.js'

/** Brightness translations for Spanish. */
export const es: BrightnessTranslations = {
  'brightness.error.noProvider':
    '@molecule/app-brightness: No se ha establecido un proveedor. Llame a setProvider() con una implementación de BrightnessProvider (p. ej., de @molecule/app-brightness-capacitor).',
}
