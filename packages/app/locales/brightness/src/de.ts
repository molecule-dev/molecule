import type { BrightnessTranslations } from './types.js'

/** Brightness translations for German. */
export const de: BrightnessTranslations = {
  'brightness.error.noProvider':
    '@molecule/app-brightness: Kein Anbieter festgelegt. Rufen Sie setProvider() mit einer BrightnessProvider-Implementierung auf (z. B. von @molecule/app-brightness-capacitor).',
}
