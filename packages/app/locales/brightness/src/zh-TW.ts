import type { BrightnessTranslations } from './types.js'

/** Brightness translations for Chinese (Traditional). */
export const zhTW: BrightnessTranslations = {
  'brightness.error.noProvider':
    '@molecule/app-brightness: 未設定提供者。請使用 BrightnessProvider 實作呼叫 setProvider()（例如，來自 @molecule/app-brightness-capacitor）。',
}
