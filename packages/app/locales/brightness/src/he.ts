import type { BrightnessTranslations } from './types.js'

/** Brightness translations for Hebrew. */
export const he: BrightnessTranslations = {
  'brightness.error.noProvider':
    '@molecule/app-brightness: לא הוגדר ספק. קרא ל-setProvider() עם מימוש של BrightnessProvider (למשל, מ-@molecule/app-brightness-capacitor).',
}
