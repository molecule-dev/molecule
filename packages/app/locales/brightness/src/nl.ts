import type { BrightnessTranslations } from './types.js'

/** Brightness translations for Dutch. */
export const nl: BrightnessTranslations = {
  'brightness.error.noProvider':
    '@molecule/app-brightness: Geen provider ingesteld. Roep setProvider() aan met een BrightnessProvider-implementatie (bijv. van @molecule/app-brightness-capacitor).',
}
