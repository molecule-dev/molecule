import type { BrightnessTranslations } from './types.js'

/** Brightness translations for Bengali. */
export const bn: BrightnessTranslations = {
  'brightness.error.noProvider':
    '@molecule/app-brightness: কোনো প্রদানকারী সেট করা হয়নি। একটি BrightnessProvider বাস্তবায়ন দিয়ে setProvider() কল করুন (যেমন @molecule/app-brightness-capacitor থেকে)।',
}
