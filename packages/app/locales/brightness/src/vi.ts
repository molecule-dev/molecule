import type { BrightnessTranslations } from './types.js'

/** Brightness translations for Vietnamese. */
export const vi: BrightnessTranslations = {
  'brightness.error.noProvider':
    '@molecule/app-brightness: Chưa thiết lập nhà cung cấp. Gọi setProvider() với triển khai BrightnessProvider (ví dụ, từ @molecule/app-brightness-capacitor).',
}
