import type { BrightnessTranslations } from './types.js'

/** Brightness translations for Yoruba. */
export const yo: BrightnessTranslations = {
  'brightness.error.noProvider':
    '@molecule/app-brightness: Ko si olupese ti a ṣeto. Pe setProvider() pẹlu imuse BrightnessProvider kan (fun apẹẹrẹ, lati @molecule/app-brightness-capacitor).',
}
