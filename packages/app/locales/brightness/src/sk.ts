import type { BrightnessTranslations } from './types.js'

/** Brightness translations for Slovak. */
export const sk: BrightnessTranslations = {
  'brightness.error.noProvider':
    '@molecule/app-brightness: Poskytovateľ nie je nastavený. Zavolajte setProvider() s implementáciou BrightnessProvider (napr. z @molecule/app-brightness-capacitor).',
}
