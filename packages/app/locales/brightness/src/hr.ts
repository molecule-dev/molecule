import type { BrightnessTranslations } from './types.js'

/** Brightness translations for Croatian. */
export const hr: BrightnessTranslations = {
  'brightness.error.noProvider':
    '@molecule/app-brightness: Pružatelj nije postavljen. Pozovite setProvider() s implementacijom BrightnessProvider (npr. iz @molecule/app-brightness-capacitor).',
}
