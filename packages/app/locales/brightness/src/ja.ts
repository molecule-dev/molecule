import type { BrightnessTranslations } from './types.js'

/** Brightness translations for Japanese. */
export const ja: BrightnessTranslations = {
  'brightness.error.noProvider':
    '@molecule/app-brightness: プロバイダーが設定されていません。BrightnessProviderの実装でsetProvider()を呼び出してください（例: @molecule/app-brightness-capacitorから）。',
}
