import type { BrightnessTranslations } from './types.js'

/** Brightness translations for Polish. */
export const pl: BrightnessTranslations = {
  'brightness.error.noProvider':
    '@molecule/app-brightness: Nie ustawiono dostawcy. Wywołaj setProvider() z implementacją BrightnessProvider (np. z @molecule/app-brightness-capacitor).',
}
