import type { BrightnessTranslations } from './types.js'

/** Brightness translations for Norwegian Bokmål. */
export const nb: BrightnessTranslations = {
  'brightness.error.noProvider':
    '@molecule/app-brightness: Ingen leverandør er satt. Kall setProvider() med en BrightnessProvider-implementasjon (f.eks. fra @molecule/app-brightness-capacitor).',
}
