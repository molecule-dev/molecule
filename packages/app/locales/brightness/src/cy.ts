import type { BrightnessTranslations } from './types.js'

/** Brightness translations for Welsh. */
export const cy: BrightnessTranslations = {
  'brightness.error.noProvider':
    "@molecule/app-brightness: Dim darparwr wedi'i osod. Galwch setProvider() gyda gweithrediad BrightnessProvider (e.e., o @molecule/app-brightness-capacitor).",
}
