import type { BrightnessTranslations } from './types.js'

/** Brightness translations for cy. */
export const cy: Partial<BrightnessTranslations> = {
  'brightness.error.noProvider':
    "@molecule/app-brightness: Dim darparwr wedi'i osod. Galwch setProvider() gyda gweithrediad BrightnessProvider.",
}
