import type { BrightnessTranslations } from './types.js'

/** Brightness translations for Persian. */
export const fa: BrightnessTranslations = {
  'brightness.error.noProvider':
    '@molecule/app-brightness: هیچ ارائه‌دهنده‌ای تنظیم نشده است. setProvider() را با یک پیاده‌سازی BrightnessProvider فراخوانی کنید (مثلاً از @molecule/app-brightness-capacitor).',
}
