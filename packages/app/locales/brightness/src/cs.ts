import type { BrightnessTranslations } from './types.js'

/** Brightness translations for Czech. */
export const cs: BrightnessTranslations = {
  'brightness.error.noProvider':
    '@molecule/app-brightness: Poskytovatel není nastaven. Zavolejte setProvider() s implementací BrightnessProvider (např. z @molecule/app-brightness-capacitor).',
}
