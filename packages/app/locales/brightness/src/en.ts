import type { BrightnessTranslations } from './types.js'

/** Brightness translations for English. */
export const en: BrightnessTranslations = {
  'brightness.error.noProvider':
    '@molecule/app-brightness: No provider set. Call setProvider() with a BrightnessProvider implementation (e.g., from @molecule/app-brightness-capacitor).',
}
