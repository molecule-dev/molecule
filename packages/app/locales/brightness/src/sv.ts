import type { BrightnessTranslations } from './types.js'

/** Brightness translations for Swedish. */
export const sv: BrightnessTranslations = {
  'brightness.error.noProvider':
    '@molecule/app-brightness: Ingen leverantör har angetts. Anropa setProvider() med en BrightnessProvider-implementering.',
}
