import type { BrightnessTranslations } from './types.js'

/** Brightness translations for Korean. */
export const ko: BrightnessTranslations = {
  'brightness.error.noProvider':
    '@molecule/app-brightness: 프로바이더가 설정되지 않았습니다. BrightnessProvider 구현으로 setProvider()를 호출하세요 (예: @molecule/app-brightness-capacitor에서).',
}
