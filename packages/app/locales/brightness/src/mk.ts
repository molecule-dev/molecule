import type { BrightnessTranslations } from './types.js'

/** Brightness translations for Macedonian. */
export const mk: BrightnessTranslations = {
  'brightness.error.noProvider':
    '@molecule/app-brightness: Не е поставен провајдер. Повикајте setProvider() со имплементација на BrightnessProvider (на пр., од @molecule/app-brightness-capacitor).',
}
