import type { BrightnessTranslations } from './types.js'

/** Brightness translations for Urdu. */
export const ur: BrightnessTranslations = {
  'brightness.error.noProvider':
    '@molecule/app-brightness: کوئی فراہم کنندہ سیٹ نہیں ہے۔ BrightnessProvider کے نفاذ کے ساتھ setProvider() کو کال کریں (مثلاً، @molecule/app-brightness-capacitor سے)۔',
}
