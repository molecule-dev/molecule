import type { BrightnessTranslations } from './types.js'

/** Brightness translations for Bulgarian. */
export const bg: BrightnessTranslations = {
  'brightness.error.noProvider':
    '@molecule/app-brightness: Не е зададен доставчик. Извикайте setProvider() с имплементация на BrightnessProvider (напр. от @molecule/app-brightness-capacitor).',
}
