import { en } from './en.js'
import type { NotificationsPageTranslations } from './types.js'

/** Notifications-page translations for ka (stub — falls back to English until reviewed). */
export const ka: NotificationsPageTranslations = { ...en }
