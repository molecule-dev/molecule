import { en } from './en.js'
import type { NotificationsPageTranslations } from './types.js'

/** Notifications-page translations for fi (stub — falls back to English until reviewed). */
export const fi: NotificationsPageTranslations = { ...en }
