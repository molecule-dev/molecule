import { en } from './en.js'
import type { NotificationsPageTranslations } from './types.js'

/** Notifications-page translations for zh (stub — falls back to English until reviewed). */
export const zh: NotificationsPageTranslations = { ...en }
