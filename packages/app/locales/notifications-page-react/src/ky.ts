import { en } from './en.js'
import type { NotificationsPageTranslations } from './types.js'

/** Notifications-page translations for ky (stub — falls back to English until reviewed). */
export const ky: NotificationsPageTranslations = { ...en }
