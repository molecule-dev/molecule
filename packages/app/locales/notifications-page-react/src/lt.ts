import { en } from './en.js'
import type { NotificationsPageTranslations } from './types.js'

/** Notifications-page translations for lt (stub — falls back to English until reviewed). */
export const lt: NotificationsPageTranslations = { ...en }
