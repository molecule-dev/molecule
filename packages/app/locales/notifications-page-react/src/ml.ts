import { en } from './en.js'
import type { NotificationsPageTranslations } from './types.js'

/** Notifications-page translations for ml (stub — falls back to English until reviewed). */
export const ml: NotificationsPageTranslations = { ...en }
