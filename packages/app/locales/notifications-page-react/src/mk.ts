import { en } from './en.js'
import type { NotificationsPageTranslations } from './types.js'

/** Notifications-page translations for mk (stub — falls back to English until reviewed). */
export const mk: NotificationsPageTranslations = { ...en }
