import { en } from './en.js'
import type { NotificationsPageTranslations } from './types.js'

/** Notifications-page translations for sk (stub — falls back to English until reviewed). */
export const sk: NotificationsPageTranslations = { ...en }
