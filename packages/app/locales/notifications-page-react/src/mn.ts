import { en } from './en.js'
import type { NotificationsPageTranslations } from './types.js'

/** Notifications-page translations for mn (stub — falls back to English until reviewed). */
export const mn: NotificationsPageTranslations = { ...en }
