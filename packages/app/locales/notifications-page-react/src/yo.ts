import { en } from './en.js'
import type { NotificationsPageTranslations } from './types.js'

/** Notifications-page translations for yo (stub — falls back to English until reviewed). */
export const yo: NotificationsPageTranslations = { ...en }
