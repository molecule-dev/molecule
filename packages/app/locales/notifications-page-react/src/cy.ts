import { en } from './en.js'
import type { NotificationsPageTranslations } from './types.js'

/** Notifications-page translations for cy (stub — falls back to English until reviewed). */
export const cy: NotificationsPageTranslations = { ...en }
