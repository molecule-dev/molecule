import { en } from './en.js'
import type { NotificationsPageTranslations } from './types.js'

/** Notifications-page translations for ga (stub — falls back to English until reviewed). */
export const ga: NotificationsPageTranslations = { ...en }
