import { en } from './en.js'
import type { NotificationsPageTranslations } from './types.js'

/** Notifications-page translations for bs (stub — falls back to English until reviewed). */
export const bs: NotificationsPageTranslations = { ...en }
