import { en } from './en.js'
import type { NotificationsPageTranslations } from './types.js'

/** Notifications-page translations for sr (stub — falls back to English until reviewed). */
export const sr: NotificationsPageTranslations = { ...en }
