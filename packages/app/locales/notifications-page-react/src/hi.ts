import { en } from './en.js'
import type { NotificationsPageTranslations } from './types.js'

/** Notifications-page translations for hi (stub — falls back to English until reviewed). */
export const hi: NotificationsPageTranslations = { ...en }
