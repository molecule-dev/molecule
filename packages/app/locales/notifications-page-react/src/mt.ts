import { en } from './en.js'
import type { NotificationsPageTranslations } from './types.js'

/** Notifications-page translations for mt (stub — falls back to English until reviewed). */
export const mt: NotificationsPageTranslations = { ...en }
