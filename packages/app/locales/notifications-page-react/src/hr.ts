import { en } from './en.js'
import type { NotificationsPageTranslations } from './types.js'

/** Notifications-page translations for hr (stub — falls back to English until reviewed). */
export const hr: NotificationsPageTranslations = { ...en }
