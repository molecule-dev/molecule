import { en } from './en.js'
import type { NotificationsPageTranslations } from './types.js'

/** Notifications-page translations for sl (stub — falls back to English until reviewed). */
export const sl: NotificationsPageTranslations = { ...en }
