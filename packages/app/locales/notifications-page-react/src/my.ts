import { en } from './en.js'
import type { NotificationsPageTranslations } from './types.js'

/** Notifications-page translations for my (stub — falls back to English until reviewed). */
export const my: NotificationsPageTranslations = { ...en }
