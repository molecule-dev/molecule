import { en } from './en.js'
import type { NotificationsPageTranslations } from './types.js'

/** Notifications-page translations for el (stub — falls back to English until reviewed). */
export const el: NotificationsPageTranslations = { ...en }
