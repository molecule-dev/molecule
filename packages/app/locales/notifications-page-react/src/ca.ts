import { en } from './en.js'
import type { NotificationsPageTranslations } from './types.js'

/** Notifications-page translations for ca (stub — falls back to English until reviewed). */
export const ca: NotificationsPageTranslations = { ...en }
