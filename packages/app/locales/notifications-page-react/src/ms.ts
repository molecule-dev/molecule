import { en } from './en.js'
import type { NotificationsPageTranslations } from './types.js'

/** Notifications-page translations for ms (stub — falls back to English until reviewed). */
export const ms: NotificationsPageTranslations = { ...en }
