import { en } from './en.js'
import type { NotificationsPageTranslations } from './types.js'

/** Notifications-page translations for tr (stub — falls back to English until reviewed). */
export const tr: NotificationsPageTranslations = { ...en }
