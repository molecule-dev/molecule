import { en } from './en.js'
import type { NotificationsPageTranslations } from './types.js'

/** Notifications-page translations for pl (stub — falls back to English until reviewed). */
export const pl: NotificationsPageTranslations = { ...en }
