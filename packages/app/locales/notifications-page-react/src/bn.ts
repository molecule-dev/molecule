import { en } from './en.js'
import type { NotificationsPageTranslations } from './types.js'

/** Notifications-page translations for bn (stub — falls back to English until reviewed). */
export const bn: NotificationsPageTranslations = { ...en }
