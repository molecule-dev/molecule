import { en } from './en.js'
import type { NotificationsPageTranslations } from './types.js'

/** Notifications-page translations for de (stub — falls back to English until reviewed). */
export const de: NotificationsPageTranslations = { ...en }
