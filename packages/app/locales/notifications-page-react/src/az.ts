import { en } from './en.js'
import type { NotificationsPageTranslations } from './types.js'

/** Notifications-page translations for az (stub — falls back to English until reviewed). */
export const az: NotificationsPageTranslations = { ...en }
