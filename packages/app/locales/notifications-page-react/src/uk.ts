import { en } from './en.js'
import type { NotificationsPageTranslations } from './types.js'

/** Notifications-page translations for uk (stub — falls back to English until reviewed). */
export const uk: NotificationsPageTranslations = { ...en }
