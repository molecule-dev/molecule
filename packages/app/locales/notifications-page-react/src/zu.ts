import { en } from './en.js'
import type { NotificationsPageTranslations } from './types.js'

/** Notifications-page translations for zu (stub — falls back to English until reviewed). */
export const zu: NotificationsPageTranslations = { ...en }
