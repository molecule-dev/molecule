import { en } from './en.js'
import type { NotificationsPageTranslations } from './types.js'

/** Notifications-page translations for pa (stub — falls back to English until reviewed). */
export const pa: NotificationsPageTranslations = { ...en }
