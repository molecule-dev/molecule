import { en } from './en.js'
import type { NotificationsPageTranslations } from './types.js'

/** Notifications-page translations for kn (stub — falls back to English until reviewed). */
export const kn: NotificationsPageTranslations = { ...en }
