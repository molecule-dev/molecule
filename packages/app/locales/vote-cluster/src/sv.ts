import type { VoteClusterTranslations } from './types.js'

/** VoteCluster translations for sv. */
export const sv: Partial<VoteClusterTranslations> = {
  'vote-cluster.upvote': 'Upprösta',
  'vote-cluster.downvote': 'Nedrösta',
}
