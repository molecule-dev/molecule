import type { VoteClusterTranslations } from './types.js'

/** VoteCluster translations for ja. */
export const ja: Partial<VoteClusterTranslations> = {
  'vote-cluster.upvote': 'アップボート',
  'vote-cluster.downvote': 'ダウンボート',
}
