import type { VoteClusterTranslations } from './types.js'

/** VoteCluster translations for fr. */
export const fr: Partial<VoteClusterTranslations> = {
  'vote-cluster.upvote': 'Vote positif',
  'vote-cluster.downvote': 'Vote négatif',
}
