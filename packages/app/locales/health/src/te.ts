import type { HealthTranslations } from './types.js'

/** Health translations for Telugu. */
export const te: HealthTranslations = {
  'health.durationHoursMinutes': '{{hours}}గం {{minutes}}ని',
  'health.durationMinutes': '{{minutes}}ని',
}
