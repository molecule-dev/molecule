import type { HealthTranslations } from './types.js'

/** Health translations for Hebrew. */
export const he: HealthTranslations = {
  'health.durationHoursMinutes': '{{hours}}ש {{minutes}}ד',
  'health.durationMinutes': '{{minutes}}ד',
}
