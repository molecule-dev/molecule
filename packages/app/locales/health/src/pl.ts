import type { HealthTranslations } from './types.js'

/** Health translations for Polish. */
export const pl: HealthTranslations = {
  'health.durationHoursMinutes': '{{hours}}godz {{minutes}}min',
  'health.durationMinutes': '{{minutes}}min',
}
