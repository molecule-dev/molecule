import type { HealthTranslations } from './types.js'

/** Health translations for de. */
export const de: Partial<HealthTranslations> = {
  'health.durationMinutes': '{{minutes}} Min',
  'health.durationHoursMinutes': '{{Std}} H<x> {{minutes}}</x> M',
}
