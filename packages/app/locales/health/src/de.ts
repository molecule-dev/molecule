import type { HealthTranslations } from './types.js'

/** Health translations for German. */
export const de: HealthTranslations = {
  'health.durationHoursMinutes': '{{hours}}Std {{minutes}}Min',
  'health.durationMinutes': '{{minutes}}Min',
}
