import type { HealthTranslations } from './types.js'

/** Health translations for id. */
export const id: Partial<HealthTranslations> = {
  'health.durationMinutes': '{{minutes}}mnt',
  'health.durationHoursMinutes': '{{jam}} H<x> menit</x> M',
}
