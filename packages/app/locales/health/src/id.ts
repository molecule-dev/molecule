import type { HealthTranslations } from './types.js'

/** Health translations for Indonesian. */
export const id: HealthTranslations = {
  'health.durationHoursMinutes': '{{hours}}j {{minutes}}m',
  'health.durationMinutes': '{{minutes}}m',
}
