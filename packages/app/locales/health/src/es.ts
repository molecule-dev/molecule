import type { HealthTranslations } from './types.js'

/** Health translations for es. */
export const es: Partial<HealthTranslations> = {
  'health.durationMinutes': '{{minutes}} min',
  'health.durationHoursMinutes': '{{horas}} h<x> {{minutos}}</x> metro',
}
