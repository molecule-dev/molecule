import type { HealthTranslations } from './types.js'

/** Health translations for Welsh. */
export const cy: HealthTranslations = {
  'health.durationHoursMinutes': '{{hours}}a {{minutes}}m',
  'health.durationMinutes': '{{minutes}}m',
}
