import type { HealthTranslations } from './types.js'

/** Health translations for Icelandic. */
export const is: HealthTranslations = {
  'health.durationHoursMinutes': '{{hours}}klst {{minutes}}mín',
  'health.durationMinutes': '{{minutes}}mín',
}
