import type { HealthTranslations } from './types.js'

/** Health translations for ja. */
export const ja: Partial<HealthTranslations> = {
  'health.durationMinutes': '{{minutes}}分',
  'health.durationHoursMinutes': '{{hours}} h {{minutes}} m',
}
