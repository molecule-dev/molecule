import type { HealthTranslations } from './types.js'

/** Health translations for Japanese. */
export const ja: HealthTranslations = {
  'health.durationHoursMinutes': '{{hours}}時間{{minutes}}分',
  'health.durationMinutes': '{{minutes}}分',
}
