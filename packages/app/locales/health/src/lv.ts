import type { HealthTranslations } from './types.js'

/** Health translations for Latvian. */
export const lv: HealthTranslations = {
  'health.durationHoursMinutes': '{{hours}}st {{minutes}}min',
  'health.durationMinutes': '{{minutes}}min',
}
