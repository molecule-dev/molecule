import type { HealthTranslations } from './types.js'

/** Health translations for Swahili. */
export const sw: HealthTranslations = {
  'health.durationHoursMinutes': '{{hours}}s {{minutes}}d',
  'health.durationMinutes': '{{minutes}}d',
}
