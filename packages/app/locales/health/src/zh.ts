import type { HealthTranslations } from './types.js'

/** Health translations for zh. */
export const zh: Partial<HealthTranslations> = {
  'health.durationMinutes': '{{minutes}}分钟',
  'health.durationHoursMinutes': '{{小时}} h<x> {{分钟}}</x>米',
}
