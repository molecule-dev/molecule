import type { HealthTranslations } from './types.js'

/** Health translations for Marathi. */
export const mr: HealthTranslations = {
  'health.durationHoursMinutes': '{{hours}}ता {{minutes}}मि',
  'health.durationMinutes': '{{minutes}}मि',
}
