import type { HealthTranslations } from './types.js'

/** Health translations for Thai. */
export const th: HealthTranslations = {
  'health.durationHoursMinutes': '{{hours}}ชม {{minutes}}น',
  'health.durationMinutes': '{{minutes}}น',
}
