import type { HealthTranslations } from './types.js'

/** Health translations for Bengali. */
export const bn: HealthTranslations = {
  'health.durationHoursMinutes': '{{hours}}ঘ {{minutes}}মি',
  'health.durationMinutes': '{{minutes}}মি',
}
