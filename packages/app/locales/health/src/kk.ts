import type { HealthTranslations } from './types.js'

/** Health translations for Kazakh. */
export const kk: HealthTranslations = {
  'health.durationHoursMinutes': '{{hours}}с {{minutes}}м',
  'health.durationMinutes': '{{minutes}}м',
}
