import type { HealthTranslations } from './types.js'

/** Health translations for Finnish. */
export const fi: HealthTranslations = {
  'health.durationHoursMinutes': '{{hours}}t {{minutes}}min',
  'health.durationMinutes': '{{minutes}}min',
}
