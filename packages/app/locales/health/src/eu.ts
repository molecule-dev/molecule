import type { HealthTranslations } from './types.js'

/** Health translations for Basque. */
export const eu: HealthTranslations = {
  'health.durationHoursMinutes': '{{hours}}o {{minutes}}m',
  'health.durationMinutes': '{{minutes}}m',
}
