import type { HealthTranslations } from './types.js'

/** Health translations for Irish. */
export const ga: HealthTranslations = {
  'health.durationHoursMinutes': '{{hours}}u {{minutes}}n',
  'health.durationMinutes': '{{minutes}}n',
}
