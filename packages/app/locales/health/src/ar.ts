import type { HealthTranslations } from './types.js'

/** Health translations for ar. */
export const ar: Partial<HealthTranslations> = {
  'health.durationMinutes': '{{minutes}} د',
  'health.durationHoursMinutes': '{{ساعات}} ح<x> {{دقائق}}</x> م',
}
