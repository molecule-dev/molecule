import type { HealthTranslations } from './types.js'

/** Health translations for sv. */
export const sv: Partial<HealthTranslations> = {
  'health.durationMinutes': '{{minutes}}min',
  'health.durationHoursMinutes': '{{timmar}} h<x> {{minuter}}</x> m',
}
