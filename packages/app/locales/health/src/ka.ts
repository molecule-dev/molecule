import type { HealthTranslations } from './types.js'

/** Health translations for Georgian. */
export const ka: HealthTranslations = {
  'health.durationHoursMinutes': '{{hours}}სთ {{minutes}}წთ',
  'health.durationMinutes': '{{minutes}}წთ',
}
