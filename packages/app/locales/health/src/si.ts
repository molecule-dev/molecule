import type { HealthTranslations } from './types.js'

/** Health translations for Sinhala. */
export const si: HealthTranslations = {
  'health.durationHoursMinutes': '{{hours}}පැ {{minutes}}මි',
  'health.durationMinutes': '{{minutes}}මි',
}
