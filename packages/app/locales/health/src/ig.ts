import type { HealthTranslations } from './types.js'

/** Health translations for Igbo. */
export const ig: HealthTranslations = {
  'health.durationHoursMinutes': '{{hours}}ch {{minutes}}nk',
  'health.durationMinutes': '{{minutes}}nk',
}
