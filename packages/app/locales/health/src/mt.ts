import type { HealthTranslations } from './types.js'

/** Health translations for Maltese. */
export const mt: HealthTranslations = {
  'health.durationHoursMinutes': '{{hours}}s {{minutes}}m',
  'health.durationMinutes': '{{minutes}}m',
}
