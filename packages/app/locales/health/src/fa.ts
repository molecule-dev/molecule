import type { HealthTranslations } from './types.js'

/** Health translations for Persian. */
export const fa: HealthTranslations = {
  'health.durationHoursMinutes': '{{hours}}س {{minutes}}د',
  'health.durationMinutes': '{{minutes}}د',
}
