import type { HealthTranslations } from './types.js'

/** Health translations for Yoruba. */
export const yo: HealthTranslations = {
  'health.durationHoursMinutes': '{{hours}}wk {{minutes}}iṣ',
  'health.durationMinutes': '{{minutes}}iṣ',
}
