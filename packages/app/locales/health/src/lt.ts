import type { HealthTranslations } from './types.js'

/** Health translations for Lithuanian. */
export const lt: HealthTranslations = {
  'health.durationHoursMinutes': '{{hours}}val {{minutes}}min',
  'health.durationMinutes': '{{minutes}}min',
}
