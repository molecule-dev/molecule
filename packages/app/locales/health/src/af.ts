import type { HealthTranslations } from './types.js'

/** Health translations for Afrikaans. */
export const af: HealthTranslations = {
  'health.durationHoursMinutes': '{{hours}}u {{minutes}}m',
  'health.durationMinutes': '{{minutes}}m',
}
