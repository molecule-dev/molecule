import type { HealthTranslations } from './types.js'

/** Health translations for Hausa. */
export const ha: HealthTranslations = {
  'health.durationHoursMinutes': '{{hours}}sa {{minutes}}min',
  'health.durationMinutes': '{{minutes}}min',
}
