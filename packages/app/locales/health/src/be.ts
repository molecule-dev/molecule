import type { HealthTranslations } from './types.js'

/** Health translations for Belarusian. */
export const be: HealthTranslations = {
  'health.durationHoursMinutes': '{{hours}}г {{minutes}}хв',
  'health.durationMinutes': '{{minutes}}хв',
}
