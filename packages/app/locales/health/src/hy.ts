import type { HealthTranslations } from './types.js'

/** Health translations for Armenian. */
export const hy: HealthTranslations = {
  'health.durationHoursMinutes': '{{hours}}ж {{minutes}}р',
  'health.durationMinutes': '{{minutes}}р',
}
