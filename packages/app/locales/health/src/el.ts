import type { HealthTranslations } from './types.js'

/** Health translations for Greek. */
export const el: HealthTranslations = {
  'health.durationHoursMinutes': '{{hours}}ω {{minutes}}λ',
  'health.durationMinutes': '{{minutes}}λ',
}
