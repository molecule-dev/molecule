import type { HealthTranslations } from './types.js'

/** Health translations for tr. */
export const tr: Partial<HealthTranslations> = {
  'health.durationMinutes': '{{minutes}}dk',
  'health.durationHoursMinutes': '{{saat}} H<x> {{dakika}}</x> M',
}
