import type { HealthTranslations } from './types.js'

/** Health translations for Nepali. */
export const ne: HealthTranslations = {
  'health.durationHoursMinutes': '{{hours}}घण्टा {{minutes}}मि',
  'health.durationMinutes': '{{minutes}}मि',
}
