import type { HealthTranslations } from './types.js'

/** Health translations for ko. */
export const ko: Partial<HealthTranslations> = {
  'health.durationMinutes': '{{minutes}}분',
  'health.durationHoursMinutes': '{{시간}} 시간<x> {{분}}</x> 중',
}
