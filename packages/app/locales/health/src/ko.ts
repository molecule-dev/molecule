import type { HealthTranslations } from './types.js'

/** Health translations for Korean. */
export const ko: HealthTranslations = {
  'health.durationHoursMinutes': '{{hours}}시간 {{minutes}}분',
  'health.durationMinutes': '{{minutes}}분',
}
