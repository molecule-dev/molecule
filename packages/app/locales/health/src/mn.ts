import type { HealthTranslations } from './types.js'

/** Health translations for Mongolian. */
export const mn: HealthTranslations = {
  'health.durationHoursMinutes': '{{hours}}ц {{minutes}}м',
  'health.durationMinutes': '{{minutes}}м',
}
