import type { HealthTranslations } from './types.js'

/** Health translations for Galician. */
export const gl: HealthTranslations = {
  'health.durationHoursMinutes': '{{hours}}h {{minutes}}m',
  'health.durationMinutes': '{{minutes}}m',
}
