import type { HealthTranslations } from './types.js'

/** Health translations for Malayalam. */
export const ml: HealthTranslations = {
  'health.durationHoursMinutes': '{{hours}}മ {{minutes}}മി',
  'health.durationMinutes': '{{minutes}}മി',
}
