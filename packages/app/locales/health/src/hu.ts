import type { HealthTranslations } from './types.js'

/** Health translations for Hungarian. */
export const hu: HealthTranslations = {
  'health.durationHoursMinutes': '{{hours}}ó {{minutes}}p',
  'health.durationMinutes': '{{minutes}}p',
}
