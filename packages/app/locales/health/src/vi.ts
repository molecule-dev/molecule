import type { HealthTranslations } from './types.js'

/** Health translations for vi. */
export const vi: Partial<HealthTranslations> = {
  'health.durationMinutes': '{{minutes}}p',
  'health.durationHoursMinutes': '{{giờ}} h<x> {{phút}}</x> m',
}
