import type { HealthTranslations } from './types.js'

/** Health translations for Tamil. */
export const ta: HealthTranslations = {
  'health.durationHoursMinutes': '{{hours}}ம {{minutes}}நி',
  'health.durationMinutes': '{{minutes}}நி',
}
