import type { HealthTranslations } from './types.js'

/** Health translations for Lao. */
export const lo: HealthTranslations = {
  'health.durationHoursMinutes': '{{hours}}ຊມ {{minutes}}ນ',
  'health.durationMinutes': '{{minutes}}ນ',
}
