import type { HealthTranslations } from './types.js'

/** Health translations for hi. */
export const hi: Partial<HealthTranslations> = {
  'health.durationMinutes': '{{minutes}}मि',
  'health.durationHoursMinutes': '{{hours}} एच {{minutes}} एम',
}
