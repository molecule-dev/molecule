import type { HealthTranslations } from './types.js'

/** Health translations for Ukrainian. */
export const uk: HealthTranslations = {
  'health.durationHoursMinutes': '{{hours}}год {{minutes}}хв',
  'health.durationMinutes': '{{minutes}}хв',
}
