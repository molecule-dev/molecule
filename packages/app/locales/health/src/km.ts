import type { HealthTranslations } from './types.js'

/** Health translations for Khmer. */
export const km: HealthTranslations = {
  'health.durationHoursMinutes': '{{hours}}ម៉ {{minutes}}ន',
  'health.durationMinutes': '{{minutes}}ន',
}
