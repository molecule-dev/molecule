import type { HealthTranslations } from './types.js'

/** Health translations for Urdu. */
export const ur: HealthTranslations = {
  'health.durationHoursMinutes': '{{hours}}گھ {{minutes}}م',
  'health.durationMinutes': '{{minutes}}م',
}
