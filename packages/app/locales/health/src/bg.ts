import type { HealthTranslations } from './types.js'

/** Health translations for Bulgarian. */
export const bg: HealthTranslations = {
  'health.durationHoursMinutes': '{{hours}}ч {{minutes}}м',
  'health.durationMinutes': '{{minutes}}м',
}
