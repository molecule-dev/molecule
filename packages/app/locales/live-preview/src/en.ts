import type { LivePreviewTranslations } from './types.js'

/** Live Preview translations for English. */
export const en: LivePreviewTranslations = {
  'livePreview.error.noProvider':
    'Live preview provider not configured. Bond a live preview provider first.',
}
