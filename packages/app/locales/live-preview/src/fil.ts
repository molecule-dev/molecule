import type { LivePreviewTranslations } from './types.js'

/** Live Preview translations for Filipino. */
export const fil: LivePreviewTranslations = {
  'livePreview.error.noProvider':
    'Hindi na-configure ang live preview provider. I-bond muna ang isang live preview provider.',
}
