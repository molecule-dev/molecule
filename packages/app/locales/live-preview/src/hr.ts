import type { LivePreviewTranslations } from './types.js'

/** Live Preview translations for Croatian. */
export const hr: LivePreviewTranslations = {
  'livePreview.error.noProvider':
    'Davatelj pregleda uživo nije konfiguriran. Prvo povežite davatelja pregleda uživo.',
}
