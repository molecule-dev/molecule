import type { LivePreviewTranslations } from './types.js'

/** Live Preview translations for Turkish. */
export const tr: LivePreviewTranslations = {
  'livePreview.error.noProvider':
    'Canlı önizleme sağlayıcısı yapılandırılmamış. Önce bir canlı önizleme sağlayıcısı bağlayın.',
}
