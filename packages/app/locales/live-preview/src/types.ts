/** Translation keys for the live-preview locale package. */
export type LivePreviewTranslationKey = 'livePreview.error.noProvider'

/** Translation record mapping live-preview keys to translated strings. */
export type LivePreviewTranslations = Record<LivePreviewTranslationKey, string>
