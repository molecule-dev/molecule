import type { DayTimelineTranslations } from './types.js'

/** DayTimeline translations for ca. */
export const ca: Partial<DayTimelineTranslations> = {
  'dayTimeline.group': 'Cronologia del dia',
}
