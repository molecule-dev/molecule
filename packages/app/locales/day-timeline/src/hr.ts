import type { DayTimelineTranslations } from './types.js'

/** DayTimeline translations for hr. */
export const hr: Partial<DayTimelineTranslations> = {
  'dayTimeline.group': 'Vremenska crta dana',
}
