import type { DayTimelineTranslations } from './types.js'

/** DayTimeline translations for it. */
export const it: Partial<DayTimelineTranslations> = {
  'dayTimeline.group': 'Timeline giornaliera',
}
