import type { DayTimelineTranslations } from './types.js'

/** DayTimeline translations for tr. */
export const tr: Partial<DayTimelineTranslations> = {
  'dayTimeline.group': 'Gün zaman çizelgesi',
}
