import type { DayTimelineTranslations } from './types.js'

/** DayTimeline translations for lt. */
export const lt: Partial<DayTimelineTranslations> = {
  'dayTimeline.group': 'Dienos laiko juosta',
}
