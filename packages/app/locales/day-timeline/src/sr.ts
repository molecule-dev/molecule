import type { DayTimelineTranslations } from './types.js'

/** DayTimeline translations for sr. */
export const sr: Partial<DayTimelineTranslations> = {
  'dayTimeline.group': 'Дневна временска линија',
}
