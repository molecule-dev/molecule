import type { DayTimelineTranslations } from './types.js'

/** DayTimeline translations for mk. */
export const mk: Partial<DayTimelineTranslations> = {
  'dayTimeline.group': 'Дневна временска рамка',
}
