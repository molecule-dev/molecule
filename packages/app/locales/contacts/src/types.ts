/** Translation keys for the contacts locale package. */
export type ContactsTranslationKey = 'contacts.unknown'

/** Translation record mapping contacts keys to translated strings. */
export type ContactsTranslations = Record<ContactsTranslationKey, string>
