import type { EmbedSnippetTranslations } from './types.js'
import { en } from './en.js'

/** Embed-snippet translations for sr — currently mirrors English (translation TBD). */
export const sr: EmbedSnippetTranslations = { ...en }
