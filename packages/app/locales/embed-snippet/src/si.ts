import type { EmbedSnippetTranslations } from './types.js'
import { en } from './en.js'

/** Embed-snippet translations for si — currently mirrors English (translation TBD). */
export const si: EmbedSnippetTranslations = { ...en }
