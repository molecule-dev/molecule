import type { EmbedSnippetTranslations } from './types.js'
import { en } from './en.js'

/** Embed-snippet translations for hr — currently mirrors English (translation TBD). */
export const hr: EmbedSnippetTranslations = { ...en }
