import type { EmbedSnippetTranslations } from './types.js'
import { en } from './en.js'

/** Embed-snippet translations for kk — currently mirrors English (translation TBD). */
export const kk: EmbedSnippetTranslations = { ...en }
