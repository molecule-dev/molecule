import type { EmbedSnippetTranslations } from './types.js'
import { en } from './en.js'

/** Embed-snippet translations for kn — currently mirrors English (translation TBD). */
export const kn: EmbedSnippetTranslations = { ...en }
