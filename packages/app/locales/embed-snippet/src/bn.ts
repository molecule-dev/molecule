import type { EmbedSnippetTranslations } from './types.js'
import { en } from './en.js'

/** Embed-snippet translations for bn — currently mirrors English (translation TBD). */
export const bn: EmbedSnippetTranslations = { ...en }
