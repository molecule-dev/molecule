import type { EmbedSnippetTranslations } from './types.js'
import { en } from './en.js'

/** Embed-snippet translations for mn — currently mirrors English (translation TBD). */
export const mn: EmbedSnippetTranslations = { ...en }
