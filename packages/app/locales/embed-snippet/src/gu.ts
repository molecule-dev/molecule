import type { EmbedSnippetTranslations } from './types.js'
import { en } from './en.js'

/** Embed-snippet translations for gu — currently mirrors English (translation TBD). */
export const gu: EmbedSnippetTranslations = { ...en }
