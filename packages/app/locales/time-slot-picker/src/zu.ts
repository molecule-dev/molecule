import type { TimeSlotPickerTranslations } from './types.js'

/** TimeSlotPicker translations for zu. */
export const zu: Partial<TimeSlotPickerTranslations> = {
  'timeSlot.aria': 'Izikhala zesikhathi',
}
