import type { TimeSlotPickerTranslations } from './types.js'

/** TimeSlotPicker translations for sw. */
export const sw: Partial<TimeSlotPickerTranslations> = {
  'timeSlot.aria': 'Nafasi za muda',
}
