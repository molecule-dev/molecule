import type { TimeSlotPickerTranslations } from './types.js'

/** TimeSlotPicker translations for sr. */
export const sr: Partial<TimeSlotPickerTranslations> = {
  'timeSlot.aria': 'Временски слотови',
}
