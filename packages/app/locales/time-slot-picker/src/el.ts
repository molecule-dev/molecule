import type { TimeSlotPickerTranslations } from './types.js'

/** TimeSlotPicker translations for el. */
export const el: Partial<TimeSlotPickerTranslations> = {
  'timeSlot.aria': 'Χρονικά διαστήματα',
}
