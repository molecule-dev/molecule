import type { TimeSlotPickerTranslations } from './types.js'

/** TimeSlotPicker translations for ar. */
export const ar: Partial<TimeSlotPickerTranslations> = {
  'timeSlot.aria': 'الفترات الزمنية',
}
