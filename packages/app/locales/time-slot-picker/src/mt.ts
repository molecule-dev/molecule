import type { TimeSlotPickerTranslations } from './types.js'

/** TimeSlotPicker translations for mt. */
export const mt: Partial<TimeSlotPickerTranslations> = {
  'timeSlot.aria': 'Slots ta&#39; ħin',
}
