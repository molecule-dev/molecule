import type { TimeSlotPickerTranslations } from './types.js'

/** TimeSlotPicker translations for ka. */
export const ka: Partial<TimeSlotPickerTranslations> = {
  'timeSlot.aria': 'დროის მონაკვეთები',
}
