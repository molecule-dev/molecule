import type { TimeSlotPickerTranslations } from './types.js'

/** TimeSlotPicker translations for lt. */
export const lt: Partial<TimeSlotPickerTranslations> = {
  'timeSlot.aria': 'Laiko intervalai',
}
