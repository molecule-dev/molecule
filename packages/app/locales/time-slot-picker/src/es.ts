import type { TimeSlotPickerTranslations } from './types.js'

/** TimeSlotPicker translations for es. */
export const es: Partial<TimeSlotPickerTranslations> = {
  'timeSlot.aria': 'franjas horarias',
}
