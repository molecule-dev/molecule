import type { TimeSlotPickerTranslations } from './types.js'

/** TimeSlotPicker translations for fil. */
export const fil: Partial<TimeSlotPickerTranslations> = {
  'timeSlot.aria': 'Mga puwang ng oras',
}
