import type { TimeSlotPickerTranslations } from './types.js'

/** TimeSlotPicker translations for te. */
export const te: Partial<TimeSlotPickerTranslations> = {
  'timeSlot.aria': 'సమయ స్లాట్‌లు',
}
