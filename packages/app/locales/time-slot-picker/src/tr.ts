import type { TimeSlotPickerTranslations } from './types.js'

/** TimeSlotPicker translations for tr. */
export const tr: Partial<TimeSlotPickerTranslations> = {
  'timeSlot.aria': 'Zaman dilimleri',
}
