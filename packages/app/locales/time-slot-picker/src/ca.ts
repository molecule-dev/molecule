import type { TimeSlotPickerTranslations } from './types.js'

/** TimeSlotPicker translations for ca. */
export const ca: Partial<TimeSlotPickerTranslations> = {
  'timeSlot.aria': 'Franges horàries',
}
