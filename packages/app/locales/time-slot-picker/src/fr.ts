import type { TimeSlotPickerTranslations } from './types.js'

/** TimeSlotPicker translations for fr. */
export const fr: Partial<TimeSlotPickerTranslations> = {
  'timeSlot.aria': 'créneaux horaires',
}
