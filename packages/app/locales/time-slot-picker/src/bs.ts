import type { TimeSlotPickerTranslations } from './types.js'

/** TimeSlotPicker translations for bs. */
export const bs: Partial<TimeSlotPickerTranslations> = {
  'timeSlot.aria': 'Vremenski termini',
}
