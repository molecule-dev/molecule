import type { TimeSlotPickerTranslations } from './types.js'

/** TimeSlotPicker translations for sl. */
export const sl: Partial<TimeSlotPickerTranslations> = {
  'timeSlot.aria': 'Časovni intervali',
}
