import type { CarouselTranslations } from './types.js'

/** Carousel translations for it. */
export const it: Partial<CarouselTranslations> = {
  'carousel.previous': 'Precedente',
  'carousel.next': 'Avanti',
}
