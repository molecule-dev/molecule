import type { AnnotationPinTranslations } from './types.js'
import { en } from './en.js'

/** Annotation-pin translations for hi — currently mirrors English (translation TBD). */
export const hi: AnnotationPinTranslations = { ...en }
