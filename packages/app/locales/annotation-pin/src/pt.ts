import type { AnnotationPinTranslations } from './types.js'
import { en } from './en.js'

/** Annotation-pin translations for pt — currently mirrors English (translation TBD). */
export const pt: AnnotationPinTranslations = { ...en }
