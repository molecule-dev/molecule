import type { AnnotationPinTranslations } from './types.js'
import { en } from './en.js'

/** Annotation-pin translations for es — currently mirrors English (translation TBD). */
export const es: AnnotationPinTranslations = { ...en }
