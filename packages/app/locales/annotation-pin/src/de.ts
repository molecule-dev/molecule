import type { AnnotationPinTranslations } from './types.js'
import { en } from './en.js'

/** Annotation-pin translations for de — currently mirrors English (translation TBD). */
export const de: AnnotationPinTranslations = { ...en }
