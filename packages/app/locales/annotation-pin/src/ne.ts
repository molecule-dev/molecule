import type { AnnotationPinTranslations } from './types.js'
import { en } from './en.js'

/** Annotation-pin translations for ne — currently mirrors English (translation TBD). */
export const ne: AnnotationPinTranslations = { ...en }
