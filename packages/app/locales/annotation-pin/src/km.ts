import type { AnnotationPinTranslations } from './types.js'
import { en } from './en.js'

/** Annotation-pin translations for km — currently mirrors English (translation TBD). */
export const km: AnnotationPinTranslations = { ...en }
