import type { AnnotationPinTranslations } from './types.js'
import { en } from './en.js'

/** Annotation-pin translations for bg — currently mirrors English (translation TBD). */
export const bg: AnnotationPinTranslations = { ...en }
