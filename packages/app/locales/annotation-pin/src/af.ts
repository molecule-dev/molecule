import type { AnnotationPinTranslations } from './types.js'
import { en } from './en.js'

/** Annotation-pin translations for af — currently mirrors English (translation TBD). */
export const af: AnnotationPinTranslations = { ...en }
