import type { CommonTranslations } from './types.js'

/** Common translations for sq. */
export const sq: Partial<CommonTranslations> = {
  'status.all': 'All ({{count}})',
  'status.overdue': 'Overdue Tasks',
}
