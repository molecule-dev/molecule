import type { CodeBlockTranslations } from './types.js'

/** CodeBlock translations for cs. */
export const cs: Partial<CodeBlockTranslations> = {
  'codeBlock.copied': 'Zkopírováno!',
  'codeBlock.copy': 'Kopírovat',
}
