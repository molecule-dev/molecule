import type { CodeBlockTranslations } from './types.js'

/** CodeBlock translations for it. */
export const it: Partial<CodeBlockTranslations> = {
  'codeBlock.copied': 'Copiato!',
  'codeBlock.copy': 'Copia',
}
