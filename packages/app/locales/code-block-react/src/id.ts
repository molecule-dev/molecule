import type { CodeBlockTranslations } from './types.js'

/** CodeBlock translations for id. */
export const id: Partial<CodeBlockTranslations> = {
  'codeBlock.copied': 'Disalin!',
  'codeBlock.copy': 'Salin',
}
