import type { CodeBlockTranslations } from './types.js'

/** CodeBlock translations for de. */
export const de: Partial<CodeBlockTranslations> = {
  'codeBlock.copied': 'Kopiert!',
  'codeBlock.copy': 'Kopieren',
}
