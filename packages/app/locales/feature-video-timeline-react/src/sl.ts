import type { VideoTimelineTranslations } from './types.js'
import { en } from './en.js'

/** Video-timeline translations for sl — currently mirrors English (translation TBD). */
export const sl: VideoTimelineTranslations = { ...en }
