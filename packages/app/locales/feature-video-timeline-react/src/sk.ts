import type { VideoTimelineTranslations } from './types.js'
import { en } from './en.js'

/** Video-timeline translations for sk — currently mirrors English (translation TBD). */
export const sk: VideoTimelineTranslations = { ...en }
