import type { VideoTimelineTranslations } from './types.js'
import { en } from './en.js'

/** Video-timeline translations for kk — currently mirrors English (translation TBD). */
export const kk: VideoTimelineTranslations = { ...en }
