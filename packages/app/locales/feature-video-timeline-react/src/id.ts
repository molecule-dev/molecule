import type { VideoTimelineTranslations } from './types.js'
import { en } from './en.js'

/** Video-timeline translations for id — currently mirrors English (translation TBD). */
export const id: VideoTimelineTranslations = { ...en }
