import type { VideoTimelineTranslations } from './types.js'
import { en } from './en.js'

/** Video-timeline translations for de — currently mirrors English (translation TBD). */
export const de: VideoTimelineTranslations = { ...en }
