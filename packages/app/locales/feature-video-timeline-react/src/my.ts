import type { VideoTimelineTranslations } from './types.js'
import { en } from './en.js'

/** Video-timeline translations for my — currently mirrors English (translation TBD). */
export const my: VideoTimelineTranslations = { ...en }
