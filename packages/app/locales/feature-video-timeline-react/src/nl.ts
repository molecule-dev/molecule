import type { VideoTimelineTranslations } from './types.js'
import { en } from './en.js'

/** Video-timeline translations for nl — currently mirrors English (translation TBD). */
export const nl: VideoTimelineTranslations = { ...en }
