import type { VideoTimelineTranslations } from './types.js'
import { en } from './en.js'

/** Video-timeline translations for tr — currently mirrors English (translation TBD). */
export const tr: VideoTimelineTranslations = { ...en }
