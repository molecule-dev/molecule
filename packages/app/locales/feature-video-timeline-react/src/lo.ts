import type { VideoTimelineTranslations } from './types.js'
import { en } from './en.js'

/** Video-timeline translations for lo — currently mirrors English (translation TBD). */
export const lo: VideoTimelineTranslations = { ...en }
