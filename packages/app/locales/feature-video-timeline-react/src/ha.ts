import type { VideoTimelineTranslations } from './types.js'
import { en } from './en.js'

/** Video-timeline translations for ha — currently mirrors English (translation TBD). */
export const ha: VideoTimelineTranslations = { ...en }
