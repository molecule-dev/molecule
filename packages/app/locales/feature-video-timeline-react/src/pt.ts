import type { VideoTimelineTranslations } from './types.js'
import { en } from './en.js'

/** Video-timeline translations for pt — currently mirrors English (translation TBD). */
export const pt: VideoTimelineTranslations = { ...en }
