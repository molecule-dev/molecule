import type { VideoTimelineTranslations } from './types.js'
import { en } from './en.js'

/** Video-timeline translations for hi — currently mirrors English (translation TBD). */
export const hi: VideoTimelineTranslations = { ...en }
