import type { VideoTimelineTranslations } from './types.js'
import { en } from './en.js'

/** Video-timeline translations for bg — currently mirrors English (translation TBD). */
export const bg: VideoTimelineTranslations = { ...en }
