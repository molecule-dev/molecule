import type { VideoTimelineTranslations } from './types.js'
import { en } from './en.js'

/** Video-timeline translations for it — currently mirrors English (translation TBD). */
export const it: VideoTimelineTranslations = { ...en }
