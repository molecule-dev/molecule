import type { VideoTimelineTranslations } from './types.js'
import { en } from './en.js'

/** Video-timeline translations for es — currently mirrors English (translation TBD). */
export const es: VideoTimelineTranslations = { ...en }
