import type { VideoTimelineTranslations } from './types.js'
import { en } from './en.js'

/** Video-timeline translations for mk — currently mirrors English (translation TBD). */
export const mk: VideoTimelineTranslations = { ...en }
