import type { VideoTimelineTranslations } from './types.js'
import { en } from './en.js'

/** Video-timeline translations for ml — currently mirrors English (translation TBD). */
export const ml: VideoTimelineTranslations = { ...en }
