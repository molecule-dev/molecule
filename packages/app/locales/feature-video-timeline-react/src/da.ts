import type { VideoTimelineTranslations } from './types.js'
import { en } from './en.js'

/** Video-timeline translations for da — currently mirrors English (translation TBD). */
export const da: VideoTimelineTranslations = { ...en }
