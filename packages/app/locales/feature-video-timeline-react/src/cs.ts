import type { VideoTimelineTranslations } from './types.js'
import { en } from './en.js'

/** Video-timeline translations for cs — currently mirrors English (translation TBD). */
export const cs: VideoTimelineTranslations = { ...en }
