import type { VideoTimelineTranslations } from './types.js'
import { en } from './en.js'

/** Video-timeline translations for am — currently mirrors English (translation TBD). */
export const am: VideoTimelineTranslations = { ...en }
