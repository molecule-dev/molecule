import type { VideoTimelineTranslations } from './types.js'
import { en } from './en.js'

/** Video-timeline translations for ms — currently mirrors English (translation TBD). */
export const ms: VideoTimelineTranslations = { ...en }
