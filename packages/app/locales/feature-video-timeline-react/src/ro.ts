import type { VideoTimelineTranslations } from './types.js'
import { en } from './en.js'

/** Video-timeline translations for ro — currently mirrors English (translation TBD). */
export const ro: VideoTimelineTranslations = { ...en }
