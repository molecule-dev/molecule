import type { VideoTimelineTranslations } from './types.js'
import { en } from './en.js'

/** Video-timeline translations for zh — currently mirrors English (translation TBD). */
export const zh: VideoTimelineTranslations = { ...en }
