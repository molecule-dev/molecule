import type { VideoTimelineTranslations } from './types.js'
import { en } from './en.js'

/** Video-timeline translations for af — currently mirrors English (translation TBD). */
export const af: VideoTimelineTranslations = { ...en }
