import type { VideoTimelineTranslations } from './types.js'
import { en } from './en.js'

/** Video-timeline translations for bn — currently mirrors English (translation TBD). */
export const bn: VideoTimelineTranslations = { ...en }
