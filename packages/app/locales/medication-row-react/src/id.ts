import type { MedicationRowTranslations } from './types.js'

/** MedicationRow translations for id. */
export const id: Partial<MedicationRowTranslations> = {
  'medication.prescribedBy': 'Diresepkan oleh',
}
