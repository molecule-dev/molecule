import type { MedicationRowTranslations } from './types.js'

/** MedicationRow translations for vi. */
export const vi: Partial<MedicationRowTranslations> = {
  'medication.prescribedBy': 'Được kê đơn bởi',
}
