import type { MedicationRowTranslations } from './types.js'

/** MedicationRow translations for fr. */
export const fr: Partial<MedicationRowTranslations> = {
  'medication.prescribedBy': 'Prescrit par',
}
