import type { MedicationRowTranslations } from './types.js'

/** MedicationRow translations for sv. */
export const sv: Partial<MedicationRowTranslations> = {
  'medication.prescribedBy': 'Förskriven av',
}
