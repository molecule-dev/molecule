import type { HeatmapTranslations } from './types.js'

/** Heatmap translations for pl. */
export const pl: Partial<HeatmapTranslations> = {
  'heatmap.weekday.sun': 'Niedz',
  'heatmap.weekday.sat': 'Sob',
}
