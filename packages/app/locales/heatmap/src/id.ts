import type { HeatmapTranslations } from './types.js'

/** Heatmap translations for id. */
export const id: Partial<HeatmapTranslations> = {
  'heatmap.weekday.sun': 'Min',
  'heatmap.weekday.sat': 'Sab',
}
