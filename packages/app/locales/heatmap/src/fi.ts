import type { HeatmapTranslations } from './types.js'

/** Heatmap translations for fi. */
export const fi: Partial<HeatmapTranslations> = {
  'heatmap.weekday.sun': 'Su',
  'heatmap.weekday.sat': 'La',
}
