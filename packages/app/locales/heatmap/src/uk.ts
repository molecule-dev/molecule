import type { HeatmapTranslations } from './types.js'

/** Heatmap translations for uk. */
export const uk: Partial<HeatmapTranslations> = {
  'heatmap.weekday.sun': 'Нд',
  'heatmap.weekday.sat': 'Сб',
}
