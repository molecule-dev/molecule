import type { HeatmapTranslations } from './types.js'

/** Heatmap translations for da. */
export const da: Partial<HeatmapTranslations> = {
  'heatmap.weekday.sun': 'Søn',
  'heatmap.weekday.sat': 'Lør',
}
