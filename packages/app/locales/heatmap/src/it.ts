import type { HeatmapTranslations } from './types.js'

/** Heatmap translations for it. */
export const it: Partial<HeatmapTranslations> = {
  'heatmap.weekday.sun': 'Dom',
  'heatmap.weekday.sat': 'Sab',
}
