import type { HeatmapTranslations } from './types.js'

/** Heatmap translations for fr. */
export const fr: Partial<HeatmapTranslations> = {
  'heatmap.weekday.sun': 'Dim',
  'heatmap.weekday.sat': 'Sam',
}
