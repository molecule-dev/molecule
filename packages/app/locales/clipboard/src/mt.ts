import type { ClipboardTranslations } from './types.js'

/** Clipboard translations for mt. */
export const mt: Partial<ClipboardTranslations> = {
  'clipboard.warn.onChangeNotSupported':
    '@molecule/app-clipboard: onChange mhux appoġġjat mill-fornitur',
  'clipboard.error.noProvider':
    "@molecule/app-clipboard: L-ebda fornitur ma ġie stabbilit. Sejħa setProvider() b'implimentazzjoni ta' ClipboardProvider (eż., minn @molecule/app-clipboard-capacitor).",
}
