import type { ClipboardTranslations } from './types.js'

/** Clipboard translations for Persian. */
export const fa: ClipboardTranslations = {
  'clipboard.error.noProvider':
    '@molecule/app-clipboard: هیچ ارائه‌دهنده‌ای تنظیم نشده است. setProvider() را با یک پیاده‌سازی ClipboardProvider فراخوانی کنید (مثلاً از @molecule/app-clipboard-capacitor).',
  'clipboard.warn.onChangeNotSupported':
    '@molecule/app-clipboard: onChange توسط ارائه‌دهنده پشتیبانی نمی‌شود',
}
