import type { ClipboardTranslations } from './types.js'

/** Clipboard translations for Bengali. */
export const bn: ClipboardTranslations = {
  'clipboard.error.noProvider':
    '@molecule/app-clipboard: কোনো প্রদানকারী সেট করা হয়নি। একটি ClipboardProvider বাস্তবায়ন দিয়ে setProvider() কল করুন (যেমন @molecule/app-clipboard-capacitor থেকে)।',
  'clipboard.warn.onChangeNotSupported':
    '@molecule/app-clipboard: onChange প্রদানকারী দ্বারা সমর্থিত নয়',
}
