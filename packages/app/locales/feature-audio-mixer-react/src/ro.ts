import type { AudioMixerTranslations } from './types.js'
import { en } from './en.js'

/** Audio-mixer translations for ro — currently mirrors English (translation TBD). */
export const ro: AudioMixerTranslations = { ...en }
