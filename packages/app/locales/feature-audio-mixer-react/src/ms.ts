import type { AudioMixerTranslations } from './types.js'
import { en } from './en.js'

/** Audio-mixer translations for ms — currently mirrors English (translation TBD). */
export const ms: AudioMixerTranslations = { ...en }
