import type { AudioMixerTranslations } from './types.js'
import { en } from './en.js'

/** Audio-mixer translations for pl — currently mirrors English (translation TBD). */
export const pl: AudioMixerTranslations = { ...en }
