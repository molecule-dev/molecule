import type { AudioMixerTranslations } from './types.js'
import { en } from './en.js'

/** Audio-mixer translations for fil — currently mirrors English (translation TBD). */
export const fil: AudioMixerTranslations = { ...en }
