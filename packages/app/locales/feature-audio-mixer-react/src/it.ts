import type { AudioMixerTranslations } from './types.js'
import { en } from './en.js'

/** Audio-mixer translations for it — currently mirrors English (translation TBD). */
export const it: AudioMixerTranslations = { ...en }
