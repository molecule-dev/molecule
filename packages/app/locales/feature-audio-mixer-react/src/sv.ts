import type { AudioMixerTranslations } from './types.js'
import { en } from './en.js'

/** Audio-mixer translations for sv — currently mirrors English (translation TBD). */
export const sv: AudioMixerTranslations = { ...en }
