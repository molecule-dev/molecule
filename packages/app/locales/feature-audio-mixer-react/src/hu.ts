import type { AudioMixerTranslations } from './types.js'
import { en } from './en.js'

/** Audio-mixer translations for hu — currently mirrors English (translation TBD). */
export const hu: AudioMixerTranslations = { ...en }
