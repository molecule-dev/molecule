import type { AudioMixerTranslations } from './types.js'
import { en } from './en.js'

/** Audio-mixer translations for hr — currently mirrors English (translation TBD). */
export const hr: AudioMixerTranslations = { ...en }
