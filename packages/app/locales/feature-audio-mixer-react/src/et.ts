import type { AudioMixerTranslations } from './types.js'
import { en } from './en.js'

/** Audio-mixer translations for et — currently mirrors English (translation TBD). */
export const et: AudioMixerTranslations = { ...en }
