import type { AudioMixerTranslations } from './types.js'
import { en } from './en.js'

/** Audio-mixer translations for ta — currently mirrors English (translation TBD). */
export const ta: AudioMixerTranslations = { ...en }
