import type { AudioMixerTranslations } from './types.js'
import { en } from './en.js'

/** Audio-mixer translations for mt — currently mirrors English (translation TBD). */
export const mt: AudioMixerTranslations = { ...en }
