import type { AudioMixerTranslations } from './types.js'
import { en } from './en.js'

/** Audio-mixer translations for es — currently mirrors English (translation TBD). */
export const es: AudioMixerTranslations = { ...en }
