import type { AudioMixerTranslations } from './types.js'
import { en } from './en.js'

/** Audio-mixer translations for ky — currently mirrors English (translation TBD). */
export const ky: AudioMixerTranslations = { ...en }
