import type { AudioMixerTranslations } from './types.js'
import { en } from './en.js'

/** Audio-mixer translations for da — currently mirrors English (translation TBD). */
export const da: AudioMixerTranslations = { ...en }
