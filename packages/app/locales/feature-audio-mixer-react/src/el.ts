import type { AudioMixerTranslations } from './types.js'
import { en } from './en.js'

/** Audio-mixer translations for el — currently mirrors English (translation TBD). */
export const el: AudioMixerTranslations = { ...en }
