import type { AudioMixerTranslations } from './types.js'
import { en } from './en.js'

/** Audio-mixer translations for ga — currently mirrors English (translation TBD). */
export const ga: AudioMixerTranslations = { ...en }
