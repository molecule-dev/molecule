import type { AudioMixerTranslations } from './types.js'
import { en } from './en.js'

/** Audio-mixer translations for ne — currently mirrors English (translation TBD). */
export const ne: AudioMixerTranslations = { ...en }
