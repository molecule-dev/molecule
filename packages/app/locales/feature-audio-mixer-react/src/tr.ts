import type { AudioMixerTranslations } from './types.js'
import { en } from './en.js'

/** Audio-mixer translations for tr — currently mirrors English (translation TBD). */
export const tr: AudioMixerTranslations = { ...en }
