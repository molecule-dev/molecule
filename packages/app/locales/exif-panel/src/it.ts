import type { ExifPanelTranslations } from './types.js'

/** ExifPanel translations for it. */
export const it: Partial<ExifPanelTranslations> = {
  'exifPanel.eyebrow': 'Metadati del fotogramma',
  'exifPanel.camera': 'Fotocamera',
}
