import type { ExifPanelTranslations } from './types.js'
import { en } from './en.js'

/** Exif-panel translations for lt — currently mirrors English (translation TBD). */
export const lt: ExifPanelTranslations = { ...en }
