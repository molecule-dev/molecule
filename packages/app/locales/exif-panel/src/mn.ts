import type { ExifPanelTranslations } from './types.js'
import { en } from './en.js'

/** Exif-panel translations for mn — currently mirrors English (translation TBD). */
export const mn: ExifPanelTranslations = { ...en }
