import type { ExifPanelTranslations } from './types.js'
import { en } from './en.js'

/** Exif-panel translations for ne — currently mirrors English (translation TBD). */
export const ne: ExifPanelTranslations = { ...en }
