import type { ExifPanelTranslations } from './types.js'
import { en } from './en.js'

/** Exif-panel translations for bg — currently mirrors English (translation TBD). */
export const bg: ExifPanelTranslations = { ...en }
