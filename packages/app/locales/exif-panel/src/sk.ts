import type { ExifPanelTranslations } from './types.js'
import { en } from './en.js'

/** Exif-panel translations for sk — currently mirrors English (translation TBD). */
export const sk: ExifPanelTranslations = { ...en }
