import type { ExifPanelTranslations } from './types.js'
import { en } from './en.js'

/** Exif-panel translations for ig — currently mirrors English (translation TBD). */
export const ig: ExifPanelTranslations = { ...en }
