import type { ExifPanelTranslations } from './types.js'
import { en } from './en.js'

/** Exif-panel translations for be — currently mirrors English (translation TBD). */
export const be: ExifPanelTranslations = { ...en }
