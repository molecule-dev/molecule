import type { ExifPanelTranslations } from './types.js'
import { en } from './en.js'

/** Exif-panel translations for sq — currently mirrors English (translation TBD). */
export const sq: ExifPanelTranslations = { ...en }
