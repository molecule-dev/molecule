import type { ExifPanelTranslations } from './types.js'
import { en } from './en.js'

/** Exif-panel translations for ha — currently mirrors English (translation TBD). */
export const ha: ExifPanelTranslations = { ...en }
