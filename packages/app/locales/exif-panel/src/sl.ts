import type { ExifPanelTranslations } from './types.js'
import { en } from './en.js'

/** Exif-panel translations for sl — currently mirrors English (translation TBD). */
export const sl: ExifPanelTranslations = { ...en }
