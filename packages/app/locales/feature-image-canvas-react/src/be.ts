import type { ImageCanvasTranslations } from './types.js'
import { en } from './en.js'

/** Image-canvas translations for be — currently mirrors English (translation TBD). */
export const be: ImageCanvasTranslations = { ...en }
