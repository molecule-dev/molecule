import type { ImageCanvasTranslations } from './types.js'
import { en } from './en.js'

/** Image-canvas translations for it — currently mirrors English (translation TBD). */
export const it: ImageCanvasTranslations = { ...en }
