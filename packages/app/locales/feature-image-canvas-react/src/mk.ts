import type { ImageCanvasTranslations } from './types.js'
import { en } from './en.js'

/** Image-canvas translations for mk — currently mirrors English (translation TBD). */
export const mk: ImageCanvasTranslations = { ...en }
