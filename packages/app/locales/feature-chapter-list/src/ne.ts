import type { ChapterListTranslations } from './types.js'
import { en } from './en.js'

/** Chapter-list translations for ne — currently mirrors English (translation TBD). */
export const ne: ChapterListTranslations = { ...en }
