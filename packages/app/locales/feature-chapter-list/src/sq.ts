import type { ChapterListTranslations } from './types.js'
import { en } from './en.js'

/** Chapter-list translations for sq — currently mirrors English (translation TBD). */
export const sq: ChapterListTranslations = { ...en }
