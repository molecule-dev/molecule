import type { ChapterListTranslations } from './types.js'
import { en } from './en.js'

/** Chapter-list translations for ml — currently mirrors English (translation TBD). */
export const ml: ChapterListTranslations = { ...en }
