import type { ChapterListTranslations } from './types.js'
import { en } from './en.js'

/** Chapter-list translations for kn — currently mirrors English (translation TBD). */
export const kn: ChapterListTranslations = { ...en }
