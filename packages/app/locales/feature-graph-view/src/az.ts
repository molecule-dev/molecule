import type { GraphViewTranslations } from './types.js'

/** Graph-view translations for az (stub — falls back to English). */
export const az: GraphViewTranslations = {
  'graphView.aria.canvas': 'Graph view',
  'graphView.aria.node': 'Node {{label}}',
  'graphView.empty': 'No nodes to display',
}
