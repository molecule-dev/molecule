import type { HabitStreakCardTranslations } from './types.js'

/** HabitStreakCard translations for de. */
export const de: Partial<HabitStreakCardTranslations> = {
  'habit.currentStreak': 'Tage in Folge',
  'habit.total': 'insgesamt',
}
