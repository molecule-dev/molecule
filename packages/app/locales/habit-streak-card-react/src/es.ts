import type { HabitStreakCardTranslations } from './types.js'

/** HabitStreakCard translations for es. */
export const es: Partial<HabitStreakCardTranslations> = {
  'habit.currentStreak': 'días seguidos',
}
