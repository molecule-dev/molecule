import type { HabitStreakCardTranslations } from './types.js'

/** HabitStreakCard translations for vi. */
export const vi: Partial<HabitStreakCardTranslations> = {
  'habit.currentStreak': 'chuỗi ngày',
  'habit.total': 'tổng',
}
