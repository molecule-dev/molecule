import type { HabitStreakCardTranslations } from './types.js'

/** HabitStreakCard translations for English. */
export const en: HabitStreakCardTranslations = {
  'habit.currentStreak': 'day streak',
  'habit.bestStreak': 'best',
  'habit.total': 'total',
}
