import type { HabitStreakCardTranslations } from './types.js'

/** HabitStreakCard translations for ar. */
export const ar: Partial<HabitStreakCardTranslations> = {
  'habit.currentStreak': 'أيام متتالية',
  'habit.total': 'الإجمالي',
}
