import type { HabitStreakCardTranslations } from './types.js'

/** HabitStreakCard translations for id. */
export const id: Partial<HabitStreakCardTranslations> = {
  'habit.currentStreak': 'rangkaian hari',
}
