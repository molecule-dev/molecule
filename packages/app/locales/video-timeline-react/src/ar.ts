import type { VideoTimelineTranslations } from './types.js'

/** VideoTimeline translations for ar. */
export const ar: Partial<VideoTimelineTranslations> = {
  'videoTimeline.zoom.in': 'تكبير',
  'videoTimeline.zoom.out': 'تصغير',
}
