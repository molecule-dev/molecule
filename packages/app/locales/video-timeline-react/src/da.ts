import type { VideoTimelineTranslations } from './types.js'

/** VideoTimeline translations for da. */
export const da: Partial<VideoTimelineTranslations> = {
  'videoTimeline.zoom.in': 'Zoom ind',
  'videoTimeline.zoom.out': 'Zoom ud',
}
