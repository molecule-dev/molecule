import type { VideoTimelineTranslations } from './types.js'

/** VideoTimeline translations for nl. */
export const nl: Partial<VideoTimelineTranslations> = {
  'videoTimeline.zoom.in': 'Inzoomen',
  'videoTimeline.zoom.out': 'Uitzoomen',
}
