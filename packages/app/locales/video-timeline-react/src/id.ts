import type { VideoTimelineTranslations } from './types.js'

/** VideoTimeline translations for id. */
export const id: Partial<VideoTimelineTranslations> = {
  'videoTimeline.zoom.in': 'Perbesar',
  'videoTimeline.zoom.out': 'Perkecil',
}
