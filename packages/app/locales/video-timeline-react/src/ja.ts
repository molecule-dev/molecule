import type { VideoTimelineTranslations } from './types.js'

/** VideoTimeline translations for ja. */
export const ja: Partial<VideoTimelineTranslations> = {
  'videoTimeline.zoom.in': '拡大',
  'videoTimeline.zoom.out': '縮小',
}
