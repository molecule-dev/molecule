import type { VideoTimelineTranslations } from './types.js'

/** VideoTimeline translations for zh. */
export const zh: Partial<VideoTimelineTranslations> = {
  'videoTimeline.zoom.in': '放大',
  'videoTimeline.zoom.out': '缩小',
}
