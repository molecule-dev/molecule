import type { VideoTimelineTranslations } from './types.js'

/** VideoTimeline translations for nb. */
export const nb: Partial<VideoTimelineTranslations> = {
  'videoTimeline.zoom.in': 'Zoom inn',
  'videoTimeline.zoom.out': 'Zoom ut',
}
