import type { VideoTimelineTranslations } from './types.js'

/** VideoTimeline translations for pt. */
export const pt: Partial<VideoTimelineTranslations> = {
  'videoTimeline.zoom.in': 'Mais zoom',
  'videoTimeline.zoom.out': 'Menos zoom',
}
