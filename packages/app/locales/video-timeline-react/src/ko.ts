import type { VideoTimelineTranslations } from './types.js'

/** VideoTimeline translations for ko. */
export const ko: Partial<VideoTimelineTranslations> = {
  'videoTimeline.zoom.in': '확대',
  'videoTimeline.zoom.out': '축소',
}
