import type { AdjustmentSliderTranslations } from './types.js'
import { en } from './en.js'

/** Adjustment-slider translations for te — currently mirrors English (translation TBD). */
export const te: AdjustmentSliderTranslations = { ...en }
