import type { AdjustmentSliderTranslations } from './types.js'
import { en } from './en.js'

/** Adjustment-slider translations for it — currently mirrors English (translation TBD). */
export const it: AdjustmentSliderTranslations = { ...en }
