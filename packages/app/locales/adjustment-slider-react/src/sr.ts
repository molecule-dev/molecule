import type { AdjustmentSliderTranslations } from './types.js'
import { en } from './en.js'

/** Adjustment-slider translations for sr — currently mirrors English (translation TBD). */
export const sr: AdjustmentSliderTranslations = { ...en }
