import type { AdjustmentSliderTranslations } from './types.js'
import { en } from './en.js'

/** Adjustment-slider translations for zu — currently mirrors English (translation TBD). */
export const zu: AdjustmentSliderTranslations = { ...en }
