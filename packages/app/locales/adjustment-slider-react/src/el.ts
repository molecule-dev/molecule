import type { AdjustmentSliderTranslations } from './types.js'
import { en } from './en.js'

/** Adjustment-slider translations for el — currently mirrors English (translation TBD). */
export const el: AdjustmentSliderTranslations = { ...en }
