import type { TrackLaneTranslations } from './types.js'
import { en } from './en.js'

/** Track-lane translations for mr — currently mirrors English (translation TBD). */
export const mr: TrackLaneTranslations = { ...en }
