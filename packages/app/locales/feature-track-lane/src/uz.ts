import type { TrackLaneTranslations } from './types.js'
import { en } from './en.js'

/** Track-lane translations for uz — currently mirrors English (translation TBD). */
export const uz: TrackLaneTranslations = { ...en }
