import type { TrackLaneTranslations } from './types.js'

/** FeatureTrackLane translations for nb. */
export const nb: Partial<TrackLaneTranslations> = {
  'trackLane.aria.lane': 'Sporfelt {{name}}',
  'trackLane.aria.clip': 'Klipp {{label}} starter kl. {{startTime}} s for {{duration}} s',
  'trackLane.aria.resize': 'Endre størrelse på klippet',
  'trackLane.header': 'Spor',
}
