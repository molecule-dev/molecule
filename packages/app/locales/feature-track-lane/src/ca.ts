import type { TrackLaneTranslations } from './types.js'

/** FeatureTrackLane translations for ca. */
export const ca: Partial<TrackLaneTranslations> = {
  'trackLane.aria.lane': 'Carril de pista {{name}}',
  'trackLane.aria.clip': 'Clip {{label}} començant a {{startTime}} s per {{duration}} s',
  'trackLane.aria.resize': 'Canvia la mida del clip',
  'trackLane.header': 'Pista',
}
