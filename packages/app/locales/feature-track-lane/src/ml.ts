import type { TrackLaneTranslations } from './types.js'
import { en } from './en.js'

/** Track-lane translations for ml — currently mirrors English (translation TBD). */
export const ml: TrackLaneTranslations = { ...en }
