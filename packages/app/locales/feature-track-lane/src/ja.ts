import type { TrackLaneTranslations } from './types.js'

/** FeatureTrackLane translations for ja. */
export const ja: Partial<TrackLaneTranslations> = {
  'trackLane.header': '追跡',
  'trackLane.aria.lane': 'トラックレーン{{名前}}',
  'trackLane.aria.clip': 'クリップ{{ラベル}}開始<x>開始時間</x>s は{{間隔}} s',
  'trackLane.aria.resize': 'クリップのサイズを変更する',
}
