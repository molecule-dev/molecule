import type { TrackLaneTranslations } from './types.js'
import { en } from './en.js'

/** Track-lane translations for bn — currently mirrors English (translation TBD). */
export const bn: TrackLaneTranslations = { ...en }
