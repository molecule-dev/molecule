import type { TrackLaneTranslations } from './types.js'

/** FeatureTrackLane translations for it. */
export const it: Partial<TrackLaneTranslations> = {
  'trackLane.header': 'Traccia',
  'trackLane.aria.lane': 'Corsia di atletica {{name}}',
  'trackLane.aria.clip': 'Clip {{label}} a partire da {{startTime}} s per {{duration}} S',
  'trackLane.aria.resize': 'Ridimensiona la clip',
}
