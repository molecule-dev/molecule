import type { TrackLaneTranslations } from './types.js'
import { en } from './en.js'

/** Track-lane translations for ha — currently mirrors English (translation TBD). */
export const ha: TrackLaneTranslations = { ...en }
