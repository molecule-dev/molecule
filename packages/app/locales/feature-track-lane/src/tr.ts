import type { TrackLaneTranslations } from './types.js'

/** FeatureTrackLane translations for tr. */
export const tr: Partial<TrackLaneTranslations> = {
  'trackLane.header': 'Takip et',
  'trackLane.aria.lane': 'Pist şeridi<x> {{isim}}</x>',
  'trackLane.aria.clip':
    'Klip<x> {{etiket}}</x> başlangıç<x> {{startTime}}</x> s için<x> {{süre}}</x> S',
  'trackLane.aria.resize': 'Klip boyutunu değiştir',
}
