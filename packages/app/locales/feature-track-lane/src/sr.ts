import type { TrackLaneTranslations } from './types.js'
import { en } from './en.js'

/** Track-lane translations for sr — currently mirrors English (translation TBD). */
export const sr: TrackLaneTranslations = { ...en }
