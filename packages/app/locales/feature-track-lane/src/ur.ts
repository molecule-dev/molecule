import type { TrackLaneTranslations } from './types.js'
import { en } from './en.js'

/** Track-lane translations for ur — currently mirrors English (translation TBD). */
export const ur: TrackLaneTranslations = { ...en }
