import type { CanvasEngineTranslations } from './types.js'

/** Canvas-engine translations for Czech. Stub — falls back to English. */
export const cs: CanvasEngineTranslations = {
  'canvasEngine.aria.surface': 'Vector canvas editor',
}
