import type { CanvasEngineTranslations } from './types.js'

/** Canvas-engine translations for Hungarian. Stub — falls back to English. */
export const hu: CanvasEngineTranslations = {
  'canvasEngine.aria.surface': 'Vector canvas editor',
}
