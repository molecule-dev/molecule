import type { CanvasEngineTranslations } from './types.js'

/** Canvas-engine translations for Lao. Stub — falls back to English. */
export const lo: CanvasEngineTranslations = {
  'canvasEngine.aria.surface': 'Vector canvas editor',
}
