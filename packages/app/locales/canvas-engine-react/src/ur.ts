import type { CanvasEngineTranslations } from './types.js'

/** Canvas-engine translations for Urdu. Stub — falls back to English. */
export const ur: CanvasEngineTranslations = {
  'canvasEngine.aria.surface': 'Vector canvas editor',
}
