import type { CanvasEngineTranslations } from './types.js'

/** Canvas-engine translations for Zulu. Stub — falls back to English. */
export const zu: CanvasEngineTranslations = {
  'canvasEngine.aria.surface': 'Vector canvas editor',
}
