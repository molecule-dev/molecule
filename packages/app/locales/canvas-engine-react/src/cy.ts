import type { CanvasEngineTranslations } from './types.js'

/** Canvas-engine translations for Welsh. Stub — falls back to English. */
export const cy: CanvasEngineTranslations = {
  'canvasEngine.aria.surface': 'Vector canvas editor',
}
