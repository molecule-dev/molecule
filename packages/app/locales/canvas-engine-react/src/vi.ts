import type { CanvasEngineTranslations } from './types.js'

/** Canvas-engine translations for Vietnamese. Stub — falls back to English. */
export const vi: CanvasEngineTranslations = {
  'canvasEngine.aria.surface': 'Vector canvas editor',
}
