import type { CanvasEngineTranslations } from './types.js'

/** Canvas-engine translations for Nepali. Stub — falls back to English. */
export const ne: CanvasEngineTranslations = {
  'canvasEngine.aria.surface': 'Vector canvas editor',
}
