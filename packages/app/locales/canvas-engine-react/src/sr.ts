import type { CanvasEngineTranslations } from './types.js'

/** Canvas-engine translations for Serbian. Stub — falls back to English. */
export const sr: CanvasEngineTranslations = {
  'canvasEngine.aria.surface': 'Vector canvas editor',
}
