import { en } from './en.js'
import type { ThreadTreeTranslations } from './types.js'

/** Thread-tree translations for lv (stub — falls back to English until reviewed). */
export const lv: ThreadTreeTranslations = { ...en }
