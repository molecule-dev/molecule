import { en } from './en.js'
import type { ThreadTreeTranslations } from './types.js'

/** Thread-tree translations for ca (stub — falls back to English until reviewed). */
export const ca: ThreadTreeTranslations = { ...en }
