import { en } from './en.js'
import type { ThreadTreeTranslations } from './types.js'

/** Thread-tree translations for ru (stub — falls back to English until reviewed). */
export const ru: ThreadTreeTranslations = { ...en }
