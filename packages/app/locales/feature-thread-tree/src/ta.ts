import { en } from './en.js'
import type { ThreadTreeTranslations } from './types.js'

/** Thread-tree translations for ta (stub — falls back to English until reviewed). */
export const ta: ThreadTreeTranslations = { ...en }
