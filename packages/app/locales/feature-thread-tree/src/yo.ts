import { en } from './en.js'
import type { ThreadTreeTranslations } from './types.js'

/** Thread-tree translations for yo (stub — falls back to English until reviewed). */
export const yo: ThreadTreeTranslations = { ...en }
