import { en } from './en.js'
import type { ThreadTreeTranslations } from './types.js'

/** Thread-tree translations for te (stub — falls back to English until reviewed). */
export const te: ThreadTreeTranslations = { ...en }
