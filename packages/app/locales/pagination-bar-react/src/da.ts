import type { PaginationBarTranslations } from './types.js'

/** PaginationBar translations for da. */
export const da: Partial<PaginationBarTranslations> = {
  'pagination.previous': 'Forrige side',
  'pagination.next': 'Næste side',
}
