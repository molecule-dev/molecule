import type { PaginationBarTranslations } from './types.js'

/** PaginationBar translations for zh. */
export const zh: Partial<PaginationBarTranslations> = {
  'pagination.previous': '上一页',
  'pagination.next': '下一页',
}
