import type { PaginationBarTranslations } from './types.js'

/** PaginationBar translations for fr. */
export const fr: Partial<PaginationBarTranslations> = {
  'pagination.previous': 'Page précédente',
  'pagination.next': 'Page suivante',
}
