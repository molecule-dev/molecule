import type { AnnotationPinTranslations } from './types.js'
import { en } from './en.js'

/** Annotation-pin translations for da — currently mirrors English (translation TBD). */
export const da: AnnotationPinTranslations = { ...en }
