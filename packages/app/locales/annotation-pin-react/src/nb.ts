import type { AnnotationPinTranslations } from './types.js'
import { en } from './en.js'

/** Annotation-pin translations for nb — currently mirrors English (translation TBD). */
export const nb: AnnotationPinTranslations = { ...en }
