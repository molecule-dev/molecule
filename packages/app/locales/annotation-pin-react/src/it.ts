import type { AnnotationPinTranslations } from './types.js'
import { en } from './en.js'

/** Annotation-pin translations for it — currently mirrors English (translation TBD). */
export const it: AnnotationPinTranslations = { ...en }
