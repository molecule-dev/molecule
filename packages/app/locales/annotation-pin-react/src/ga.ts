import type { AnnotationPinTranslations } from './types.js'
import { en } from './en.js'

/** Annotation-pin translations for ga — currently mirrors English (translation TBD). */
export const ga: AnnotationPinTranslations = { ...en }
