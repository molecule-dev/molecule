import type { AnnotationPinTranslations } from './types.js'
import { en } from './en.js'

/** Annotation-pin translations for lo — currently mirrors English (translation TBD). */
export const lo: AnnotationPinTranslations = { ...en }
