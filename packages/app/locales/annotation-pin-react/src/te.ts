import type { AnnotationPinTranslations } from './types.js'
import { en } from './en.js'

/** Annotation-pin translations for te — currently mirrors English (translation TBD). */
export const te: AnnotationPinTranslations = { ...en }
