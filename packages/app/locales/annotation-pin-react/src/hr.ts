import type { AnnotationPinTranslations } from './types.js'
import { en } from './en.js'

/** Annotation-pin translations for hr — currently mirrors English (translation TBD). */
export const hr: AnnotationPinTranslations = { ...en }
