import type { AnnotationPinTranslations } from './types.js'
import { en } from './en.js'

/** Annotation-pin translations for pa — currently mirrors English (translation TBD). */
export const pa: AnnotationPinTranslations = { ...en }
