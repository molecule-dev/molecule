import type { AnnotationPinTranslations } from './types.js'
import { en } from './en.js'

/** Annotation-pin translations for ky — currently mirrors English (translation TBD). */
export const ky: AnnotationPinTranslations = { ...en }
