import type { AnnotationPinTranslations } from './types.js'
import { en } from './en.js'

/** Annotation-pin translations for yo — currently mirrors English (translation TBD). */
export const yo: AnnotationPinTranslations = { ...en }
