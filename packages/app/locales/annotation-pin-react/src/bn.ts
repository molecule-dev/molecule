import type { AnnotationPinTranslations } from './types.js'
import { en } from './en.js'

/** Annotation-pin translations for bn — currently mirrors English (translation TBD). */
export const bn: AnnotationPinTranslations = { ...en }
