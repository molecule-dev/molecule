import type { AnnotationPinTranslations } from './types.js'
import { en } from './en.js'

/** Annotation-pin translations for mn — currently mirrors English (translation TBD). */
export const mn: AnnotationPinTranslations = { ...en }
