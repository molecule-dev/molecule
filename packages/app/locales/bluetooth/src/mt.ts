import type { BluetoothTranslations } from './types.js'

/** Bluetooth translations for mt. */
export const mt: Partial<BluetoothTranslations> = {
  'bluetooth.error.noProvider':
    "@molecule/app-bluetooth: L-ebda fornitur ma ġie stabbilit. Sejħa setProvider() b'implimentazzjoni ta' BluetoothProvider (eż., minn @molecule/app-bluetooth-capacitor).",
}
