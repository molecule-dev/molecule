import type { BluetoothTranslations } from './types.js'

/** Bluetooth translations for Maltese. */
export const mt: BluetoothTranslations = {
  'bluetooth.error.noProvider':
    "@molecule/app-bluetooth: L-ebda fornitur mhux issettjat. Sejjah setProvider() b'implimentazzjoni ta' BluetoothProvider (eż., minn @molecule/app-bluetooth-capacitor).",
}
