import type { BluetoothTranslations } from './types.js'

/** Bluetooth translations for Slovak. */
export const sk: BluetoothTranslations = {
  'bluetooth.error.noProvider':
    '@molecule/app-bluetooth: Poskytovateľ nie je nastavený. Zavolajte setProvider() s implementáciou BluetoothProvider (napr. z @molecule/app-bluetooth-capacitor).',
}
