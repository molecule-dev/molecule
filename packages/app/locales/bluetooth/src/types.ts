/** Translation keys for the bluetooth locale package. */
export type BluetoothTranslationKey = 'bluetooth.error.noProvider'

/** Translation record mapping bluetooth keys to translated strings. */
export type BluetoothTranslations = Record<BluetoothTranslationKey, string>
