import type { BluetoothTranslations } from './types.js'

/** Bluetooth translations for Arabic. */
export const ar: BluetoothTranslations = {
  'bluetooth.error.noProvider':
    '@molecule/app-bluetooth: لم يتم تعيين مزود. قم باستدعاء setProvider() مع تنفيذ BluetoothProvider (مثلاً من @molecule/app-bluetooth-capacitor).',
}
