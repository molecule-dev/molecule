import type { BluetoothTranslations } from './types.js'

/** Bluetooth translations for Chinese (Traditional). */
export const zhTW: BluetoothTranslations = {
  'bluetooth.error.noProvider':
    '@molecule/app-bluetooth: 未設定提供者。請使用 BluetoothProvider 實作呼叫 setProvider()（例如，來自 @molecule/app-bluetooth-capacitor）。',
}
