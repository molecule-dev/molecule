import type { BluetoothTranslations } from './types.js'

/** Bluetooth translations for Bulgarian. */
export const bg: BluetoothTranslations = {
  'bluetooth.error.noProvider':
    '@molecule/app-bluetooth: Не е зададен доставчик. Извикайте setProvider() с имплементация на BluetoothProvider (напр. от @molecule/app-bluetooth-capacitor).',
}
