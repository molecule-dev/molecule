import type { BluetoothTranslations } from './types.js'

/** Bluetooth translations for Yoruba. */
export const yo: BluetoothTranslations = {
  'bluetooth.error.noProvider':
    '@molecule/app-bluetooth: Ko si olupese ti a ṣeto. Pe setProvider() pẹlu imuse BluetoothProvider (fun apẹẹrẹ, lati @molecule/app-bluetooth-capacitor).',
}
