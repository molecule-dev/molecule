import type { BluetoothTranslations } from './types.js'

/** Bluetooth translations for Persian. */
export const fa: BluetoothTranslations = {
  'bluetooth.error.noProvider':
    '@molecule/app-bluetooth: هیچ ارائه‌دهنده‌ای تنظیم نشده است. setProvider() را با یک پیاده‌سازی BluetoothProvider فراخوانی کنید (مثلاً از @molecule/app-bluetooth-capacitor).',
}
