import type { BluetoothTranslations } from './types.js'

/** Bluetooth translations for Indonesian. */
export const id: BluetoothTranslations = {
  'bluetooth.error.noProvider':
    '@molecule/app-bluetooth: Provider belum diatur. Panggil setProvider() dengan implementasi BluetoothProvider (mis., dari @molecule/app-bluetooth-capacitor).',
}
