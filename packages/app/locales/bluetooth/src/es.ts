import type { BluetoothTranslations } from './types.js'

/** Bluetooth translations for Spanish. */
export const es: BluetoothTranslations = {
  'bluetooth.error.noProvider':
    '@molecule/app-bluetooth: No se ha establecido un proveedor. Llame a setProvider() con una implementación de BluetoothProvider.',
}
