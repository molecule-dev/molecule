import type { BluetoothTranslations } from './types.js'

/** Bluetooth translations for Bengali. */
export const bn: BluetoothTranslations = {
  'bluetooth.error.noProvider':
    '@molecule/app-bluetooth: কোনো প্রোভাইডার সেট করা হয়নি। একটি BluetoothProvider ইমপ্লিমেন্টেশন দিয়ে setProvider() কল করুন (যেমন @molecule/app-bluetooth-capacitor থেকে)।',
}
