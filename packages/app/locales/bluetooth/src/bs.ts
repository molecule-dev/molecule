import type { BluetoothTranslations } from './types.js'

/** Bluetooth translations for Bosnian. */
export const bs: BluetoothTranslations = {
  'bluetooth.error.noProvider':
    '@molecule/app-bluetooth: Provajder nije postavljen. Pozovite setProvider() sa BluetoothProvider implementacijom (npr. iz @molecule/app-bluetooth-capacitor).',
}
