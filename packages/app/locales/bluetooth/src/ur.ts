import type { BluetoothTranslations } from './types.js'

/** Bluetooth translations for Urdu. */
export const ur: BluetoothTranslations = {
  'bluetooth.error.noProvider':
    '@molecule/app-bluetooth: کوئی فراہم کنندہ مقرر نہیں ہے۔ BluetoothProvider کے نفاذ کے ساتھ setProvider() کو کال کریں (مثلاً، @molecule/app-bluetooth-capacitor سے)۔',
}
