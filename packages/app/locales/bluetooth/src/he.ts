import type { BluetoothTranslations } from './types.js'

/** Bluetooth translations for Hebrew. */
export const he: BluetoothTranslations = {
  'bluetooth.error.noProvider':
    '@molecule/app-bluetooth: לא הוגדר ספק. קראו ל-setProvider() עם מימוש של BluetoothProvider (למשל, מ-@molecule/app-bluetooth-capacitor).',
}
