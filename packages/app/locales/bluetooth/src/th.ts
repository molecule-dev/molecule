import type { BluetoothTranslations } from './types.js'

/** Bluetooth translations for Thai. */
export const th: BluetoothTranslations = {
  'bluetooth.error.noProvider':
    '@molecule/app-bluetooth: ยังไม่ได้ตั้งค่าผู้ให้บริการ เรียก setProvider() พร้อมการใช้งาน BluetoothProvider (เช่น จาก @molecule/app-bluetooth-capacitor)',
}
