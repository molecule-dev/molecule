import type { BluetoothTranslations } from './types.js'

/** Bluetooth translations for Polish. */
export const pl: BluetoothTranslations = {
  'bluetooth.error.noProvider':
    '@molecule/app-bluetooth: Nie ustawiono dostawcy. Wywołaj setProvider() z implementacją BluetoothProvider (np. z @molecule/app-bluetooth-capacitor).',
}
