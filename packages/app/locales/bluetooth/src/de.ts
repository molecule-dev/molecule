import type { BluetoothTranslations } from './types.js'

/** Bluetooth translations for German. */
export const de: BluetoothTranslations = {
  'bluetooth.error.noProvider':
    '@molecule/app-bluetooth: Kein Anbieter gesetzt. Rufen Sie setProvider() mit einer BluetoothProvider-Implementierung auf (z. B. von @molecule/app-bluetooth-capacitor).',
}
