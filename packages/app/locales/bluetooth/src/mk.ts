import type { BluetoothTranslations } from './types.js'

/** Bluetooth translations for Macedonian. */
export const mk: BluetoothTranslations = {
  'bluetooth.error.noProvider':
    '@molecule/app-bluetooth: Не е поставен давател. Повикајте setProvider() со имплементација на BluetoothProvider (на пр., од @molecule/app-bluetooth-capacitor).',
}
