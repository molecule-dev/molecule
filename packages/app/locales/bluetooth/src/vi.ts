import type { BluetoothTranslations } from './types.js'

/** Bluetooth translations for Vietnamese. */
export const vi: BluetoothTranslations = {
  'bluetooth.error.noProvider':
    '@molecule/app-bluetooth: Chưa thiết lập nhà cung cấp. Gọi setProvider() với một triển khai BluetoothProvider (ví dụ, từ @molecule/app-bluetooth-capacitor).',
}
