import type { BluetoothTranslations } from './types.js'

/** Bluetooth translations for Swedish. */
export const sv: BluetoothTranslations = {
  'bluetooth.error.noProvider':
    '@molecule/app-bluetooth: Ingen leverantör inställd. Anropa setProvider() med en BluetoothProvider-implementering (t.ex. från @molecule/app-bluetooth-capacitor).',
}
