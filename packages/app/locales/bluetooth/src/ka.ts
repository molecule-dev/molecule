import type { BluetoothTranslations } from './types.js'

/** Bluetooth translations for Georgian. */
export const ka: BluetoothTranslations = {
  'bluetooth.error.noProvider':
    '@molecule/app-bluetooth: პროვაიდერი არ არის დაყენებული. გამოიძახეთ setProvider() BluetoothProvider იმპლემენტაციით (მაგ., @molecule/app-bluetooth-capacitor-დან).',
}
