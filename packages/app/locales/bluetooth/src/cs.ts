import type { BluetoothTranslations } from './types.js'

/** Bluetooth translations for Czech. */
export const cs: BluetoothTranslations = {
  'bluetooth.error.noProvider':
    '@molecule/app-bluetooth: Poskytovatel není nastaven. Zavolejte setProvider() s implementací BluetoothProvider (např. z @molecule/app-bluetooth-capacitor).',
}
