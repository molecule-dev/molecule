import type { PianoRollTranslations } from './types.js'
import { en } from './en.js'

/** Piano-roll translations for is — currently mirrors English (translation TBD). */
export const is: PianoRollTranslations = { ...en }
