import type { PianoRollTranslations } from './types.js'
import { en } from './en.js'

/** Piano-roll translations for ne — currently mirrors English (translation TBD). */
export const ne: PianoRollTranslations = { ...en }
