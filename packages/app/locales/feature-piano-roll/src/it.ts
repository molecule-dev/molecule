import type { PianoRollTranslations } from './types.js'
import { en } from './en.js'

/** Piano-roll translations for it — currently mirrors English (translation TBD). */
export const it: PianoRollTranslations = { ...en }
