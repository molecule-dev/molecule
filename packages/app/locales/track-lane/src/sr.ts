import type { TrackLaneTranslations } from './types.js'

/** TrackLane translations for sr. */
export const sr: Partial<TrackLaneTranslations> = {
  'trackLane.aria.lane': 'Стаза',
  'trackLane.aria.resize': 'Промени величину клипа',
  'trackLane.aria.clip':
    'Клип<x> {{label}}</x> почевши од<x> {{време почетка}}</x> с за<x> {{трајање}}</x> с',
}
