import type { TrackLaneTranslations } from './types.js'

/** TrackLane translations for sv. */
export const sv: Partial<TrackLaneTranslations> = {
  'trackLane.aria.lane': 'Spårfil',
  'trackLane.aria.resize': 'Ändra storlek på klippet',
  'trackLane.aria.clip':
    'Klämma<x> {{märka}}</x> börjar kl.<x> {{starttid}}</x> s för<x> {{varaktighet}}</x> s',
}
