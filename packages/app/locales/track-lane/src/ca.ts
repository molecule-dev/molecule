import type { TrackLaneTranslations } from './types.js'

/** TrackLane translations for ca. */
export const ca: Partial<TrackLaneTranslations> = {
  'trackLane.aria.lane': 'Carril de pista',
  'trackLane.aria.resize': 'Canvia la mida del clip',
  'trackLane.aria.clip':
    'Clip<x> {{etiqueta}}</x> començant a<x> {{horad&#39;inici}}</x> s per<x> {{durada}}</x> s',
}
