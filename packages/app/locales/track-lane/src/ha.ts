import type { TrackLaneTranslations } from './types.js'

/** TrackLane translations for ha. */
export const ha: Partial<TrackLaneTranslations> = {
  'trackLane.aria.lane': 'Layin hanya',
  'trackLane.aria.resize': 'Sake girman kilif ɗin',
  'trackLane.aria.clip':
    'Faifan Bidiyo<x> {{lakabi}}</x> farawa daga<x> {{Lokacin Farawa}}</x> s don<x> {{lokaci}}</x> s',
}
