import type { TrackLaneTranslations } from './types.js'

/** TrackLane translations for bs. */
export const bs: Partial<TrackLaneTranslations> = {
  'trackLane.aria.lane': 'Staza',
  'trackLane.aria.resize': 'Promijeni veličinu isječka',
  'trackLane.aria.clip':
    'Isječak<x> {{label}}</x> počevši od<x> {{početno vrijeme}}</x> s za<x> {{trajanje}}</x> s',
}
