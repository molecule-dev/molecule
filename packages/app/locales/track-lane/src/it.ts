import type { TrackLaneTranslations } from './types.js'

/** TrackLane translations for it. */
export const it: Partial<TrackLaneTranslations> = {
  'trackLane.aria.lane': 'Corsia di atletica',
  'trackLane.aria.resize': 'Ridimensiona la clip',
  'trackLane.aria.clip':
    'Clip<x> {{etichetta}}</x> a partire da<x> {{startTime}}</x> s per<x> {{durata}}</x> S',
}
