import type { TrackLaneTranslations } from './types.js'

/** TrackLane translations for pl. */
export const pl: Partial<TrackLaneTranslations> = {
  'trackLane.aria.lane': 'Tor wyścigowy',
  'trackLane.aria.resize': 'Zmień rozmiar klipu',
  'trackLane.aria.clip':
    'Klips<x> {{etykieta}}</x> zaczynając od<x> {{czas rozpoczęcia}}</x> s dla<x> {{czas trwania}}</x> S',
}
