import type { TrackLaneTranslations } from './types.js'

/** TrackLane translations for pt. */
export const pt: Partial<TrackLaneTranslations> = {
  'trackLane.aria.lane': 'faixa de pista',
  'trackLane.aria.resize': 'Redimensionar clipe',
  'trackLane.aria.clip':
    'Grampo<x> {{rótulo}}</x> começando em<x> {{startTime}}</x> s para<x> {{duração}}</x> s',
}
