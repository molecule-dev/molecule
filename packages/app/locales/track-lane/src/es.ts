import type { TrackLaneTranslations } from './types.js'

/** TrackLane translations for es. */
export const es: Partial<TrackLaneTranslations> = {
  'trackLane.aria.lane': 'Carril de pista',
  'trackLane.aria.resize': 'Cambiar el tamaño del clip',
  'trackLane.aria.clip':
    'Acortar<x> {{etiqueta}}</x> comenzando en<x> {{startTime}}</x> s para<x> {{duración}}</x> s',
}
