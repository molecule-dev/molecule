import type { TrackLaneTranslations } from './types.js'

/** TrackLane translations for hi. */
export const hi: Partial<TrackLaneTranslations> = {
  'trackLane.aria.lane': 'ट्रैक लेन',
  'trackLane.aria.resize': 'क्लिप का आकार बदलें',
  'trackLane.aria.clip':
    'क्लिप<x> {{लेबल}}</x> से शुरू होकर<x> {{समय शुरू}}</x> के लिए<x> {{अवधि}}</x> एस',
}
