import type { TrackLaneTranslations } from './types.js'

/** TrackLane translations for fi. */
export const fi: Partial<TrackLaneTranslations> = {
  'trackLane.aria.lane': 'Ratakaista',
  'trackLane.aria.resize': 'Muuta leikkeen kokoa',
  'trackLane.aria.clip':
    'Klipsi<x> {{etiketti}}</x> alkaen klo<x> {{aloitusaika}}</x> s varten<x> {{kesto}}</x> s',
}
