import type { TrackLaneTranslations } from './types.js'

/** TrackLane translations for bg. */
export const bg: Partial<TrackLaneTranslations> = {
  'trackLane.aria.lane': 'Пътека',
  'trackLane.aria.resize': 'Преоразмеряване на клипа',
  'trackLane.aria.clip':
    'Клип<x> {{етикет}}</x> започвайки от<x> {{стартов час}}</x> за<x> {{продължителност}}</x> с',
}
