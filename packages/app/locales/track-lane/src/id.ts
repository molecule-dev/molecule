import type { TrackLaneTranslations } from './types.js'

/** TrackLane translations for id. */
export const id: Partial<TrackLaneTranslations> = {
  'trackLane.aria.lane': 'Jalur lintasan',
  'trackLane.aria.resize': 'Ubah ukuran klip',
  'trackLane.aria.clip':
    'Klip<x> {{label}}</x> dimulai dari<x> {{startTime}}</x> s untuk<x> {{lamanya}}</x> S',
}
