import type { TrackLaneTranslations } from './types.js'

/** TrackLane translations for lt. */
export const lt: Partial<TrackLaneTranslations> = {
  'trackLane.aria.lane': 'Bėgių juosta',
  'trackLane.aria.resize': 'Keisti klipo dydį',
  'trackLane.aria.clip':
    'Klipas<x> {{etiketė}}</x> pradedant nuo<x> {{pradžios laikas}}</x> s skirtas<x> {{duration}}</x> s',
}
