import type { TrackLaneTranslations } from './types.js'

/** TrackLane translations for ar. */
export const ar: Partial<TrackLaneTranslations> = {
  'trackLane.aria.lane': 'مسار السكة الحديدية',
  'trackLane.aria.resize': 'تغيير حجم المقطع',
  'trackLane.aria.clip':
    'مقطع فيديو<x> {{ملصق}}</x> ابتداءً من<x> {{startTime}}</x> حرف S لـ<x> {{مدة}}</x> s',
}
