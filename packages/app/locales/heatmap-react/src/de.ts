import type { HeatmapTranslations } from './types.js'

/** Heatmap translations for de. */
export const de: Partial<HeatmapTranslations> = {
  'heatmap.weekday.sun': 'So',
  'heatmap.weekday.sat': 'Sa',
}
