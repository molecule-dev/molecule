import type { HeatmapTranslations } from './types.js'

/** Heatmap translations for ar. */
export const ar: Partial<HeatmapTranslations> = {
  'heatmap.weekday.sun': 'الأحد',
  'heatmap.weekday.sat': 'السبت',
}
