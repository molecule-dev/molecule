import type { HeatmapTranslations } from './types.js'

/** Heatmap translations for ms. */
export const ms: Partial<HeatmapTranslations> = {
  'heatmap.weekday.sun': 'Ahd',
  'heatmap.weekday.sat': 'Sab',
}
