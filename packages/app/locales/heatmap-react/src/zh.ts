import type { HeatmapTranslations } from './types.js'

/** Heatmap translations for zh. */
export const zh: Partial<HeatmapTranslations> = {
  'heatmap.weekday.sun': '周日',
  'heatmap.weekday.sat': '周六',
}
