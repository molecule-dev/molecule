import type { HeatmapTranslations } from './types.js'

/** Heatmap translations for ko. */
export const ko: Partial<HeatmapTranslations> = {
  'heatmap.weekday.sun': '일',
  'heatmap.weekday.sat': '토',
}
