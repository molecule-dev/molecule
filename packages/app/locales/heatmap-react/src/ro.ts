import type { HeatmapTranslations } from './types.js'

/** Heatmap translations for ro. */
export const ro: Partial<HeatmapTranslations> = {
  'heatmap.weekday.sun': 'Dum',
  'heatmap.weekday.sat': 'Sâm',
}
