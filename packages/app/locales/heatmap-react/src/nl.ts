import type { HeatmapTranslations } from './types.js'

/** Heatmap translations for nl. */
export const nl: Partial<HeatmapTranslations> = {
  'heatmap.weekday.sun': 'Zo',
  'heatmap.weekday.sat': 'Za',
}
