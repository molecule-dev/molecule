import type { HeatmapTranslations } from './types.js'

/** Heatmap translations for tr. */
export const tr: Partial<HeatmapTranslations> = {
  'heatmap.weekday.sun': 'Paz',
  'heatmap.weekday.sat': 'Cmt',
}
