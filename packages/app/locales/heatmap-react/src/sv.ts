import type { HeatmapTranslations } from './types.js'

/** Heatmap translations for sv. */
export const sv: Partial<HeatmapTranslations> = {
  'heatmap.weekday.sun': 'Sön',
  'heatmap.weekday.sat': 'Lör',
}
