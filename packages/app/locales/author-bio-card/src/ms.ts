import type { AuthorBioCardTranslations } from './types.js'

/** AuthorBioCard translations for ms. */
export const ms: Partial<AuthorBioCardTranslations> = {
  'authorBioCard.follow': 'Ikut',
  'authorBioCard.following': 'Mengikuti',
}
