import type { CanvasEngineTranslations } from './types.js'

/** Canvas-engine translations for Chinese (Simplified). Stub — falls back to English. */
export const zh: CanvasEngineTranslations = {
  'canvasEngine.aria.surface': 'Vector canvas editor',
}
