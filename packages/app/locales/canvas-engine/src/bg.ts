import type { CanvasEngineTranslations } from './types.js'

/** Canvas-engine translations for Bulgarian. Stub — falls back to English. */
export const bg: CanvasEngineTranslations = {
  'canvasEngine.aria.surface': 'Vector canvas editor',
}
