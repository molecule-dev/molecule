import type { CanvasEngineTranslations } from './types.js'

/** Canvas-engine translations for Lithuanian. Stub — falls back to English. */
export const lt: CanvasEngineTranslations = {
  'canvasEngine.aria.surface': 'Vector canvas editor',
}
