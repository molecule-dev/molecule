import type { CanvasEngineTranslations } from './types.js'

/** Canvas-engine translations for Croatian. Stub — falls back to English. */
export const hr: CanvasEngineTranslations = {
  'canvasEngine.aria.surface': 'Vector canvas editor',
}
