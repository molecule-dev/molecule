import type { CanvasEngineTranslations } from './types.js'

/** Canvas-engine translations for Albanian. Stub — falls back to English. */
export const sq: CanvasEngineTranslations = {
  'canvasEngine.aria.surface': 'Vector canvas editor',
}
