import type { CanvasEngineTranslations } from './types.js'

/** Canvas-engine translations for Slovak. Stub — falls back to English. */
export const sk: CanvasEngineTranslations = {
  'canvasEngine.aria.surface': 'Vector canvas editor',
}
