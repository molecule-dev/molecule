import type { CanvasEngineTranslations } from './types.js'

/** Canvas-engine translations for Greek. Stub — falls back to English. */
export const el: CanvasEngineTranslations = {
  'canvasEngine.aria.surface': 'Vector canvas editor',
}
