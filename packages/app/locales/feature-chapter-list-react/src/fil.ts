import type { ChapterListTranslations } from './types.js'
import { en } from './en.js'

/** Chapter-list translations for fil — currently mirrors English (translation TBD). */
export const fil: ChapterListTranslations = { ...en }
