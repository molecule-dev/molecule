import type { ChapterListTranslations } from './types.js'
import { en } from './en.js'

/** Chapter-list translations for fi — currently mirrors English (translation TBD). */
export const fi: ChapterListTranslations = { ...en }
