import type { ChapterListTranslations } from './types.js'
import { en } from './en.js'

/** Chapter-list translations for ja — currently mirrors English (translation TBD). */
export const ja: ChapterListTranslations = { ...en }
