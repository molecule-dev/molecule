import type { ChapterListTranslations } from './types.js'
import { en } from './en.js'

/** Chapter-list translations for mn — currently mirrors English (translation TBD). */
export const mn: ChapterListTranslations = { ...en }
