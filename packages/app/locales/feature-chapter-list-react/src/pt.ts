import type { ChapterListTranslations } from './types.js'
import { en } from './en.js'

/** Chapter-list translations for pt — currently mirrors English (translation TBD). */
export const pt: ChapterListTranslations = { ...en }
