import type { ChapterListTranslations } from './types.js'
import { en } from './en.js'

/** Chapter-list translations for id — currently mirrors English (translation TBD). */
export const id: ChapterListTranslations = { ...en }
