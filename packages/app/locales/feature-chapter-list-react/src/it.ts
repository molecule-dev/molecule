import type { ChapterListTranslations } from './types.js'
import { en } from './en.js'

/** Chapter-list translations for it — currently mirrors English (translation TBD). */
export const it: ChapterListTranslations = { ...en }
