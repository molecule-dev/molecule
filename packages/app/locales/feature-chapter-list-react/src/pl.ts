import type { ChapterListTranslations } from './types.js'
import { en } from './en.js'

/** Chapter-list translations for pl — currently mirrors English (translation TBD). */
export const pl: ChapterListTranslations = { ...en }
