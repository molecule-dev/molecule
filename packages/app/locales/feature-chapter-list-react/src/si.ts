import type { ChapterListTranslations } from './types.js'
import { en } from './en.js'

/** Chapter-list translations for si — currently mirrors English (translation TBD). */
export const si: ChapterListTranslations = { ...en }
