import type { ChapterListTranslations } from './types.js'
import { en } from './en.js'

/** Chapter-list translations for de — currently mirrors English (translation TBD). */
export const de: ChapterListTranslations = { ...en }
