import type { ChapterListTranslations } from './types.js'
import { en } from './en.js'

/** Chapter-list translations for sv — currently mirrors English (translation TBD). */
export const sv: ChapterListTranslations = { ...en }
