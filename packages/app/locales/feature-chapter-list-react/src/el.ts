import type { ChapterListTranslations } from './types.js'
import { en } from './en.js'

/** Chapter-list translations for el — currently mirrors English (translation TBD). */
export const el: ChapterListTranslations = { ...en }
