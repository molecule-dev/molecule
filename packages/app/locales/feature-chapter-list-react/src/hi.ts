import type { ChapterListTranslations } from './types.js'
import { en } from './en.js'

/** Chapter-list translations for hi — currently mirrors English (translation TBD). */
export const hi: ChapterListTranslations = { ...en }
