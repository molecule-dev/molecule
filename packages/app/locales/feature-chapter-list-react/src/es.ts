import type { ChapterListTranslations } from './types.js'
import { en } from './en.js'

/** Chapter-list translations for es — currently mirrors English (translation TBD). */
export const es: ChapterListTranslations = { ...en }
