import type { ChapterListTranslations } from './types.js'
import { en } from './en.js'

/** Chapter-list translations for zu — currently mirrors English (translation TBD). */
export const zu: ChapterListTranslations = { ...en }
