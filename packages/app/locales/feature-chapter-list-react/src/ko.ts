import type { ChapterListTranslations } from './types.js'
import { en } from './en.js'

/** Chapter-list translations for ko — currently mirrors English (translation TBD). */
export const ko: ChapterListTranslations = { ...en }
