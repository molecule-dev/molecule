import type { ChapterListTranslations } from './types.js'
import { en } from './en.js'

/** Chapter-list translations for ky — currently mirrors English (translation TBD). */
export const ky: ChapterListTranslations = { ...en }
