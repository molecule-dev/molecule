import type { ChapterListTranslations } from './types.js'
import { en } from './en.js'

/** Chapter-list translations for pa — currently mirrors English (translation TBD). */
export const pa: ChapterListTranslations = { ...en }
