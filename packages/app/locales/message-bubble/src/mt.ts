import type { MessageBubbleTranslations } from './types.js'

/** MessageBubble translations for mt. */
export const mt: Partial<MessageBubbleTranslations> = {
  'composer.placeholder': 'Ikteb messaġġ…',
  'composer.send': 'Ibgħat',
}
