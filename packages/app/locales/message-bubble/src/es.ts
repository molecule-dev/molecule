import type { MessageBubbleTranslations } from './types.js'

/** MessageBubble translations for es. */
export const es: Partial<MessageBubbleTranslations> = {
  'composer.placeholder': 'Escribe un mensaje…',
  'composer.send': 'Enviar',
}
