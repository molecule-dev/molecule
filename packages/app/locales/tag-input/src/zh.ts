import type { TagInputTranslations } from './types.js'

/** TagInput translations for zh. */
export const zh: Partial<TagInputTranslations> = {
  'tagChip.remove': '移除',
  'tagInput.placeholder': '添加标签…',
}
