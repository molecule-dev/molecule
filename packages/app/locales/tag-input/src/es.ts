import type { TagInputTranslations } from './types.js'

/** TagInput translations for es. */
export const es: Partial<TagInputTranslations> = {
  'tagChip.remove': 'Eliminar',
  'tagInput.placeholder': 'Añade una etiqueta…',
}
