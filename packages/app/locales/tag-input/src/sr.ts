import type { TagInputTranslations } from './types.js'

/** TagInput translations for sr. */
export const sr: Partial<TagInputTranslations> = {
  'tagChip.remove': 'Уклони',
  'tagInput.placeholder': 'Додај ознаку…',
}
