import type { TagInputTranslations } from './types.js'

/** TagInput translations for it. */
export const it: Partial<TagInputTranslations> = {
  'tagChip.remove': 'Rimuovi',
  'tagInput.placeholder': 'Aggiungi un tag…',
}
