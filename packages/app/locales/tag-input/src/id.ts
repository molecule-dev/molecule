import type { TagInputTranslations } from './types.js'

/** TagInput translations for id. */
export const id: Partial<TagInputTranslations> = {
  'tagChip.remove': 'Hapus',
  'tagInput.placeholder': 'Tambahkan tag…',
}
