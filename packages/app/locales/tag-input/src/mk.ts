import type { TagInputTranslations } from './types.js'

/** TagInput translations for mk. */
export const mk: Partial<TagInputTranslations> = {
  'tagChip.remove': 'Отстрани',
  'tagInput.placeholder': 'Додај ознака…',
}
