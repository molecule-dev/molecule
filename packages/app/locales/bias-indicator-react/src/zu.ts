import type { BiasIndicatorTranslations } from './types.js'
import { en } from './en.js'

/** Bias-indicator translations for zu — currently mirrors English (translation TBD). */
export const zu: BiasIndicatorTranslations = { ...en }
