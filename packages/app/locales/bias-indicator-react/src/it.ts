import type { BiasIndicatorTranslations } from './types.js'
import { en } from './en.js'

/** Bias-indicator translations for it — currently mirrors English (translation TBD). */
export const it: BiasIndicatorTranslations = { ...en }
