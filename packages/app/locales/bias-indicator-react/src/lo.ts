import type { BiasIndicatorTranslations } from './types.js'
import { en } from './en.js'

/** Bias-indicator translations for lo — currently mirrors English (translation TBD). */
export const lo: BiasIndicatorTranslations = { ...en }
