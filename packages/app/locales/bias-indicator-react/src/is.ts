import type { BiasIndicatorTranslations } from './types.js'
import { en } from './en.js'

/** Bias-indicator translations for is — currently mirrors English (translation TBD). */
export const is: BiasIndicatorTranslations = { ...en }
