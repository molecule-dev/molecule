import type { BiasIndicatorTranslations } from './types.js'
import { en } from './en.js'

/** Bias-indicator translations for km — currently mirrors English (translation TBD). */
export const km: BiasIndicatorTranslations = { ...en }
