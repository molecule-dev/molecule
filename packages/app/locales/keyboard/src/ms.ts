import type { KeyboardTranslations } from './types.js'

/** Keyboard translations for Malay. */
export const ms: KeyboardTranslations = {
  'keyboard.error.noProvider':
    '@molecule/app-keyboard: Tiada pembekal ditetapkan. Panggil setProvider() dengan pelaksanaan KeyboardProvider (cth., daripada @molecule/app-keyboard-capacitor).',
}
