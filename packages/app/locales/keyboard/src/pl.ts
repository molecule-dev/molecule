import type { KeyboardTranslations } from './types.js'

/** Keyboard translations for Polish. */
export const pl: KeyboardTranslations = {
  'keyboard.error.noProvider':
    '@molecule/app-keyboard: Nie ustawiono dostawcy. Wywołaj setProvider() z implementacją KeyboardProvider (np. z @molecule/app-keyboard-capacitor).',
}
