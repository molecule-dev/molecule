import type { KeyboardTranslations } from './types.js'

/** Keyboard translations for ca. */
export const ca: Partial<KeyboardTranslations> = {
  'keyboard.error.noProvider':
    "@molecule/app-keyboard: No s'ha definit cap proveïdor. Crida setProvider() amb una implementació de KeyboardProvider (per exemple, des de @molecule/app-keyboard-react-native).",
}
