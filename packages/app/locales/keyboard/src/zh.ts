import type { KeyboardTranslations } from './types.js'

/** Keyboard translations for Chinese. */
export const zh: KeyboardTranslations = {
  'keyboard.error.noProvider':
    '@molecule/app-keyboard: 未设置提供程序。请使用 KeyboardProvider 实现调用 setProvider()（例如，来自 @molecule/app-keyboard-capacitor）。',
}
