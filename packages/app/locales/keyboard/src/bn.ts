import type { KeyboardTranslations } from './types.js'

/** Keyboard translations for Bengali. */
export const bn: KeyboardTranslations = {
  'keyboard.error.noProvider':
    '@molecule/app-keyboard: কোনো প্রদানকারী সেট করা হয়নি। একটি KeyboardProvider বাস্তবায়ন দিয়ে setProvider() কল করুন (যেমন @molecule/app-keyboard-capacitor থেকে)।',
}
