import type { KeyboardTranslations } from './types.js'

/** Keyboard translations for Portuguese. */
export const pt: KeyboardTranslations = {
  'keyboard.error.noProvider':
    '@molecule/app-keyboard: Nenhum provedor definido. Chame setProvider() com uma implementação de KeyboardProvider (ex., de @molecule/app-keyboard-capacitor).',
}
