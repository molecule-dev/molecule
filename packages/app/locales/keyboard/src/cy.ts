import type { KeyboardTranslations } from './types.js'

/** Keyboard translations for cy. */
export const cy: Partial<KeyboardTranslations> = {
  'keyboard.error.noProvider':
    "@molecule/app-keyboard: Dim darparwr wedi'i osod. Galwch setProvider() gyda gweithrediad KeyboardProvider (e.e., o @molecule/app-keyboard-capacitor).",
}
