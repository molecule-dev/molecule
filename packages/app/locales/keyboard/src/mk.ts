import type { KeyboardTranslations } from './types.js'

/** Keyboard translations for Macedonian. */
export const mk: KeyboardTranslations = {
  'keyboard.error.noProvider':
    '@molecule/app-keyboard: Не е поставен провајдер. Повикајте setProvider() со имплементација на KeyboardProvider (на пр., од @molecule/app-keyboard-capacitor).',
}
