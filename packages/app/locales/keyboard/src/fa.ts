import type { KeyboardTranslations } from './types.js'

/** Keyboard translations for Persian. */
export const fa: KeyboardTranslations = {
  'keyboard.error.noProvider':
    '@molecule/app-keyboard: هیچ ارائه‌دهنده‌ای تنظیم نشده است. setProvider() را با یک پیاده‌سازی KeyboardProvider فراخوانی کنید (مثلاً از @molecule/app-keyboard-capacitor).',
}
