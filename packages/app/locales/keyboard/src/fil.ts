import type { KeyboardTranslations } from './types.js'

/** Keyboard translations for Filipino. */
export const fil: KeyboardTranslations = {
  'keyboard.error.noProvider':
    '@molecule/app-keyboard: Walang provider na naka-set. Tumawag ng setProvider() na may KeyboardProvider na implementasyon (hal., mula sa @molecule/app-keyboard-capacitor).',
}
