import type { KeyboardTranslations } from './types.js'

/** Keyboard translations for Georgian. */
export const ka: KeyboardTranslations = {
  'keyboard.error.noProvider':
    '@molecule/app-keyboard: პროვაიდერი არ არის დაყენებული. გამოიძახეთ setProvider() KeyboardProvider იმპლემენტაციით (მაგ., @molecule/app-keyboard-capacitor-იდან).',
}
