import type { KeyboardTranslations } from './types.js'

/** Keyboard translations for Hungarian. */
export const hu: KeyboardTranslations = {
  'keyboard.error.noProvider':
    '@molecule/app-keyboard: Nincs szolgáltató beállítva. Hívja meg a setProvider()-t egy KeyboardProvider implementációval (pl. a @molecule/app-keyboard-capacitor-ból).',
}
