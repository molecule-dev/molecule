import type { KeyboardTranslations } from './types.js'

/** Keyboard translations for Hebrew. */
export const he: KeyboardTranslations = {
  'keyboard.error.noProvider':
    '@molecule/app-keyboard: לא הוגדר ספק. קרא ל-setProvider() עם מימוש של KeyboardProvider (למשל, מ-@molecule/app-keyboard-capacitor).',
}
