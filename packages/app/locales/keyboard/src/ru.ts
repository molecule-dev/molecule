import type { KeyboardTranslations } from './types.js'

/** Keyboard translations for Russian. */
export const ru: KeyboardTranslations = {
  'keyboard.error.noProvider':
    '@molecule/app-keyboard: Провайдер не установлен. Вызовите setProvider() с реализацией KeyboardProvider (например, из @molecule/app-keyboard-capacitor).',
}
