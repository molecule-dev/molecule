import type { KeyboardTranslations } from './types.js'

/** Keyboard translations for Thai. */
export const th: KeyboardTranslations = {
  'keyboard.error.noProvider':
    '@molecule/app-keyboard: ยังไม่ได้ตั้งค่าผู้ให้บริการ เรียก setProvider() พร้อมการใช้งาน KeyboardProvider (เช่น จาก @molecule/app-keyboard-capacitor)',
}
