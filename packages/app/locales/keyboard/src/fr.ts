import type { KeyboardTranslations } from './types.js'

/** Keyboard translations for French. */
export const fr: KeyboardTranslations = {
  'keyboard.error.noProvider':
    '@molecule/app-keyboard: Aucun fournisseur défini. Appelez setProvider() avec une implémentation de KeyboardProvider (par ex., depuis @molecule/app-keyboard-capacitor).',
}
