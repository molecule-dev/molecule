import type { KeyboardTranslations } from './types.js'

/** Keyboard translations for Maltese. */
export const mt: KeyboardTranslations = {
  'keyboard.error.noProvider':
    "@molecule/app-keyboard: L-ebda fornitur mhux issettjat. Sejjah setProvider() b'implimentazzjoni ta' KeyboardProvider (eż., minn @molecule/app-keyboard-capacitor).",
}
