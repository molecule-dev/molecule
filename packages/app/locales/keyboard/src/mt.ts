import type { KeyboardTranslations } from './types.js'

/** Keyboard translations for mt. */
export const mt: Partial<KeyboardTranslations> = {
  'keyboard.error.noProvider':
    '@molecule/app-keyboard: L-ebda fornitur ma ġie stabbilit. Sejjaħ setProvider() b&#39;implimentazzjoni ta&#39; KeyboardProvider (eż., minn @molecule/app-keyboard-capacitor).',
}
