import type { KeyboardTranslations } from './types.js'

/** Keyboard translations for Korean. */
export const ko: KeyboardTranslations = {
  'keyboard.error.noProvider':
    '@molecule/app-keyboard: 프로바이더가 설정되지 않았습니다. KeyboardProvider 구현으로 setProvider()를 호출하세요 (예: @molecule/app-keyboard-capacitor에서).',
}
