import type { KeyboardTranslations } from './types.js'

/** Keyboard translations for Swedish. */
export const sv: KeyboardTranslations = {
  'keyboard.error.noProvider':
    '@molecule/app-keyboard: Ingen leverantör har angetts. Anropa setProvider() med en KeyboardProvider-implementering (t.ex. från @molecule/app-keyboard-capacitor).',
}
