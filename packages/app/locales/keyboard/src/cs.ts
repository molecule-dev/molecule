import type { KeyboardTranslations } from './types.js'

/** Keyboard translations for Czech. */
export const cs: KeyboardTranslations = {
  'keyboard.error.noProvider':
    '@molecule/app-keyboard: Poskytovatel není nastaven. Zavolejte setProvider() s implementací KeyboardProvider (např. z @molecule/app-keyboard-capacitor).',
}
