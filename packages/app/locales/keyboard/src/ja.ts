import type { KeyboardTranslations } from './types.js'

/** Keyboard translations for Japanese. */
export const ja: KeyboardTranslations = {
  'keyboard.error.noProvider':
    '@molecule/app-keyboard: プロバイダーが設定されていません。KeyboardProviderの実装でsetProvider()を呼び出してください（例: @molecule/app-keyboard-capacitorから）。',
}
