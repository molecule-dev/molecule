import type { KeyboardTranslations } from './types.js'

/** Keyboard translations for Hausa. */
export const ha: KeyboardTranslations = {
  'keyboard.error.noProvider':
    '@molecule/app-keyboard: Ba a saita mai samarwa ba. Kira setProvider() tare da aiwatar KeyboardProvider (misali, daga @molecule/app-keyboard-capacitor).',
}
