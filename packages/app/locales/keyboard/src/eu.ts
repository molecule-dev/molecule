import type { KeyboardTranslations } from './types.js'

/** Keyboard translations for Basque. */
export const eu: KeyboardTranslations = {
  'keyboard.error.noProvider':
    '@molecule/app-keyboard: Ez da hornitzailerik ezarri. Deitu setProvider() KeyboardProvider inplementazio batekin (adib., @molecule/app-keyboard-capacitor-etik).',
}
