import type { KeyboardTranslations } from './types.js'

/** Keyboard translations for Greek. */
export const el: KeyboardTranslations = {
  'keyboard.error.noProvider':
    '@molecule/app-keyboard: Δεν έχει οριστεί πάροχος. Καλέστε setProvider() με μια υλοποίηση KeyboardProvider (π.χ. από @molecule/app-keyboard-capacitor).',
}
