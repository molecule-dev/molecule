import type { KeyboardTranslations } from './types.js'

/** Keyboard translations for Azerbaijani. */
export const az: KeyboardTranslations = {
  'keyboard.error.noProvider':
    '@molecule/app-keyboard: Provayder təyin edilməyib. KeyboardProvider tətbiqi ilə setProvider() çağırın (məs., @molecule/app-keyboard-capacitor-dan).',
}
