import type { KeyboardTranslations } from './types.js'

/** Keyboard translations for Bosnian. */
export const bs: KeyboardTranslations = {
  'keyboard.error.noProvider':
    '@molecule/app-keyboard: Provajder nije postavljen. Pozovite setProvider() sa KeyboardProvider implementacijom (npr. iz @molecule/app-keyboard-capacitor).',
}
