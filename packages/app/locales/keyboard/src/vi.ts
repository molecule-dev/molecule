import type { KeyboardTranslations } from './types.js'

/** Keyboard translations for Vietnamese. */
export const vi: KeyboardTranslations = {
  'keyboard.error.noProvider':
    '@molecule/app-keyboard: Chưa thiết lập nhà cung cấp. Gọi setProvider() với triển khai KeyboardProvider (ví dụ, từ @molecule/app-keyboard-capacitor).',
}
