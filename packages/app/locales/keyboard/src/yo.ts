import type { KeyboardTranslations } from './types.js'

/** Keyboard translations for Yoruba. */
export const yo: KeyboardTranslations = {
  'keyboard.error.noProvider':
    '@molecule/app-keyboard: Ko si olupese ti a ṣeto. Pe setProvider() pẹlu imuse KeyboardProvider kan (fun apẹẹrẹ, lati @molecule/app-keyboard-capacitor).',
}
