import type { KeyboardTranslations } from './types.js'

/** Keyboard translations for German. */
export const de: KeyboardTranslations = {
  'keyboard.error.noProvider':
    '@molecule/app-keyboard: Kein Anbieter festgelegt. Rufen Sie setProvider() mit einer KeyboardProvider-Implementierung auf (z. B. von @molecule/app-keyboard-capacitor).',
}
