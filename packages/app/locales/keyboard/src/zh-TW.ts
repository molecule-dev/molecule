import type { KeyboardTranslations } from './types.js'

/** Keyboard translations for Chinese (Traditional). */
export const zhTW: KeyboardTranslations = {
  'keyboard.error.noProvider':
    '@molecule/app-keyboard: 未設定提供者。請使用 KeyboardProvider 實作呼叫 setProvider()（例如，來自 @molecule/app-keyboard-capacitor）。',
}
