import type { BadgeTranslations } from './types.js'

/** Badge translations for Filipino. */
export const fil: BadgeTranslations = {
  'badge.error.noProvider': 'Hindi na-configure ang badge provider.',
}
