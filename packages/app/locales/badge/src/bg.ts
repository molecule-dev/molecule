import type { BadgeTranslations } from './types.js'

/** Badge translations for Bulgarian. */
export const bg: BadgeTranslations = {
  'badge.error.noProvider': 'Доставчикът на значки не е конфигуриран.',
}
