/** Translation keys for the badge locale package. */
export type BadgeTranslationKey = 'badge.error.noProvider'

/** Translation record mapping badge keys to translated strings. */
export type BadgeTranslations = Record<BadgeTranslationKey, string>
