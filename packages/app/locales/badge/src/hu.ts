import type { BadgeTranslations } from './types.js'

/** Badge translations for Hungarian. */
export const hu: BadgeTranslations = {
  'badge.error.noProvider': 'A jelvény szolgáltató nincs konfigurálva.',
}
