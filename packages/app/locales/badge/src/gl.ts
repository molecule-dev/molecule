import type { BadgeTranslations } from './types.js'

/** Badge translations for Galician. */
export const gl: BadgeTranslations = {
  'badge.error.noProvider': 'O provedor de insignias non está configurado.',
}
