import type { BadgeTranslations } from './types.js'

/** Badge translations for Macedonian. */
export const mk: BadgeTranslations = {
  'badge.error.noProvider': 'Давателот на значки не е конфигуриран.',
}
