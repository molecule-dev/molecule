import type { BadgeTranslations } from './types.js'

/** Badge translations for Croatian. */
export const hr: BadgeTranslations = {
  'badge.error.noProvider': 'Pružatelj značaka nije konfiguriran.',
}
