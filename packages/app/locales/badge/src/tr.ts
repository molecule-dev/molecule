import type { BadgeTranslations } from './types.js'

/** Badge translations for Turkish. */
export const tr: BadgeTranslations = {
  'badge.error.noProvider': 'Rozet sağlayıcısı yapılandırılmamış.',
}
