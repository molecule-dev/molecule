import type { BadgeTranslations } from './types.js'

/** Badge translations for Maltese. */
export const mt: BadgeTranslations = {
  'badge.error.noProvider': 'Il-fornitur tal-badge mhux ikkonfigurat.',
}
