import type { BadgeTranslations } from './types.js'

/** Badge translations for Italian. */
export const it: BadgeTranslations = {
  'badge.error.noProvider': 'Il provider del badge non è configurato.',
}
