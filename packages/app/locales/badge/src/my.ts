import type { BadgeTranslations } from './types.js'

/** Badge translations for Burmese. */
export const my: BadgeTranslations = {
  'badge.error.noProvider': 'Badge provider ကို configure မလုပ်ရသေးပါ။',
}
