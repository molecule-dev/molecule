import type { BadgeTranslations } from './types.js'

/** Badge translations for Spanish. */
export const es: BadgeTranslations = {
  'badge.error.noProvider': 'El proveedor de insignias no está configurado.',
}
