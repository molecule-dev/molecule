import type { BadgeTranslations } from './types.js'

/** Badge translations for Romanian. */
export const ro: BadgeTranslations = {
  'badge.error.noProvider': 'Furnizorul de insigne nu este configurat.',
}
