import type { BadgeTranslations } from './types.js'

/** Badge translations for Portuguese. */
export const pt: BadgeTranslations = {
  'badge.error.noProvider': 'O fornecedor de emblemas não está configurado.',
}
