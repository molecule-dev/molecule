import type { TrackLaneTranslations } from './types.js'
import { en } from './en.js'

/** Track-lane translations for tr — currently mirrors English (translation TBD). */
export const tr: TrackLaneTranslations = { ...en }
