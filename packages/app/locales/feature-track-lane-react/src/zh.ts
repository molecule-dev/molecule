import type { TrackLaneTranslations } from './types.js'
import { en } from './en.js'

/** Track-lane translations for zh — currently mirrors English (translation TBD). */
export const zh: TrackLaneTranslations = { ...en }
