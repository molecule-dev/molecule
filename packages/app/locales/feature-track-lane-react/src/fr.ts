import type { TrackLaneTranslations } from './types.js'
import { en } from './en.js'

/** Track-lane translations for fr — currently mirrors English (translation TBD). */
export const fr: TrackLaneTranslations = { ...en }
