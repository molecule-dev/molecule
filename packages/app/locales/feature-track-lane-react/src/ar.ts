import type { TrackLaneTranslations } from './types.js'
import { en } from './en.js'

/** Track-lane translations for ar — currently mirrors English (translation TBD). */
export const ar: TrackLaneTranslations = { ...en }
