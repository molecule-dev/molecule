import type { TrackLaneTranslations } from './types.js'
import { en } from './en.js'

/** Track-lane translations for bs — currently mirrors English (translation TBD). */
export const bs: TrackLaneTranslations = { ...en }
