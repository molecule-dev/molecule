import type { TrackLaneTranslations } from './types.js'
import { en } from './en.js'

/** Track-lane translations for hr — currently mirrors English (translation TBD). */
export const hr: TrackLaneTranslations = { ...en }
