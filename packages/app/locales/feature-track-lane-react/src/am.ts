import type { TrackLaneTranslations } from './types.js'
import { en } from './en.js'

/** Track-lane translations for am — currently mirrors English (translation TBD). */
export const am: TrackLaneTranslations = { ...en }
