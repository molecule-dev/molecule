import type { TrackLaneTranslations } from './types.js'
import { en } from './en.js'

/** Track-lane translations for ms — currently mirrors English (translation TBD). */
export const ms: TrackLaneTranslations = { ...en }
