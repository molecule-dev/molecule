import type { TrackLaneTranslations } from './types.js'
import { en } from './en.js'

/** Track-lane translations for hi — currently mirrors English (translation TBD). */
export const hi: TrackLaneTranslations = { ...en }
