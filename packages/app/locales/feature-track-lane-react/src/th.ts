import type { TrackLaneTranslations } from './types.js'
import { en } from './en.js'

/** Track-lane translations for th — currently mirrors English (translation TBD). */
export const th: TrackLaneTranslations = { ...en }
