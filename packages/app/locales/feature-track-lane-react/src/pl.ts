import type { TrackLaneTranslations } from './types.js'
import { en } from './en.js'

/** Track-lane translations for pl — currently mirrors English (translation TBD). */
export const pl: TrackLaneTranslations = { ...en }
