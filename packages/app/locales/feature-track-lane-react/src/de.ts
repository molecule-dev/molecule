import type { TrackLaneTranslations } from './types.js'
import { en } from './en.js'

/** Track-lane translations for de — currently mirrors English (translation TBD). */
export const de: TrackLaneTranslations = { ...en }
