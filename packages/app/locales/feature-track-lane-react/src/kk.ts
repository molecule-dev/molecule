import type { TrackLaneTranslations } from './types.js'
import { en } from './en.js'

/** Track-lane translations for kk — currently mirrors English (translation TBD). */
export const kk: TrackLaneTranslations = { ...en }
