import type { TrackLaneTranslations } from './types.js'
import { en } from './en.js'

/** Track-lane translations for gu — currently mirrors English (translation TBD). */
export const gu: TrackLaneTranslations = { ...en }
