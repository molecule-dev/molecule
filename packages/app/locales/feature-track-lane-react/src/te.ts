import type { TrackLaneTranslations } from './types.js'
import { en } from './en.js'

/** Track-lane translations for te — currently mirrors English (translation TBD). */
export const te: TrackLaneTranslations = { ...en }
