import type { TrackLaneTranslations } from './types.js'
import { en } from './en.js'

/** Track-lane translations for uk — currently mirrors English (translation TBD). */
export const uk: TrackLaneTranslations = { ...en }
