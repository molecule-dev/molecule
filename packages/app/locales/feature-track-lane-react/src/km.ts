import type { TrackLaneTranslations } from './types.js'
import { en } from './en.js'

/** Track-lane translations for km — currently mirrors English (translation TBD). */
export const km: TrackLaneTranslations = { ...en }
