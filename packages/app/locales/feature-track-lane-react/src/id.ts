import type { TrackLaneTranslations } from './types.js'
import { en } from './en.js'

/** Track-lane translations for id — currently mirrors English (translation TBD). */
export const id: TrackLaneTranslations = { ...en }
