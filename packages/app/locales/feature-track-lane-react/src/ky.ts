import type { TrackLaneTranslations } from './types.js'
import { en } from './en.js'

/** Track-lane translations for ky — currently mirrors English (translation TBD). */
export const ky: TrackLaneTranslations = { ...en }
