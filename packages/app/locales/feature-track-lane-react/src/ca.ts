import type { TrackLaneTranslations } from './types.js'
import { en } from './en.js'

/** Track-lane translations for ca — currently mirrors English (translation TBD). */
export const ca: TrackLaneTranslations = { ...en }
