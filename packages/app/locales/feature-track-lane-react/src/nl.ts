import type { TrackLaneTranslations } from './types.js'
import { en } from './en.js'

/** Track-lane translations for nl — currently mirrors English (translation TBD). */
export const nl: TrackLaneTranslations = { ...en }
