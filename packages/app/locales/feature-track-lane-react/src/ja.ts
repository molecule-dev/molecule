import type { TrackLaneTranslations } from './types.js'
import { en } from './en.js'

/** Track-lane translations for ja — currently mirrors English (translation TBD). */
export const ja: TrackLaneTranslations = { ...en }
