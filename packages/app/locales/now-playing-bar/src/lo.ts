import type { NowPlayingBarTranslations } from './types.js'
import { en } from './en.js'

/** Now-playing-bar translations for lo — currently mirrors English (translation TBD). */
export const lo: NowPlayingBarTranslations = { ...en }
