import type { NowPlayingBarTranslations } from './types.js'
import { en } from './en.js'

/** Now-playing-bar translations for bg — currently mirrors English (translation TBD). */
export const bg: NowPlayingBarTranslations = { ...en }
