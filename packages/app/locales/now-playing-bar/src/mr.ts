import type { NowPlayingBarTranslations } from './types.js'
import { en } from './en.js'

/** Now-playing-bar translations for mr — currently mirrors English (translation TBD). */
export const mr: NowPlayingBarTranslations = { ...en }
