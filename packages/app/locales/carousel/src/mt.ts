import type { CarouselTranslations } from './types.js'

/** Carousel translations for mt. */
export const mt: Partial<CarouselTranslations> = {
  'carousel.previous': 'Preċedenti',
  'carousel.next': 'Li jmiss',
}
