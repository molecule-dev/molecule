import type { CarouselTranslations } from './types.js'

/** Carousel translations for is. */
export const is: Partial<CarouselTranslations> = {
  'carousel.previous': 'Fyrri',
  'carousel.next': 'Næst',
}
