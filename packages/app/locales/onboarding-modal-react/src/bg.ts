import type { OnboardingModalTranslations } from './types.js'

/** OnboardingModal translations for bg. */
export const bg: Partial<OnboardingModalTranslations> = {
  'onboarding.done': 'Започнете',
}
