import type { ThreeViewerTranslations } from './types.js'
import { en } from './en.js'

/** Three-viewer translations for be — currently mirrors English (translation TBD). */
export const be: ThreeViewerTranslations = { ...en }
