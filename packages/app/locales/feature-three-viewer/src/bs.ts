import type { ThreeViewerTranslations } from './types.js'
import { en } from './en.js'

/** Three-viewer translations for bs — currently mirrors English (translation TBD). */
export const bs: ThreeViewerTranslations = { ...en }
