import type { ThreeViewerTranslations } from './types.js'
import { en } from './en.js'

/** Three-viewer translations for ne — currently mirrors English (translation TBD). */
export const ne: ThreeViewerTranslations = { ...en }
