import type { ThreeViewerTranslations } from './types.js'
import { en } from './en.js'

/** Three-viewer translations for sv — currently mirrors English (translation TBD). */
export const sv: ThreeViewerTranslations = { ...en }
