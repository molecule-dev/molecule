import type { ThreeViewerTranslations } from './types.js'
import { en } from './en.js'

/** Three-viewer translations for mk — currently mirrors English (translation TBD). */
export const mk: ThreeViewerTranslations = { ...en }
