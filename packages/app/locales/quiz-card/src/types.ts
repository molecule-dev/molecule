/** Translation keys for the quiz-card locale package. */
export type QuizCardTranslationKey = 'quizCard.submit'

/** Translation record mapping quiz-card-react keys to translated strings. */
export type QuizCardTranslations = Record<QuizCardTranslationKey, string>
