import type { BiasIndicatorTranslations } from './types.js'
import { en } from './en.js'

/** Bias-indicator translations for bg — currently mirrors English (translation TBD). */
export const bg: BiasIndicatorTranslations = { ...en }
