import type { BiasIndicatorTranslations } from './types.js'
import { en } from './en.js'

/** Bias-indicator translations for ne — currently mirrors English (translation TBD). */
export const ne: BiasIndicatorTranslations = { ...en }
