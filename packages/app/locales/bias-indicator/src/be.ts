import type { BiasIndicatorTranslations } from './types.js'
import { en } from './en.js'

/** Bias-indicator translations for be — currently mirrors English (translation TBD). */
export const be: BiasIndicatorTranslations = { ...en }
