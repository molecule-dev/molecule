import type { ThreeViewerTranslations } from './types.js'
import { en } from './en.js'

/** Three-viewer translations for ms — currently mirrors English (translation TBD). */
export const ms: ThreeViewerTranslations = { ...en }
