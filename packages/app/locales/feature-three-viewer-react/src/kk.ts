import type { ThreeViewerTranslations } from './types.js'
import { en } from './en.js'

/** Three-viewer translations for kk — currently mirrors English (translation TBD). */
export const kk: ThreeViewerTranslations = { ...en }
