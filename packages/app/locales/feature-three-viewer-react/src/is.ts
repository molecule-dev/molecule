import type { ThreeViewerTranslations } from './types.js'
import { en } from './en.js'

/** Three-viewer translations for is — currently mirrors English (translation TBD). */
export const is: ThreeViewerTranslations = { ...en }
