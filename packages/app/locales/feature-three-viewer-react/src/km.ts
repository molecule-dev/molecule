import type { ThreeViewerTranslations } from './types.js'
import { en } from './en.js'

/** Three-viewer translations for km — currently mirrors English (translation TBD). */
export const km: ThreeViewerTranslations = { ...en }
