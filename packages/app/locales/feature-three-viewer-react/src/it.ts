import type { ThreeViewerTranslations } from './types.js'
import { en } from './en.js'

/** Three-viewer translations for it — currently mirrors English (translation TBD). */
export const it: ThreeViewerTranslations = { ...en }
