import type { ThreeViewerTranslations } from './types.js'
import { en } from './en.js'

/** Three-viewer translations for sw — currently mirrors English (translation TBD). */
export const sw: ThreeViewerTranslations = { ...en }
