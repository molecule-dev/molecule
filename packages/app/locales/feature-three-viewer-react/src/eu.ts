import type { ThreeViewerTranslations } from './types.js'
import { en } from './en.js'

/** Three-viewer translations for eu — currently mirrors English (translation TBD). */
export const eu: ThreeViewerTranslations = { ...en }
