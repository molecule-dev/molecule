import type { ThreeViewerTranslations } from './types.js'
import { en } from './en.js'

/** Three-viewer translations for mr — currently mirrors English (translation TBD). */
export const mr: ThreeViewerTranslations = { ...en }
