import type { ThreeViewerTranslations } from './types.js'
import { en } from './en.js'

/** Three-viewer translations for id — currently mirrors English (translation TBD). */
export const id: ThreeViewerTranslations = { ...en }
