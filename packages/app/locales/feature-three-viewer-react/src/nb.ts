import type { ThreeViewerTranslations } from './types.js'
import { en } from './en.js'

/** Three-viewer translations for nb — currently mirrors English (translation TBD). */
export const nb: ThreeViewerTranslations = { ...en }
