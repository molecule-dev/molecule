import { en } from './en.js'
import type { TransportBarTranslations } from './types.js'

/** Transport-bar translations for pa (stub — falls back to English until reviewed). */
export const pa: TransportBarTranslations = { ...en }
