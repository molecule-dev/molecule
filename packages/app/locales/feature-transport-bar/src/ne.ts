import { en } from './en.js'
import type { TransportBarTranslations } from './types.js'

/** Transport-bar translations for ne (stub — falls back to English until reviewed). */
export const ne: TransportBarTranslations = { ...en }
