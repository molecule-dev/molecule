import { en } from './en.js'
import type { TransportBarTranslations } from './types.js'

/** Transport-bar translations for et (stub — falls back to English until reviewed). */
export const et: TransportBarTranslations = { ...en }
