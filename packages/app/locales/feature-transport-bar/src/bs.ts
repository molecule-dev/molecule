import { en } from './en.js'
import type { TransportBarTranslations } from './types.js'

/** Transport-bar translations for bs (stub — falls back to English until reviewed). */
export const bs: TransportBarTranslations = { ...en }
