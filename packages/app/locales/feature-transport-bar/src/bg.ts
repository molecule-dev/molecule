import { en } from './en.js'
import type { TransportBarTranslations } from './types.js'

/** Transport-bar translations for bg (stub — falls back to English until reviewed). */
export const bg: TransportBarTranslations = { ...en }
