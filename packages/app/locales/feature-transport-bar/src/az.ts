import { en } from './en.js'
import type { TransportBarTranslations } from './types.js'

/** Transport-bar translations for az (stub — falls back to English until reviewed). */
export const az: TransportBarTranslations = { ...en }
