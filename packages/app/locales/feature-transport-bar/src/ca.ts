import { en } from './en.js'
import type { TransportBarTranslations } from './types.js'

/** Transport-bar translations for ca (stub — falls back to English until reviewed). */
export const ca: TransportBarTranslations = { ...en }
