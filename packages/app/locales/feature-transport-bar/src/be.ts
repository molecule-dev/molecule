import { en } from './en.js'
import type { TransportBarTranslations } from './types.js'

/** Transport-bar translations for be (stub — falls back to English until reviewed). */
export const be: TransportBarTranslations = { ...en }
