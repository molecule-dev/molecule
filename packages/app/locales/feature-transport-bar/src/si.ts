import { en } from './en.js'
import type { TransportBarTranslations } from './types.js'

/** Transport-bar translations for si (stub — falls back to English until reviewed). */
export const si: TransportBarTranslations = { ...en }
