import { en } from './en.js'
import type { TransportBarTranslations } from './types.js'

/** Transport-bar translations for yo (stub — falls back to English until reviewed). */
export const yo: TransportBarTranslations = { ...en }
