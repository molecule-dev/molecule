import { en } from './en.js'
import type { TransportBarTranslations } from './types.js'

/** Transport-bar translations for my (stub — falls back to English until reviewed). */
export const my: TransportBarTranslations = { ...en }
