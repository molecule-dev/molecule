import { en } from './en.js'
import type { TransportBarTranslations } from './types.js'

/** Transport-bar translations for kn (stub — falls back to English until reviewed). */
export const kn: TransportBarTranslations = { ...en }
