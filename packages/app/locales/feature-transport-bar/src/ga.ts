import { en } from './en.js'
import type { TransportBarTranslations } from './types.js'

/** Transport-bar translations for ga (stub — falls back to English until reviewed). */
export const ga: TransportBarTranslations = { ...en }
