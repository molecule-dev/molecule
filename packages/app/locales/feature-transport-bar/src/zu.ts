import { en } from './en.js'
import type { TransportBarTranslations } from './types.js'

/** Transport-bar translations for zu (stub — falls back to English until reviewed). */
export const zu: TransportBarTranslations = { ...en }
