import { en } from './en.js'
import type { TransportBarTranslations } from './types.js'

/** Transport-bar translations for af (stub — falls back to English until reviewed). */
export const af: TransportBarTranslations = { ...en }
