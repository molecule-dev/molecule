import { en } from './en.js'
import type { TransportBarTranslations } from './types.js'

/** Transport-bar translations for uz (stub — falls back to English until reviewed). */
export const uz: TransportBarTranslations = { ...en }
