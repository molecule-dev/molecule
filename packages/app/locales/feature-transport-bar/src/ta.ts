import { en } from './en.js'
import type { TransportBarTranslations } from './types.js'

/** Transport-bar translations for ta (stub — falls back to English until reviewed). */
export const ta: TransportBarTranslations = { ...en }
