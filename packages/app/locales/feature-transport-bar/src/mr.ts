import { en } from './en.js'
import type { TransportBarTranslations } from './types.js'

/** Transport-bar translations for mr (stub — falls back to English until reviewed). */
export const mr: TransportBarTranslations = { ...en }
