import type { BarcodeScannerTranslations } from './types.js'

/** BarcodeScanner translations for it. */
export const it: Partial<BarcodeScannerTranslations> = {
  'barcodeScanner.status.scanning': 'Scansione…',
}
