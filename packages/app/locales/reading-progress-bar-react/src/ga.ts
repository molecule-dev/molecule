import type { ReadingProgressBarTranslations } from './types.js'

/** Reading-progress-bar translations for ga. Stub — falls back to English. */
export const ga: ReadingProgressBarTranslations = {
  'readingProgressBar.aria.label': 'Reading progress',
}
