import type { ScreenOrientationTranslations } from './types.js'

/** Screen Orientation translations for Hungarian. */
export const hu: ScreenOrientationTranslations = {
  'screenOrientation.error.noProvider':
    '@molecule/app-screen-orientation: Nincs szolgáltató beállítva. Hívja meg a setProvider()-t egy ScreenOrientationProvider implementációval (pl. a @molecule/app-screen-orientation-capacitor-ból).',
}
