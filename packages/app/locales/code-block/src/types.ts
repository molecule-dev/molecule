/** Translation keys for the code-block locale package. */
export type CodeBlockTranslationKey = 'codeBlock.copied' | 'codeBlock.copy'

/** Translation record mapping code-block-react keys to translated strings. */
export type CodeBlockTranslations = Record<CodeBlockTranslationKey, string>
