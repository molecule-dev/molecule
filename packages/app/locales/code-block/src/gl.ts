import type { CodeBlockTranslations } from './types.js'

/** CodeBlock translations for gl. */
export const gl: Partial<CodeBlockTranslations> = {
  'codeBlock.copied': 'Copiado!',
  'codeBlock.copy': 'Copiar',
}
