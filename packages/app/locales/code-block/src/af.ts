import type { CodeBlockTranslations } from './types.js'

/** CodeBlock translations for af. */
export const af: Partial<CodeBlockTranslations> = {
  'codeBlock.copied': 'Gekopieer!',
  'codeBlock.copy': 'Kopieer',
}
