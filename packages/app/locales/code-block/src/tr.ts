import type { CodeBlockTranslations } from './types.js'

/** CodeBlock translations for tr. */
export const tr: Partial<CodeBlockTranslations> = {
  'codeBlock.copied': 'Kopyalandı!',
  'codeBlock.copy': 'Kopyala',
}
