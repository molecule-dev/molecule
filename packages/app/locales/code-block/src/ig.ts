import type { CodeBlockTranslations } from './types.js'

/** CodeBlock translations for ig. */
export const ig: Partial<CodeBlockTranslations> = {
  'codeBlock.copied': 'E depụtaghachiri ya!',
  'codeBlock.copy': 'Detuo',
}
