import type { CodeBlockTranslations } from './types.js'

/** CodeBlock translations for bs. */
export const bs: Partial<CodeBlockTranslations> = {
  'codeBlock.copied': 'Kopirano!',
  'codeBlock.copy': 'Kopiraj',
}
