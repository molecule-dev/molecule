import type { CodeBlockTranslations } from './types.js'

/** CodeBlock translations for is. */
export const is: Partial<CodeBlockTranslations> = {
  'codeBlock.copied': 'Afritað!',
  'codeBlock.copy': 'Afrita',
}
