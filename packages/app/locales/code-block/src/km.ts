import type { CodeBlockTranslations } from './types.js'

/** CodeBlock translations for km. */
export const km: Partial<CodeBlockTranslations> = {
  'codeBlock.copied': 'បានចម្លងហើយ!',
  'codeBlock.copy': 'ចម្លង',
}
