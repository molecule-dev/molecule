import type { CodeBlockTranslations } from './types.js'

/** CodeBlock translations for bg. */
export const bg: Partial<CodeBlockTranslations> = {
  'codeBlock.copied': 'Копирано!',
  'codeBlock.copy': 'Копиране',
}
