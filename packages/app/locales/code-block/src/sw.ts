import type { CodeBlockTranslations } from './types.js'

/** CodeBlock translations for sw. */
export const sw: Partial<CodeBlockTranslations> = {
  'codeBlock.copied': 'Imenakiliwa!',
  'codeBlock.copy': 'Nakili',
}
