import type { CodeBlockTranslations } from './types.js'

/** CodeBlock translations for et. */
export const et: Partial<CodeBlockTranslations> = {
  'codeBlock.copied': 'Kopeeritud!',
  'codeBlock.copy': 'Kopeeri',
}
