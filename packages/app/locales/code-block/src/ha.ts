import type { CodeBlockTranslations } from './types.js'

/** CodeBlock translations for ha. */
export const ha: Partial<CodeBlockTranslations> = {
  'codeBlock.copied': 'An kwafi!',
  'codeBlock.copy': 'Kwafi',
}
