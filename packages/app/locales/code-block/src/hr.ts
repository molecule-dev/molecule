import type { CodeBlockTranslations } from './types.js'

/** CodeBlock translations for hr. */
export const hr: Partial<CodeBlockTranslations> = {
  'codeBlock.copied': 'Kopirano!',
  'codeBlock.copy': 'Kopirati',
}
