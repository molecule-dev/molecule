import type { CodeBlockTranslations } from './types.js'

/** CodeBlock translations for lt. */
export const lt: Partial<CodeBlockTranslations> = {
  'codeBlock.copied': 'Nukopijuota!',
  'codeBlock.copy': 'Kopijuoti',
}
