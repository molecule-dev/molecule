import type { CodeBlockTranslations } from './types.js'

/** CodeBlock translations for lo. */
export const lo: Partial<CodeBlockTranslations> = {
  'codeBlock.copied': 'ສຳເນົາແລ້ວ!',
  'codeBlock.copy': 'ສຳເນົາ',
}
