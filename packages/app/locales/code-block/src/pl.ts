import type { CodeBlockTranslations } from './types.js'

/** CodeBlock translations for pl. */
export const pl: Partial<CodeBlockTranslations> = {
  'codeBlock.copied': 'Skopiowano!',
  'codeBlock.copy': 'Kopiuj',
}
