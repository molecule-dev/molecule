import type { CodeBlockTranslations } from './types.js'

/** CodeBlock translations for sr. */
export const sr: Partial<CodeBlockTranslations> = {
  'codeBlock.copied': 'Копирано!',
  'codeBlock.copy': 'Копија',
}
