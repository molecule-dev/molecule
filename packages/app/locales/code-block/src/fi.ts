import type { CodeBlockTranslations } from './types.js'

/** CodeBlock translations for fi. */
export const fi: Partial<CodeBlockTranslations> = {
  'codeBlock.copied': 'Kopioitu!',
  'codeBlock.copy': 'Kopioi',
}
