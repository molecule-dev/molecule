import type { CodeBlockTranslations } from './types.js'

/** CodeBlock translations for mk. */
export const mk: Partial<CodeBlockTranslations> = {
  'codeBlock.copied': 'Копирано!',
  'codeBlock.copy': 'Копирај',
}
