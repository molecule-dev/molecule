import type { CodeBlockTranslations } from './types.js'

/** CodeBlock translations for zu. */
export const zu: Partial<CodeBlockTranslations> = {
  'codeBlock.copied': 'Kukopishiwe!',
  'codeBlock.copy': 'Kopisha',
}
