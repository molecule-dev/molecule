import type { BarcodeScannerTranslations } from './types.js'
import { en } from './en.js'

/** Barcode-scanner translations for hr — currently mirrors English (translation TBD). */
export const hr: BarcodeScannerTranslations = { ...en }
