import type { BarcodeScannerTranslations } from './types.js'
import { en } from './en.js'

/** Barcode-scanner translations for am — currently mirrors English (translation TBD). */
export const am: BarcodeScannerTranslations = { ...en }
