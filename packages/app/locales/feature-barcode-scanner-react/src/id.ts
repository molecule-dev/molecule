import type { BarcodeScannerTranslations } from './types.js'
import { en } from './en.js'

/** Barcode-scanner translations for id — currently mirrors English (translation TBD). */
export const id: BarcodeScannerTranslations = { ...en }
