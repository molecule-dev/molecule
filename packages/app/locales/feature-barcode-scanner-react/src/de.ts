import type { BarcodeScannerTranslations } from './types.js'
import { en } from './en.js'

/** Barcode-scanner translations for de — currently mirrors English (translation TBD). */
export const de: BarcodeScannerTranslations = { ...en }
