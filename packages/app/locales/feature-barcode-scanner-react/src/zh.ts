import type { BarcodeScannerTranslations } from './types.js'
import { en } from './en.js'

/** Barcode-scanner translations for zh — currently mirrors English (translation TBD). */
export const zh: BarcodeScannerTranslations = { ...en }
