import type { BarcodeScannerTranslations } from './types.js'
import { en } from './en.js'

/** Barcode-scanner translations for ka — currently mirrors English (translation TBD). */
export const ka: BarcodeScannerTranslations = { ...en }
