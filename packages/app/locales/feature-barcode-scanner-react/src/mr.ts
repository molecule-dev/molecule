import type { BarcodeScannerTranslations } from './types.js'
import { en } from './en.js'

/** Barcode-scanner translations for mr — currently mirrors English (translation TBD). */
export const mr: BarcodeScannerTranslations = { ...en }
