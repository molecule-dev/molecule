import type { BarcodeScannerTranslations } from './types.js'
import { en } from './en.js'

/** Barcode-scanner translations for uz — currently mirrors English (translation TBD). */
export const uz: BarcodeScannerTranslations = { ...en }
