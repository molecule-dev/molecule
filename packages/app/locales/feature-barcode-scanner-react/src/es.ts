import type { BarcodeScannerTranslations } from './types.js'
import { en } from './en.js'

/** Barcode-scanner translations for es — currently mirrors English (translation TBD). */
export const es: BarcodeScannerTranslations = { ...en }
