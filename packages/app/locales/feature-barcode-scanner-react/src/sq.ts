import type { BarcodeScannerTranslations } from './types.js'
import { en } from './en.js'

/** Barcode-scanner translations for sq — currently mirrors English (translation TBD). */
export const sq: BarcodeScannerTranslations = { ...en }
