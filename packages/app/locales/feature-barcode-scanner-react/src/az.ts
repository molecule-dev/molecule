import type { BarcodeScannerTranslations } from './types.js'
import { en } from './en.js'

/** Barcode-scanner translations for az — currently mirrors English (translation TBD). */
export const az: BarcodeScannerTranslations = { ...en }
