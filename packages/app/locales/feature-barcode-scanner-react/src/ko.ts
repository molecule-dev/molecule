import type { BarcodeScannerTranslations } from './types.js'
import { en } from './en.js'

/** Barcode-scanner translations for ko — currently mirrors English (translation TBD). */
export const ko: BarcodeScannerTranslations = { ...en }
