import type { BarcodeScannerTranslations } from './types.js'
import { en } from './en.js'

/** Barcode-scanner translations for is — currently mirrors English (translation TBD). */
export const is: BarcodeScannerTranslations = { ...en }
