import type { BarcodeScannerTranslations } from './types.js'
import { en } from './en.js'

/** Barcode-scanner translations for kk — currently mirrors English (translation TBD). */
export const kk: BarcodeScannerTranslations = { ...en }
