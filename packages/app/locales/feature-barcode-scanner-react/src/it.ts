import type { BarcodeScannerTranslations } from './types.js'
import { en } from './en.js'

/** Barcode-scanner translations for it — currently mirrors English (translation TBD). */
export const it: BarcodeScannerTranslations = { ...en }
