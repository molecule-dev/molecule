import type { BarcodeScannerTranslations } from './types.js'
import { en } from './en.js'

/** Barcode-scanner translations for nl — currently mirrors English (translation TBD). */
export const nl: BarcodeScannerTranslations = { ...en }
