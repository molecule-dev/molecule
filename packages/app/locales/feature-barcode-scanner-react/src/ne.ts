import type { BarcodeScannerTranslations } from './types.js'
import { en } from './en.js'

/** Barcode-scanner translations for ne — currently mirrors English (translation TBD). */
export const ne: BarcodeScannerTranslations = { ...en }
