import type { BarcodeScannerTranslations } from './types.js'
import { en } from './en.js'

/** Barcode-scanner translations for vi — currently mirrors English (translation TBD). */
export const vi: BarcodeScannerTranslations = { ...en }
