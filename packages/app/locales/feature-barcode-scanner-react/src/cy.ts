import type { BarcodeScannerTranslations } from './types.js'
import { en } from './en.js'

/** Barcode-scanner translations for cy — currently mirrors English (translation TBD). */
export const cy: BarcodeScannerTranslations = { ...en }
