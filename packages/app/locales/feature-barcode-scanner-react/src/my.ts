import type { BarcodeScannerTranslations } from './types.js'
import { en } from './en.js'

/** Barcode-scanner translations for my — currently mirrors English (translation TBD). */
export const my: BarcodeScannerTranslations = { ...en }
