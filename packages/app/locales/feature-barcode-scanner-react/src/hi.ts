import type { BarcodeScannerTranslations } from './types.js'
import { en } from './en.js'

/** Barcode-scanner translations for hi — currently mirrors English (translation TBD). */
export const hi: BarcodeScannerTranslations = { ...en }
