import type { NowPlayingBarTranslations } from './types.js'
import { en } from './en.js'

/** Now-playing-bar translations for te — currently mirrors English (translation TBD). */
export const te: NowPlayingBarTranslations = { ...en }
