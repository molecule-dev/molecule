import type { NowPlayingBarTranslations } from './types.js'
import { en } from './en.js'

/** Now-playing-bar translations for zu — currently mirrors English (translation TBD). */
export const zu: NowPlayingBarTranslations = { ...en }
