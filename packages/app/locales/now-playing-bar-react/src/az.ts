import type { NowPlayingBarTranslations } from './types.js'
import { en } from './en.js'

/** Now-playing-bar translations for az — currently mirrors English (translation TBD). */
export const az: NowPlayingBarTranslations = { ...en }
