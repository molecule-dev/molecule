import type { NowPlayingBarTranslations } from './types.js'
import { en } from './en.js'

/** Now-playing-bar translations for id — currently mirrors English (translation TBD). */
export const id: NowPlayingBarTranslations = { ...en }
