import type { NowPlayingBarTranslations } from './types.js'
import { en } from './en.js'

/** Now-playing-bar translations for mk — currently mirrors English (translation TBD). */
export const mk: NowPlayingBarTranslations = { ...en }
