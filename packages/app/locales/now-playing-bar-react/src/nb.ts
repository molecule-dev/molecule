import type { NowPlayingBarTranslations } from './types.js'
import { en } from './en.js'

/** Now-playing-bar translations for nb — currently mirrors English (translation TBD). */
export const nb: NowPlayingBarTranslations = { ...en }
