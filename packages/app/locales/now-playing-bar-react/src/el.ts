import type { NowPlayingBarTranslations } from './types.js'
import { en } from './en.js'

/** Now-playing-bar translations for el — currently mirrors English (translation TBD). */
export const el: NowPlayingBarTranslations = { ...en }
