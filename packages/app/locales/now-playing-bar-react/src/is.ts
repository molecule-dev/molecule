import type { NowPlayingBarTranslations } from './types.js'
import { en } from './en.js'

/** Now-playing-bar translations for is — currently mirrors English (translation TBD). */
export const is: NowPlayingBarTranslations = { ...en }
