import type { NowPlayingBarTranslations } from './types.js'
import { en } from './en.js'

/** Now-playing-bar translations for sv — currently mirrors English (translation TBD). */
export const sv: NowPlayingBarTranslations = { ...en }
