import type { EmbedSnippetTranslations } from './types.js'
import { en } from './en.js'

/** Embed-snippet translations for zu — currently mirrors English (translation TBD). */
export const zu: EmbedSnippetTranslations = { ...en }
