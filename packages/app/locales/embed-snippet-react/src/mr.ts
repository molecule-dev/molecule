import type { EmbedSnippetTranslations } from './types.js'
import { en } from './en.js'

/** Embed-snippet translations for mr — currently mirrors English (translation TBD). */
export const mr: EmbedSnippetTranslations = { ...en }
