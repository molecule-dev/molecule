import type { EmbedSnippetTranslations } from './types.js'
import { en } from './en.js'

/** Embed-snippet translations for sv — currently mirrors English (translation TBD). */
export const sv: EmbedSnippetTranslations = { ...en }
