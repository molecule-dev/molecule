import type { EmbedSnippetTranslations } from './types.js'
import { en } from './en.js'

/** Embed-snippet translations for nb — currently mirrors English (translation TBD). */
export const nb: EmbedSnippetTranslations = { ...en }
