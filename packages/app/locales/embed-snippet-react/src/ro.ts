import type { EmbedSnippetTranslations } from './types.js'
import { en } from './en.js'

/** Embed-snippet translations for ro — currently mirrors English (translation TBD). */
export const ro: EmbedSnippetTranslations = { ...en }
