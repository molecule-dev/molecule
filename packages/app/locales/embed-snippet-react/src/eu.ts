import type { EmbedSnippetTranslations } from './types.js'
import { en } from './en.js'

/** Embed-snippet translations for eu — currently mirrors English (translation TBD). */
export const eu: EmbedSnippetTranslations = { ...en }
