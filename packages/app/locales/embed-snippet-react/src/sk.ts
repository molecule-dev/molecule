import type { EmbedSnippetTranslations } from './types.js'
import { en } from './en.js'

/** Embed-snippet translations for sk — currently mirrors English (translation TBD). */
export const sk: EmbedSnippetTranslations = { ...en }
