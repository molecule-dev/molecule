import type { EmbedSnippetTranslations } from './types.js'
import { en } from './en.js'

/** Embed-snippet translations for te — currently mirrors English (translation TBD). */
export const te: EmbedSnippetTranslations = { ...en }
