import type { EmbedSnippetTranslations } from './types.js'
import { en } from './en.js'

/** Embed-snippet translations for fa — currently mirrors English (translation TBD). */
export const fa: EmbedSnippetTranslations = { ...en }
