import type { EmbedSnippetTranslations } from './types.js'
import { en } from './en.js'

/** Embed-snippet translations for sl — currently mirrors English (translation TBD). */
export const sl: EmbedSnippetTranslations = { ...en }
