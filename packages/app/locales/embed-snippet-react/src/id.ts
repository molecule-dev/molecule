import type { EmbedSnippetTranslations } from './types.js'
import { en } from './en.js'

/** Embed-snippet translations for id — currently mirrors English (translation TBD). */
export const id: EmbedSnippetTranslations = { ...en }
