import type { EmbedSnippetTranslations } from './types.js'
import { en } from './en.js'

/** Embed-snippet translations for it — currently mirrors English (translation TBD). */
export const it: EmbedSnippetTranslations = { ...en }
