import type { EmbedSnippetTranslations } from './types.js'
import { en } from './en.js'

/** Embed-snippet translations for km — currently mirrors English (translation TBD). */
export const km: EmbedSnippetTranslations = { ...en }
