import type { EmbedSnippetTranslations } from './types.js'
import { en } from './en.js'

/** Embed-snippet translations for lt — currently mirrors English (translation TBD). */
export const lt: EmbedSnippetTranslations = { ...en }
