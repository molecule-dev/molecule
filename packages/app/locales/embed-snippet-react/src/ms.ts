import type { EmbedSnippetTranslations } from './types.js'
import { en } from './en.js'

/** Embed-snippet translations for ms — currently mirrors English (translation TBD). */
export const ms: EmbedSnippetTranslations = { ...en }
