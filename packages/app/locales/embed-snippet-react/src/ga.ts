import type { EmbedSnippetTranslations } from './types.js'
import { en } from './en.js'

/** Embed-snippet translations for ga — currently mirrors English (translation TBD). */
export const ga: EmbedSnippetTranslations = { ...en }
