import type { NfcTranslations } from './types.js'

/** Nfc translations for Slovak. */
export const sk: NfcTranslations = {
  'nfc.error.noProvider':
    '@molecule/app-nfc: Poskytovatel nie je nastaveny. Zavolajte setProvider() s implementaciou NfcProvider (napr. z @molecule/app-nfc-capacitor).',
}
