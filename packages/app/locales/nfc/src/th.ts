import type { NfcTranslations } from './types.js'

/** Nfc translations for Thai. */
export const th: NfcTranslations = {
  'nfc.error.noProvider':
    '@molecule/app-nfc: ยังไม่ได้ตั้งค่าผู้ให้บริการ เรียก setProvider() พร้อมการใช้งาน NfcProvider (เช่น จาก @molecule/app-nfc-capacitor)',
}
