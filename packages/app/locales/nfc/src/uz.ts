import type { NfcTranslations } from './types.js'

/** Nfc translations for uz. */
export const uz: Partial<NfcTranslations> = {
  'nfc.error.noProvider':
    '@molecule/app-nfc: Provayder o&#39;rnatilmagan. NfcProvider implementatsiyasi bilan setProvider() ni chaqiring (masalan, @molecule/app-nfc-capacitor dan).',
}
