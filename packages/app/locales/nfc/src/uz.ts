import type { NfcTranslations } from './types.js'

/** Nfc translations for Uzbek. */
export const uz: NfcTranslations = {
  'nfc.error.noProvider':
    "@molecule/app-nfc: Provayder o'rnatilmagan. setProvider() ni NfcProvider ilovasi bilan chaqiring (masalan, @molecule/app-nfc-capacitor dan).",
}
