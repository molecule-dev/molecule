import type { NfcTranslations } from './types.js'

/** Nfc translations for Belarusian. */
export const be: NfcTranslations = {
  'nfc.error.noProvider':
    '@molecule/app-nfc: Правайдар не ўстаноўлены. Выклічце setProvider() з рэалізацыяй NfcProvider (напр., з @molecule/app-nfc-capacitor).',
}
