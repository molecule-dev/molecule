import type { NfcTranslations } from './types.js'

/** Nfc translations for Telugu. */
export const te: NfcTranslations = {
  'nfc.error.noProvider':
    '@molecule/app-nfc: ప్రొవైడర్ సెట్ చేయబడలేదు. NfcProvider అమలుతో setProvider() ని కాల్ చేయండి (ఉదా., @molecule/app-nfc-capacitor నుండి).',
}
