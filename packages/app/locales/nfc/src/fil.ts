import type { NfcTranslations } from './types.js'

/** Nfc translations for Filipino. */
export const fil: NfcTranslations = {
  'nfc.error.noProvider':
    '@molecule/app-nfc: Walang provider na naka-set. Tawagin ang setProvider() na may NfcProvider na implementasyon (hal., mula sa @molecule/app-nfc-capacitor).',
}
