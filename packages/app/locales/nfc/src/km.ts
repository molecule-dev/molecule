import type { NfcTranslations } from './types.js'

/** Nfc translations for Khmer. */
export const km: NfcTranslations = {
  'nfc.error.noProvider':
    '@molecule/app-nfc: មិនបានកំណត់អ្នកផ្តល់សេវា។ ហៅ setProvider() ជាមួយការអនុវត្ត NfcProvider (ឧ., ពី @molecule/app-nfc-capacitor)។',
}
