import type { NfcTranslations } from './types.js'

/** Nfc translations for Zulu. */
export const zu: NfcTranslations = {
  'nfc.error.noProvider':
    '@molecule/app-nfc: Akukho mhlinzeki osetiwe. Shayela setProvider() ngokusetshenziswa kwe-NfcProvider (isib., kusuka ku-@molecule/app-nfc-capacitor).',
}
