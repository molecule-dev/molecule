import type { NfcTranslations } from './types.js'

/** Nfc translations for Marathi. */
export const mr: NfcTranslations = {
  'nfc.error.noProvider':
    '@molecule/app-nfc: प्रदाता सेट केलेला नाही. NfcProvider अंमलबजावणीसह setProvider() कॉल करा (उदा., @molecule/app-nfc-capacitor वरून).',
}
