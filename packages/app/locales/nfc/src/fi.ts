import type { NfcTranslations } from './types.js'

/** Nfc translations for Finnish. */
export const fi: NfcTranslations = {
  'nfc.error.noProvider':
    '@molecule/app-nfc: Palveluntarjoajaa ei ole asetettu. Kutsu setProvider() NfcProvider-toteutuksella (esim. @molecule/app-nfc-capacitor).',
}
