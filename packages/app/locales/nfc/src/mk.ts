import type { NfcTranslations } from './types.js'

/** Nfc translations for Macedonian. */
export const mk: NfcTranslations = {
  'nfc.error.noProvider':
    '@molecule/app-nfc: Не е поставен провајдер. Повикајте setProvider() со имплементација на NfcProvider (на пр., од @molecule/app-nfc-capacitor).',
}
