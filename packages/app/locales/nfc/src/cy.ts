import type { NfcTranslations } from './types.js'

/** Nfc translations for Welsh. */
export const cy: NfcTranslations = {
  'nfc.error.noProvider':
    "@molecule/app-nfc: Nid yw'r darparwr wedi'i osod. Galwch setProvider() gyda gweithrediad NfcProvider (e.e., o @molecule/app-nfc-capacitor).",
}
