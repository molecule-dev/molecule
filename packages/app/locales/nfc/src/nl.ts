import type { NfcTranslations } from './types.js'

/** Nfc translations for Dutch. */
export const nl: NfcTranslations = {
  'nfc.error.noProvider':
    '@molecule/app-nfc: Geen provider ingesteld. Roep setProvider() aan met een NfcProvider-implementatie (bijv. van @molecule/app-nfc-capacitor).',
}
