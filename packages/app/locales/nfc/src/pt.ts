import type { NfcTranslations } from './types.js'

/** Nfc translations for Portuguese. */
export const pt: NfcTranslations = {
  'nfc.error.noProvider':
    '@molecule/app-nfc: Nenhum provedor definido. Chame setProvider() com uma implementacao de NfcProvider (ex., de @molecule/app-nfc-capacitor).',
}
