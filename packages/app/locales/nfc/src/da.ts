import type { NfcTranslations } from './types.js'

/** Nfc translations for Danish. */
export const da: NfcTranslations = {
  'nfc.error.noProvider':
    '@molecule/app-nfc: Ingen udbyder er sat. Kald setProvider() med en NfcProvider-implementering (f.eks. fra @molecule/app-nfc-capacitor).',
}
