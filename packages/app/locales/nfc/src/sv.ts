import type { NfcTranslations } from './types.js'

/** Nfc translations for Swedish. */
export const sv: NfcTranslations = {
  'nfc.error.noProvider':
    '@molecule/app-nfc: Ingen leverantor har angetts. Anropa setProvider() med en NfcProvider-implementering (t.ex. fran @molecule/app-nfc-capacitor).',
}
