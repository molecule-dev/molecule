import type { NfcTranslations } from './types.js'

/** Nfc translations for Yoruba. */
export const yo: NfcTranslations = {
  'nfc.error.noProvider':
    '@molecule/app-nfc: Ko si olupese ti a eto. Pe setProvider() pelu imuse NfcProvider (fun apeere, lati @molecule/app-nfc-capacitor).',
}
