import type { NfcTranslations } from './types.js'

/** Nfc translations for Indonesian. */
export const id: NfcTranslations = {
  'nfc.error.noProvider':
    '@molecule/app-nfc: Penyedia belum diatur. Panggil setProvider() dengan implementasi NfcProvider (mis., dari @molecule/app-nfc-capacitor).',
}
