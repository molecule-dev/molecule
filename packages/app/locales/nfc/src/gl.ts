import type { NfcTranslations } from './types.js'

/** Nfc translations for Galician. */
export const gl: NfcTranslations = {
  'nfc.error.noProvider':
    '@molecule/app-nfc: Non se estableceu ningún provedor. Chame a setProvider() cunha implementación de NfcProvider (p. ex., de @molecule/app-nfc-capacitor).',
}
