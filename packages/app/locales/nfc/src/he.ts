import type { NfcTranslations } from './types.js'

/** Nfc translations for Hebrew. */
export const he: NfcTranslations = {
  'nfc.error.noProvider':
    '@molecule/app-nfc: לא הוגדר ספק. קרא ל-setProvider() עם מימוש של NfcProvider (למשל, מ-@molecule/app-nfc-capacitor).',
}
