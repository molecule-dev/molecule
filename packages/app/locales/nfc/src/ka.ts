import type { NfcTranslations } from './types.js'

/** Nfc translations for Georgian. */
export const ka: NfcTranslations = {
  'nfc.error.noProvider':
    '@molecule/app-nfc: პროვაიდერი არ არის დაყენებული. გამოიძახეთ setProvider() NfcProvider იმპლემენტაციით.',
}
