import type { NfcTranslations } from './types.js'

/** Nfc translations for German. */
export const de: NfcTranslations = {
  'nfc.error.noProvider':
    '@molecule/app-nfc: Kein Anbieter gesetzt. Rufen Sie setProvider() mit einer NfcProvider-Implementierung auf (z. B. von @molecule/app-nfc-capacitor).',
}
