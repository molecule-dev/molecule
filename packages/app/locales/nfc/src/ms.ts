import type { NfcTranslations } from './types.js'

/** Nfc translations for Malay. */
export const ms: NfcTranslations = {
  'nfc.error.noProvider':
    '@molecule/app-nfc: Pembekal belum ditetapkan. Panggil setProvider() dengan pelaksanaan NfcProvider (cth., dari @molecule/app-nfc-capacitor).',
}
