import type { NfcTranslations } from './types.js'

/** Nfc translations for Hausa. */
export const ha: NfcTranslations = {
  'nfc.error.noProvider':
    '@molecule/app-nfc: Ba a saita mai bayarwa ba. Kira setProvider() tare da aiwatar da NfcProvider (misali, daga @molecule/app-nfc-capacitor).',
}
