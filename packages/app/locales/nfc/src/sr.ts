import type { NfcTranslations } from './types.js'

/** Nfc translations for Serbian. */
export const sr: NfcTranslations = {
  'nfc.error.noProvider':
    '@molecule/app-nfc: Provajder nije postavljen. Pozovite setProvider() sa implementacijom NfcProvider (npr. iz @molecule/app-nfc-capacitor).',
}
