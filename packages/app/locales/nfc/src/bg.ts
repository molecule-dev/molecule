import type { NfcTranslations } from './types.js'

/** Nfc translations for Bulgarian. */
export const bg: NfcTranslations = {
  'nfc.error.noProvider':
    '@molecule/app-nfc: Не е зададен доставчик. Извикайте setProvider() с имплементация на NfcProvider (напр. от @molecule/app-nfc-capacitor).',
}
