import type { NfcTranslations } from './types.js'

/** Nfc translations for Greek. */
export const el: NfcTranslations = {
  'nfc.error.noProvider':
    '@molecule/app-nfc: Δεν έχει οριστεί πάροχος. Καλέστε setProvider() με μια υλοποίηση NfcProvider (π.χ., από @molecule/app-nfc-capacitor).',
}
