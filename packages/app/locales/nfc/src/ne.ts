import type { NfcTranslations } from './types.js'

/** Nfc translations for Nepali. */
export const ne: NfcTranslations = {
  'nfc.error.noProvider':
    '@molecule/app-nfc: प्रदायक सेट गरिएको छैन। NfcProvider कार्यान्वयनसँग setProvider() कल गर्नुहोस् (उदा., @molecule/app-nfc-capacitor बाट)।',
}
