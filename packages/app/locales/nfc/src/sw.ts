import type { NfcTranslations } from './types.js'

/** Nfc translations for Swahili. */
export const sw: NfcTranslations = {
  'nfc.error.noProvider':
    '@molecule/app-nfc: Hakuna mtoa huduma aliyewekwa. Piga simu setProvider() na utekelezaji wa NfcProvider (mfano, kutoka @molecule/app-nfc-capacitor).',
}
