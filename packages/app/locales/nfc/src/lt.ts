import type { NfcTranslations } from './types.js'

/** Nfc translations for Lithuanian. */
export const lt: NfcTranslations = {
  'nfc.error.noProvider':
    '@molecule/app-nfc: Teikiatejas nenustatytas. Iskvieskite setProvider() su NfcProvider realizacija (pvz., is @molecule/app-nfc-capacitor).',
}
