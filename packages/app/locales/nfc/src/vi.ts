import type { NfcTranslations } from './types.js'

/** Nfc translations for Vietnamese. */
export const vi: NfcTranslations = {
  'nfc.error.noProvider':
    '@molecule/app-nfc: Chua thiet lap nha cung cap. Goi setProvider() voi mot trien khai NfcProvider (vi du, tu @molecule/app-nfc-capacitor).',
}
