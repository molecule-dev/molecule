import type { NfcTranslations } from './types.js'

/** Nfc translations for Korean. */
export const ko: NfcTranslations = {
  'nfc.error.noProvider':
    '@molecule/app-nfc: 공급자가 설정되지 않았습니다. NfcProvider 구현으로 setProvider()를 호출하세요 (예: @molecule/app-nfc-capacitor에서).',
}
