import type { NfcTranslations } from './types.js'

/** Nfc translations for Persian. */
export const fa: NfcTranslations = {
  'nfc.error.noProvider':
    '@molecule/app-nfc: ارائه‌دهنده‌ای تنظیم نشده است. setProvider() را با یک پیاده‌سازی NfcProvider فراخوانی کنید (مثلاً از @molecule/app-nfc-capacitor).',
}
