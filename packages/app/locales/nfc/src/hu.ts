import type { NfcTranslations } from './types.js'

/** Nfc translations for Hungarian. */
export const hu: NfcTranslations = {
  'nfc.error.noProvider':
    '@molecule/app-nfc: Nincs szolgáltató beállítva. Hívja meg a setProvider()-t egy NfcProvider implementációval (pl. a @molecule/app-nfc-capacitor-ból).',
}
