import type { NfcTranslations } from './types.js'

/** Nfc translations for Latvian. */
export const lv: NfcTranslations = {
  'nfc.error.noProvider':
    '@molecule/app-nfc: Nav iestatits pakalpojumu sniedzejas. Izsauciet setProvider() ar NfcProvider implementaciju (piem., no @molecule/app-nfc-capacitor).',
}
