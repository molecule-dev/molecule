import type { NfcTranslations } from './types.js'

/** Nfc translations for Russian. */
export const ru: NfcTranslations = {
  'nfc.error.noProvider':
    '@molecule/app-nfc: Провайдер не установлен. Вызовите setProvider() с реализацией NfcProvider (напр., из @molecule/app-nfc-capacitor).',
}
