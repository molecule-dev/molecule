import type { NfcTranslations } from './types.js'

/** Nfc translations for Bengali. */
export const bn: NfcTranslations = {
  'nfc.error.noProvider':
    '@molecule/app-nfc: কোনো প্রোভাইডার সেট করা হয়নি। একটি NfcProvider ইমপ্লিমেন্টেশন দিয়ে setProvider() কল করুন (যেমন, @molecule/app-nfc-capacitor থেকে)।',
}
