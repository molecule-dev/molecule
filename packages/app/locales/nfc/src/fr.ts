import type { NfcTranslations } from './types.js'

/** Nfc translations for French. */
export const fr: NfcTranslations = {
  'nfc.error.noProvider':
    '@molecule/app-nfc: Aucun fournisseur défini. Appelez setProvider() avec une implémentation de NfcProvider (par ex., depuis @molecule/app-nfc-capacitor).',
}
