import type { NfcTranslations } from './types.js'

/** Nfc translations for Arabic. */
export const ar: NfcTranslations = {
  'nfc.error.noProvider':
    '@molecule/app-nfc: لم يتم تعيين مزود. استدعِ setProvider() مع تنفيذ NfcProvider (مثلاً من @molecule/app-nfc-capacitor).',
}
