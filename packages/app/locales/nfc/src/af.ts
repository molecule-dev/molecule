import type { NfcTranslations } from './types.js'

/** Nfc translations for Afrikaans. */
export const af: NfcTranslations = {
  'nfc.error.noProvider':
    "@molecule/app-nfc: Geen verskaffer gestel nie. Roep setProvider() met 'n NfcProvider-implementering (bv. van @molecule/app-nfc-capacitor).",
}
