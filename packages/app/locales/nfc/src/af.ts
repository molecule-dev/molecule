import type { NfcTranslations } from './types.js'

/** Nfc translations for af. */
export const af: Partial<NfcTranslations> = {
  'nfc.error.noProvider':
    "@molecule/app-nfc: Geen verskaffer gestel nie. Roep setProvider() met 'n NfcProvider-implementering (bv. vanaf @molecule/app-nfc-capacitor).",
}
