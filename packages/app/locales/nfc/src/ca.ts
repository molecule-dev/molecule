import type { NfcTranslations } from './types.js'

/** Nfc translations for Catalan. */
export const ca: NfcTranslations = {
  'nfc.error.noProvider':
    "@molecule/app-nfc: No s'ha establert cap proveïdor. Crideu setProvider() amb una implementació de NfcProvider (p. ex., de @molecule/app-nfc-capacitor).",
}
