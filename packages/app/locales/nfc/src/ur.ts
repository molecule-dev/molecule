import type { NfcTranslations } from './types.js'

/** Nfc translations for Urdu. */
export const ur: NfcTranslations = {
  'nfc.error.noProvider':
    '@molecule/app-nfc: کوئی فراہم کنندہ مقرر نہیں ہے۔ NfcProvider نفاذ کے ساتھ setProvider() کو کال کریں (مثلاً، @molecule/app-nfc-capacitor سے)۔',
}
