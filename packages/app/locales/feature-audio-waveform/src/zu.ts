import type { AudioWaveformTranslations } from './types.js'
import { en } from './en.js'

/** Audio-waveform translations for zu — currently mirrors English (translation TBD). */
export const zu: AudioWaveformTranslations = { ...en }
