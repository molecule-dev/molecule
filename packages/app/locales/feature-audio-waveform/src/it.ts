import type { AudioWaveformTranslations } from './types.js'
import { en } from './en.js'

/** Audio-waveform translations for it — currently mirrors English (translation TBD). */
export const it: AudioWaveformTranslations = { ...en }
