import type { AudioWaveformTranslations } from './types.js'
import { en } from './en.js'

/** Audio-waveform translations for sr — currently mirrors English (translation TBD). */
export const sr: AudioWaveformTranslations = { ...en }
