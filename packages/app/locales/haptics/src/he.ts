import type { HapticsTranslations } from './types.js'

/** Haptics translations for Hebrew. */
export const he: HapticsTranslations = {
  'haptics.error.noProvider':
    '@molecule/app-haptics: לא הוגדר ספק. קרא ל-setProvider() עם מימוש של HapticsProvider (למשל, מ-@molecule/app-haptics-capacitor).',
}
