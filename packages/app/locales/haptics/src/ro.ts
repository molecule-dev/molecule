import type { HapticsTranslations } from './types.js'

/** Haptics translations for Romanian. */
export const ro: HapticsTranslations = {
  'haptics.error.noProvider':
    '@molecule/app-haptics: Niciun furnizor setat. Apelati setProvider() cu o implementare HapticsProvider (de ex., din @molecule/app-haptics-capacitor).',
}
