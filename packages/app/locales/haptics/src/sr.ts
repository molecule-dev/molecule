import type { HapticsTranslations } from './types.js'

/** Haptics translations for Serbian. */
export const sr: HapticsTranslations = {
  'haptics.error.noProvider':
    '@molecule/app-haptics: Provajder nije postavljen. Pozovite setProvider() sa implementacijom HapticsProvider (npr. iz @molecule/app-haptics-capacitor).',
}
