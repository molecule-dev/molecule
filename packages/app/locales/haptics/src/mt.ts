import type { HapticsTranslations } from './types.js'

/** Haptics translations for mt. */
export const mt: Partial<HapticsTranslations> = {
  'haptics.error.noProvider':
    "@molecule/app-haptics: L-ebda fornitur ma ġie stabbilit. Sejħa setProvider() b'implimentazzjoni ta' HapticsProvider.",
}
