import type { HapticsTranslations } from './types.js'

/** Haptics translations for Maltese. */
export const mt: HapticsTranslations = {
  'haptics.error.noProvider':
    "@molecule/app-haptics: L-ebda fornitur mhux issettjat. Sejjah setProvider() b'implimentazzjoni ta' HapticsProvider (eż., minn @molecule/app-haptics-capacitor).",
}
