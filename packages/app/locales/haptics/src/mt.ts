import type { HapticsTranslations } from './types.js'

/** Haptics translations for mt. */
export const mt: Partial<HapticsTranslations> = {
  'haptics.error.noProvider':
    '@molecule/app-haptics: L-ebda fornitur ma ġie stabbilit. Sejħa setProvider() b&#39;implimentazzjoni ta&#39; HapticsProvider (eż., minn @molecule/app-haptics-capacitor).',
}
