import type { HapticsTranslations } from './types.js'

/** Haptics translations for Persian. */
export const fa: HapticsTranslations = {
  'haptics.error.noProvider':
    '@molecule/app-haptics: ارائه‌دهنده‌ای تنظیم نشده است. setProvider() را با یک پیاده‌سازی HapticsProvider فراخوانی کنید (مثلاً از @molecule/app-haptics-capacitor).',
}
