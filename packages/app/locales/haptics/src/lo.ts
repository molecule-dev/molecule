import type { HapticsTranslations } from './types.js'

/** Haptics translations for Lao. */
export const lo: HapticsTranslations = {
  'haptics.error.noProvider':
    '@molecule/app-haptics: ບໍ່ໄດ້ຕັ້ງຜູ້ໃຫ້ບໍລິການ. ເອີ້ນ setProvider() ດ້ວຍການຈັດຕັ້ງປະຕິບັດ HapticsProvider (ເຊັ່ນ, ຈາກ @molecule/app-haptics-capacitor).',
}
