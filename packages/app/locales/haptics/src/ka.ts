import type { HapticsTranslations } from './types.js'

/** Haptics translations for Georgian. */
export const ka: HapticsTranslations = {
  'haptics.error.noProvider':
    '@molecule/app-haptics: პროვაიდერი არ არის დაყენებული. გამოიძახეთ setProvider() HapticsProvider იმპლემენტაციით (მაგ., @molecule/app-haptics-capacitor-დან).',
}
