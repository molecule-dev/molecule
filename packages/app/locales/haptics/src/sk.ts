import type { HapticsTranslations } from './types.js'

/** Haptics translations for Slovak. */
export const sk: HapticsTranslations = {
  'haptics.error.noProvider':
    '@molecule/app-haptics: Poskytovatel nie je nastaveny. Zavolajte setProvider() s implementaciou HapticsProvider (napr. z @molecule/app-haptics-capacitor).',
}
