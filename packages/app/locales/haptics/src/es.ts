import type { HapticsTranslations } from './types.js'

/** Haptics translations for Spanish. */
export const es: HapticsTranslations = {
  'haptics.error.noProvider':
    '@molecule/app-haptics: No se ha establecido un proveedor. Llame a setProvider() con una implementación de HapticsProvider (p. ej., de @molecule/app-haptics-capacitor).',
}
