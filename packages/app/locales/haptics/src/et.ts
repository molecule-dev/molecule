import type { HapticsTranslations } from './types.js'

/** Haptics translations for Estonian. */
export const et: HapticsTranslations = {
  'haptics.error.noProvider':
    '@molecule/app-haptics: Teenusepakkujat pole määratud. Kutsuge setProvider() koos HapticsProvider teostusega (nt @molecule/app-haptics-capacitor).',
}
