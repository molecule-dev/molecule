import type { HapticsTranslations } from './types.js'

/** Haptics translations for Arabic. */
export const ar: HapticsTranslations = {
  'haptics.error.noProvider':
    '@molecule/app-haptics: لم يتم تعيين مزود. استدعِ setProvider() مع تنفيذ HapticsProvider (مثلاً من @molecule/app-haptics-capacitor).',
}
