import type { HapticsTranslations } from './types.js'

/** Haptics translations for Yoruba. */
export const yo: HapticsTranslations = {
  'haptics.error.noProvider':
    '@molecule/app-haptics: Ko si olupese ti a eto. Pe setProvider() pelu imuse HapticsProvider (fun apeere, lati @molecule/app-haptics-capacitor).',
}
