import type { HapticsTranslations } from './types.js'

/** Haptics translations for Macedonian. */
export const mk: HapticsTranslations = {
  'haptics.error.noProvider':
    '@molecule/app-haptics: Не е поставен провајдер. Повикајте setProvider() со имплементација на HapticsProvider (на пр., од @molecule/app-haptics-capacitor).',
}
