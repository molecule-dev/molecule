import type { HapticsTranslations } from './types.js'

/** Haptics translations for Czech. */
export const cs: HapticsTranslations = {
  'haptics.error.noProvider':
    '@molecule/app-haptics: Poskytovatel není nastaven. Zavolejte setProvider() s implementací HapticsProvider (např. z @molecule/app-haptics-capacitor).',
}
