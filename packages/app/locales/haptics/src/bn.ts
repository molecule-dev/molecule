import type { HapticsTranslations } from './types.js'

/** Haptics translations for Bengali. */
export const bn: HapticsTranslations = {
  'haptics.error.noProvider':
    '@molecule/app-haptics: কোনো প্রোভাইডার সেট করা হয়নি। একটি HapticsProvider ইমপ্লিমেন্টেশন দিয়ে setProvider() কল করুন (যেমন, @molecule/app-haptics-capacitor থেকে)।',
}
