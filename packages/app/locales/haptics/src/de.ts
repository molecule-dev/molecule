import type { HapticsTranslations } from './types.js'

/** Haptics translations for German. */
export const de: HapticsTranslations = {
  'haptics.error.noProvider':
    '@molecule/app-haptics: Kein Anbieter gesetzt. Rufen Sie setProvider() mit einer HapticsProvider-Implementierung auf (z. B. von @molecule/app-haptics-capacitor).',
}
