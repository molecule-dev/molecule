import type { HapticsTranslations } from './types.js'

/** Haptics translations for Urdu. */
export const ur: HapticsTranslations = {
  'haptics.error.noProvider':
    '@molecule/app-haptics: کوئی فراہم کنندہ مقرر نہیں ہے۔ HapticsProvider نفاذ کے ساتھ setProvider() کو کال کریں (مثلاً، @molecule/app-haptics-capacitor سے)۔',
}
