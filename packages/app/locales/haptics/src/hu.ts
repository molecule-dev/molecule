import type { HapticsTranslations } from './types.js'

/** Haptics translations for Hungarian. */
export const hu: HapticsTranslations = {
  'haptics.error.noProvider':
    '@molecule/app-haptics: Nincs szolgáltató beállítva. Hívja meg a setProvider()-t egy HapticsProvider implementációval (pl. a @molecule/app-haptics-capacitor-ból).',
}
