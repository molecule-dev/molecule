import type { HapticsTranslations } from './types.js'

/** Haptics translations for Swedish. */
export const sv: HapticsTranslations = {
  'haptics.error.noProvider':
    '@molecule/app-haptics: Ingen leverantor har angetts. Anropa setProvider() med en HapticsProvider-implementering (t.ex. fran @molecule/app-haptics-capacitor).',
}
