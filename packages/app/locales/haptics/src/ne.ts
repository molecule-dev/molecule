import type { HapticsTranslations } from './types.js'

/** Haptics translations for Nepali. */
export const ne: HapticsTranslations = {
  'haptics.error.noProvider':
    '@molecule/app-haptics: प्रदायक सेट गरिएको छैन। HapticsProvider कार्यान्वयनसँग setProvider() कल गर्नुहोस् (उदा., @molecule/app-haptics-capacitor बाट)।',
}
