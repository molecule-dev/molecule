import type { HapticsTranslations } from './types.js'

/** Haptics translations for Bulgarian. */
export const bg: HapticsTranslations = {
  'haptics.error.noProvider':
    '@molecule/app-haptics: Не е зададен доставчик. Извикайте setProvider() с имплементация на HapticsProvider (напр. от @molecule/app-haptics-capacitor).',
}
