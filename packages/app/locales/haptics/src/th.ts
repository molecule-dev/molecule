import type { HapticsTranslations } from './types.js'

/** Haptics translations for Thai. */
export const th: HapticsTranslations = {
  'haptics.error.noProvider':
    '@molecule/app-haptics: ยังไม่ได้ตั้งค่าผู้ให้บริการ เรียก setProvider() พร้อมการใช้งาน HapticsProvider (เช่น จาก @molecule/app-haptics-capacitor)',
}
