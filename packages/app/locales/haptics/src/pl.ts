import type { HapticsTranslations } from './types.js'

/** Haptics translations for Polish. */
export const pl: HapticsTranslations = {
  'haptics.error.noProvider':
    '@molecule/app-haptics: Nie ustawiono dostawcy. Wywolaj setProvider() z implementacja HapticsProvider (np. z @molecule/app-haptics-capacitor).',
}
