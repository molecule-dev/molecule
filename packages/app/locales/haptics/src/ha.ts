import type { HapticsTranslations } from './types.js'

/** Haptics translations for Hausa. */
export const ha: HapticsTranslations = {
  'haptics.error.noProvider':
    '@molecule/app-haptics: Ba a saita mai bayarwa ba. Kira setProvider() tare da aiwatar da HapticsProvider (misali, daga @molecule/app-haptics-capacitor).',
}
