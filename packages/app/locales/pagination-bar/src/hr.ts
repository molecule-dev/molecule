import type { PaginationBarTranslations } from './types.js'

/** PaginationBar translations for hr. */
export const hr: Partial<PaginationBarTranslations> = {
  'pagination.previous': 'Prethodna stranica',
  'pagination.next': 'Sljedeća stranica',
  'pagination.pageSize': 'Veličina stranice',
}
