import type { PaginationBarTranslations } from './types.js'

/** PaginationBar translations for km. */
export const km: Partial<PaginationBarTranslations> = {
  'pagination.previous': 'ទំព័រមុន',
  'pagination.next': 'ទំព័របន្ទាប់',
}
