import type { PaginationBarTranslations } from './types.js'

/** PaginationBar translations for ro. */
export const ro: Partial<PaginationBarTranslations> = {
  'pagination.previous': 'Pagina anterioară',
  'pagination.next': 'Pagina următoare',
  'pagination.pageSize': 'Dimensiunea paginii',
}
