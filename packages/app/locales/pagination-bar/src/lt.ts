import type { PaginationBarTranslations } from './types.js'

/** PaginationBar translations for lt. */
export const lt: Partial<PaginationBarTranslations> = {
  'pagination.previous': 'Ankstesnis puslapis',
  'pagination.next': 'Kitas puslapis',
  'pagination.pageSize': 'Puslapio dydis',
}
