import type { PaginationBarTranslations } from './types.js'

/** PaginationBar translations for is. */
export const is: Partial<PaginationBarTranslations> = {
  'pagination.previous': 'Fyrri síða',
  'pagination.next': 'Næsta síða',
  'pagination.pageSize': 'Síðustærð',
}
