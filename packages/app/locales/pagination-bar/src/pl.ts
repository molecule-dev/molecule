import type { PaginationBarTranslations } from './types.js'

/** PaginationBar translations for pl. */
export const pl: Partial<PaginationBarTranslations> = {
  'pagination.previous': 'Poprzednia strona',
  'pagination.next': 'Następna strona',
}
