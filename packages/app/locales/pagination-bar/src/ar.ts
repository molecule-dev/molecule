import type { PaginationBarTranslations } from './types.js'

/** PaginationBar translations for ar. */
export const ar: Partial<PaginationBarTranslations> = {
  'pagination.previous': 'الصفحة السابقة',
  'pagination.next': 'الصفحة التالية',
  'pagination.pageSize': 'حجم الصفحة',
}
