import type { PaginationBarTranslations } from './types.js'

/** PaginationBar translations for be. */
export const be: Partial<PaginationBarTranslations> = {
  'pagination.previous': 'Папярэдняя старонка',
  'pagination.next': 'Наступная старонка',
  'pagination.pageSize': 'Памер старонкі',
}
