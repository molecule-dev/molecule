import type { PaginationBarTranslations } from './types.js'

/** PaginationBar translations for vi. */
export const vi: Partial<PaginationBarTranslations> = {
  'pagination.previous': 'Trang trước',
  'pagination.next': 'Trang tiếp',
}
