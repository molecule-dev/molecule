import type { VideoPlayerTranslations } from './types.js'

/** VideoPlayer translations for lt. */
export const lt: Partial<VideoPlayerTranslations> = {
  'video.pause': 'Pauzė',
  'video.play': 'Žaisti',
}
