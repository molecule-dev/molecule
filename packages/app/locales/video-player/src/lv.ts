import type { VideoPlayerTranslations } from './types.js'

/** VideoPlayer translations for lv. */
export const lv: Partial<VideoPlayerTranslations> = {
  'video.pause': 'Pauze',
  'video.play': 'Spēlēt',
}
