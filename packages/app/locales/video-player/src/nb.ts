import type { VideoPlayerTranslations } from './types.js'

/** VideoPlayer translations for nb. */
export const nb: Partial<VideoPlayerTranslations> = {
  'video.pause': 'Sett på pause',
  'video.play': 'Spill av',
}
