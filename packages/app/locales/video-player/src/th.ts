import type { VideoPlayerTranslations } from './types.js'

/** VideoPlayer translations for th. */
export const th: Partial<VideoPlayerTranslations> = {
  'video.pause': 'หยุดชั่วคราว',
  'video.play': 'เล่น',
}
