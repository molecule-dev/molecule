import type { VideoPlayerTranslations } from './types.js'

/** VideoPlayer translations for id. */
export const id: Partial<VideoPlayerTranslations> = {
  'video.pause': 'Jeda',
  'video.play': 'Putar',
}
