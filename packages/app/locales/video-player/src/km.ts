import type { VideoPlayerTranslations } from './types.js'

/** VideoPlayer translations for km. */
export const km: Partial<VideoPlayerTranslations> = {
  'video.pause': 'ផ្អាក',
  'video.play': 'លេង',
}
