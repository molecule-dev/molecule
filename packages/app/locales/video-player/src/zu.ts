import type { VideoPlayerTranslations } from './types.js'

/** VideoPlayer translations for zu. */
export const zu: Partial<VideoPlayerTranslations> = {
  'video.pause': 'Misa isikhashana',
  'video.play': 'Dlala',
}
