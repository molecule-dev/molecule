import type { VideoPlayerTranslations } from './types.js'

/** VideoPlayer translations for is. */
export const is: Partial<VideoPlayerTranslations> = {
  'video.pause': 'Gera hlé',
  'video.play': 'Spila',
}
