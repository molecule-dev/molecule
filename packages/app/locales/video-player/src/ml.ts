import type { VideoPlayerTranslations } from './types.js'

/** VideoPlayer translations for ml. */
export const ml: Partial<VideoPlayerTranslations> = {
  'video.pause': 'താൽക്കാലികമായി നിർത്തുക',
  'video.play': 'കളിക്കുക',
}
