import type { VideoPlayerTranslations } from './types.js'

/** VideoPlayer translations for ar. */
export const ar: Partial<VideoPlayerTranslations> = {
  'video.pause': 'إيقاف مؤقت',
  'video.play': 'تشغيل',
}
