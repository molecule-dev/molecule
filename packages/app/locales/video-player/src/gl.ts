import type { VideoPlayerTranslations } from './types.js'

/** VideoPlayer translations for gl. */
export const gl: Partial<VideoPlayerTranslations> = {
  'video.pause': 'Pausa',
  'video.play': 'Reproducir',
}
