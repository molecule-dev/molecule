import type { VideoPlayerTranslations } from './types.js'

/** VideoPlayer translations for te. */
export const te: Partial<VideoPlayerTranslations> = {
  'video.pause': 'విరామం',
  'video.play': 'ఆట',
}
