import type { VideoPlayerTranslations } from './types.js'

/** VideoPlayer translations for si. */
export const si: Partial<VideoPlayerTranslations> = {
  'video.pause': 'විරාමය',
  'video.play': 'සෙල්ලම් කරන්න',
}
