import type { VideoPlayerTranslations } from './types.js'

/** VideoPlayer translations for mt. */
export const mt: Partial<VideoPlayerTranslations> = {
  'video.pause': 'Pawża',
  'video.play': 'Ilgħab',
}
