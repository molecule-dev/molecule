import type { VideoPlayerTranslations } from './types.js'

/** VideoPlayer translations for ms. */
export const ms: Partial<VideoPlayerTranslations> = {
  'video.pause': 'Jeda',
  'video.play': 'Main',
}
