import type { VideoPlayerTranslations } from './types.js'

/** VideoPlayer translations for de. */
export const de: Partial<VideoPlayerTranslations> = {
  'video.pause': 'Pausieren',
  'video.play': 'Abspielen',
}
