import type { VideoPlayerTranslations } from './types.js'

/** VideoPlayer translations for es. */
export const es: Partial<VideoPlayerTranslations> = {
  'video.pause': 'Pausar',
  'video.play': 'Reproducir',
}
