import type { VideoPlayerTranslations } from './types.js'

/** VideoPlayer translations for ja. */
export const ja: Partial<VideoPlayerTranslations> = {
  'video.pause': '一時停止',
  'video.play': '再生',
}
