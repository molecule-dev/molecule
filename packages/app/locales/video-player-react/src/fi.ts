import type { VideoPlayerTranslations } from './types.js'

/** VideoPlayer translations for fi. */
export const fi: Partial<VideoPlayerTranslations> = {
  'video.pause': 'Tauko',
  'video.play': 'Toista',
}
