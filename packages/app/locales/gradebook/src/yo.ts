import type { GradebookTranslations } from './types.js'
import { en } from './en.js'

/** Gradebook translations for yo — currently mirrors English (translation TBD). */
export const yo: GradebookTranslations = { ...en }
