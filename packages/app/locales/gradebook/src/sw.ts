import type { GradebookTranslations } from './types.js'
import { en } from './en.js'

/** Gradebook translations for sw — currently mirrors English (translation TBD). */
export const sw: GradebookTranslations = { ...en }
