import type { GradebookTranslations } from './types.js'
import { en } from './en.js'

/** Gradebook translations for bg — currently mirrors English (translation TBD). */
export const bg: GradebookTranslations = { ...en }
