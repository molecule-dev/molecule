import type { GradebookTranslations } from './types.js'
import { en } from './en.js'

/** Gradebook translations for zu — currently mirrors English (translation TBD). */
export const zu: GradebookTranslations = { ...en }
