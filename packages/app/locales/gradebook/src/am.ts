import type { GradebookTranslations } from './types.js'
import { en } from './en.js'

/** Gradebook translations for am — currently mirrors English (translation TBD). */
export const am: GradebookTranslations = { ...en }
