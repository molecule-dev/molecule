import type { GradebookTranslations } from './types.js'
import { en } from './en.js'

/** Gradebook translations for fa — currently mirrors English (translation TBD). */
export const fa: GradebookTranslations = { ...en }
