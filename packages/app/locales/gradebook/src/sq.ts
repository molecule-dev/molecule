import type { GradebookTranslations } from './types.js'
import { en } from './en.js'

/** Gradebook translations for sq — currently mirrors English (translation TBD). */
export const sq: GradebookTranslations = { ...en }
