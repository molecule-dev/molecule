import type { GradebookTranslations } from './types.js'
import { en } from './en.js'

/** Gradebook translations for mt — currently mirrors English (translation TBD). */
export const mt: GradebookTranslations = { ...en }
