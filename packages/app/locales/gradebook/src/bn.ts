import type { GradebookTranslations } from './types.js'
import { en } from './en.js'

/** Gradebook translations for bn — currently mirrors English (translation TBD). */
export const bn: GradebookTranslations = { ...en }
