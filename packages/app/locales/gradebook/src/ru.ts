import type { GradebookTranslations } from './types.js'
import { en } from './en.js'

/** Gradebook translations for ru — currently mirrors English (translation TBD). */
export const ru: GradebookTranslations = { ...en }
