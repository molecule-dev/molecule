import type { GradebookTranslations } from './types.js'
import { en } from './en.js'

/** Gradebook translations for gl — currently mirrors English (translation TBD). */
export const gl: GradebookTranslations = { ...en }
