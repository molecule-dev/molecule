import type { GradebookTranslations } from './types.js'
import { en } from './en.js'

/** Gradebook translations for th — currently mirrors English (translation TBD). */
export const th: GradebookTranslations = { ...en }
