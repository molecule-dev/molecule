import type { BarcodeScannerTranslations } from './types.js'
import { en } from './en.js'

/** Barcode-scanner translations for si — currently mirrors English (translation TBD). */
export const si: BarcodeScannerTranslations = { ...en }
