import type { BarcodeScannerTranslations } from './types.js'
import { en } from './en.js'

/** Barcode-scanner translations for ta — currently mirrors English (translation TBD). */
export const ta: BarcodeScannerTranslations = { ...en }
