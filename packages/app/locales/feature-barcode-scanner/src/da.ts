import type { BarcodeScannerTranslations } from './types.js'
import { en } from './en.js'

/** Barcode-scanner translations for da — currently mirrors English (translation TBD). */
export const da: BarcodeScannerTranslations = { ...en }
