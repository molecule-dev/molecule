import type { BarcodeScannerTranslations } from './types.js'
import { en } from './en.js'

/** Barcode-scanner translations for bs — currently mirrors English (translation TBD). */
export const bs: BarcodeScannerTranslations = { ...en }
