import type { BarcodeScannerTranslations } from './types.js'
import { en } from './en.js'

/** Barcode-scanner translations for kn — currently mirrors English (translation TBD). */
export const kn: BarcodeScannerTranslations = { ...en }
