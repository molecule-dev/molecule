import type { BarcodeScannerTranslations } from './types.js'
import { en } from './en.js'

/** Barcode-scanner translations for mk — currently mirrors English (translation TBD). */
export const mk: BarcodeScannerTranslations = { ...en }
