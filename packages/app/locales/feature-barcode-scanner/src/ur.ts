import type { BarcodeScannerTranslations } from './types.js'
import { en } from './en.js'

/** Barcode-scanner translations for ur — currently mirrors English (translation TBD). */
export const ur: BarcodeScannerTranslations = { ...en }
