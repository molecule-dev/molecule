import type { SubscriptionPlanCardTranslations } from './types.js'

/** SubscriptionPlanCard translations for is. */
export const is: Partial<SubscriptionPlanCardTranslations> = {
  'plan.recommended': 'Mælt með',
}
