import type { ThreadTreeTranslations } from './types.js'

/** ThreadTree translations for pt. */
export const pt: Partial<ThreadTreeTranslations> = {
  'threadTree.upvote': 'Voto positivo',
  'threadTree.reply': 'Responder',
}
