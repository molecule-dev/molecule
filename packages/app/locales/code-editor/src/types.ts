/** Translation keys for the code-editor locale package. */
export type CodeEditorTranslationKey = 'codeEditor.error.noProvider'

/** Translation record mapping code-editor keys to translated strings. */
export type CodeEditorTranslations = Record<CodeEditorTranslationKey, string>
