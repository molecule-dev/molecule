import type { MotionTranslations } from './types.js'

/** Motion translations for Hebrew. */
export const he: MotionTranslations = {
  'motion.error.noProvider':
    '@molecule/app-motion: לא הוגדר ספק. קרא ל-setProvider() עם מימוש של MotionProvider (למשל, מ-@molecule/app-motion-capacitor).',
}
