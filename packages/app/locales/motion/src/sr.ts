import type { MotionTranslations } from './types.js'

/** Motion translations for Serbian. */
export const sr: MotionTranslations = {
  'motion.error.noProvider':
    '@molecule/app-motion: Provajder nije postavljen. Pozovite setProvider() sa implementacijom MotionProvider (npr. iz @molecule/app-motion-capacitor).',
}
