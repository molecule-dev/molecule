import type { MotionTranslations } from './types.js'

/** Motion translations for English. */
export const en: MotionTranslations = {
  'motion.error.noProvider':
    '@molecule/app-motion: No provider set. Call setProvider() with a MotionProvider implementation (e.g., from @molecule/app-motion-capacitor).',
}
