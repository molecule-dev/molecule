import type { MotionTranslations } from './types.js'

/** Motion translations for Bengali. */
export const bn: MotionTranslations = {
  'motion.error.noProvider':
    '@molecule/app-motion: কোনো প্রোভাইডার সেট করা হয়নি। একটি MotionProvider ইমপ্লিমেন্টেশন দিয়ে setProvider() কল করুন (যেমন, @molecule/app-motion-capacitor থেকে)।',
}
