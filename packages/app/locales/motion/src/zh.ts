import type { MotionTranslations } from './types.js'

/** Motion translations for Chinese. */
export const zh: MotionTranslations = {
  'motion.error.noProvider':
    '@molecule/app-motion: 未设置提供程序。请使用 MotionProvider 实现调用 setProvider()（例如，来自 @molecule/app-motion-capacitor）。',
}
