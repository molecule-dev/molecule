import type { MotionTranslations } from './types.js'

/** Motion translations for Hungarian. */
export const hu: MotionTranslations = {
  'motion.error.noProvider':
    '@molecule/app-motion: Nincs szolgáltató beállítva. Hívja meg a setProvider()-t egy MotionProvider implementációval (pl. a @molecule/app-motion-capacitor-ból).',
}
