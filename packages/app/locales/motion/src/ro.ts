import type { MotionTranslations } from './types.js'

/** Motion translations for Romanian. */
export const ro: MotionTranslations = {
  'motion.error.noProvider':
    '@molecule/app-motion: Niciun furnizor setat. Apelati setProvider() cu o implementare MotionProvider (de ex., din @molecule/app-motion-capacitor).',
}
