import type { MotionTranslations } from './types.js'

/** Motion translations for Slovak. */
export const sk: MotionTranslations = {
  'motion.error.noProvider':
    '@molecule/app-motion: Poskytovatel nie je nastaveny. Zavolajte setProvider() s implementaciou MotionProvider (napr. z @molecule/app-motion-capacitor).',
}
