import type { MotionTranslations } from './types.js'

/** Motion translations for Spanish. */
export const es: MotionTranslations = {
  'motion.error.noProvider':
    '@molecule/app-motion: No se ha establecido un proveedor. Llame a setProvider() con una implementación de MotionProvider (p. ej., de @molecule/app-motion-capacitor).',
}
