import type { MotionTranslations } from './types.js'

/** Motion translations for Georgian. */
export const ka: MotionTranslations = {
  'motion.error.noProvider':
    '@molecule/app-motion: პროვაიდერი არ არის დაყენებული. გამოიძახეთ setProvider() MotionProvider იმპლემენტაციით (მაგ., @molecule/app-motion-capacitor-დან).',
}
