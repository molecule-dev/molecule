import type { MotionTranslations } from './types.js'

/** Motion translations for Nepali. */
export const ne: MotionTranslations = {
  'motion.error.noProvider':
    '@molecule/app-motion: प्रदायक सेट गरिएको छैन। MotionProvider कार्यान्वयनसँग setProvider() कल गर्नुहोस् (उदा., @molecule/app-motion-capacitor बाट)।',
}
