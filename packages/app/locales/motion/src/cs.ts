import type { MotionTranslations } from './types.js'

/** Motion translations for Czech. */
export const cs: MotionTranslations = {
  'motion.error.noProvider':
    '@molecule/app-motion: Poskytovatel není nastaven. Zavolejte setProvider() s implementací MotionProvider (např. z @molecule/app-motion-capacitor).',
}
