import type { MotionTranslations } from './types.js'

/** Motion translations for Bulgarian. */
export const bg: MotionTranslations = {
  'motion.error.noProvider':
    '@molecule/app-motion: Не е зададен доставчик. Извикайте setProvider() с имплементация на MotionProvider (напр. от @molecule/app-motion-capacitor).',
}
