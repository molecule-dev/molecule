import type { MotionTranslations } from './types.js'

/** Motion translations for Polish. */
export const pl: MotionTranslations = {
  'motion.error.noProvider':
    '@molecule/app-motion: Nie ustawiono dostawcy. Wywolaj setProvider() z implementacja MotionProvider (np. z @molecule/app-motion-capacitor).',
}
