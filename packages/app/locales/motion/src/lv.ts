import type { MotionTranslations } from './types.js'

/** Motion translations for Latvian. */
export const lv: MotionTranslations = {
  'motion.error.noProvider':
    '@molecule/app-motion: Nav iestatits pakalpojumu sniedzejas. Izsauciet setProvider() ar MotionProvider implementaciju (piem., no @molecule/app-motion-capacitor).',
}
