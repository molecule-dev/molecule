import type { MotionTranslations } from './types.js'

/** Motion translations for Swedish. */
export const sv: MotionTranslations = {
  'motion.error.noProvider':
    '@molecule/app-motion: Ingen leverantor har angetts. Anropa setProvider() med en MotionProvider-implementering (t.ex. fran @molecule/app-motion-capacitor).',
}
