import type { MotionTranslations } from './types.js'

/** Motion translations for Arabic. */
export const ar: MotionTranslations = {
  'motion.error.noProvider':
    '@molecule/app-motion: لم يتم تعيين مزود. استدعِ setProvider() مع تنفيذ MotionProvider (مثلاً من @molecule/app-motion-capacitor).',
}
