import type { MotionTranslations } from './types.js'

/** Motion translations for Persian. */
export const fa: MotionTranslations = {
  'motion.error.noProvider':
    '@molecule/app-motion: ارائه‌دهنده‌ای تنظیم نشده است. setProvider() را با یک پیاده‌سازی MotionProvider فراخوانی کنید (مثلاً از @molecule/app-motion-capacitor).',
}
