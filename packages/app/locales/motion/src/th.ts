import type { MotionTranslations } from './types.js'

/** Motion translations for Thai. */
export const th: MotionTranslations = {
  'motion.error.noProvider':
    '@molecule/app-motion: ยังไม่ได้ตั้งค่าผู้ให้บริการ เรียก setProvider() พร้อมการใช้งาน MotionProvider (เช่น จาก @molecule/app-motion-capacitor)',
}
