import type { MotionTranslations } from './types.js'

/** Motion translations for Chinese (Traditional). */
export const zhTW: MotionTranslations = {
  'motion.error.noProvider':
    '@molecule/app-motion: 未設定提供者。請使用 MotionProvider 實作呼叫 setProvider()（例如，來自 @molecule/app-motion-capacitor）。',
}
