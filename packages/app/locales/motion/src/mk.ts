import type { MotionTranslations } from './types.js'

/** Motion translations for Macedonian. */
export const mk: MotionTranslations = {
  'motion.error.noProvider':
    '@molecule/app-motion: Не е поставен провајдер. Повикајте setProvider() со имплементација на MotionProvider (на пр., од @molecule/app-motion-capacitor).',
}
