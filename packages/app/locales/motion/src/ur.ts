import type { MotionTranslations } from './types.js'

/** Motion translations for Urdu. */
export const ur: MotionTranslations = {
  'motion.error.noProvider':
    '@molecule/app-motion: کوئی فراہم کنندہ مقرر نہیں ہے۔ MotionProvider نفاذ کے ساتھ setProvider() کو کال کریں (مثلاً، @molecule/app-motion-capacitor سے)۔',
}
