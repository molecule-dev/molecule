import type { MotionTranslations } from './types.js'

/** Motion translations for Maltese. */
export const mt: MotionTranslations = {
  'motion.error.noProvider':
    "@molecule/app-motion: L-ebda fornitur mhux issettjat. Sejjah setProvider() b'implimentazzjoni ta' MotionProvider (ez., minn @molecule/app-motion-capacitor).",
}
