import type { MotionTranslations } from './types.js'

/** Motion translations for mt. */
export const mt: Partial<MotionTranslations> = {
  'motion.error.noProvider':
    "@molecule/app-motion: L-ebda fornitur ma ġie stabbilit. Sejħa setProvider() b'implimentazzjoni ta' MotionProvider (eż., minn @molecule/app-motion-capacitor).",
}
