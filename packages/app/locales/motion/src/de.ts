import type { MotionTranslations } from './types.js'

/** Motion translations for German. */
export const de: MotionTranslations = {
  'motion.error.noProvider':
    '@molecule/app-motion: Kein Anbieter gesetzt. Rufen Sie setProvider() mit einer MotionProvider-Implementierung auf.',
}
