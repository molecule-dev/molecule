import type { MotionTranslations } from './types.js'

/** Motion translations for Croatian. */
export const hr: MotionTranslations = {
  'motion.error.noProvider':
    '@molecule/app-motion: Pružatelj nije postavljen. Pozovite setProvider() s implementacijom MotionProvider (npr. iz @molecule/app-motion-capacitor).',
}
