import type { MotionTranslations } from './types.js'

/** Motion translations for Yoruba. */
export const yo: MotionTranslations = {
  'motion.error.noProvider':
    '@molecule/app-motion: Ko si olupese ti a eto. Pe setProvider() pelu imuse MotionProvider (fun apeere, lati @molecule/app-motion-capacitor).',
}
