import type { MotionTranslations } from './types.js'

/** Motion translations for Filipino. */
export const fil: MotionTranslations = {
  'motion.error.noProvider':
    '@molecule/app-motion: Walang provider na naka-set. Tawagin ang setProvider() na may MotionProvider na implementasyon (hal., mula sa @molecule/app-motion-capacitor).',
}
