import type { MotionTranslations } from './types.js'

/** Motion translations for Hausa. */
export const ha: MotionTranslations = {
  'motion.error.noProvider':
    '@molecule/app-motion: Ba a saita mai bayarwa ba. Kira setProvider() tare da aiwatar da MotionProvider (misali, daga @molecule/app-motion-capacitor).',
}
