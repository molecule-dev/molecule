/** Translation keys for the geolocation locale package. */
export type GeolocationTranslationKey = 'geolocation.error.notSupported'

/** Translation record mapping geolocation keys to translated strings. */
export type GeolocationTranslations = Record<GeolocationTranslationKey, string>
