import type { VideoScrubberTranslations } from './types.js'
import { en } from './en.js'

/** Video-scrubber translations for my — currently mirrors English (translation TBD). */
export const my: VideoScrubberTranslations = { ...en }
