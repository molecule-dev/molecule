import type { VideoScrubberTranslations } from './types.js'
import { en } from './en.js'

/** Video-scrubber translations for zu — currently mirrors English (translation TBD). */
export const zu: VideoScrubberTranslations = { ...en }
