import type { VideoScrubberTranslations } from './types.js'
import { en } from './en.js'

/** Video-scrubber translations for lt — currently mirrors English (translation TBD). */
export const lt: VideoScrubberTranslations = { ...en }
