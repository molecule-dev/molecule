import type { VideoScrubberTranslations } from './types.js'
import { en } from './en.js'

/** Video-scrubber translations for sr — currently mirrors English (translation TBD). */
export const sr: VideoScrubberTranslations = { ...en }
