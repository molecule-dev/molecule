import type { VideoScrubberTranslations } from './types.js'
import { en } from './en.js'

/** Video-scrubber translations for kn — currently mirrors English (translation TBD). */
export const kn: VideoScrubberTranslations = { ...en }
