import type { VideoScrubberTranslations } from './types.js'
import { en } from './en.js'

/** Video-scrubber translations for es — currently mirrors English (translation TBD). */
export const es: VideoScrubberTranslations = { ...en }
