import type { VideoScrubberTranslations } from './types.js'
import { en } from './en.js'

/** Video-scrubber translations for mr — currently mirrors English (translation TBD). */
export const mr: VideoScrubberTranslations = { ...en }
