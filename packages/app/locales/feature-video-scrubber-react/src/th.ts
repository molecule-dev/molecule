import type { VideoScrubberTranslations } from './types.js'
import { en } from './en.js'

/** Video-scrubber translations for th — currently mirrors English (translation TBD). */
export const th: VideoScrubberTranslations = { ...en }
