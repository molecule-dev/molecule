import type { VideoScrubberTranslations } from './types.js'
import { en } from './en.js'

/** Video-scrubber translations for he — currently mirrors English (translation TBD). */
export const he: VideoScrubberTranslations = { ...en }
