import type { UtilitiesTranslations } from './types.js'

/** Utilities translations for it. */
export const it: Partial<UtilitiesTranslations> = {
  'error.unknown': 'Si è verificato un errore imprevisto.',
}
