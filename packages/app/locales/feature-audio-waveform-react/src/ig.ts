import type { AudioWaveformTranslations } from './types.js'
import { en } from './en.js'

/** Audio-waveform translations for ig — currently mirrors English (translation TBD). */
export const ig: AudioWaveformTranslations = { ...en }
