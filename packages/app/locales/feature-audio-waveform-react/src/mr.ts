import type { AudioWaveformTranslations } from './types.js'
import { en } from './en.js'

/** Audio-waveform translations for mr — currently mirrors English (translation TBD). */
export const mr: AudioWaveformTranslations = { ...en }
