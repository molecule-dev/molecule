import type { AudioWaveformTranslations } from './types.js'
import { en } from './en.js'

/** Audio-waveform translations for ne — currently mirrors English (translation TBD). */
export const ne: AudioWaveformTranslations = { ...en }
