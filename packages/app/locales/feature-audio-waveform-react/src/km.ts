import type { AudioWaveformTranslations } from './types.js'
import { en } from './en.js'

/** Audio-waveform translations for km — currently mirrors English (translation TBD). */
export const km: AudioWaveformTranslations = { ...en }
