import type { AdjustmentSliderTranslations } from './types.js'
import { en } from './en.js'

/** Adjustment-slider translations for kk — currently mirrors English (translation TBD). */
export const kk: AdjustmentSliderTranslations = { ...en }
