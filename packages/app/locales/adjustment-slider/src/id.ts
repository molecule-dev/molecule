import type { AdjustmentSliderTranslations } from './types.js'
import { en } from './en.js'

/** Adjustment-slider translations for id — currently mirrors English (translation TBD). */
export const id: AdjustmentSliderTranslations = { ...en }
