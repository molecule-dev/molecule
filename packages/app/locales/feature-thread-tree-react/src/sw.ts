import { en } from './en.js'
import type { ThreadTreeTranslations } from './types.js'

/** Thread-tree translations for sw (stub — falls back to English until reviewed). */
export const sw: ThreadTreeTranslations = { ...en }
