import { en } from './en.js'
import type { ThreadTreeTranslations } from './types.js'

/** Thread-tree translations for ka (stub — falls back to English until reviewed). */
export const ka: ThreadTreeTranslations = { ...en }
