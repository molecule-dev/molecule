import { en } from './en.js'
import type { ThreadTreeTranslations } from './types.js'

/** Thread-tree translations for he (stub — falls back to English until reviewed). */
export const he: ThreadTreeTranslations = { ...en }
