import { en } from './en.js'
import type { ThreadTreeTranslations } from './types.js'

/** Thread-tree translations for bn (stub — falls back to English until reviewed). */
export const bn: ThreadTreeTranslations = { ...en }
