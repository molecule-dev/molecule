import { en } from './en.js'
import type { ThreadTreeTranslations } from './types.js'

/** Thread-tree translations for sq (stub — falls back to English until reviewed). */
export const sq: ThreadTreeTranslations = { ...en }
