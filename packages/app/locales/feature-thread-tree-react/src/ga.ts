import { en } from './en.js'
import type { ThreadTreeTranslations } from './types.js'

/** Thread-tree translations for ga (stub — falls back to English until reviewed). */
export const ga: ThreadTreeTranslations = { ...en }
