import { en } from './en.js'
import type { ThreadTreeTranslations } from './types.js'

/** Thread-tree translations for de (stub — falls back to English until reviewed). */
export const de: ThreadTreeTranslations = { ...en }
