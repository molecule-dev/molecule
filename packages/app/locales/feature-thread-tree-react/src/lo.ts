import { en } from './en.js'
import type { ThreadTreeTranslations } from './types.js'

/** Thread-tree translations for lo (stub — falls back to English until reviewed). */
export const lo: ThreadTreeTranslations = { ...en }
