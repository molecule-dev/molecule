import { en } from './en.js'
import type { ThreadTreeTranslations } from './types.js'

/** Thread-tree translations for is (stub — falls back to English until reviewed). */
export const is: ThreadTreeTranslations = { ...en }
