import { en } from './en.js'
import type { ThreadTreeTranslations } from './types.js'

/** Thread-tree translations for zh (stub — falls back to English until reviewed). */
export const zh: ThreadTreeTranslations = { ...en }
