import { en } from './en.js'
import type { ThreadTreeTranslations } from './types.js'

/** Thread-tree translations for fil (stub — falls back to English until reviewed). */
export const fil: ThreadTreeTranslations = { ...en }
