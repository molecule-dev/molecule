import { en } from './en.js'
import type { ThreadTreeTranslations } from './types.js'

/** Thread-tree translations for ro (stub — falls back to English until reviewed). */
export const ro: ThreadTreeTranslations = { ...en }
