import { en } from './en.js'
import type { ThreadTreeTranslations } from './types.js'

/** Thread-tree translations for da (stub — falls back to English until reviewed). */
export const da: ThreadTreeTranslations = { ...en }
