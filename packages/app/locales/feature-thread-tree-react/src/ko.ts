import { en } from './en.js'
import type { ThreadTreeTranslations } from './types.js'

/** Thread-tree translations for ko (stub — falls back to English until reviewed). */
export const ko: ThreadTreeTranslations = { ...en }
