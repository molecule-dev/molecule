import { en } from './en.js'
import type { ThreadTreeTranslations } from './types.js'

/** Thread-tree translations for ig (stub — falls back to English until reviewed). */
export const ig: ThreadTreeTranslations = { ...en }
