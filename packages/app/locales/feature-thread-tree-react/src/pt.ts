import { en } from './en.js'
import type { ThreadTreeTranslations } from './types.js'

/** Thread-tree translations for pt (stub — falls back to English until reviewed). */
export const pt: ThreadTreeTranslations = { ...en }
