import { en } from './en.js'
import type { ThreadTreeTranslations } from './types.js'

/** Thread-tree translations for bs (stub — falls back to English until reviewed). */
export const bs: ThreadTreeTranslations = { ...en }
