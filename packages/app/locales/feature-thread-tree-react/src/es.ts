import { en } from './en.js'
import type { ThreadTreeTranslations } from './types.js'

/** Thread-tree translations for es (stub — falls back to English until reviewed). */
export const es: ThreadTreeTranslations = { ...en }
