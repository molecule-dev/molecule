import { en } from './en.js'
import type { ThreadTreeTranslations } from './types.js'

/** Thread-tree translations for ms (stub — falls back to English until reviewed). */
export const ms: ThreadTreeTranslations = { ...en }
