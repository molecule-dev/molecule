import { en } from './en.js'
import type { ThreadTreeTranslations } from './types.js'

/** Thread-tree translations for fr (stub — falls back to English until reviewed). */
export const fr: ThreadTreeTranslations = { ...en }
