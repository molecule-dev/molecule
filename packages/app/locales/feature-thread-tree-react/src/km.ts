import { en } from './en.js'
import type { ThreadTreeTranslations } from './types.js'

/** Thread-tree translations for km (stub — falls back to English until reviewed). */
export const km: ThreadTreeTranslations = { ...en }
