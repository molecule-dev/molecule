import { en } from './en.js'
import type { ThreadTreeTranslations } from './types.js'

/** Thread-tree translations for ja (stub — falls back to English until reviewed). */
export const ja: ThreadTreeTranslations = { ...en }
