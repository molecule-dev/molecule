import { en } from './en.js'
import type { ThreadTreeTranslations } from './types.js'

/** Thread-tree translations for pa (stub — falls back to English until reviewed). */
export const pa: ThreadTreeTranslations = { ...en }
