import { en } from './en.js'
import type { ThreadTreeTranslations } from './types.js'

/** Thread-tree translations for hr (stub — falls back to English until reviewed). */
export const hr: ThreadTreeTranslations = { ...en }
