import { en } from './en.js'
import type { ThreadTreeTranslations } from './types.js'

/** Thread-tree translations for cs (stub — falls back to English until reviewed). */
export const cs: ThreadTreeTranslations = { ...en }
