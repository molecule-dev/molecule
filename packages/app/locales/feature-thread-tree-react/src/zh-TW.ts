import { en } from './en.js'
import type { ThreadTreeTranslations } from './types.js'

/** Thread-tree translations for zh-TW (stub — falls back to English until reviewed). */
export const zhTW: ThreadTreeTranslations = { ...en }
