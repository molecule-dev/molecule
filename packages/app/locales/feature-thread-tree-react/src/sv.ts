import { en } from './en.js'
import type { ThreadTreeTranslations } from './types.js'

/** Thread-tree translations for sv (stub — falls back to English until reviewed). */
export const sv: ThreadTreeTranslations = { ...en }
