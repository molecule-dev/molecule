/** Translation keys for the feature-image-canvas locale package. */
export type ImageCanvasTranslationKey =
  | 'imageCanvas.aria.region'
  | 'imageCanvas.aria.canvas'
  | 'imageCanvas.loading'
  | 'imageCanvas.error'

/** Translation record mapping image-canvas keys to translated strings. */
export type ImageCanvasTranslations = Record<ImageCanvasTranslationKey, string>
