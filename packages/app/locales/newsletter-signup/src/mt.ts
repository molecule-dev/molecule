import type { NewsletterSignupTranslations } from './types.js'

/** NewsletterSignup translations for mt. */
export const mt: Partial<NewsletterSignupTranslations> = {
  'newsletter.placeholder': 'L-email tiegħek',
  'newsletter.subscribing': 'Qed tabbona…',
  'newsletter.subscribe': 'Abbona',
}
