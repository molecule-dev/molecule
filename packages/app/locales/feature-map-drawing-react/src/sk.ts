import type { MapDrawingTranslations } from './types.js'
import { en } from './en.js'

/** Map-drawing translations for sk — currently mirrors English (translation TBD). */
export const sk: MapDrawingTranslations = { ...en }
