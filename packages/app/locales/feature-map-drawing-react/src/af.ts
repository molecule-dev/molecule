import type { MapDrawingTranslations } from './types.js'
import { en } from './en.js'

/** Map-drawing translations for af — currently mirrors English (translation TBD). */
export const af: MapDrawingTranslations = { ...en }
