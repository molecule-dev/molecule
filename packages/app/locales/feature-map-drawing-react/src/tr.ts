import type { MapDrawingTranslations } from './types.js'
import { en } from './en.js'

/** Map-drawing translations for tr — currently mirrors English (translation TBD). */
export const tr: MapDrawingTranslations = { ...en }
