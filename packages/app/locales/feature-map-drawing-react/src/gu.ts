import type { MapDrawingTranslations } from './types.js'
import { en } from './en.js'

/** Map-drawing translations for gu — currently mirrors English (translation TBD). */
export const gu: MapDrawingTranslations = { ...en }
