import type { MapDrawingTranslations } from './types.js'
import { en } from './en.js'

/** Map-drawing translations for fi — currently mirrors English (translation TBD). */
export const fi: MapDrawingTranslations = { ...en }
