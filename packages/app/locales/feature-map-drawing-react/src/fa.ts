import type { MapDrawingTranslations } from './types.js'
import { en } from './en.js'

/** Map-drawing translations for fa — currently mirrors English (translation TBD). */
export const fa: MapDrawingTranslations = { ...en }
