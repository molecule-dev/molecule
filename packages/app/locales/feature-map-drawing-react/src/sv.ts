import type { MapDrawingTranslations } from './types.js'
import { en } from './en.js'

/** Map-drawing translations for sv — currently mirrors English (translation TBD). */
export const sv: MapDrawingTranslations = { ...en }
