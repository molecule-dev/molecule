import type { MapDrawingTranslations } from './types.js'
import { en } from './en.js'

/** Map-drawing translations for sl — currently mirrors English (translation TBD). */
export const sl: MapDrawingTranslations = { ...en }
