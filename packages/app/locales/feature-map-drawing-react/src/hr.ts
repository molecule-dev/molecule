import type { MapDrawingTranslations } from './types.js'
import { en } from './en.js'

/** Map-drawing translations for hr — currently mirrors English (translation TBD). */
export const hr: MapDrawingTranslations = { ...en }
