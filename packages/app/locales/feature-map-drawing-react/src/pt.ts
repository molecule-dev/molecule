import type { MapDrawingTranslations } from './types.js'
import { en } from './en.js'

/** Map-drawing translations for pt — currently mirrors English (translation TBD). */
export const pt: MapDrawingTranslations = { ...en }
