import type { MapDrawingTranslations } from './types.js'
import { en } from './en.js'

/** Map-drawing translations for it — currently mirrors English (translation TBD). */
export const it: MapDrawingTranslations = { ...en }
