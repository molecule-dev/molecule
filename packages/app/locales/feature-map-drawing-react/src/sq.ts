import type { MapDrawingTranslations } from './types.js'
import { en } from './en.js'

/** Map-drawing translations for sq — currently mirrors English (translation TBD). */
export const sq: MapDrawingTranslations = { ...en }
