import type { MapDrawingTranslations } from './types.js'
import { en } from './en.js'

/** Map-drawing translations for fil — currently mirrors English (translation TBD). */
export const fil: MapDrawingTranslations = { ...en }
