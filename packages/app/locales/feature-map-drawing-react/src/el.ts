import type { MapDrawingTranslations } from './types.js'
import { en } from './en.js'

/** Map-drawing translations for el — currently mirrors English (translation TBD). */
export const el: MapDrawingTranslations = { ...en }
