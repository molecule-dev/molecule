import type { MapDrawingTranslations } from './types.js'
import { en } from './en.js'

/** Map-drawing translations for bs — currently mirrors English (translation TBD). */
export const bs: MapDrawingTranslations = { ...en }
