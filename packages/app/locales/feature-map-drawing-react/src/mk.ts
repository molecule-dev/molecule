import type { MapDrawingTranslations } from './types.js'
import { en } from './en.js'

/** Map-drawing translations for mk — currently mirrors English (translation TBD). */
export const mk: MapDrawingTranslations = { ...en }
