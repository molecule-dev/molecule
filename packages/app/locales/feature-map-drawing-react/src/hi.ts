import type { MapDrawingTranslations } from './types.js'
import { en } from './en.js'

/** Map-drawing translations for hi — currently mirrors English (translation TBD). */
export const hi: MapDrawingTranslations = { ...en }
