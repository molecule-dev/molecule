import type { MapDrawingTranslations } from './types.js'
import { en } from './en.js'

/** Map-drawing translations for uk — currently mirrors English (translation TBD). */
export const uk: MapDrawingTranslations = { ...en }
