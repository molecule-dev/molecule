import type { MapDrawingTranslations } from './types.js'
import { en } from './en.js'

/** Map-drawing translations for ro — currently mirrors English (translation TBD). */
export const ro: MapDrawingTranslations = { ...en }
