import type { MapDrawingTranslations } from './types.js'
import { en } from './en.js'

/** Map-drawing translations for mn — currently mirrors English (translation TBD). */
export const mn: MapDrawingTranslations = { ...en }
