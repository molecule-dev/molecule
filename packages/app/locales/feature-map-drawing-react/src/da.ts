import type { MapDrawingTranslations } from './types.js'
import { en } from './en.js'

/** Map-drawing translations for da — currently mirrors English (translation TBD). */
export const da: MapDrawingTranslations = { ...en }
