import type { MapDrawingTranslations } from './types.js'
import { en } from './en.js'

/** Map-drawing translations for de — currently mirrors English (translation TBD). */
export const de: MapDrawingTranslations = { ...en }
