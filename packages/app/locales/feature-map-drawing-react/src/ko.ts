import type { MapDrawingTranslations } from './types.js'
import { en } from './en.js'

/** Map-drawing translations for ko — currently mirrors English (translation TBD). */
export const ko: MapDrawingTranslations = { ...en }
