import type { MapDrawingTranslations } from './types.js'
import { en } from './en.js'

/** Map-drawing translations for id — currently mirrors English (translation TBD). */
export const id: MapDrawingTranslations = { ...en }
