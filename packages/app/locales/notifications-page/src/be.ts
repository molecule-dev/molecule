import { en } from './en.js'
import type { NotificationsPageTranslations } from './types.js'

/** Notifications-page translations for be (stub — falls back to English until reviewed). */
export const be: NotificationsPageTranslations = { ...en }
