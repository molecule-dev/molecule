import { en } from './en.js'
import type { NotificationsPageTranslations } from './types.js'

/** Notifications-page translations for ha (stub — falls back to English until reviewed). */
export const ha: NotificationsPageTranslations = { ...en }
