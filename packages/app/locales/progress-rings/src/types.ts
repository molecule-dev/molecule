/** Translation keys for the progress-rings locale package. */
export type ProgressRingsTranslationKey = 'progressRings.summary' | 'progressRings.ring'

/** Translation record mapping progress-rings keys to translated strings. */
export type ProgressRingsTranslations = Record<ProgressRingsTranslationKey, string>
