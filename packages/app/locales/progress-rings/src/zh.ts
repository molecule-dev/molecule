import type { ProgressRingsTranslations } from './types.js'

/** Progress-rings translations for Chinese (Simplified). */
export const zh: ProgressRingsTranslations = {
  'progressRings.summary': '进度环（{{count}} 个环）',
  'progressRings.ring': '环 {{index}}',
}
