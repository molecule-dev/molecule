import type { AudioEffectsRackTranslations } from './types.js'
import { en } from './en.js'

/** Audio-effects-rack translations for it — currently mirrors English (translation TBD). */
export const it: AudioEffectsRackTranslations = { ...en }
