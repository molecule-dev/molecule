import type { AudioMixerTranslations } from './types.js'
import { en } from './en.js'

/** Audio-mixer translations for bs — currently mirrors English (translation TBD). */
export const bs: AudioMixerTranslations = { ...en }
