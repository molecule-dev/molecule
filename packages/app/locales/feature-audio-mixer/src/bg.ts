import type { AudioMixerTranslations } from './types.js'
import { en } from './en.js'

/** Audio-mixer translations for bg — currently mirrors English (translation TBD). */
export const bg: AudioMixerTranslations = { ...en }
