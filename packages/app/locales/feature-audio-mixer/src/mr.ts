import type { AudioMixerTranslations } from './types.js'
import { en } from './en.js'

/** Audio-mixer translations for mr — currently mirrors English (translation TBD). */
export const mr: AudioMixerTranslations = { ...en }
