import type { AudioMixerTranslations } from './types.js'
import { en } from './en.js'

/** Audio-mixer translations for bn — currently mirrors English (translation TBD). */
export const bn: AudioMixerTranslations = { ...en }
