import type { AudioMixerTranslations } from './types.js'
import { en } from './en.js'

/** Audio-mixer translations for af — currently mirrors English (translation TBD). */
export const af: AudioMixerTranslations = { ...en }
