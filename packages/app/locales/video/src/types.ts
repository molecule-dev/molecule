/** Translation keys for the video locale package. */
export type VideoTranslationKey = 'video.source.label'

/** Translation record mapping video keys to translated strings. */
export type VideoTranslations = Record<VideoTranslationKey, string>
