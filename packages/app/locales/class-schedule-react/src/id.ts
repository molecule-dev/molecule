import type { ClassScheduleTranslations } from './types.js'
import { en } from './en.js'

/** Class-schedule translations for id — currently mirrors English (translation TBD). */
export const id: ClassScheduleTranslations = { ...en }
