import type { ClassScheduleTranslations } from './types.js'
import { en } from './en.js'

/** Class-schedule translations for bg — currently mirrors English (translation TBD). */
export const bg: ClassScheduleTranslations = { ...en }
