import type { ClassScheduleTranslations } from './types.js'
import { en } from './en.js'

/** Class-schedule translations for mt — currently mirrors English (translation TBD). */
export const mt: ClassScheduleTranslations = { ...en }
