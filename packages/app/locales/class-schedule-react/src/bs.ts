import type { ClassScheduleTranslations } from './types.js'
import { en } from './en.js'

/** Class-schedule translations for bs — currently mirrors English (translation TBD). */
export const bs: ClassScheduleTranslations = { ...en }
