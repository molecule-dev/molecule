import type { ClassScheduleTranslations } from './types.js'
import { en } from './en.js'

/** Class-schedule translations for ne — currently mirrors English (translation TBD). */
export const ne: ClassScheduleTranslations = { ...en }
