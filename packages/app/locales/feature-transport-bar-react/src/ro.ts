import { en } from './en.js'
import type { TransportBarTranslations } from './types.js'

/** Transport-bar translations for ro (stub — falls back to English until reviewed). */
export const ro: TransportBarTranslations = { ...en }
