import { en } from './en.js'
import type { TransportBarTranslations } from './types.js'

/** Transport-bar translations for cs (stub — falls back to English until reviewed). */
export const cs: TransportBarTranslations = { ...en }
