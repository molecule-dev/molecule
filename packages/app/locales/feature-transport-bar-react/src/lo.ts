import { en } from './en.js'
import type { TransportBarTranslations } from './types.js'

/** Transport-bar translations for lo (stub — falls back to English until reviewed). */
export const lo: TransportBarTranslations = { ...en }
