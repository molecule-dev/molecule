import { en } from './en.js'
import type { TransportBarTranslations } from './types.js'

/** Transport-bar translations for tr (stub — falls back to English until reviewed). */
export const tr: TransportBarTranslations = { ...en }
