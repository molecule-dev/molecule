import { en } from './en.js'
import type { TransportBarTranslations } from './types.js'

/** Transport-bar translations for fi (stub — falls back to English until reviewed). */
export const fi: TransportBarTranslations = { ...en }
