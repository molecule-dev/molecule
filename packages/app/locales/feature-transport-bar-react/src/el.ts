import { en } from './en.js'
import type { TransportBarTranslations } from './types.js'

/** Transport-bar translations for el (stub — falls back to English until reviewed). */
export const el: TransportBarTranslations = { ...en }
