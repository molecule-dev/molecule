import { en } from './en.js'
import type { TransportBarTranslations } from './types.js'

/** Transport-bar translations for nl (stub — falls back to English until reviewed). */
export const nl: TransportBarTranslations = { ...en }
