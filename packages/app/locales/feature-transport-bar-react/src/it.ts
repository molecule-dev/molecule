import { en } from './en.js'
import type { TransportBarTranslations } from './types.js'

/** Transport-bar translations for it (stub — falls back to English until reviewed). */
export const it: TransportBarTranslations = { ...en }
