import { en } from './en.js'
import type { TransportBarTranslations } from './types.js'

/** Transport-bar translations for sv (stub — falls back to English until reviewed). */
export const sv: TransportBarTranslations = { ...en }
