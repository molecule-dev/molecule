import { en } from './en.js'
import type { TransportBarTranslations } from './types.js'

/** Transport-bar translations for hi (stub — falls back to English until reviewed). */
export const hi: TransportBarTranslations = { ...en }
