import { en } from './en.js'
import type { TransportBarTranslations } from './types.js'

/** Transport-bar translations for ar (stub — falls back to English until reviewed). */
export const ar: TransportBarTranslations = { ...en }
