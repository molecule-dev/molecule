import { en } from './en.js'
import type { TransportBarTranslations } from './types.js'

/** Transport-bar translations for ja (stub — falls back to English until reviewed). */
export const ja: TransportBarTranslations = { ...en }
