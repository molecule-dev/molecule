import { en } from './en.js'
import type { TransportBarTranslations } from './types.js'

/** Transport-bar translations for de (stub — falls back to English until reviewed). */
export const de: TransportBarTranslations = { ...en }
