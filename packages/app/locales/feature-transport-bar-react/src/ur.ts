import { en } from './en.js'
import type { TransportBarTranslations } from './types.js'

/** Transport-bar translations for ur (stub — falls back to English until reviewed). */
export const ur: TransportBarTranslations = { ...en }
