import { en } from './en.js'
import type { TransportBarTranslations } from './types.js'

/** Transport-bar translations for pl (stub — falls back to English until reviewed). */
export const pl: TransportBarTranslations = { ...en }
