import { en } from './en.js'
import type { TransportBarTranslations } from './types.js'

/** Transport-bar translations for es (stub — falls back to English until reviewed). */
export const es: TransportBarTranslations = { ...en }
