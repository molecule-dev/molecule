import { en } from './en.js'
import type { TransportBarTranslations } from './types.js'

/** Transport-bar translations for he (stub — falls back to English until reviewed). */
export const he: TransportBarTranslations = { ...en }
