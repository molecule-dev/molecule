import { en } from './en.js'
import type { TransportBarTranslations } from './types.js'

/** Transport-bar translations for nb (stub — falls back to English until reviewed). */
export const nb: TransportBarTranslations = { ...en }
