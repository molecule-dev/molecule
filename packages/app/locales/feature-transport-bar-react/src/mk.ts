import { en } from './en.js'
import type { TransportBarTranslations } from './types.js'

/** Transport-bar translations for mk (stub — falls back to English until reviewed). */
export const mk: TransportBarTranslations = { ...en }
