import { en } from './en.js'
import type { TransportBarTranslations } from './types.js'

/** Transport-bar translations for sw (stub — falls back to English until reviewed). */
export const sw: TransportBarTranslations = { ...en }
