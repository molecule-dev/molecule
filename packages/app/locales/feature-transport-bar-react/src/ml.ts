import { en } from './en.js'
import type { TransportBarTranslations } from './types.js'

/** Transport-bar translations for ml (stub — falls back to English until reviewed). */
export const ml: TransportBarTranslations = { ...en }
