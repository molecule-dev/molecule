import { en } from './en.js'
import type { TransportBarTranslations } from './types.js'

/** Transport-bar translations for eu (stub — falls back to English until reviewed). */
export const eu: TransportBarTranslations = { ...en }
