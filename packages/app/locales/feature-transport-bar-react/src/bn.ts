import { en } from './en.js'
import type { TransportBarTranslations } from './types.js'

/** Transport-bar translations for bn (stub — falls back to English until reviewed). */
export const bn: TransportBarTranslations = { ...en }
