import { en } from './en.js'
import type { TransportBarTranslations } from './types.js'

/** Transport-bar translations for ms (stub — falls back to English until reviewed). */
export const ms: TransportBarTranslations = { ...en }
