import { en } from './en.js'
import type { TransportBarTranslations } from './types.js'

/** Transport-bar translations for hu (stub — falls back to English until reviewed). */
export const hu: TransportBarTranslations = { ...en }
