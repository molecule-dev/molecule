import type { VersionTranslations } from './types.js'

/** Version translations for Portuguese. */
export const pt: VersionTranslations = {
  'version.error.fetchFailed': 'Falha ao buscar a versão: {{status}}',
}
