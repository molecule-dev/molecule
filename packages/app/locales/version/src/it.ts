import type { VersionTranslations } from './types.js'

/** Version translations for Italian. */
export const it: VersionTranslations = {
  'version.error.fetchFailed': 'Impossibile recuperare la versione: {{status}}',
}
