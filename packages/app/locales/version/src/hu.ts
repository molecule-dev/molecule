import type { VersionTranslations } from './types.js'

/** Version translations for Hungarian. */
export const hu: VersionTranslations = {
  'version.error.fetchFailed': 'A verzió lekérése sikertelen: {{status}}',
}
