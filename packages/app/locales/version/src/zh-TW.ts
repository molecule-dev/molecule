import type { VersionTranslations } from './types.js'

/** Version translations for Chinese (Traditional). */
export const zhTW: VersionTranslations = {
  'version.error.fetchFailed': '取得版本失敗: {{status}}',
}
