import type { VersionTranslations } from './types.js'

/** Version translations for Yoruba. */
export const yo: VersionTranslations = {
  'version.error.fetchFailed': 'Ko le gba ẹya: {{status}}',
}
