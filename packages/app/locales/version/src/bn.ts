import type { VersionTranslations } from './types.js'

/** Version translations for Bengali. */
export const bn: VersionTranslations = {
  'version.error.fetchFailed': 'সংস্করণ আনতে ব্যর্থ: {{status}}',
}
