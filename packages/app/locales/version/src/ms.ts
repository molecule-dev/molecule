import type { VersionTranslations } from './types.js'

/** Version translations for Malay. */
export const ms: VersionTranslations = {
  'version.error.fetchFailed': 'Gagal mendapatkan versi: {{status}}',
}
