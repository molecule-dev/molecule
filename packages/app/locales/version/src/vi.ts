import type { VersionTranslations } from './types.js'

/** Version translations for Vietnamese. */
export const vi: VersionTranslations = {
  'version.error.fetchFailed': 'Không thể lấy phiên bản: {{status}}',
}
