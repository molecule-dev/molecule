import type { VersionTranslations } from './types.js'

/** Version translations for Lithuanian. */
export const lt: VersionTranslations = {
  'version.error.fetchFailed': 'Nepavyko gauti versijos: {{status}}',
}
