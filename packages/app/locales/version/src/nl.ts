import type { VersionTranslations } from './types.js'

/** Version translations for Dutch. */
export const nl: VersionTranslations = {
  'version.error.fetchFailed': 'Kan versie niet ophalen: {{status}}',
}
