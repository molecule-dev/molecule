import type { VersionTranslations } from './types.js'

/** Version translations for Indonesian. */
export const id: VersionTranslations = {
  'version.error.fetchFailed': 'Gagal mengambil versi: {{status}}',
}
