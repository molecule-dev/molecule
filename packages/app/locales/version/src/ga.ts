import type { VersionTranslations } from './types.js'

/** Version translations for Irish. */
export const ga: VersionTranslations = {
  'version.error.fetchFailed': 'Theip ar fháil an leagain: {{status}}',
}
