import type { VersionTranslations } from './types.js'

/** Version translations for Croatian. */
export const hr: VersionTranslations = {
  'version.error.fetchFailed': 'Dohvaćanje verzije nije uspjelo: {{status}}',
}
