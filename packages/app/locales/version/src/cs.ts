import type { VersionTranslations } from './types.js'

/** Version translations for Czech. */
export const cs: VersionTranslations = {
  'version.error.fetchFailed': 'Nepodařilo se načíst verzi: {{status}}',
}
