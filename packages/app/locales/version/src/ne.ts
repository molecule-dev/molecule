import type { VersionTranslations } from './types.js'

/** Version translations for Nepali. */
export const ne: VersionTranslations = {
  'version.error.fetchFailed': 'संस्करण ल्याउन असफल: {{status}}',
}
