import type { VersionTranslations } from './types.js'

/** Version translations for Spanish. */
export const es: VersionTranslations = {
  'version.error.fetchFailed': 'Error al obtener la versión: {{status}}',
}
