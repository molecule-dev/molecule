import type { VersionTranslations } from './types.js'

/** Version translations for Polish. */
export const pl: VersionTranslations = {
  'version.error.fetchFailed': 'Nie udało się pobrać wersji: {{status}}',
}
