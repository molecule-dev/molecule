import type { VersionTranslations } from './types.js'

/** Version translations for Slovak. */
export const sk: VersionTranslations = {
  'version.error.fetchFailed': 'Nepodarilo sa načítať verziu: {{status}}',
}
