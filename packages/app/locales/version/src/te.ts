import type { VersionTranslations } from './types.js'

/** Version translations for Telugu. */
export const te: VersionTranslations = {
  'version.error.fetchFailed': 'సంస్కరణను పొందడంలో విఫలమైంది: {{status}}',
}
