import type { VersionTranslations } from './types.js'

/** Version translations for Maltese. */
export const mt: VersionTranslations = {
  'version.error.fetchFailed': 'Falla fil-kisba tal-verzjoni: {{status}}',
}
