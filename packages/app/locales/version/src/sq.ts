import type { VersionTranslations } from './types.js'

/** Version translations for Albanian. */
export const sq: VersionTranslations = {
  'version.error.fetchFailed': 'Dështoi marrja e versionit: {{status}}',
}
