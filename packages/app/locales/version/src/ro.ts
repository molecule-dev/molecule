import type { VersionTranslations } from './types.js'

/** Version translations for Romanian. */
export const ro: VersionTranslations = {
  'version.error.fetchFailed': 'Nu s-a putut obține versiunea: {{status}}',
}
