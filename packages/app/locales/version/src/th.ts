import type { VersionTranslations } from './types.js'

/** Version translations for Thai. */
export const th: VersionTranslations = {
  'version.error.fetchFailed': 'ไม่สามารถดึงเวอร์ชันได้: {{status}}',
}
