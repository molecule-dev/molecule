import type { VersionTranslations } from './types.js'

/** Version translations for English. */
export const en: VersionTranslations = {
  'version.error.fetchFailed': 'Failed to fetch version: {{status}}',
}
