import type { VersionTranslations } from './types.js'

/** Version translations for Hebrew. */
export const he: VersionTranslations = {
  'version.error.fetchFailed': 'שליפת הגרסה נכשלה: {{status}}',
}
