import type { VersionTranslations } from './types.js'

/** Version translations for Galician. */
export const gl: VersionTranslations = {
  'version.error.fetchFailed': 'Erro ao obter a versión: {{status}}',
}
