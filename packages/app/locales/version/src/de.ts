import type { VersionTranslations } from './types.js'

/** Version translations for German. */
export const de: VersionTranslations = {
  'version.error.fetchFailed': 'Version konnte nicht abgerufen werden: {{status}}',
}
