import type { VersionTranslations } from './types.js'

/** Version translations for Turkish. */
export const tr: VersionTranslations = {
  'version.error.fetchFailed': 'Sürüm alınamadı: {{status}}',
}
