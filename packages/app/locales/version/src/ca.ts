import type { VersionTranslations } from './types.js'

/** Version translations for ca. */
export const ca: Partial<VersionTranslations> = {
  'version.error.fetchFailed': 'No s&#39;ha pogut obtenir la versió:<x> {{estat}}</x>',
}
