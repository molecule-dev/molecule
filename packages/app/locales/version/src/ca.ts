import type { VersionTranslations } from './types.js'

/** Version translations for Catalan. */
export const ca: VersionTranslations = {
  'version.error.fetchFailed': "No s'ha pogut obtenir la versió: {{status}}",
}
