import type { VersionTranslations } from './types.js'

/** Version translations for Swedish. */
export const sv: VersionTranslations = {
  'version.error.fetchFailed': 'Kunde inte hämta version: {{status}}',
}
