import type { VersionTranslations } from './types.js'

/** Version translations for Bulgarian. */
export const bg: VersionTranslations = {
  'version.error.fetchFailed': 'Неуспешно извличане на версията: {{status}}',
}
