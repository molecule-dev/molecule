import type { VersionTranslations } from './types.js'

/** Version translations for Icelandic. */
export const is: VersionTranslations = {
  'version.error.fetchFailed': 'Ekki tókst að sækja útgáfu: {{status}}',
}
