import type { ClassScheduleTranslations } from './types.js'

/** ClassSchedule translations for es. */
export const es: Partial<ClassScheduleTranslations> = {
  'classSchedule.aria.region': 'Horario semanal de clases',
  'classSchedule.aria.event': '{{weekday}} {{start}} – {{end}}',
  'classSchedule.aria.slot': 'Espacio vacío, {{weekday}} {{time}}',
}
