import type { RoutingTranslations } from './types.js'

/** Routing translations for Igbo. */
export const ig: RoutingTranslations = {
  'routing.error.missingParam': 'Achọpụtaghị parameter "{{name}}" maka ụzọ "{{pattern}}"',
  'routing.error.routeNotFound': 'Ahụghị ụzọ "{{name}}"',
  'routing.error.useMoleculeRouterOutsideProvider':
    "A ga-eji useMoleculeRouter n'ime MoleculeRouterProvider",
}
