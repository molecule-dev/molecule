import type { ShareTranslations } from './types.js'

/** Share translations for Italian. */
export const it: ShareTranslations = {
  'share.error.noProvider': 'Il provider di condivisione non è configurato.',
}
