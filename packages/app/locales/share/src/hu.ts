import type { ShareTranslations } from './types.js'

/** Share translations for Hungarian. */
export const hu: ShareTranslations = {
  'share.error.noProvider': 'A megosztási szolgáltató nincs beállítva.',
}
