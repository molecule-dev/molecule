import type { ShareTranslations } from './types.js'

/** Share translations for Maltese. */
export const mt: ShareTranslations = {
  'share.error.noProvider': 'Il-provajder tal-qsim mhux konfigurat.',
}
