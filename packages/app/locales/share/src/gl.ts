import type { ShareTranslations } from './types.js'

/** Share translations for Galician. */
export const gl: ShareTranslations = {
  'share.error.noProvider': 'O provedor de compartición non está configurado.',
}
