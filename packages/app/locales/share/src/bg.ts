import type { ShareTranslations } from './types.js'

/** Share translations for Bulgarian. */
export const bg: ShareTranslations = {
  'share.error.noProvider': 'Доставчикът за споделяне не е конфигуриран.',
}
