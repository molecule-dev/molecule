import type { ShareTranslations } from './types.js'

/** Share translations for Romanian. */
export const ro: ShareTranslations = {
  'share.error.noProvider': 'Furnizorul de partajare nu este configurat.',
}
