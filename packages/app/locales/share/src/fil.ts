import type { ShareTranslations } from './types.js'

/** Share translations for Filipino. */
export const fil: ShareTranslations = {
  'share.error.noProvider': 'Hindi naka-configure ang share provider.',
}
