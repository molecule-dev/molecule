import type { ExifPanelTranslations } from './types.js'
import { en } from './en.js'

/** Exif-panel translations for bs — currently mirrors English (translation TBD). */
export const bs: ExifPanelTranslations = { ...en }
