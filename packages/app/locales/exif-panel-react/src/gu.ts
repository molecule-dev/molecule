import type { ExifPanelTranslations } from './types.js'
import { en } from './en.js'

/** Exif-panel translations for gu — currently mirrors English (translation TBD). */
export const gu: ExifPanelTranslations = { ...en }
