import type { ExifPanelTranslations } from './types.js'
import { en } from './en.js'

/** Exif-panel translations for et — currently mirrors English (translation TBD). */
export const et: ExifPanelTranslations = { ...en }
