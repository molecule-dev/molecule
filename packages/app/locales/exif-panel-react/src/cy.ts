import type { ExifPanelTranslations } from './types.js'
import { en } from './en.js'

/** Exif-panel translations for cy — currently mirrors English (translation TBD). */
export const cy: ExifPanelTranslations = { ...en }
