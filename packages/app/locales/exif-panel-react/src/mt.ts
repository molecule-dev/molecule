import type { ExifPanelTranslations } from './types.js'
import { en } from './en.js'

/** Exif-panel translations for mt — currently mirrors English (translation TBD). */
export const mt: ExifPanelTranslations = { ...en }
