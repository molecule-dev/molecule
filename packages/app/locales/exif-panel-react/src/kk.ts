import type { ExifPanelTranslations } from './types.js'
import { en } from './en.js'

/** Exif-panel translations for kk — currently mirrors English (translation TBD). */
export const kk: ExifPanelTranslations = { ...en }
