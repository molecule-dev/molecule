import type { ExifPanelTranslations } from './types.js'
import { en } from './en.js'

/** Exif-panel translations for nl — currently mirrors English (translation TBD). */
export const nl: ExifPanelTranslations = { ...en }
