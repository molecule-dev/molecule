import type { ExifPanelTranslations } from './types.js'
import { en } from './en.js'

/** Exif-panel translations for sw — currently mirrors English (translation TBD). */
export const sw: ExifPanelTranslations = { ...en }
