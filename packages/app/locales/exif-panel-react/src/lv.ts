import type { ExifPanelTranslations } from './types.js'
import { en } from './en.js'

/** Exif-panel translations for lv — currently mirrors English (translation TBD). */
export const lv: ExifPanelTranslations = { ...en }
