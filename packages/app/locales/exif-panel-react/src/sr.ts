import type { ExifPanelTranslations } from './types.js'
import { en } from './en.js'

/** Exif-panel translations for sr — currently mirrors English (translation TBD). */
export const sr: ExifPanelTranslations = { ...en }
