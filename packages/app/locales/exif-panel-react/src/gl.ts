import type { ExifPanelTranslations } from './types.js'
import { en } from './en.js'

/** Exif-panel translations for gl — currently mirrors English (translation TBD). */
export const gl: ExifPanelTranslations = { ...en }
