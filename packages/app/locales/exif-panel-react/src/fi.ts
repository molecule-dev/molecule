import type { ExifPanelTranslations } from './types.js'
import { en } from './en.js'

/** Exif-panel translations for fi — currently mirrors English (translation TBD). */
export const fi: ExifPanelTranslations = { ...en }
