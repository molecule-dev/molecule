import type { ExifPanelTranslations } from './types.js'
import { en } from './en.js'

/** Exif-panel translations for yo — currently mirrors English (translation TBD). */
export const yo: ExifPanelTranslations = { ...en }
