import type { ExifPanelTranslations } from './types.js'
import { en } from './en.js'

/** Exif-panel translations for zu — currently mirrors English (translation TBD). */
export const zu: ExifPanelTranslations = { ...en }
