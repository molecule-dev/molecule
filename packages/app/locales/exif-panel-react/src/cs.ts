import type { ExifPanelTranslations } from './types.js'
import { en } from './en.js'

/** Exif-panel translations for cs — currently mirrors English (translation TBD). */
export const cs: ExifPanelTranslations = { ...en }
