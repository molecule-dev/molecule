import type { ExifPanelTranslations } from './types.js'
import { en } from './en.js'

/** Exif-panel translations for de — currently mirrors English (translation TBD). */
export const de: ExifPanelTranslations = { ...en }
