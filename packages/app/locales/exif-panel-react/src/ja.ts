import type { ExifPanelTranslations } from './types.js'
import { en } from './en.js'

/** Exif-panel translations for ja — currently mirrors English (translation TBD). */
export const ja: ExifPanelTranslations = { ...en }
