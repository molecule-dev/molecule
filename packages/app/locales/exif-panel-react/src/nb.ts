import type { ExifPanelTranslations } from './types.js'
import { en } from './en.js'

/** Exif-panel translations for nb — currently mirrors English (translation TBD). */
export const nb: ExifPanelTranslations = { ...en }
