import type { ExifPanelTranslations } from './types.js'
import { en } from './en.js'

/** Exif-panel translations for az — currently mirrors English (translation TBD). */
export const az: ExifPanelTranslations = { ...en }
