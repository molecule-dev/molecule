import type { ExifPanelTranslations } from './types.js'
import { en } from './en.js'

/** Exif-panel translations for is — currently mirrors English (translation TBD). */
export const is: ExifPanelTranslations = { ...en }
