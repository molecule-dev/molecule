import type { ExifPanelTranslations } from './types.js'
import { en } from './en.js'

/** Exif-panel translations for ml — currently mirrors English (translation TBD). */
export const ml: ExifPanelTranslations = { ...en }
