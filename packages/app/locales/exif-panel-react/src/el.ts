import type { ExifPanelTranslations } from './types.js'
import { en } from './en.js'

/** Exif-panel translations for el — currently mirrors English (translation TBD). */
export const el: ExifPanelTranslations = { ...en }
