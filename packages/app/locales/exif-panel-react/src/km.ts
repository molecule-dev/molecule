import type { ExifPanelTranslations } from './types.js'
import { en } from './en.js'

/** Exif-panel translations for km — currently mirrors English (translation TBD). */
export const km: ExifPanelTranslations = { ...en }
