import type { ExifPanelTranslations } from './types.js'
import { en } from './en.js'

/** Exif-panel translations for fa — currently mirrors English (translation TBD). */
export const fa: ExifPanelTranslations = { ...en }
