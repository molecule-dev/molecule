import type { ExifPanelTranslations } from './types.js'
import { en } from './en.js'

/** Exif-panel translations for te — currently mirrors English (translation TBD). */
export const te: ExifPanelTranslations = { ...en }
