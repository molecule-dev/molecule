import type { ExifPanelTranslations } from './types.js'
import { en } from './en.js'

/** Exif-panel translations for ga — currently mirrors English (translation TBD). */
export const ga: ExifPanelTranslations = { ...en }
