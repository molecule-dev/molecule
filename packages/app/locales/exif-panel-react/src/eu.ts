import type { ExifPanelTranslations } from './types.js'
import { en } from './en.js'

/** Exif-panel translations for eu — currently mirrors English (translation TBD). */
export const eu: ExifPanelTranslations = { ...en }
