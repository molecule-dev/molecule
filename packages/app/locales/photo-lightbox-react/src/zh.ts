import type { PhotoLightboxTranslations } from './types.js'

/** PhotoLightbox translations for zh. */
export const zh: Partial<PhotoLightboxTranslations> = {
  'lightbox.close': '关闭',
  'lightbox.previous': '上一个',
  'lightbox.next': '下一步',
}
