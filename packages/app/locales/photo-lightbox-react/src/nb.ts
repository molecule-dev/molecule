import type { PhotoLightboxTranslations } from './types.js'

/** PhotoLightbox translations for nb. */
export const nb: Partial<PhotoLightboxTranslations> = {
  'lightbox.close': 'Lukk',
  'lightbox.previous': 'Forrige',
}
