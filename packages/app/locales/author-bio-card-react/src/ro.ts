import type { AuthorBioCardTranslations } from './types.js'

/** AuthorBioCard translations for ro. */
export const ro: Partial<AuthorBioCardTranslations> = {
  'authorBioCard.follow': 'Urmăriți',
  'authorBioCard.following': 'Urmărit',
}
