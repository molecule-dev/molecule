/** Translation keys for the photo-lightbox locale package. */
export type PhotoLightboxTranslationKey = 'lightbox.close' | 'lightbox.previous' | 'lightbox.next'

/** Translation record mapping photo-lightbox-react keys to translated strings. */
export type PhotoLightboxTranslations = Record<PhotoLightboxTranslationKey, string>
