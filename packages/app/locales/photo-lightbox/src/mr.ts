import type { PhotoLightboxTranslations } from './types.js'

/** PhotoLightbox translations for mr. */
export const mr: Partial<PhotoLightboxTranslations> = {
  'lightbox.close': 'बंद करा',
  'lightbox.previous': 'मागील',
  'lightbox.next': 'पुढील',
}
