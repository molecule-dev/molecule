import type { PhotoLightboxTranslations } from './types.js'

/** PhotoLightbox translations for zu. */
export const zu: Partial<PhotoLightboxTranslations> = {
  'lightbox.close': 'Vala',
  'lightbox.previous': 'Okwangaphambilini',
  'lightbox.next': 'Olandelayo',
}
