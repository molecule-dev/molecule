import type { AudioRecorderTranslations } from './types.js'

/** AudioRecorder translations for ms. */
export const ms: Partial<AudioRecorderTranslations> = {
  'audioRecorder.pause': 'Jeda',
  'audioRecorder.resume': 'Sambung semula',
  'audioRecorder.statusPaused': 'Dijeda',
}
