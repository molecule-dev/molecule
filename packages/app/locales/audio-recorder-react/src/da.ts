import type { AudioRecorderTranslations } from './types.js'

/** AudioRecorder translations for da. */
export const da: Partial<AudioRecorderTranslations> = {
  'audioRecorder.pause': 'Sæt på pause',
  'audioRecorder.resume': 'Genoptag',
  'audioRecorder.statusPaused': 'Sat på pause',
}
