import type { AudioRecorderTranslations } from './types.js'

/** AudioRecorder translations for cs. */
export const cs: Partial<AudioRecorderTranslations> = {
  'audioRecorder.pause': 'Pozastavit',
  'audioRecorder.resume': 'Obnovit',
  'audioRecorder.statusPaused': 'Pozastaveno',
}
