import type { AudioRecorderTranslations } from './types.js'

/** AudioRecorder translations for el. */
export const el: Partial<AudioRecorderTranslations> = {
  'audioRecorder.pause': 'Παύση',
  'audioRecorder.resume': 'Συνέχιση',
  'audioRecorder.statusPaused': 'Σε παύση',
}
