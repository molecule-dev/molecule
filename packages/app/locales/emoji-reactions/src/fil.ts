import type { EmojiReactionsTranslations } from './types.js'

/** EmojiReactions translations for fil. */
export const fil: Partial<EmojiReactionsTranslations> = {
  'reactions.add': 'Magdagdag ng reaksyon',
}
