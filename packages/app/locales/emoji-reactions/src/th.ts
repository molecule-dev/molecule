import type { EmojiReactionsTranslations } from './types.js'

/** EmojiReactions translations for th. */
export const th: Partial<EmojiReactionsTranslations> = {
  'reactions.add': 'เพิ่มปฏิกิริยา',
}
