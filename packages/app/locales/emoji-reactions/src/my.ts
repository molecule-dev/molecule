import type { EmojiReactionsTranslations } from './types.js'

/** EmojiReactions translations for my. */
export const my: Partial<EmojiReactionsTranslations> = {
  'reactions.add': 'တုံ့ပြန်မှုထည့်ပါ',
}
