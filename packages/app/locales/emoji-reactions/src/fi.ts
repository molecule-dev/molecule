import type { EmojiReactionsTranslations } from './types.js'

/** EmojiReactions translations for fi. */
export const fi: Partial<EmojiReactionsTranslations> = {
  'reactions.add': 'Lisää reaktio',
}
