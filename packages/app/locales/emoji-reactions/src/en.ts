import type { EmojiReactionsTranslations } from './types.js'

/** EmojiReactions translations for English. */
export const en: EmojiReactionsTranslations = {
  'reactions.add': 'Add reaction',
  'reactions.count': '{{count}} reactions',
}
