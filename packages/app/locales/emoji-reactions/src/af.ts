import type { EmojiReactionsTranslations } from './types.js'

/** EmojiReactions translations for af. */
export const af: Partial<EmojiReactionsTranslations> = {
  'reactions.add': 'Voeg reaksie by',
}
