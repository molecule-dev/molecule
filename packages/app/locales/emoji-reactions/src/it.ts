import type { EmojiReactionsTranslations } from './types.js'

/** EmojiReactions translations for it. */
export const it: Partial<EmojiReactionsTranslations> = {
  'reactions.add': 'Aggiungi reazione',
}
