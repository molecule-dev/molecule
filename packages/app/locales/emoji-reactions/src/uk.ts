import type { EmojiReactionsTranslations } from './types.js'

/** EmojiReactions translations for uk. */
export const uk: Partial<EmojiReactionsTranslations> = {
  'reactions.add': 'Додати реакцію',
}
