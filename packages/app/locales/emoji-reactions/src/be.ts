import type { EmojiReactionsTranslations } from './types.js'

/** EmojiReactions translations for be. */
export const be: Partial<EmojiReactionsTranslations> = {
  'reactions.add': 'Дадаць рэакцыю',
}
