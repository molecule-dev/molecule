import type { EmojiReactionsTranslations } from './types.js'

/** EmojiReactions translations for bn. */
export const bn: Partial<EmojiReactionsTranslations> = {
  'reactions.add': 'প্রতিক্রিয়া যোগ করুন',
}
