import type { EmojiReactionsTranslations } from './types.js'

/** EmojiReactions translations for si. */
export const si: Partial<EmojiReactionsTranslations> = {
  'reactions.add': 'ප්‍රතිචාරය එක් කරන්න',
}
