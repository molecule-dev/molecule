import type { EmojiReactionsTranslations } from './types.js'

/** EmojiReactions translations for zu. */
export const zu: Partial<EmojiReactionsTranslations> = {
  'reactions.add': 'Engeza ukusabela',
}
