import type { EmojiReactionsTranslations } from './types.js'

/** EmojiReactions translations for nb. */
export const nb: Partial<EmojiReactionsTranslations> = {
  'reactions.add': 'Legg til reaksjon',
}
