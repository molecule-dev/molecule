import type { EmojiReactionsTranslations } from './types.js'

/** EmojiReactions translations for sl. */
export const sl: Partial<EmojiReactionsTranslations> = {
  'reactions.add': 'Dodaj reakcijo',
}
