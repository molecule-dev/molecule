import type { EmojiReactionsTranslations } from './types.js'

/** EmojiReactions translations for es. */
export const es: Partial<EmojiReactionsTranslations> = {
  'reactions.add': 'Agregar reacción',
}
