import type { EmojiReactionsTranslations } from './types.js'

/** EmojiReactions translations for lt. */
export const lt: Partial<EmojiReactionsTranslations> = {
  'reactions.add': 'Pridėti reakciją',
}
