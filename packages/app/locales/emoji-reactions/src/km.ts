import type { EmojiReactionsTranslations } from './types.js'

/** EmojiReactions translations for km. */
export const km: Partial<EmojiReactionsTranslations> = {
  'reactions.add': 'បន្ថែមប្រតិកម្ម',
}
