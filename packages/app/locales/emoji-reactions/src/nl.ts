import type { EmojiReactionsTranslations } from './types.js'

/** EmojiReactions translations for nl. */
export const nl: Partial<EmojiReactionsTranslations> = {
  'reactions.add': 'Voeg een reactie toe',
}
