import type { EmojiReactionsTranslations } from './types.js'

/** EmojiReactions translations for el. */
export const el: Partial<EmojiReactionsTranslations> = {
  'reactions.add': 'Προσθήκη αντίδρασης',
}
