import type { EmojiReactionsTranslations } from './types.js'

/** EmojiReactions translations for cs. */
export const cs: Partial<EmojiReactionsTranslations> = {
  'reactions.add': 'Přidat reakci',
}
