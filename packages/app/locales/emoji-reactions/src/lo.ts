import type { EmojiReactionsTranslations } from './types.js'

/** EmojiReactions translations for lo. */
export const lo: Partial<EmojiReactionsTranslations> = {
  'reactions.add': 'ເພີ່ມປະຕິກິລິຍາ',
}
