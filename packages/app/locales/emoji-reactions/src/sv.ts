import type { EmojiReactionsTranslations } from './types.js'

/** EmojiReactions translations for sv. */
export const sv: Partial<EmojiReactionsTranslations> = {
  'reactions.add': 'Lägg till reaktion',
}
