import type { EmojiReactionsTranslations } from './types.js'

/** EmojiReactions translations for pt. */
export const pt: Partial<EmojiReactionsTranslations> = {
  'reactions.add': 'Adicionar reação',
}
