import type { EmojiReactionsTranslations } from './types.js'

/** EmojiReactions translations for vi. */
export const vi: Partial<EmojiReactionsTranslations> = {
  'reactions.add': 'Thêm phản ứng',
}
