import type { EmojiReactionsTranslations } from './types.js'

/** EmojiReactions translations for de. */
export const de: Partial<EmojiReactionsTranslations> = {
  'reactions.add': 'Reaktion hinzufügen',
}
