import type { EmojiReactionsTranslations } from './types.js'

/** EmojiReactions translations for ms. */
export const ms: Partial<EmojiReactionsTranslations> = {
  'reactions.add': 'Tambah tindak balas',
}
