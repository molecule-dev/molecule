import type { EmojiReactionsTranslations } from './types.js'

/** EmojiReactions translations for eu. */
export const eu: Partial<EmojiReactionsTranslations> = {
  'reactions.add': 'Gehitu erreakzioa',
}
