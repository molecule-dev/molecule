import type { EmojiReactionsTranslations } from './types.js'

/** EmojiReactions translations for mt. */
export const mt: Partial<EmojiReactionsTranslations> = {
  'reactions.add': 'Żid ir-reazzjoni',
}
