import type { EmojiReactionsTranslations } from './types.js'

/** EmojiReactions translations for ca. */
export const ca: Partial<EmojiReactionsTranslations> = {
  'reactions.add': 'Afegeix una reacció',
}
