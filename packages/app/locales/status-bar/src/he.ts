import type { StatusBarTranslations } from './types.js'

/** Status Bar translations for Hebrew. */
export const he: StatusBarTranslations = {
  'statusBar.error.noProvider':
    '@molecule/app-status-bar: לא הוגדר ספק. קרא ל-setProvider() עם מימוש של StatusBarProvider (למשל, מ-@molecule/app-status-bar-capacitor).',
}
