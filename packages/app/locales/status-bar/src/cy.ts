import type { StatusBarTranslations } from './types.js'

/** StatusBar translations for cy. */
export const cy: Partial<StatusBarTranslations> = {
  'statusBar.error.noProvider':
    "@molecule/app-status-bar: Dim darparwr wedi'i osod. Galwch setProvider() gyda gweithrediad StatusBarProvider (e.e., o @molecule/app-status-bar-capacitor).",
}
