import type { StatusBarTranslations } from './types.js'

/** Status Bar translations for Macedonian. */
export const mk: StatusBarTranslations = {
  'statusBar.error.noProvider':
    '@molecule/app-status-bar: Не е поставен провајдер. Повикајте setProvider() со имплементација на StatusBarProvider (на пр., од @molecule/app-status-bar-capacitor).',
}
