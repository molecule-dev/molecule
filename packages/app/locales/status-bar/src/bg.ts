import type { StatusBarTranslations } from './types.js'

/** Status Bar translations for Bulgarian. */
export const bg: StatusBarTranslations = {
  'statusBar.error.noProvider':
    '@molecule/app-status-bar: Не е зададен доставчик. Извикайте setProvider() с имплементация на StatusBarProvider (напр. от @molecule/app-status-bar-capacitor).',
}
