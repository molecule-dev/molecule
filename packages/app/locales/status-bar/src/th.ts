import type { StatusBarTranslations } from './types.js'

/** Status Bar translations for Thai. */
export const th: StatusBarTranslations = {
  'statusBar.error.noProvider':
    '@molecule/app-status-bar: ยังไม่ได้ตั้งค่าผู้ให้บริการ เรียก setProvider() พร้อมการใช้งาน StatusBarProvider (เช่น จาก @molecule/app-status-bar-capacitor)',
}
