import type { StatusBarTranslations } from './types.js'

/** StatusBar translations for mt. */
export const mt: Partial<StatusBarTranslations> = {
  'statusBar.error.noProvider':
    "@molecule/app-status-bar: L-ebda fornitur ma ġie stabbilit. Sejħa setProvider() b'implimentazzjoni ta' StatusBarProvider (eż., minn @molecule/app-status-bar-react-native).",
}
