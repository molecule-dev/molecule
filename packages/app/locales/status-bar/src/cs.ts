import type { StatusBarTranslations } from './types.js'

/** Status Bar translations for Czech. */
export const cs: StatusBarTranslations = {
  'statusBar.error.noProvider':
    '@molecule/app-status-bar: Poskytovatel není nastaven. Zavolejte setProvider() s implementací StatusBarProvider (např. z @molecule/app-status-bar-capacitor).',
}
