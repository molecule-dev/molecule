import type { StatusBarTranslations } from './types.js'

/** Status Bar translations for Yoruba. */
export const yo: StatusBarTranslations = {
  'statusBar.error.noProvider':
    '@molecule/app-status-bar: Ko si olupese ti a ṣeto. Pe setProvider() pẹlu imuse StatusBarProvider kan (fun apẹẹrẹ, lati @molecule/app-status-bar-capacitor).',
}
