import type { StatusBarTranslations } from './types.js'

/** Status Bar translations for Chinese (Traditional). */
export const zhTW: StatusBarTranslations = {
  'statusBar.error.noProvider':
    '@molecule/app-status-bar: 未設定提供者。請使用 StatusBarProvider 實作呼叫 setProvider()（例如，來自 @molecule/app-status-bar-capacitor）。',
}
