import type { StatusBarTranslations } from './types.js'

/** Status Bar translations for Urdu. */
export const ur: StatusBarTranslations = {
  'statusBar.error.noProvider':
    '@molecule/app-status-bar: کوئی فراہم کنندہ سیٹ نہیں ہے۔ StatusBarProvider کے نفاذ کے ساتھ setProvider() کو کال کریں (مثلاً، @molecule/app-status-bar-capacitor سے)۔',
}
