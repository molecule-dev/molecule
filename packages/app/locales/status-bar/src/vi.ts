import type { StatusBarTranslations } from './types.js'

/** Status Bar translations for Vietnamese. */
export const vi: StatusBarTranslations = {
  'statusBar.error.noProvider':
    '@molecule/app-status-bar: Chưa thiết lập nhà cung cấp. Gọi setProvider() với triển khai StatusBarProvider (ví dụ, từ @molecule/app-status-bar-capacitor).',
}
