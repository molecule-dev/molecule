import type { StatusBarTranslations } from './types.js'

/** Status Bar translations for Bosnian. */
export const bs: StatusBarTranslations = {
  'statusBar.error.noProvider':
    '@molecule/app-status-bar: Provajder nije postavljen. Pozovite setProvider() sa StatusBarProvider implementacijom (npr. iz @molecule/app-status-bar-capacitor).',
}
