import type { StatusBarTranslations } from './types.js'

/** Status Bar translations for Polish. */
export const pl: StatusBarTranslations = {
  'statusBar.error.noProvider':
    '@molecule/app-status-bar: Nie ustawiono dostawcy. Wywołaj setProvider() z implementacją StatusBarProvider (np. z @molecule/app-status-bar-capacitor).',
}
