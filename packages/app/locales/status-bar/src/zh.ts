import type { StatusBarTranslations } from './types.js'

/** Status Bar translations for Chinese. */
export const zh: StatusBarTranslations = {
  'statusBar.error.noProvider':
    '@molecule/app-status-bar: 未设置提供程序。请使用 StatusBarProvider 实现调用 setProvider()（例如，来自 @molecule/app-status-bar-capacitor）。',
}
