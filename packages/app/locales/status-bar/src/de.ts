import type { StatusBarTranslations } from './types.js'

/** Status Bar translations for German. */
export const de: StatusBarTranslations = {
  'statusBar.error.noProvider':
    '@molecule/app-status-bar: Kein Anbieter festgelegt. Rufen Sie setProvider() mit einer StatusBarProvider-Implementierung auf (z. B. von @molecule/app-status-bar-capacitor).',
}
