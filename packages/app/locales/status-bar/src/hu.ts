import type { StatusBarTranslations } from './types.js'

/** Status Bar translations for Hungarian. */
export const hu: StatusBarTranslations = {
  'statusBar.error.noProvider':
    '@molecule/app-status-bar: Nincs szolgáltató beállítva. Hívja meg a setProvider()-t egy StatusBarProvider implementációval (pl. a @molecule/app-status-bar-capacitor-ból).',
}
