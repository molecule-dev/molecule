import type { StatusBarTranslations } from './types.js'

/** Status Bar translations for Japanese. */
export const ja: StatusBarTranslations = {
  'statusBar.error.noProvider':
    '@molecule/app-status-bar: プロバイダーが設定されていません。StatusBarProviderの実装でsetProvider()を呼び出してください（例: @molecule/app-status-bar-capacitorから）。',
}
