import type { StatusBarTranslations } from './types.js'

/** Status Bar translations for Persian. */
export const fa: StatusBarTranslations = {
  'statusBar.error.noProvider':
    '@molecule/app-status-bar: هیچ ارائه‌دهنده‌ای تنظیم نشده است. setProvider() را با یک پیاده‌سازی StatusBarProvider فراخوانی کنید (مثلاً از @molecule/app-status-bar-capacitor).',
}
