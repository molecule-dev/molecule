import type { MessageBubbleTranslations } from './types.js'

/** MessageBubble translations for zh. */
export const zh: Partial<MessageBubbleTranslations> = {
  'composer.placeholder': '写一条消息…',
  'composer.send': '发送',
}
