import type { MessageBubbleTranslations } from './types.js'

/** MessageBubble translations for ja. */
export const ja: Partial<MessageBubbleTranslations> = {
  'composer.placeholder': 'メッセージを書く…',
  'composer.send': '送信',
}
