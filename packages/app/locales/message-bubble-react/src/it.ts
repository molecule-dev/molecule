import type { MessageBubbleTranslations } from './types.js'

/** MessageBubble translations for it. */
export const it: Partial<MessageBubbleTranslations> = {
  'composer.placeholder': 'Scriva un messaggio…',
  'composer.send': 'Invia',
}
