import type { HabitStreakCardTranslations } from './types.js'

/** HabitStreakCard translations for bg. */
export const bg: Partial<HabitStreakCardTranslations> = {
  'habit.currentStreak': 'дневна серия',
  'habit.bestStreak': 'най-добрият',
  'habit.total': 'общо',
}
