import type { HabitStreakCardTranslations } from './types.js'

/** HabitStreakCard translations for bs. */
export const bs: Partial<HabitStreakCardTranslations> = {
  'habit.currentStreak': 'dnevni niz',
  'habit.bestStreak': 'najbolji',
  'habit.total': 'ukupno',
}
