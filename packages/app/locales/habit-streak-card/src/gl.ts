import type { HabitStreakCardTranslations } from './types.js'

/** HabitStreakCard translations for gl. */
export const gl: Partial<HabitStreakCardTranslations> = {
  'habit.currentStreak': 'racha diúrna',
  'habit.bestStreak': 'mellor',
  'habit.total': 'total',
}
