import type { HabitStreakCardTranslations } from './types.js'

/** HabitStreakCard translations for ta. */
export const ta: Partial<HabitStreakCardTranslations> = {
  'habit.currentStreak': 'நாள் தொடர்',
  'habit.bestStreak': 'சிறந்தது',
  'habit.total': 'மொத்தம்',
}
