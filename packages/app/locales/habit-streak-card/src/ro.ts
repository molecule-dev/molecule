import type { HabitStreakCardTranslations } from './types.js'

/** HabitStreakCard translations for ro. */
export const ro: Partial<HabitStreakCardTranslations> = {
  'habit.currentStreak': 'serie de zile',
  'habit.bestStreak': 'cel mai bun',
  'habit.total': 'total',
}
