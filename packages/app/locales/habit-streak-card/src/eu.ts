import type { HabitStreakCardTranslations } from './types.js'

/** HabitStreakCard translations for eu. */
export const eu: Partial<HabitStreakCardTranslations> = {
  'habit.currentStreak': 'egun-bolada',
  'habit.bestStreak': 'onena',
  'habit.total': 'guztira',
}
