import type { HabitStreakCardTranslations } from './types.js'

/** HabitStreakCard translations for zu. */
export const zu: Partial<HabitStreakCardTranslations> = {
  'habit.currentStreak': 'uchungechunge losuku',
  'habit.bestStreak': 'okungcono kakhulu',
  'habit.total': 'isamba',
}
