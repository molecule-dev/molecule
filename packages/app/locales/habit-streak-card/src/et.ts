import type { HabitStreakCardTranslations } from './types.js'

/** HabitStreakCard translations for et. */
export const et: Partial<HabitStreakCardTranslations> = {
  'habit.currentStreak': 'päevajada',
  'habit.bestStreak': 'parim',
  'habit.total': 'kokku',
}
