import type { HabitStreakCardTranslations } from './types.js'

/** HabitStreakCard translations for mt. */
export const mt: Partial<HabitStreakCardTranslations> = {
  'habit.currentStreak': "sensiela ta' ġurnata",
  'habit.bestStreak': 'l-aqwa',
  'habit.total': 'totali',
}
