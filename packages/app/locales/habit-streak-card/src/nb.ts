import type { HabitStreakCardTranslations } from './types.js'

/** HabitStreakCard translations for nb. */
export const nb: Partial<HabitStreakCardTranslations> = {
  'habit.total': 'totalt',
  'habit.currentStreak': 'dagsrekke',
  'habit.bestStreak': 'beste',
}
