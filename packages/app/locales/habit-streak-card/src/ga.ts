import type { HabitStreakCardTranslations } from './types.js'

/** HabitStreakCard translations for ga. */
export const ga: Partial<HabitStreakCardTranslations> = {
  'habit.currentStreak': 'sraith lae',
  'habit.bestStreak': 'is fearr',
  'habit.total': 'iomlán',
}
