import type { HabitStreakCardTranslations } from './types.js'

/** HabitStreakCard translations for sk. */
export const sk: Partial<HabitStreakCardTranslations> = {
  'habit.currentStreak': 'denná séria',
  'habit.bestStreak': 'najlepší',
  'habit.total': 'spolu',
}
