import type { HabitStreakCardTranslations } from './types.js'

/** HabitStreakCard translations for sw. */
export const sw: Partial<HabitStreakCardTranslations> = {
  'habit.currentStreak': 'mfululizo wa siku',
  'habit.bestStreak': 'bora zaidi',
  'habit.total': 'jumla',
}
