import type { HabitStreakCardTranslations } from './types.js'

/** HabitStreakCard translations for sl. */
export const sl: Partial<HabitStreakCardTranslations> = {
  'habit.currentStreak': 'dnevni niz',
  'habit.bestStreak': 'najboljše',
  'habit.total': 'skupaj',
}
