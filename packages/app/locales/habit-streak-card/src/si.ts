import type { HabitStreakCardTranslations } from './types.js'

/** HabitStreakCard translations for si. */
export const si: Partial<HabitStreakCardTranslations> = {
  'habit.currentStreak': 'දින රේඛාව',
  'habit.bestStreak': 'හොඳම',
  'habit.total': 'මුළු',
}
