/** Translation keys for the habit-streak-card locale package. */
export type HabitStreakCardTranslationKey =
  | 'habit.currentStreak'
  | 'habit.bestStreak'
  | 'habit.total'

/** Translation record mapping habit-streak-card-react keys to translated strings. */
export type HabitStreakCardTranslations = Record<HabitStreakCardTranslationKey, string>
