import type { HabitStreakCardTranslations } from './types.js'

/** HabitStreakCard translations for uk. */
export const uk: Partial<HabitStreakCardTranslations> = {
  'habit.total': 'всього',
  'habit.currentStreak': 'денна смуга',
  'habit.bestStreak': 'найкращий',
}
