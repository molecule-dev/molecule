import type { HabitStreakCardTranslations } from './types.js'

/** HabitStreakCard translations for ms. */
export const ms: Partial<HabitStreakCardTranslations> = {
  'habit.total': 'jumlah',
  'habit.currentStreak': 'rentetan hari',
  'habit.bestStreak': 'terbaik',
}
