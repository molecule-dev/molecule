import type { HabitStreakCardTranslations } from './types.js'

/** HabitStreakCard translations for sr. */
export const sr: Partial<HabitStreakCardTranslations> = {
  'habit.currentStreak': 'дневни низ',
  'habit.bestStreak': 'најбољи',
  'habit.total': 'укупно',
}
