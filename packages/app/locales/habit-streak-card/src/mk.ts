import type { HabitStreakCardTranslations } from './types.js'

/** HabitStreakCard translations for mk. */
export const mk: Partial<HabitStreakCardTranslations> = {
  'habit.currentStreak': 'дневна низа',
  'habit.bestStreak': 'најдобро',
  'habit.total': 'вкупно',
}
