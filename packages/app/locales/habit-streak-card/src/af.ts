import type { HabitStreakCardTranslations } from './types.js'

/** HabitStreakCard translations for af. */
export const af: Partial<HabitStreakCardTranslations> = {
  'habit.currentStreak': 'dagreeks',
  'habit.bestStreak': 'beste',
  'habit.total': 'totaal',
}
