import type { HabitStreakCardTranslations } from './types.js'

/** HabitStreakCard translations for ko. */
export const ko: Partial<HabitStreakCardTranslations> = {
  'habit.currentStreak': '일 연속',
  'habit.total': '전체',
}
