import type { HabitStreakCardTranslations } from './types.js'

/** HabitStreakCard translations for bn. */
export const bn: Partial<HabitStreakCardTranslations> = {
  'habit.currentStreak': 'দিনের ধারা',
  'habit.bestStreak': 'সেরা',
  'habit.total': 'মোট',
}
