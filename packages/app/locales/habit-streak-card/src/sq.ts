import type { HabitStreakCardTranslations } from './types.js'

/** HabitStreakCard translations for sq. */
export const sq: Partial<HabitStreakCardTranslations> = {
  'habit.currentStreak': 'seri ditore',
  'habit.bestStreak': 'më i miri',
  'habit.total': 'total',
}
