import type { HabitStreakCardTranslations } from './types.js'

/** HabitStreakCard translations for mr. */
export const mr: Partial<HabitStreakCardTranslations> = {
  'habit.currentStreak': 'सलग दिवस',
  'habit.bestStreak': 'सर्वोत्तम',
  'habit.total': 'एकूण',
}
