import type { HabitStreakCardTranslations } from './types.js'

/** HabitStreakCard translations for be. */
export const be: Partial<HabitStreakCardTranslations> = {
  'habit.currentStreak': 'дзённая паласа',
  'habit.bestStreak': 'найлепшы',
  'habit.total': 'агульная сума',
}
