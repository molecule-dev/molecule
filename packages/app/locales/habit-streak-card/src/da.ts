import type { HabitStreakCardTranslations } from './types.js'

/** HabitStreakCard translations for da. */
export const da: Partial<HabitStreakCardTranslations> = {
  'habit.total': 'i alt',
  'habit.currentStreak': 'dagsrække',
  'habit.bestStreak': 'bedst',
}
