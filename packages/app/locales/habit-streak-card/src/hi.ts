import type { HabitStreakCardTranslations } from './types.js'

/** HabitStreakCard translations for hi. */
export const hi: Partial<HabitStreakCardTranslations> = {
  'habit.currentStreak': 'दिन की लय',
  'habit.total': 'कुल',
}
