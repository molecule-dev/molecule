import type { HabitStreakCardTranslations } from './types.js'

/** HabitStreakCard translations for lv. */
export const lv: Partial<HabitStreakCardTranslations> = {
  'habit.currentStreak': 'dienas sērija',
  'habit.bestStreak': 'labākais',
  'habit.total': 'kopā',
}
