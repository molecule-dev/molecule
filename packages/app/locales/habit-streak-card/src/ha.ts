import type { HabitStreakCardTranslations } from './types.js'

/** HabitStreakCard translations for ha. */
export const ha: Partial<HabitStreakCardTranslations> = {
  'habit.currentStreak': 'jerin rana',
  'habit.bestStreak': 'mafi kyau',
  'habit.total': 'jimilla',
}
