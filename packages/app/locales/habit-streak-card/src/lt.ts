import type { HabitStreakCardTranslations } from './types.js'

/** HabitStreakCard translations for lt. */
export const lt: Partial<HabitStreakCardTranslations> = {
  'habit.currentStreak': 'dienos serija',
  'habit.bestStreak': 'geriausias',
  'habit.total': 'iš viso',
}
