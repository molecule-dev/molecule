import type { HabitStreakCardTranslations } from './types.js'

/** HabitStreakCard translations for az. */
export const az: Partial<HabitStreakCardTranslations> = {
  'habit.currentStreak': 'gün seriyası',
  'habit.bestStreak': 'ən yaxşı',
  'habit.total': 'cəmi',
}
