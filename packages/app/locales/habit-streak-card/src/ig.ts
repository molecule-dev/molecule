import type { HabitStreakCardTranslations } from './types.js'

/** HabitStreakCard translations for ig. */
export const ig: Partial<HabitStreakCardTranslations> = {
  'habit.currentStreak': 'usoro ụbọchị',
  'habit.bestStreak': 'kacha mma',
  'habit.total': 'mkpokọta',
}
