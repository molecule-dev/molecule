import type { HabitStreakCardTranslations } from './types.js'

/** HabitStreakCard translations for ca. */
export const ca: Partial<HabitStreakCardTranslations> = {
  'habit.currentStreak': 'ratxa de dia',
  'habit.bestStreak': 'millor',
  'habit.total': 'total',
}
