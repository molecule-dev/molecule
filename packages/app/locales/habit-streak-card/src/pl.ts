import type { HabitStreakCardTranslations } from './types.js'

/** HabitStreakCard translations for pl. */
export const pl: Partial<HabitStreakCardTranslations> = {
  'habit.currentStreak': 'dni z rzędu',
  'habit.total': 'łącznie',
}
