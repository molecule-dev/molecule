import type { HabitStreakCardTranslations } from './types.js'

/** HabitStreakCard translations for uz. */
export const uz: Partial<HabitStreakCardTranslations> = {
  'habit.currentStreak': 'kunlik seriya',
  'habit.bestStreak': 'eng yaxshi',
  'habit.total': 'jami',
}
