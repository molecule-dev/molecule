import type { HabitStreakCardTranslations } from './types.js'

/** HabitStreakCard translations for pt. */
export const pt: Partial<HabitStreakCardTranslations> = {
  'habit.currentStreak': 'dias de sequência',
  'habit.bestStreak': 'melhor',
  'habit.total': 'total',
}
