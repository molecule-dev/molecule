import type { HabitStreakCardTranslations } from './types.js'

/** HabitStreakCard translations for cs. */
export const cs: Partial<HabitStreakCardTranslations> = {
  'habit.total': 'celkem',
  'habit.currentStreak': 'denní pruh',
  'habit.bestStreak': 'nejlepší',
}
