import type { ReadingProgressBarTranslations } from './types.js'

/** Reading-progress-bar translations for my. Stub — falls back to English. */
export const my: ReadingProgressBarTranslations = {
  'readingProgressBar.aria.label': 'Reading progress',
}
