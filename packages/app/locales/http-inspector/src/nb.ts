import type { HttpInspectorTranslations } from './types.js'

/** HttpInspector translations for nb. */
export const nb: Partial<HttpInspectorTranslations> = {
  'httpInspector.sending': 'Sender…',
  'httpInspector.body': 'Brødtekst',
}
