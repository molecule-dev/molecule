# @molecule/app-keyboard-react-native

## 1.0.1

### Patch Changes

- Ship the generated package documentation.

  These seven native bonds sit one directory deeper than the rest of the fleet
  (packages/app/bonds/native/<category>/<provider>) and were outside both the
  workspaces globs and the publish tooling, so 1.0.0 never reached npm at all.
