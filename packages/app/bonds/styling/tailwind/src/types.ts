/**
 * Type definitions for Tailwind CSS styling utilities.
 *
 * @module
 */

import type { Theme, ThemeBreakpoints } from '@molecule/app-theme'

// Re-export core types so existing consumers don't break
export type { ClassValue, CVAConfig } from '@molecule/app-styling'

// Re-export theme types for convenience
export type { Theme, ThemeBreakpoints }
