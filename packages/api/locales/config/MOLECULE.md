# @molecule/api-locales-config

Translations for @molecule/api-config in 79 languages

## Purpose

Provides translations for the `@molecule/api-config` package which has 9 translation keys.

## Languages

79 languages supported: af, am, ar, az, be, bg, bn, bs, ca, cs, cy, da, de, el, en, es, et, eu, fa, fi, fil, fr, ga, gl, gu, ha, he, hi, hr, hu, hy, id, ig, is, it, ja, ka, kk, km, kn, ko, ky, lo, lt, lv, mk, ml, mn, mr, ms, mt, my, nb, ne, nl, pa, pl, pt, ro, ru, si, sk, sl, sq, sr, sv, sw, ta, te, th, tr, uk, ur, uz, vi, yo, zh, zh-TW, zu.

## Usage

```typescript
import { af, am, ar } from '@molecule/api-locales-config'
import type { ConfigTranslationKey, ConfigTranslations } from '@molecule/api-locales-config'
```

## Translation Keys

| Key | English |
|-----|---------|
| `config.error.required` | Required configuration  |
| `config.error.mustBeNumber` | Configuration  |
| `config.error.minValue` | Configuration  |
| `config.error.maxValue` | Configuration  |
| `config.error.mustBeBoolean` | Configuration  |
| `config.error.mustBeJson` | Configuration  |
| `config.error.patternMismatch` | Configuration  |
| `config.error.invalidEnum` | Configuration  |
| `config.error.validationNotSupported` | Current configuration provider does not support validation. |

## Metadata

- **Type:** locales
- **Category:** i18n
- **Stack:** api
- **Translates:** `@molecule/api-config`
