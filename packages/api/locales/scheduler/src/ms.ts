import type { SchedulerTranslations } from './types.js'

/** Scheduler translations for Malay. */
export const ms: SchedulerTranslations = {
  'scheduler.error.noProvider':
    'Pembekal penjadual tidak dikonfigurasikan. Panggil setProvider() terlebih dahulu.',
}
