import type { SchedulerTranslations } from './types.js'

/** Scheduler translations for Swedish. */
export const sv: SchedulerTranslations = {
  'scheduler.error.noProvider':
    'Schemaläggarens leverantör är inte konfigurerad. Anropa setProvider() först.',
}
