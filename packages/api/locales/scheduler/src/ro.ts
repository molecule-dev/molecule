import type { SchedulerTranslations } from './types.js'

/** Scheduler translations for Romanian. */
export const ro: SchedulerTranslations = {
  'scheduler.error.noProvider':
    'Furnizorul planificatorului nu este configurat. Apelați mai întâi setProvider().',
}
