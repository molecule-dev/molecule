import type { SchedulerTranslations } from './types.js'

/** Scheduler translations for Afrikaans. */
export const af: SchedulerTranslations = {
  'scheduler.error.noProvider':
    'Skeduleerder-verskaffer is nie gekonfigureer nie. Roep eers setProvider() aan.',
}
