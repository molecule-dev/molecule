import type { SchedulerTranslations } from './types.js'

/** Scheduler translations for Chinese (Simplified). */
export const zh: SchedulerTranslations = {
  'scheduler.error.noProvider': '调度器提供程序未配置。请先调用 setProvider()。',
}
