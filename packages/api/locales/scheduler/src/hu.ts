import type { SchedulerTranslations } from './types.js'

/** Scheduler translations for Hungarian. */
export const hu: SchedulerTranslations = {
  'scheduler.error.noProvider':
    'Az ütemező szolgáltató nincs konfigurálva. Először hívja meg a setProvider() függvényt.',
}
