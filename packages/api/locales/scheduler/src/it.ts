import type { SchedulerTranslations } from './types.js'

/** Scheduler translations for Italian. */
export const it: SchedulerTranslations = {
  'scheduler.error.noProvider':
    'Provider dello scheduler non configurato. Chiamare prima setProvider().',
}
