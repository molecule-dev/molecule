import type { SchedulerTranslations } from './types.js'

/** Scheduler translations for Filipino. */
export const fil: SchedulerTranslations = {
  'scheduler.error.noProvider':
    'Hindi na-configure ang provider ng scheduler. Tawagan muna ang setProvider().',
}
