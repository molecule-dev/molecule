import type { SchedulerTranslations } from './types.js'

/** Scheduler translations for Hebrew. */
export const he: SchedulerTranslations = {
  'scheduler.error.noProvider': 'ספק מתזמן לא הוגדר. קרא ל-setProvider() תחילה.',
}
