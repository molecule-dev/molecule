import type { SchedulerTranslations } from './types.js'

/** Scheduler translations for Croatian. */
export const hr: SchedulerTranslations = {
  'scheduler.error.noProvider':
    'Pružatelj raspoređivača nije konfiguriran. Prvo pozovite setProvider().',
}
