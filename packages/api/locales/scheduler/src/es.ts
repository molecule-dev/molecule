import type { SchedulerTranslations } from './types.js'

/** Scheduler translations for Spanish. */
export const es: SchedulerTranslations = {
  'scheduler.error.noProvider':
    'Proveedor del planificador no configurado. Llame primero a setProvider().',
}
