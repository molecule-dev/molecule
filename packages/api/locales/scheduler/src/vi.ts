import type { SchedulerTranslations } from './types.js'

/** Scheduler translations for Vietnamese. */
export const vi: SchedulerTranslations = {
  'scheduler.error.noProvider':
    'Nhà cung cấp bộ lập lịch chưa được cấu hình. Hãy gọi setProvider() trước.',
}
