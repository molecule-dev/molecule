import type { SchedulerTranslations } from './types.js'

/** Scheduler translations for Macedonian. */
export const mk: SchedulerTranslations = {
  'scheduler.error.noProvider':
    'Давателот на распоредувачот не е конфигуриран. Прво повикајте setProvider().',
}
