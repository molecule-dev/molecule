/** Translation keys for the scheduler locale package. */
export type SchedulerTranslationKey = 'scheduler.error.noProvider'

/** Translation record mapping scheduler keys to translated strings. */
export type SchedulerTranslations = Record<SchedulerTranslationKey, string>
