import type { SchedulerTranslations } from './types.js'

/** Scheduler translations for Turkish. */
export const tr: SchedulerTranslations = {
  'scheduler.error.noProvider':
    'Zamanlayıcı sağlayıcısı yapılandırılmamış. Önce setProvider() çağrısını yapın.',
}
