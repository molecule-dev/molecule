import type { SchedulerTranslations } from './types.js'

/** Scheduler translations for Polish. */
export const pl: SchedulerTranslations = {
  'scheduler.error.noProvider':
    'Dostawca harmonogramu nie jest skonfigurowany. Najpierw wywołaj setProvider().',
}
