import type { SchedulerTranslations } from './types.js'

/** Scheduler translations for Czech. */
export const cs: SchedulerTranslations = {
  'scheduler.error.noProvider': 'Scheduler provider not configured. Call setProvider() first.',
}
