import type { QueueTranslations } from './types.js'

/** Queue translations for English. */
export const en: QueueTranslations = {
  'queue.error.noProvider': 'Queue provider not configured. Call setProvider() first.',
}
