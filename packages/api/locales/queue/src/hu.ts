import type { QueueTranslations } from './types.js'

/** Queue translations for Hungarian. */
export const hu: QueueTranslations = {
  'queue.error.noProvider':
    'A sor szolgáltató nincs konfigurálva. Először hívja meg a setProvider()-t.',
}
