import type { QueueTranslations } from './types.js'

/** Queue translations for Galician. */
export const gl: QueueTranslations = {
  'queue.error.noProvider':
    'O provedor de cola non está configurado. Chame primeiro a setProvider().',
}
