import type { QueueTranslations } from './types.js'

/** Queue translations for Romanian. */
export const ro: QueueTranslations = {
  'queue.error.noProvider':
    'Furnizorul de coadă nu este configurat. Apelați mai întâi setProvider().',
}
