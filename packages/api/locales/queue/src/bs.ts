import type { QueueTranslations } from './types.js'

/** Queue translations for Bosnian. */
export const bs: QueueTranslations = {
  'queue.error.noProvider': 'Provajder reda nije konfigurisan. Prvo pozovite setProvider().',
}
