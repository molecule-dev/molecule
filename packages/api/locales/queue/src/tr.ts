import type { QueueTranslations } from './types.js'

/** Queue translations for Turkish. */
export const tr: QueueTranslations = {
  'queue.error.noProvider':
    'Kuyruk sağlayıcısı yapılandırılmamış. Önce setProvider() çağrısı yapın.',
}
