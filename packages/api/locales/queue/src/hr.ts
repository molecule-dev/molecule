import type { QueueTranslations } from './types.js'

/** Queue translations for Croatian. */
export const hr: QueueTranslations = {
  'queue.error.noProvider':
    'Pružatelj reda čekanja nije konfiguriran. Najprije pozovite setProvider().',
}
