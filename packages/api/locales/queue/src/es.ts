import type { QueueTranslations } from './types.js'

/** Queue translations for Spanish. */
export const es: QueueTranslations = {
  'queue.error.noProvider':
    'El proveedor de cola no está configurado. Llame primero a setProvider().',
}
