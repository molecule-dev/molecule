/** Translation keys for the queue locale package. */
export type QueueTranslationKey = 'queue.error.noProvider'

/** Translation record mapping queue keys to translated strings. */
export type QueueTranslations = Record<QueueTranslationKey, string>
