import type { QueueTranslations } from './types.js'

/** Queue translations for Bulgarian. */
export const bg: QueueTranslations = {
  'queue.error.noProvider':
    'Доставчикът на опашката не е конфигуриран. Първо извикайте setProvider().',
}
