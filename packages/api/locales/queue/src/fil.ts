import type { QueueTranslations } from './types.js'

/** Queue translations for Filipino. */
export const fil: QueueTranslations = {
  'queue.error.noProvider':
    'Hindi naka-configure ang queue provider. Tumawag muna ng setProvider().',
}
