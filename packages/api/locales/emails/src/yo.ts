import type { EmailsTranslations } from './types.js'

/** Emails translations for Yoruba. */
export const yo: EmailsTranslations = {
  'emails.error.noProvider': 'Gbigbe imeeli ko ti ni atunto. Pe setTransport() ni akoko.',
}
