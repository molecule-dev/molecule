import type { EmailsTranslations } from './types.js'

/** Emails translations for Spanish. */
export const es: EmailsTranslations = {
  'emails.error.noProvider':
    'El transporte de correo no está configurado. Llame primero a setTransport().',
}
