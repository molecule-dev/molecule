import type { EmailsTranslations } from './types.js'

/** Emails translations for Lithuanian. */
export const lt: EmailsTranslations = {
  'emails.error.noProvider':
    'El. pasto transportas nesukonfigiruotas. Pirmiausiai iskvieskite setTransport().',
}
