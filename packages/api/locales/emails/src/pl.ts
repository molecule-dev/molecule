import type { EmailsTranslations } from './types.js'

/** Emails translations for Polish. */
export const pl: EmailsTranslations = {
  'emails.error.noProvider':
    'Transport e-mail nie jest skonfigurowany. Najpierw wywolaj setTransport().',
}
