import type { EmailsTranslations } from './types.js'

/** Emails translations for Italian. */
export const it: EmailsTranslations = {
  'emails.error.noProvider': 'Il trasporto email non configurato. Chiamare prima setTransport().',
}
