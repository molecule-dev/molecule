import type { EmailsTranslations } from './types.js'

/** Emails translations for Croatian. */
export const hr: EmailsTranslations = {
  'emails.error.noProvider': 'Transport e-pošte nije konfiguriran. Prvo pozovite setTransport().',
}
