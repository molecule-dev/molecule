import type { EmailsTranslations } from './types.js'

/** Emails translations for Bengali. */
export const bn: EmailsTranslations = {
  'emails.error.noProvider': 'ইমেইল ট্রান্সপোর্ট কনফিগার করা হয়নি। প্রথমে setTransport() কল করুন।',
}
