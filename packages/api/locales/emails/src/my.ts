import type { EmailsTranslations } from './types.js'

/** Emails translations for Burmese. */
export const my: EmailsTranslations = {
  'emails.error.noProvider':
    'Email transport ကို configure မလုပ်ရသေးပါ။ setTransport() ကို အရင်ခေါ်ပါ။',
}
