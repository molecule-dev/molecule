import type { EmailsTranslations } from './types.js'

/** Emails translations for English. */
export const en: EmailsTranslations = {
  'emails.error.noProvider': 'Email transport not configured. Call setTransport() first.',
}
