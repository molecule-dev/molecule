import type { EmailsTranslations } from './types.js'

/** Emails translations for Malayalam. */
export const ml: EmailsTranslations = {
  'emails.error.noProvider':
    'ഇമെയില്‍ ട്രാന്‍സ്പോര്‍ട്ട് കോണ്‍ഫിഗര്‍ ചെയ്തിട്ടില്ല. ആദ്യം setTransport() കോള്‍ ചെയ്യുക.',
}
