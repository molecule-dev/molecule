import type { EmailsTranslations } from './types.js'

/** Emails translations for Irish. */
export const ga: EmailsTranslations = {
  'emails.error.noProvider': 'Níl iompar ríomhphoist cumraithe. Glaoigh ar setTransport() ar dtús.',
}
