import type { EmailsTranslations } from './types.js'

/** Emails translations for Afrikaans. */
export const af: EmailsTranslations = {
  'emails.error.noProvider': 'E-posvervoer is nie gekonfigureer nie. Roep eers setTransport() aan.',
}
