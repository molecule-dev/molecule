import type { EmailsTranslations } from './types.js'

/** Emails translations for Hungarian. */
export const hu: EmailsTranslations = {
  'emails.error.noProvider':
    'Az e-mail szállítás nincs konfigurálva. Először hívja meg a setTransport()-t.',
}
