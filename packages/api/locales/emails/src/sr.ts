import type { EmailsTranslations } from './types.js'

/** Emails translations for Serbian. */
export const sr: EmailsTranslations = {
  'emails.error.noProvider': 'Transport e-poste nije konfigurisan. Prvo pozovite setTransport().',
}
