import type { EmailsTranslations } from './types.js'

/** Emails translations for Korean. */
export const ko: EmailsTranslations = {
  'emails.error.noProvider': '이메일 전송이 구성되지 않았습니다. 먼저 setTransport()를 호출하세요.',
}
