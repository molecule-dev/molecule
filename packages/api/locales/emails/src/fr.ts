import type { EmailsTranslations } from './types.js'

/** Emails translations for French. */
export const fr: EmailsTranslations = {
  'emails.error.noProvider':
    "Le transport de courrier n'est pas configuré. Appelez d'abord setTransport().",
}
