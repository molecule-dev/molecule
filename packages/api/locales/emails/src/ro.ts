import type { EmailsTranslations } from './types.js'

/** Emails translations for Romanian. */
export const ro: EmailsTranslations = {
  'emails.error.noProvider':
    'Transportul de e-mail nu este configurat. Apelati mai intai setTransport().',
}
