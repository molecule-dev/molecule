import type { EmailsTranslations } from './types.js'

/** Emails translations for Arabic. */
export const ar: EmailsTranslations = {
  'emails.error.noProvider': 'لم يتم تكوين نقل البريد الإلكتروني. استدعِ setTransport() أولاً.',
}
