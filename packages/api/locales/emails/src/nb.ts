import type { EmailsTranslations } from './types.js'

/** Emails translations for Norwegian Bokmål. */
export const nb: EmailsTranslations = {
  'emails.error.noProvider': 'E-posttransport er ikke konfigurert. Kall setTransport() forst.',
}
