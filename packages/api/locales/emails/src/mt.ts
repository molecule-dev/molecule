import type { EmailsTranslations } from './types.js'

/** Emails translations for Maltese. */
export const mt: EmailsTranslations = {
  'emails.error.noProvider':
    'It-trasport tal-email mhux ikkonfigurat. Sejjah setTransport() l-ewwel.',
}
