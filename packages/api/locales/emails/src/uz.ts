import type { EmailsTranslations } from './types.js'

/** Emails translations for Uzbek. */
export const uz: EmailsTranslations = {
  'emails.error.noProvider': 'Email transporti sozlanmagan. Avval setTransport() ni chaqiring.',
}
