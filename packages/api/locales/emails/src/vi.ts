import type { EmailsTranslations } from './types.js'

/** Emails translations for Vietnamese. */
export const vi: EmailsTranslations = {
  'emails.error.noProvider': 'Van chuyen email chua duoc cau hinh. Hay goi setTransport() truoc.',
}
