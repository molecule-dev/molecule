import type { EmailsTranslations } from './types.js'

/** Emails translations for Greek. */
export const el: EmailsTranslations = {
  'emails.error.noProvider': 'Η μεταφορά email δεν έχει ρυθμιστεί. Καλέστε πρώτα setTransport().',
}
