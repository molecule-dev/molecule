import type { EmailsTranslations } from './types.js'

/** Emails translations for German. */
export const de: EmailsTranslations = {
  'emails.error.noProvider':
    'E-Mail-Transport nicht konfiguriert. Rufen Sie zuerst setTransport() auf.',
}
