import type { EmailsTranslations } from './types.js'

/** Emails translations for Lao. */
export const lo: EmailsTranslations = {
  'emails.error.noProvider': 'ການຂົນສົ່ງອີເມວບໍ່ໄດ້ຕັ້ງຄ່າ. ກະລຸນາໂທ setTransport() ກ່ອນ.',
}
