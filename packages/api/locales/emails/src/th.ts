import type { EmailsTranslations } from './types.js'

/** Emails translations for Thai. */
export const th: EmailsTranslations = {
  'emails.error.noProvider': 'ยังไม่ได้กำหนดค่าการขนส่งอีเมล เรียก setTransport() ก่อน',
}
