import type { EmailsTranslations } from './types.js'

/** Emails translations for Estonian. */
export const et: EmailsTranslations = {
  'emails.error.noProvider': 'E-posti transport pole seadistatud. Kutsuge esmalt setTransport().',
}
