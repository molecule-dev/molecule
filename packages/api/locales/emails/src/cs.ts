import type { EmailsTranslations } from './types.js'

/** Emails translations for Czech. */
export const cs: EmailsTranslations = {
  'emails.error.noProvider':
    'Přenos e-mailů není nakonfigurován. Nejprve zavolejte setTransport().',
}
