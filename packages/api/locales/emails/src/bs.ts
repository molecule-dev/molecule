import type { EmailsTranslations } from './types.js'

/** Emails translations for Bosnian. */
export const bs: EmailsTranslations = {
  'emails.error.noProvider': 'Transport e-pošte nije konfigurisan. Prvo pozovite setTransport().',
}
