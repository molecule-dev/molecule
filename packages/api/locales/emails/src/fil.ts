import type { EmailsTranslations } from './types.js'

/** Emails translations for Filipino. */
export const fil: EmailsTranslations = {
  'emails.error.noProvider':
    'Hindi naka-configure ang email transport. Tawagan muna ang setTransport().',
}
