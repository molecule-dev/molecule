import type { EmailsTranslations } from './types.js'

/** Emails translations for Malay. */
export const ms: EmailsTranslations = {
  'emails.error.noProvider':
    'Pengangkutan e-mel tidak dikonfigurasi. Panggil setTransport() terlebih dahulu.',
}
