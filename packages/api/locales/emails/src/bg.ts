import type { EmailsTranslations } from './types.js'

/** Emails translations for Bulgarian. */
export const bg: EmailsTranslations = {
  'emails.error.noProvider':
    'Транспортът за имейл не е конфигуриран. Първо извикайте setTransport().',
}
