import type { EmailsTranslations } from './types.js'

/** Emails translations for Hausa. */
export const ha: EmailsTranslations = {
  'emails.error.noProvider': 'Ba a saita sufurin imel ba. Da fari a kira setTransport().',
}
