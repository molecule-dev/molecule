import type { EmailsTranslations } from './types.js'

/** Emails translations for Japanese. */
export const ja: EmailsTranslations = {
  'emails.error.noProvider':
    'メールトランスポートが設定されていません。先にsetTransport()を呼び出してください。',
}
