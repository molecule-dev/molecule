import type { EmailsTranslations } from './types.js'

/** Emails translations for Indonesian. */
export const id: EmailsTranslations = {
  'emails.error.noProvider':
    'Transportasi email belum dikonfigurasi. Panggil setTransport() terlebih dahulu.',
}
