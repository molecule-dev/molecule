import type { EmailsTranslations } from './types.js'

/** Emails translations for Hebrew. */
export const he: EmailsTranslations = {
  'emails.error.noProvider': 'תעבורת דוא"ל לא מוגדרת. קרא קודם ל-setTransport().',
}
