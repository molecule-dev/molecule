import type { EmailsTranslations } from './types.js'

/** Emails translations for Danish. */
export const da: EmailsTranslations = {
  'emails.error.noProvider': 'E-mailtransport er ikke konfigureret. Kald setTransport() først.',
}
