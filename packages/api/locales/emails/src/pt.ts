import type { EmailsTranslations } from './types.js'

/** Emails translations for Portuguese. */
export const pt: EmailsTranslations = {
  'emails.error.noProvider':
    'O transporte de e-mail nao esta configurado. Chame setTransport() primeiro.',
}
