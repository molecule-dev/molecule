import type { EmailsTranslations } from './types.js'

/** Emails translations for Icelandic. */
export const is: EmailsTranslations = {
  'emails.error.noProvider':
    'Tolvupostflutningur er ekki stilltur. Kalladhu a setTransport() fyrst.',
}
