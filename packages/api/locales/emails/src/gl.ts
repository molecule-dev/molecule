import type { EmailsTranslations } from './types.js'

/** Emails translations for Galician. */
export const gl: EmailsTranslations = {
  'emails.error.noProvider':
    'O transporte de correo non está configurado. Chame primeiro a setTransport().',
}
