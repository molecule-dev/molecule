import type { EmailsTranslations } from './types.js'

/** Emails translations for Dutch. */
export const nl: EmailsTranslations = {
  'emails.error.noProvider':
    'E-mailtransport is niet geconfigureerd. Roep eerst setTransport() aan.',
}
