import type { EmailsTranslations } from './types.js'

/** Emails translations for Swedish. */
export const sv: EmailsTranslations = {
  'emails.error.noProvider': 'E-posttransport ar inte konfigurerad. Anropa setTransport() forst.',
}
