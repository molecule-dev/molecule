import type { EmailsTranslations } from './types.js'

/** Emails translations for Turkish. */
export const tr: EmailsTranslations = {
  'emails.error.noProvider': 'E-posta aktarimi yapilandirilmamis. Once setTransport() cagirin.',
}
