import type { EmailsTranslations } from './types.js'

/** Emails translations for Albanian. */
export const sq: EmailsTranslations = {
  'emails.error.noProvider':
    'Transporti i email-it nuk eshte konfiguruar. Thirrni fillimisht setTransport().',
}
