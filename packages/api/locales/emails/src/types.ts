/** Translation keys for the emails locale package. */
export type EmailsTranslationKey = 'emails.error.noProvider'

/** Translation record mapping emails keys to translated strings. */
export type EmailsTranslations = Record<EmailsTranslationKey, string>
