import type { CacheTranslations } from './types.js'

/** Cache translations for English. */
export const en: CacheTranslations = {
  'cache.error.noProvider': 'Cache provider not configured. Call setProvider() first.',
}
