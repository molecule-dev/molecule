import type { CacheTranslations } from './types.js'

/** Cache translations for Portuguese. */
export const pt: CacheTranslations = {
  'cache.error.noProvider': 'Provedor de cache não configurado. Chame setProvider() primeiro.',
}
