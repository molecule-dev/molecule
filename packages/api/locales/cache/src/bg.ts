import type { CacheTranslations } from './types.js'

/** Cache translations for Bulgarian. */
export const bg: CacheTranslations = {
  'cache.error.noProvider': 'Доставчикът на кеш не е конфигуриран. Първо извикайте setProvider().',
}
