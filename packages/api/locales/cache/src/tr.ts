import type { CacheTranslations } from './types.js'

/** Cache translations for Turkish. */
export const tr: CacheTranslations = {
  'cache.error.noProvider': 'Önbellek sağlayıcı yapılandırılmamış. Önce setProvider() çağırın.',
}
