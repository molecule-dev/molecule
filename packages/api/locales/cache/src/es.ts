import type { CacheTranslations } from './types.js'

/** Cache translations for Spanish. */
export const es: CacheTranslations = {
  'cache.error.noProvider': 'Proveedor de caché no configurado. Llame primero a setProvider().',
}
