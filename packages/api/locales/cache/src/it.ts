import type { CacheTranslations } from './types.js'

/** Cache translations for Italian. */
export const it: CacheTranslations = {
  'cache.error.noProvider': 'Provider di cache non configurato. Chiamare prima setProvider().',
}
