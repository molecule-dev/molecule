import type { CacheTranslations } from './types.js'

/** Cache translations for Romanian. */
export const ro: CacheTranslations = {
  'cache.error.noProvider':
    'Furnizorul de cache nu este configurat. Apelați mai întâi setProvider().',
}
