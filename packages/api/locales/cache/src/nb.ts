import type { CacheTranslations } from './types.js'

/** Cache translations for Norwegian Bokmål. */
export const nb: CacheTranslations = {
  'cache.error.noProvider': 'Cache-leverandør er ikke konfigurert. Kall setProvider() først.',
}
