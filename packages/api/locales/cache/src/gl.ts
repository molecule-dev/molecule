import type { CacheTranslations } from './types.js'

/** Cache translations for Galician. */
export const gl: CacheTranslations = {
  'cache.error.noProvider':
    'O provedor de caché non está configurado. Chame primeiro a setProvider().',
}
