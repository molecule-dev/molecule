import type { CacheTranslations } from './types.js'

/** Cache translations for Filipino. */
export const fil: CacheTranslations = {
  'cache.error.noProvider':
    'Hindi naka-configure ang cache provider. Tumawag muna ng setProvider().',
}
