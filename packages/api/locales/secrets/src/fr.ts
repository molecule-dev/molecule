import type { SecretsTranslations } from './types.js'

/** Secrets translations for French. */
export const fr: SecretsTranslations = {
  'secrets.error.required': "Le secret requis '{{key}}' n'est pas défini.",
  'secrets.error.patternMismatch': "Le secret '{{key}}' ne correspond pas au modèle attendu",
}
