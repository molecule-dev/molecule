import type { SecretsTranslations } from './types.js'

/** Secrets translations for Portuguese. */
export const pt: SecretsTranslations = {
  'secrets.error.required': "O segredo obrigatório '{{key}}' não está definido.",
  'secrets.error.patternMismatch': "O segredo '{{key}}' não corresponde ao padrão esperado",
}
