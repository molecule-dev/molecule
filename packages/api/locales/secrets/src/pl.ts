import type { SecretsTranslations } from './types.js'

/** Secrets translations for Polish. */
export const pl: SecretsTranslations = {
  'secrets.error.required': "Wymagany sekret '{{key}}' nie jest ustawiony.",
  'secrets.error.patternMismatch': "Sekret '{{key}}' nie pasuje do oczekiwanego wzorca",
}
