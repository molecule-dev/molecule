import type { SecretsTranslations } from './types.js'

/** Secrets translations for Malay. */
export const ms: SecretsTranslations = {
  'secrets.error.required': "Rahsia yang diperlukan '{{key}}' tidak ditetapkan.",
  'secrets.error.patternMismatch': "Rahsia '{{key}}' tidak sepadan dengan corak yang dijangka",
}
