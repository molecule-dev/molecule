import type { SecretsTranslations } from './types.js'

/** Secrets translations for Estonian. */
export const et: SecretsTranslations = {
  'secrets.error.required': "Nõutud saladus '{{key}}' ei ole määratud.",
  'secrets.error.patternMismatch': "Saladus '{{key}}' ei vasta oodatud mustrile",
}
