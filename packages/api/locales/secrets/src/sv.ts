import type { SecretsTranslations } from './types.js'

/** Secrets translations for Swedish. */
export const sv: SecretsTranslations = {
  'secrets.error.required': "Nödvändig hemlighet '{{key}}' är inte inställd.",
  'secrets.error.patternMismatch': "Hemlighet '{{key}}' matchar inte det förväntade mönstret",
}
