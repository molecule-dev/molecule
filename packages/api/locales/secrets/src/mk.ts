import type { SecretsTranslations } from './types.js'

/** Secrets translations for Macedonian. */
export const mk: SecretsTranslations = {
  'secrets.error.required': "Потребната тајна '{{key}}' не е поставена.",
  'secrets.error.patternMismatch': "Тајната '{{key}}' не се совпаѓа со очекуваната шема",
}
