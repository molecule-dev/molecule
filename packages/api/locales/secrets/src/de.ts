import type { SecretsTranslations } from './types.js'

/** Secrets translations for German. */
export const de: SecretsTranslations = {
  'secrets.error.required': "Erforderliches Geheimnis '{{key}}' ist nicht gesetzt.",
  'secrets.error.patternMismatch': "Geheimnis '{{key}}' entspricht nicht dem erwarteten Muster",
}
