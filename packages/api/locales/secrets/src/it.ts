import type { SecretsTranslations } from './types.js'

/** Secrets translations for Italian. */
export const it: SecretsTranslations = {
  'secrets.error.required': "Il segreto richiesto '{{key}}' non è impostato.",
  'secrets.error.patternMismatch': "Il segreto '{{key}}' non corrisponde al modello previsto",
}
