import type { SecretsTranslations } from './types.js'

/** Secrets translations for Russian. */
export const ru: SecretsTranslations = {
  'secrets.error.required': "Обязательный секрет '{{key}}' не установлен.",
  'secrets.error.patternMismatch': "Секрет '{{key}}' не соответствует ожидаемому шаблону",
}
