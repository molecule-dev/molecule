import type { SecretsTranslations } from './types.js'

/** Secrets translations for Hungarian. */
export const hu: SecretsTranslations = {
  'secrets.error.required': "A kötelező titok '{{key}}' nincs beállítva.",
  'secrets.error.patternMismatch': "A titok '{{key}}' nem felel meg a várt mintának",
}
