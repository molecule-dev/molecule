import type { SecretsTranslations } from './types.js'

/** Secrets translations for Japanese. */
export const ja: SecretsTranslations = {
  'secrets.error.required': "必須のシークレット '{{key}}' が設定されていません。",
  'secrets.error.patternMismatch': "シークレット '{{key}}' が予期されたパターンと一致しません",
}
