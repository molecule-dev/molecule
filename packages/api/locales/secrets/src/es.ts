import type { SecretsTranslations } from './types.js'

/** Secrets translations for Spanish. */
export const es: SecretsTranslations = {
  'secrets.error.required': "El secreto requerido '{{key}}' no está configurado.",
  'secrets.error.patternMismatch': "El secreto '{{key}}' no coincide con el patrón esperado",
}
