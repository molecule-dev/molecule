import type { SecretsTranslations } from './types.js'

/** Secrets translations for Chinese (Traditional). */
export const zhTW: SecretsTranslations = {
  'secrets.error.required': "未設定所需的密鑰 '{{key}}'。",
  'secrets.error.patternMismatch': "密鑰 '{{key}}' 與預期模式不匹配",
}
