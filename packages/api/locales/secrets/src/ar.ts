import type { SecretsTranslations } from './types.js'

/** Secrets translations for Arabic. */
export const ar: SecretsTranslations = {
  'secrets.error.required': "السر المطلوب '{{key}}' غير محدد.",
  'secrets.error.patternMismatch': "السر '{{key}}' لا يتطابق مع النمط المتوقع",
}
