import type { SecretsTranslations } from './types.js'

/** Secrets translations for Ukrainian. */
export const uk: SecretsTranslations = {
  'secrets.error.required': "Обов'язковий секрет '{{key}}' не встановлено.",
  'secrets.error.patternMismatch': "Секрет '{{key}}' не відповідає очікуваному шаблону",
}
