import type { SecretsTranslations } from './types.js'

/** Secrets translations for Belarusian. */
export const be: SecretsTranslations = {
  'secrets.error.required': "Неабходны сакрэт '{{key}}' не ўсталяваны.",
  'secrets.error.patternMismatch': "Сакрэт '{{key}}' не адпавядае чаканаму шаблону",
}
