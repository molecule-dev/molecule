import type { AnalyticsTranslations } from './types.js'

/** Analytics translations for Turkish. */
export const tr: AnalyticsTranslations = {
  'analytics.error.noProvider': 'Analitik sağlayıcı yapılandırılmamış. Önce setProvider() çağırın.',
  'analytics.error.noGroupSupport': 'Analitik sağlayıcı group() desteklemiyor.',
}
