import type { DatabaseTranslations } from './types.js'

/** Database translations for Italian. */
export const it: DatabaseTranslations = {
  'database.error.noProvider': 'Il pool del database non configurato. Chiamare prima setPool().',
  'database.error.storeNotConfigured': 'DataStore non configurato. Chiamare prima setStore().',
}
