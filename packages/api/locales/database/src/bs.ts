import type { DatabaseTranslations } from './types.js'

/** Database translations for Bosnian. */
export const bs: DatabaseTranslations = {
  'database.error.noProvider': 'Pool baze podataka nije konfigurisan. Prvo pozovite setPool().',
  'database.error.storeNotConfigured': 'DataStore nije konfigurisan. Prvo pozovite setStore().',
}
