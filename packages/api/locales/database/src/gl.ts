import type { DatabaseTranslations } from './types.js'

/** Database translations for Galician. */
export const gl: DatabaseTranslations = {
  'database.error.noProvider':
    'O pool de base de datos non está configurado. Chame primeiro a setPool().',
  'database.error.storeNotConfigured':
    'DataStore non está configurado. Chame primeiro a setStore().',
}
