import type { SecretsDopplerTranslations } from './types.js'

/** Secrets Doppler translations for English. */
export const en: SecretsDopplerTranslations = {
  'secrets.doppler.error.tokenNotConfigured':
    'Doppler token not configured. Set DOPPLER_TOKEN or pass token option.',
  'secrets.doppler.error.apiError': 'Doppler API error: {{status}} {{error}}',
}
