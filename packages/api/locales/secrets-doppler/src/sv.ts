import type { SecretsDopplerTranslations } from './types.js'

/** Secrets Doppler translations for Swedish. */
export const sv: SecretsDopplerTranslations = {
  'secrets.doppler.error.tokenNotConfigured':
    'Doppler-token inte konfigurerad. Ange DOPPLER_TOKEN eller skicka tokenalternativ.',
  'secrets.doppler.error.apiError': 'Doppler API-fel: {{status}} {{error}}',
}
