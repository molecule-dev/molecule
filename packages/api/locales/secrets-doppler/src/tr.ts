import type { SecretsDopplerTranslations } from './types.js'

/** Secrets Doppler translations for Turkish. */
export const tr: SecretsDopplerTranslations = {
  'secrets.doppler.error.tokenNotConfigured':
    'Doppler token yapılandırılmamış. DOPPLER_TOKEN ayarlayın veya token seçeneğini geçirin.',
  'secrets.doppler.error.apiError': 'Doppler API hatası: {{status}} {{error}}',
}
