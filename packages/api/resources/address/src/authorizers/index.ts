/**
 * Address authorizers.
 *
 * All address operations require authentication and are scoped to the current
 * user via the session — ownership is enforced inside the service. No
 * additional authorizers needed.
 *
 * @module
 */

export {}
