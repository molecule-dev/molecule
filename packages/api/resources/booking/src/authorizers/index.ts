/**
 * Booking authorizers.
 *
 * Booking routes use the `authenticate` middleware declared in route definitions,
 * so no custom authorizers are needed at this time. Ownership checks are
 * performed in individual handlers.
 *
 * @module
 */
