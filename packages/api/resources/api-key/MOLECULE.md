# @molecule/api-resource-api-key

Hashed API tokens with scopes, masking, rotation, revocation, last-used tracking.

## Type
`resource`

## Injection Notes

### Requirements
- None

### Post-Injection Steps
- Run `npm install` to install dependencies
- Run `npm run build` to compile

### Known Limitations
- None yet
