/**
 * Inventory authorizers.
 *
 * Inventory routes use the `authenticate` middleware declared in route definitions.
 * Additional authorization (e.g. admin-only for stock updates) is performed within
 * individual handlers as needed.
 *
 * @module
 */
