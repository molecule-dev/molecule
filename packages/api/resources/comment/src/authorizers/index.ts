/**
 * Comment authorizers.
 *
 * Comment ownership is enforced in the service layer — only the comment
 * owner can update or delete their comments. No additional authorizers needed.
 *
 * @module
 */
