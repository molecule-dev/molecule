# dependencies
node_modules

# build output
dist
*.tsbuildinfo
*.js.map
*.d.ts
!packages/app/frameworks/react-native-ui/src/react-native.d.ts
# Hand-written ambient declare-module typings for runtime-loaded native peers
# (react-native, expo-*, @react-native-*) live in src/ and must be tracked.
!packages/app/bonds/**/src/**/*.d.ts
*.d.ts.map

# compiled JS from TypeScript (keep hand-written .js like eslint.config.js)
packages/**/src/**/*.js
packages/**/vitest.config.js
vitest.workspace.js

# testing
coverage

# misc
.DS_Store
.env*
.vscode

# logs
*.log
npm-debug.log*

# keys
.keys

# Transient failure ledgers written by the publish/trust scripts.
.publish-failures.json
.trust-failures.json
.trust-state.json

# CI dist-cache index (written by scripts/dist-cache-manifest.mjs, restored from
# the Actions cache). Build-output metadata, never source.
.dist-manifest.json
